// Generated by utils/generate-programmatic-view-tags.mjs. Do not edit.
#ifndef ANGULAR_TS_VIEW_TAGS_HPP_
#define ANGULAR_TS_VIEW_TAGS_HPP_

#include "angular_ts/wasm.hpp"

namespace angular_ts::tags {

/** Creates a `a` element. */
inline ViewHandle a(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("a", properties_json, children);
}

/** Creates a `abbr` element. */
inline ViewHandle abbr(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("abbr", properties_json, children);
}

/** Creates a `address` element. */
inline ViewHandle address(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("address", properties_json, children);
}

/** Creates a `area` element. */
inline ViewHandle area(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("area", properties_json, children);
}

/** Creates a `article` element. */
inline ViewHandle article(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("article", properties_json, children);
}

/** Creates a `aside` element. */
inline ViewHandle aside(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("aside", properties_json, children);
}

/** Creates a `audio` element. */
inline ViewHandle audio(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("audio", properties_json, children);
}

/** Creates a `b` element. */
inline ViewHandle b(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("b", properties_json, children);
}

/** Creates a `base` element. */
inline ViewHandle base(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("base", properties_json, children);
}

/** Creates a `bdi` element. */
inline ViewHandle bdi(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("bdi", properties_json, children);
}

/** Creates a `bdo` element. */
inline ViewHandle bdo(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("bdo", properties_json, children);
}

/** Creates a `blockquote` element. */
inline ViewHandle blockquote(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("blockquote", properties_json, children);
}

/** Creates a `body` element. */
inline ViewHandle body(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("body", properties_json, children);
}

/** Creates a `br` element. */
inline ViewHandle br(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("br", properties_json, children);
}

/** Creates a `button` element. */
inline ViewHandle button(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("button", properties_json, children);
}

/** Creates a `canvas` element. */
inline ViewHandle canvas(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("canvas", properties_json, children);
}

/** Creates a `caption` element. */
inline ViewHandle caption(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("caption", properties_json, children);
}

/** Creates a `cite` element. */
inline ViewHandle cite(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("cite", properties_json, children);
}

/** Creates a `code` element. */
inline ViewHandle code(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("code", properties_json, children);
}

/** Creates a `col` element. */
inline ViewHandle col(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("col", properties_json, children);
}

/** Creates a `colgroup` element. */
inline ViewHandle colgroup(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("colgroup", properties_json, children);
}

/** Creates a `data` element. */
inline ViewHandle data(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("data", properties_json, children);
}

/** Creates a `datalist` element. */
inline ViewHandle datalist(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("datalist", properties_json, children);
}

/** Creates a `dd` element. */
inline ViewHandle dd(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("dd", properties_json, children);
}

/** Creates a `del` element. */
inline ViewHandle del(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("del", properties_json, children);
}

/** Creates a `details` element. */
inline ViewHandle details(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("details", properties_json, children);
}

/** Creates a `dfn` element. */
inline ViewHandle dfn(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("dfn", properties_json, children);
}

/** Creates a `dialog` element. */
inline ViewHandle dialog(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("dialog", properties_json, children);
}

/** Creates a `div` element. */
inline ViewHandle div(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("div", properties_json, children);
}

/** Creates a `dl` element. */
inline ViewHandle dl(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("dl", properties_json, children);
}

/** Creates a `dt` element. */
inline ViewHandle dt(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("dt", properties_json, children);
}

/** Creates a `em` element. */
inline ViewHandle em(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("em", properties_json, children);
}

/** Creates a `embed` element. */
inline ViewHandle embed(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("embed", properties_json, children);
}

/** Creates a `fieldset` element. */
inline ViewHandle fieldset(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("fieldset", properties_json, children);
}

/** Creates a `figcaption` element. */
inline ViewHandle figcaption(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("figcaption", properties_json, children);
}

/** Creates a `figure` element. */
inline ViewHandle figure(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("figure", properties_json, children);
}

/** Creates a `footer` element. */
inline ViewHandle footer(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("footer", properties_json, children);
}

/** Creates a `form` element. */
inline ViewHandle form(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("form", properties_json, children);
}

/** Creates a `h1` element. */
inline ViewHandle h1(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("h1", properties_json, children);
}

/** Creates a `h2` element. */
inline ViewHandle h2(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("h2", properties_json, children);
}

/** Creates a `h3` element. */
inline ViewHandle h3(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("h3", properties_json, children);
}

/** Creates a `h4` element. */
inline ViewHandle h4(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("h4", properties_json, children);
}

/** Creates a `h5` element. */
inline ViewHandle h5(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("h5", properties_json, children);
}

/** Creates a `h6` element. */
inline ViewHandle h6(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("h6", properties_json, children);
}

/** Creates a `head` element. */
inline ViewHandle head(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("head", properties_json, children);
}

/** Creates a `header` element. */
inline ViewHandle header(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("header", properties_json, children);
}

/** Creates a `hgroup` element. */
inline ViewHandle hgroup(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("hgroup", properties_json, children);
}

/** Creates a `hr` element. */
inline ViewHandle hr(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("hr", properties_json, children);
}

/** Creates a `html` element. */
inline ViewHandle html(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("html", properties_json, children);
}

/** Creates a `i` element. */
inline ViewHandle i(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("i", properties_json, children);
}

/** Creates a `iframe` element. */
inline ViewHandle iframe(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("iframe", properties_json, children);
}

/** Creates a `img` element. */
inline ViewHandle img(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("img", properties_json, children);
}

/** Creates a `input` element. */
inline ViewHandle input(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("input", properties_json, children);
}

/** Creates a `ins` element. */
inline ViewHandle ins(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("ins", properties_json, children);
}

/** Creates a `kbd` element. */
inline ViewHandle kbd(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("kbd", properties_json, children);
}

/** Creates a `label` element. */
inline ViewHandle label(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("label", properties_json, children);
}

/** Creates a `legend` element. */
inline ViewHandle legend(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("legend", properties_json, children);
}

/** Creates a `li` element. */
inline ViewHandle li(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("li", properties_json, children);
}

/** Creates a `link` element. */
inline ViewHandle link(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("link", properties_json, children);
}

/** Creates a `main` element. */
inline ViewHandle main(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("main", properties_json, children);
}

/** Creates a `map` element. */
inline ViewHandle map(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("map", properties_json, children);
}

/** Creates a `mark` element. */
inline ViewHandle mark(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("mark", properties_json, children);
}

/** Creates a `menu` element. */
inline ViewHandle menu(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("menu", properties_json, children);
}

/** Creates a `meta` element. */
inline ViewHandle meta(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("meta", properties_json, children);
}

/** Creates a `meter` element. */
inline ViewHandle meter(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("meter", properties_json, children);
}

/** Creates a `nav` element. */
inline ViewHandle nav(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("nav", properties_json, children);
}

/** Creates a `noscript` element. */
inline ViewHandle noscript(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("noscript", properties_json, children);
}

/** Creates a `object` element. */
inline ViewHandle object(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("object", properties_json, children);
}

/** Creates a `ol` element. */
inline ViewHandle ol(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("ol", properties_json, children);
}

/** Creates a `optgroup` element. */
inline ViewHandle optgroup(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("optgroup", properties_json, children);
}

/** Creates a `option` element. */
inline ViewHandle option(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("option", properties_json, children);
}

/** Creates a `output` element. */
inline ViewHandle output(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("output", properties_json, children);
}

/** Creates a `p` element. */
inline ViewHandle p(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("p", properties_json, children);
}

/** Creates a `picture` element. */
inline ViewHandle picture(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("picture", properties_json, children);
}

/** Creates a `pre` element. */
inline ViewHandle pre(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("pre", properties_json, children);
}

/** Creates a `progress` element. */
inline ViewHandle progress(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("progress", properties_json, children);
}

/** Creates a `q` element. */
inline ViewHandle q(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("q", properties_json, children);
}

/** Creates a `rp` element. */
inline ViewHandle rp(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("rp", properties_json, children);
}

/** Creates a `rt` element. */
inline ViewHandle rt(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("rt", properties_json, children);
}

/** Creates a `ruby` element. */
inline ViewHandle ruby(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("ruby", properties_json, children);
}

/** Creates a `s` element. */
inline ViewHandle s(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("s", properties_json, children);
}

/** Creates a `samp` element. */
inline ViewHandle samp(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("samp", properties_json, children);
}

/** Creates a `script` element. */
inline ViewHandle script(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("script", properties_json, children);
}

/** Creates a `search` element. */
inline ViewHandle search(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("search", properties_json, children);
}

/** Creates a `section` element. */
inline ViewHandle section(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("section", properties_json, children);
}

/** Creates a `select` element. */
inline ViewHandle select(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("select", properties_json, children);
}

/** Creates a `slot` element. */
inline ViewHandle slot(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("slot", properties_json, children);
}

/** Creates a `small` element. */
inline ViewHandle small(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("small", properties_json, children);
}

/** Creates a `source` element. */
inline ViewHandle source(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("source", properties_json, children);
}

/** Creates a `span` element. */
inline ViewHandle span(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("span", properties_json, children);
}

/** Creates a `strong` element. */
inline ViewHandle strong(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("strong", properties_json, children);
}

/** Creates a `style` element. */
inline ViewHandle style(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("style", properties_json, children);
}

/** Creates a `sub` element. */
inline ViewHandle sub(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("sub", properties_json, children);
}

/** Creates a `summary` element. */
inline ViewHandle summary(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("summary", properties_json, children);
}

/** Creates a `sup` element. */
inline ViewHandle sup(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("sup", properties_json, children);
}

/** Creates a `table` element. */
inline ViewHandle table(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("table", properties_json, children);
}

/** Creates a `tbody` element. */
inline ViewHandle tbody(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("tbody", properties_json, children);
}

/** Creates a `td` element. */
inline ViewHandle td(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("td", properties_json, children);
}

/** Creates a `template` element. */
inline ViewHandle template_tag(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("template", properties_json, children);
}

/** Creates a `textarea` element. */
inline ViewHandle textarea(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("textarea", properties_json, children);
}

/** Creates a `tfoot` element. */
inline ViewHandle tfoot(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("tfoot", properties_json, children);
}

/** Creates a `th` element. */
inline ViewHandle th(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("th", properties_json, children);
}

/** Creates a `thead` element. */
inline ViewHandle thead(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("thead", properties_json, children);
}

/** Creates a `time` element. */
inline ViewHandle time(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("time", properties_json, children);
}

/** Creates a `title` element. */
inline ViewHandle title(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("title", properties_json, children);
}

/** Creates a `tr` element. */
inline ViewHandle tr(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("tr", properties_json, children);
}

/** Creates a `track` element. */
inline ViewHandle track(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("track", properties_json, children);
}

/** Creates a `u` element. */
inline ViewHandle u(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("u", properties_json, children);
}

/** Creates a `ul` element. */
inline ViewHandle ul(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("ul", properties_json, children);
}

/** Creates a `var` element. */
inline ViewHandle var(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("var", properties_json, children);
}

/** Creates a `video` element. */
inline ViewHandle video(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("video", properties_json, children);
}

/** Creates a `wbr` element. */
inline ViewHandle wbr(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("wbr", properties_json, children);
}

/** Creates a `acronym` element. */
inline ViewHandle acronym(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("acronym", properties_json, children);
}

/** Creates a `applet` element. */
inline ViewHandle applet(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("applet", properties_json, children);
}

/** Creates a `basefont` element. */
inline ViewHandle basefont(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("basefont", properties_json, children);
}

/** Creates a `bgsound` element. */
inline ViewHandle bgsound(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("bgsound", properties_json, children);
}

/** Creates a `big` element. */
inline ViewHandle big(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("big", properties_json, children);
}

/** Creates a `blink` element. */
inline ViewHandle blink(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("blink", properties_json, children);
}

/** Creates a `center` element. */
inline ViewHandle center(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("center", properties_json, children);
}

/** Creates a `dir` element. */
inline ViewHandle dir(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("dir", properties_json, children);
}

/** Creates a `font` element. */
inline ViewHandle font(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("font", properties_json, children);
}

/** Creates a `frame` element. */
inline ViewHandle frame(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("frame", properties_json, children);
}

/** Creates a `frameset` element. */
inline ViewHandle frameset(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("frameset", properties_json, children);
}

/** Creates a `isindex` element. */
inline ViewHandle isindex(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("isindex", properties_json, children);
}

/** Creates a `keygen` element. */
inline ViewHandle keygen(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("keygen", properties_json, children);
}

/** Creates a `listing` element. */
inline ViewHandle listing(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("listing", properties_json, children);
}

/** Creates a `marquee` element. */
inline ViewHandle marquee(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("marquee", properties_json, children);
}

/** Creates a `menuitem` element. */
inline ViewHandle menuitem(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("menuitem", properties_json, children);
}

/** Creates a `multicol` element. */
inline ViewHandle multicol(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("multicol", properties_json, children);
}

/** Creates a `nextid` element. */
inline ViewHandle nextid(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("nextid", properties_json, children);
}

/** Creates a `nobr` element. */
inline ViewHandle nobr(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("nobr", properties_json, children);
}

/** Creates a `noembed` element. */
inline ViewHandle noembed(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("noembed", properties_json, children);
}

/** Creates a `noframes` element. */
inline ViewHandle noframes(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("noframes", properties_json, children);
}

/** Creates a `param` element. */
inline ViewHandle param(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("param", properties_json, children);
}

/** Creates a `plaintext` element. */
inline ViewHandle plaintext(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("plaintext", properties_json, children);
}

/** Creates a `rb` element. */
inline ViewHandle rb(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("rb", properties_json, children);
}

/** Creates a `rtc` element. */
inline ViewHandle rtc(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("rtc", properties_json, children);
}

/** Creates a `spacer` element. */
inline ViewHandle spacer(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("spacer", properties_json, children);
}

/** Creates a `strike` element. */
inline ViewHandle strike(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("strike", properties_json, children);
}

/** Creates a `tt` element. */
inline ViewHandle tt(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("tt", properties_json, children);
}

/** Creates a `xmp` element. */
inline ViewHandle xmp(
    std::string_view properties_json = "{}",
    const std::vector<ViewHandle>& children = {}) {
  return ProgrammaticViewTags().Tag("xmp", properties_json, children);
}

}  // namespace angular_ts::tags

#endif  // ANGULAR_TS_VIEW_TAGS_HPP_
