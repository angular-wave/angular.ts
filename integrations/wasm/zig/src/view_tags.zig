// Generated by utils/generate-programmatic-view-tags.mjs. Do not edit.
const builtin = @import("builtin");

pub const ProgrammaticViewChild = u32;

pub const ProgrammaticViewTags = struct {
    namespace_uri: []const u8 = "",

    pub fn withNamespace(namespace_uri: []const u8) ProgrammaticViewTags {
        return .{ .namespace_uri = namespace_uri };
    }

    pub fn tag(
        self: ProgrammaticViewTags,
        name: []const u8,
        properties_json: []const u8,
        children: []const ProgrammaticViewChild,
    ) ProgrammaticViewChild {
        if (builtin.target.cpu.arch == .wasm32 or builtin.target.cpu.arch == .wasm64) {
            return view_tag(
                ptr(self.namespace_uri),
                len(self.namespace_uri),
                ptr(name),
                len(name),
                ptr(properties_json),
                len(properties_json),
                @intFromPtr(children.ptr),
                @intCast(children.len),
            );
        }
        return if (name.len == 0) 0 else 41;
    }
};

pub const elements = ProgrammaticViewTags{};

pub fn text(value: []const u8) ProgrammaticViewChild {
    if (builtin.target.cpu.arch == .wasm32 or builtin.target.cpu.arch == .wasm64) {
        return view_text(ptr(value), len(value));
    }
    return if (value.len == 0) 0 else 42;
}

pub fn release(child: ProgrammaticViewChild) bool {
    if (builtin.target.cpu.arch == .wasm32 or builtin.target.cpu.arch == .wasm64) {
        return view_release(child) != 0;
    }
    return child != 0;
}

extern "angular_ts" fn view_tag(
    namespace_ptr: u32,
    namespace_len: u32,
    name_ptr: u32,
    name_len: u32,
    properties_ptr: u32,
    properties_len: u32,
    children_ptr: u32,
    children_len: u32,
) ProgrammaticViewChild;
extern "angular_ts" fn view_text(value_ptr: u32, value_len: u32) ProgrammaticViewChild;
extern "angular_ts" fn view_release(child: ProgrammaticViewChild) u32;

fn ptr(value: []const u8) u32 {
    return @intCast(@intFromPtr(value.ptr));
}

fn len(value: []const u8) u32 {
    return @intCast(value.len);
}

/// Creates a `a` element.
pub fn a(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("a", properties_json, children);
}

/// Creates a `abbr` element.
pub fn abbr(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("abbr", properties_json, children);
}

/// Creates a `address` element.
pub fn address(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("address", properties_json, children);
}

/// Creates a `area` element.
pub fn area(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("area", properties_json, children);
}

/// Creates a `article` element.
pub fn article(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("article", properties_json, children);
}

/// Creates a `aside` element.
pub fn aside(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("aside", properties_json, children);
}

/// Creates a `audio` element.
pub fn audio(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("audio", properties_json, children);
}

/// Creates a `b` element.
pub fn b(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("b", properties_json, children);
}

/// Creates a `base` element.
pub fn base(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("base", properties_json, children);
}

/// Creates a `bdi` element.
pub fn bdi(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("bdi", properties_json, children);
}

/// Creates a `bdo` element.
pub fn bdo(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("bdo", properties_json, children);
}

/// Creates a `blockquote` element.
pub fn blockquote(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("blockquote", properties_json, children);
}

/// Creates a `body` element.
pub fn body(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("body", properties_json, children);
}

/// Creates a `br` element.
pub fn br(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("br", properties_json, children);
}

/// Creates a `button` element.
pub fn button(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("button", properties_json, children);
}

/// Creates a `canvas` element.
pub fn canvas(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("canvas", properties_json, children);
}

/// Creates a `caption` element.
pub fn caption(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("caption", properties_json, children);
}

/// Creates a `cite` element.
pub fn cite(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("cite", properties_json, children);
}

/// Creates a `code` element.
pub fn code(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("code", properties_json, children);
}

/// Creates a `col` element.
pub fn col(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("col", properties_json, children);
}

/// Creates a `colgroup` element.
pub fn colgroup(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("colgroup", properties_json, children);
}

/// Creates a `data` element.
pub fn data(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("data", properties_json, children);
}

/// Creates a `datalist` element.
pub fn datalist(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("datalist", properties_json, children);
}

/// Creates a `dd` element.
pub fn dd(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("dd", properties_json, children);
}

/// Creates a `del` element.
pub fn del(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("del", properties_json, children);
}

/// Creates a `details` element.
pub fn details(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("details", properties_json, children);
}

/// Creates a `dfn` element.
pub fn dfn(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("dfn", properties_json, children);
}

/// Creates a `dialog` element.
pub fn dialog(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("dialog", properties_json, children);
}

/// Creates a `div` element.
pub fn div(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("div", properties_json, children);
}

/// Creates a `dl` element.
pub fn dl(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("dl", properties_json, children);
}

/// Creates a `dt` element.
pub fn dt(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("dt", properties_json, children);
}

/// Creates a `em` element.
pub fn em(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("em", properties_json, children);
}

/// Creates a `embed` element.
pub fn embed(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("embed", properties_json, children);
}

/// Creates a `fieldset` element.
pub fn fieldset(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("fieldset", properties_json, children);
}

/// Creates a `figcaption` element.
pub fn figcaption(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("figcaption", properties_json, children);
}

/// Creates a `figure` element.
pub fn figure(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("figure", properties_json, children);
}

/// Creates a `footer` element.
pub fn footer(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("footer", properties_json, children);
}

/// Creates a `form` element.
pub fn form(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("form", properties_json, children);
}

/// Creates a `h1` element.
pub fn h1(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("h1", properties_json, children);
}

/// Creates a `h2` element.
pub fn h2(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("h2", properties_json, children);
}

/// Creates a `h3` element.
pub fn h3(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("h3", properties_json, children);
}

/// Creates a `h4` element.
pub fn h4(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("h4", properties_json, children);
}

/// Creates a `h5` element.
pub fn h5(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("h5", properties_json, children);
}

/// Creates a `h6` element.
pub fn h6(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("h6", properties_json, children);
}

/// Creates a `head` element.
pub fn head(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("head", properties_json, children);
}

/// Creates a `header` element.
pub fn header(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("header", properties_json, children);
}

/// Creates a `hgroup` element.
pub fn hgroup(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("hgroup", properties_json, children);
}

/// Creates a `hr` element.
pub fn hr(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("hr", properties_json, children);
}

/// Creates a `html` element.
pub fn html(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("html", properties_json, children);
}

/// Creates a `i` element.
pub fn i(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("i", properties_json, children);
}

/// Creates a `iframe` element.
pub fn iframe(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("iframe", properties_json, children);
}

/// Creates a `img` element.
pub fn img(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("img", properties_json, children);
}

/// Creates a `input` element.
pub fn input(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("input", properties_json, children);
}

/// Creates a `ins` element.
pub fn ins(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("ins", properties_json, children);
}

/// Creates a `kbd` element.
pub fn kbd(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("kbd", properties_json, children);
}

/// Creates a `label` element.
pub fn label(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("label", properties_json, children);
}

/// Creates a `legend` element.
pub fn legend(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("legend", properties_json, children);
}

/// Creates a `li` element.
pub fn li(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("li", properties_json, children);
}

/// Creates a `link` element.
pub fn link(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("link", properties_json, children);
}

/// Creates a `main` element.
pub fn main(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("main", properties_json, children);
}

/// Creates a `map` element.
pub fn map(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("map", properties_json, children);
}

/// Creates a `mark` element.
pub fn mark(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("mark", properties_json, children);
}

/// Creates a `menu` element.
pub fn menu(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("menu", properties_json, children);
}

/// Creates a `meta` element.
pub fn meta(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("meta", properties_json, children);
}

/// Creates a `meter` element.
pub fn meter(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("meter", properties_json, children);
}

/// Creates a `nav` element.
pub fn nav(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("nav", properties_json, children);
}

/// Creates a `noscript` element.
pub fn noscript(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("noscript", properties_json, children);
}

/// Creates a `object` element.
pub fn object(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("object", properties_json, children);
}

/// Creates a `ol` element.
pub fn ol(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("ol", properties_json, children);
}

/// Creates a `optgroup` element.
pub fn optgroup(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("optgroup", properties_json, children);
}

/// Creates a `option` element.
pub fn option(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("option", properties_json, children);
}

/// Creates a `output` element.
pub fn output(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("output", properties_json, children);
}

/// Creates a `p` element.
pub fn p(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("p", properties_json, children);
}

/// Creates a `picture` element.
pub fn picture(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("picture", properties_json, children);
}

/// Creates a `pre` element.
pub fn pre(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("pre", properties_json, children);
}

/// Creates a `progress` element.
pub fn progress(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("progress", properties_json, children);
}

/// Creates a `q` element.
pub fn q(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("q", properties_json, children);
}

/// Creates a `rp` element.
pub fn rp(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("rp", properties_json, children);
}

/// Creates a `rt` element.
pub fn rt(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("rt", properties_json, children);
}

/// Creates a `ruby` element.
pub fn ruby(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("ruby", properties_json, children);
}

/// Creates a `s` element.
pub fn s(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("s", properties_json, children);
}

/// Creates a `samp` element.
pub fn samp(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("samp", properties_json, children);
}

/// Creates a `script` element.
pub fn script(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("script", properties_json, children);
}

/// Creates a `search` element.
pub fn search(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("search", properties_json, children);
}

/// Creates a `section` element.
pub fn section(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("section", properties_json, children);
}

/// Creates a `select` element.
pub fn select(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("select", properties_json, children);
}

/// Creates a `slot` element.
pub fn slot(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("slot", properties_json, children);
}

/// Creates a `small` element.
pub fn small(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("small", properties_json, children);
}

/// Creates a `source` element.
pub fn source(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("source", properties_json, children);
}

/// Creates a `span` element.
pub fn span(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("span", properties_json, children);
}

/// Creates a `strong` element.
pub fn strong(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("strong", properties_json, children);
}

/// Creates a `style` element.
pub fn style(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("style", properties_json, children);
}

/// Creates a `sub` element.
pub fn sub(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("sub", properties_json, children);
}

/// Creates a `summary` element.
pub fn summary(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("summary", properties_json, children);
}

/// Creates a `sup` element.
pub fn sup(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("sup", properties_json, children);
}

/// Creates a `table` element.
pub fn table(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("table", properties_json, children);
}

/// Creates a `tbody` element.
pub fn tbody(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("tbody", properties_json, children);
}

/// Creates a `td` element.
pub fn td(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("td", properties_json, children);
}

/// Creates a `template` element.
pub fn template(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("template", properties_json, children);
}

/// Creates a `textarea` element.
pub fn textarea(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("textarea", properties_json, children);
}

/// Creates a `tfoot` element.
pub fn tfoot(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("tfoot", properties_json, children);
}

/// Creates a `th` element.
pub fn th(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("th", properties_json, children);
}

/// Creates a `thead` element.
pub fn thead(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("thead", properties_json, children);
}

/// Creates a `time` element.
pub fn time(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("time", properties_json, children);
}

/// Creates a `title` element.
pub fn title(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("title", properties_json, children);
}

/// Creates a `tr` element.
pub fn tr(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("tr", properties_json, children);
}

/// Creates a `track` element.
pub fn track(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("track", properties_json, children);
}

/// Creates a `u` element.
pub fn u(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("u", properties_json, children);
}

/// Creates a `ul` element.
pub fn ul(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("ul", properties_json, children);
}

/// Creates a `var` element.
pub fn var_tag(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("var", properties_json, children);
}

/// Creates a `video` element.
pub fn video(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("video", properties_json, children);
}

/// Creates a `wbr` element.
pub fn wbr(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("wbr", properties_json, children);
}

/// Creates a `acronym` element.
pub fn acronym(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("acronym", properties_json, children);
}

/// Creates a `applet` element.
pub fn applet(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("applet", properties_json, children);
}

/// Creates a `basefont` element.
pub fn basefont(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("basefont", properties_json, children);
}

/// Creates a `bgsound` element.
pub fn bgsound(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("bgsound", properties_json, children);
}

/// Creates a `big` element.
pub fn big(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("big", properties_json, children);
}

/// Creates a `blink` element.
pub fn blink(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("blink", properties_json, children);
}

/// Creates a `center` element.
pub fn center(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("center", properties_json, children);
}

/// Creates a `dir` element.
pub fn dir(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("dir", properties_json, children);
}

/// Creates a `font` element.
pub fn font(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("font", properties_json, children);
}

/// Creates a `frame` element.
pub fn frame(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("frame", properties_json, children);
}

/// Creates a `frameset` element.
pub fn frameset(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("frameset", properties_json, children);
}

/// Creates a `isindex` element.
pub fn isindex(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("isindex", properties_json, children);
}

/// Creates a `keygen` element.
pub fn keygen(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("keygen", properties_json, children);
}

/// Creates a `listing` element.
pub fn listing(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("listing", properties_json, children);
}

/// Creates a `marquee` element.
pub fn marquee(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("marquee", properties_json, children);
}

/// Creates a `menuitem` element.
pub fn menuitem(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("menuitem", properties_json, children);
}

/// Creates a `multicol` element.
pub fn multicol(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("multicol", properties_json, children);
}

/// Creates a `nextid` element.
pub fn nextid(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("nextid", properties_json, children);
}

/// Creates a `nobr` element.
pub fn nobr(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("nobr", properties_json, children);
}

/// Creates a `noembed` element.
pub fn noembed(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("noembed", properties_json, children);
}

/// Creates a `noframes` element.
pub fn noframes(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("noframes", properties_json, children);
}

/// Creates a `param` element.
pub fn param(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("param", properties_json, children);
}

/// Creates a `plaintext` element.
pub fn plaintext(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("plaintext", properties_json, children);
}

/// Creates a `rb` element.
pub fn rb(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("rb", properties_json, children);
}

/// Creates a `rtc` element.
pub fn rtc(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("rtc", properties_json, children);
}

/// Creates a `spacer` element.
pub fn spacer(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("spacer", properties_json, children);
}

/// Creates a `strike` element.
pub fn strike(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("strike", properties_json, children);
}

/// Creates a `tt` element.
pub fn tt(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("tt", properties_json, children);
}

/// Creates a `xmp` element.
pub fn xmp(
    properties_json: []const u8,
    children: []const ProgrammaticViewChild,
) ProgrammaticViewChild {
    return elements.tag("xmp", properties_json, children);
}
