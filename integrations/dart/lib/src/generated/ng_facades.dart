// Generated by integrations/dart/tool/generate_dart_bindings.mjs.
// Do not edit by hand.
// ignore_for_file: non_constant_identifier_names

import 'dart:js_interop';
import 'dart:js_interop_unsafe';

import 'package:web/web.dart';

import '../facade.dart';
import '../scope.dart';
import '../unsafe.dart' as unsafe;

const Object _undefinedArgument = Object();

JSAny? _callFunction(JSFunction function, List<JSAny?> args) {
  return switch (args.length) {
    0 => function.callAsFunction(null),
    1 => function.callAsFunction(null, args[0]),
    2 => function.callAsFunction(null, args[0], args[1]),
    3 => function.callAsFunction(null, args[0], args[1], args[2]),
    4 => function.callAsFunction(null, args[0], args[1], args[2], args[3]),
    _ => throw UnsupportedError(
        'Generated function calls support up to 4 arguments',
      ),
  };
}

bool? _toDartBool(JSAny? value) {
  return value == null ? null : (value as JSBoolean).toDart;
}

String? _toDartString(JSAny? value) {
  return value == null ? null : (value as JSString).toDart;
}

num? _toDartNumber(JSAny? value) {
  return value == null ? null : (value as JSNumber).toDartDouble;
}

int? _toDartInt(JSAny? value) {
  return value == null ? null : (value as JSNumber).toDartInt;
}

/// Base class for generated raw AngularTS JS facades.
base class GeneratedNgFacade extends AngularTsJsFacade {
  /// Creates a generated raw AngularTS JS facade.
  const GeneratedNgFacade(super.raw);
}

/// Public AngularTS AnchorScrollService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnchorScrollService`.
base class GeneratedNgAnchorScrollService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnchorScrollService.
  const GeneratedNgAnchorScrollService(super.raw);

  /// Invoke anchor scrolling.
  ///
  /// Parameters:
  /// - [hashOrElement]: Value supplied for the hashOrElement parameter.
  void call([Object? hashOrElement = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(hashOrElement, _undefinedArgument)) {
      args.add(unsafe.dartToJs(hashOrElement));
    }

    unsafe.jsToDart<void>(_callFunction(raw as JSFunction, args));
  }

  /// Vertical scroll offset. Can be a number, a function returning a number, or an Element whose
  /// offsetTop will be used.
  Object? get yOffset =>
      unsafe.jsToDart<Object?>(raw.getProperty('yOffset'.toJS));
}

/// Main AngularTS runtime entry point with the full built-in `ng` module configured by default.
///
/// Raw JavaScript facade for `ng.Angular`.
base class GeneratedNgAngular extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Angular.
  const GeneratedNgAngular(super.raw);

  /// Base class for user-authored AngularTS custom elements.
  Object? get ScopeElement =>
      unsafe.jsToDart<Object?>(raw.getProperty('ScopeElement'.toJS));

  /// JSX-free real-DOM tag factories for programmatic component views.
  Object? get tags => unsafe.jsToDart<Object?>(raw.getProperty('tags'.toJS));

  /// Explicit programmatic-view binding and element helpers.
  Object? get view => unsafe.jsToDart<Object?>(raw.getProperty('view'.toJS));

  /// Sub-application instances created when multiple `ng-app` roots are initialized.
  Object? get subapps =>
      unsafe.jsToDart<Object?>(raw.getProperty('subapps'.toJS));

  /// Application-wide event bus, available after bootstrap providers are created.
  Object? get eventBus =>
      unsafe.jsToDart<Object?>(raw.getProperty('eventBus'.toJS));

  /// Application injector, available after `bootstrap()` or `injector()` completes.
  Object? get currentInjector =>
      unsafe.jsToDart<Object?>(raw.getProperty('currentInjector'.toJS));

  /// Root scope for the bootstrapped application.
  Object? get rootScope =>
      unsafe.jsToDart<Object?>(raw.getProperty('rootScope'.toJS));

  /// AngularTS version string replaced at build time.
  String get version => _toDartString(raw.getProperty('version'.toJS))!;

  /// AngularTS version string replaced at build time.
  set version(String value) {
    raw.setProperty('version'.toJS, unsafe.dartToJs(value));
  }

  /// Retrieve the controller instance cached on a compiled DOM element.
  Object? get getController =>
      unsafe.jsToDart<Object?>(raw.getProperty('getController'.toJS));

  /// Retrieve the injector cached on a bootstrapped DOM element.
  Object? get getInjector =>
      unsafe.jsToDart<Object?>(raw.getProperty('getInjector'.toJS));

  /// Retrieve the scope cached on a compiled DOM element.
  Object? get getScope =>
      unsafe.jsToDart<Object?>(raw.getProperty('getScope'.toJS));

  /// Read an element attribute by normalized directive-style name.
  Object? get getNormalizedAttr =>
      unsafe.jsToDart<Object?>(raw.getProperty('getNormalizedAttr'.toJS));

  /// Return the actual DOM attribute name for a normalized directive-style name.
  Object? get getNormalizedAttrName =>
      unsafe.jsToDart<Object?>(raw.getProperty('getNormalizedAttrName'.toJS));

  /// Return whether an element has an attribute matching a normalized name.
  Object? get hasNormalizedAttr =>
      unsafe.jsToDart<Object?>(raw.getProperty('hasNormalizedAttr'.toJS));

  /// Configure how values embedded in framework error messages are formatted.
  Object? get errorFormattingConfig =>
      unsafe.jsToDart<Object?>(raw.getProperty('errorFormattingConfig'.toJS));

  /// Public injection token names keyed by token value.
  Object? get tokens =>
      unsafe.jsToDart<Object?>(raw.getProperty('tokens'.toJS));

  /// Registers the configured built-in `ng` module for this runtime instance.
  Object? registerNgModule() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('registerNgModule'.toJS, const <JSAny?>[]));
  }

  /// The `angular.module` is a global place for creating, registering and retrieving AngularTS
  /// modules. All modules (AngularTS core or 3rd party) that should be available to an application
  /// must be registered using this mechanism. Passing one argument retrieves an existing ng.NgModule,
  /// whereas passing more than one argument creates a new ng.NgModule # Module A module is a
  /// collection of services, directives, controllers, filters, workers, WebAssembly modules, and
  /// configuration information. `angular.module` is used to configure the auto.$injector `$injector`.
  /// ```js // Create a new module let myModule = angular.module('myModule', []); // register a new
  /// service myModule.value('appName', 'MyCoolApp'); // configure built-in services with typed object
  /// config. myModule.config({ location: { hashPrefix: '!', }, }); ``` Then you can create an
  /// injector and load your modules like this: ```js let injector = angular.injector(['ng',
  /// 'myModule']) ``` However it's more likely that you'll use the `ng-app` directive or
  /// `bootstrap()` to simplify this process.
  ///
  /// Parameters:
  /// - [name]: The name of the module to create or retrieve.
  /// - [requires]: If specified then new module is being created. If unspecified then the module is being retrieved for further configuration.
  /// - [configFn]: Optional configuration function for the module that gets passed to `NgModule.config()`.
  Object? module(String name,
      [Object? requires = _undefinedArgument,
      Object? configFn = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(requires, _undefinedArgument)) {
      args.add(unsafe.dartToJs(requires));
    }
    if (!identical(configFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(configFn));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('module'.toJS, args));
  }

  /// Dispatches an invocation event to either an injectable service or a named scope. The event
  /// `type` identifies the target and the payload contains the expression to evaluate against that
  /// target.
  ///
  /// Parameters:
  /// - [event]: Value supplied for the event parameter.
  bool dispatchEvent(Event event) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(event));

    return _toDartBool(raw.callMethodVarArgs('dispatchEvent'.toJS, args))!;
  }

  /// Fire-and-forget. Accepts a single string: `"&lt;target&gt;.&lt;expression&gt;"`
  ///
  /// Parameters:
  /// - [input]: Value supplied for the input parameter.
  void emit(String input) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(input));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('emit'.toJS, args));
  }

  /// Await result. Accepts a single string: `"&lt;target&gt;.&lt;expression&gt;"`
  ///
  /// Parameters:
  /// - [input]: Value supplied for the input parameter.
  JSPromise<JSAny?> call(String input) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(input));

    return raw.callMethodVarArgs('call'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Use this function to manually start up AngularTS application. AngularTS will detect if it has
  /// been loaded into the browser more than once and only allow the first loaded script to be
  /// bootstrapped and will report a warning to the browser console for each of the subsequent
  /// scripts. This prevents strange results in applications, where otherwise multiple instances of
  /// AngularTS try to work on the DOM. **Note:** Do not bootstrap the app on an element with a
  /// directive that uses transclusion, such as `ng-if`, `ng-include`, or `ng-view`. Doing this
  /// misplaces the app root element and injector, causing animations to stop working and making the
  /// injector inaccessible from outside the app. ```html &lt;!doctype html&gt; &lt;html&gt; &lt;body&gt; &lt;div
  /// ng-controller="WelcomeController"&gt; {{greeting}} &lt;/div&gt; &lt;script src="angular.js"&gt;&lt;/script&gt;
  /// &lt;script&gt; let app = angular.module('demo', []) .controller('WelcomeController', ['$scope',
  /// function($scope) { $scope.greeting = 'Welcome!'; }]); angular.bootstrap(document, ['demo']);
  /// &lt;/script&gt; &lt;/body&gt; &lt;/html&gt; ```
  ///
  /// Parameters:
  /// - [element]: DOM element which is the root of AngularTS application.
  /// - [modules]: an array of modules to load into the application. Each item in the array should be the name of a predefined module or a (DI annotated) function that will be invoked by the injector as a `config` block. See `angular.module()`.
  Object? bootstrap(Object? element, [Object? modules = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    if (!identical(modules, _undefinedArgument)) {
      args.add(unsafe.dartToJs(modules));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('bootstrap'.toJS, args));
  }

  /// Create a standalone injector without bootstrapping the DOM.
  ///
  /// Parameters:
  /// - [modules]: Module names or config functions to load.
  Object? injector(Object? modules) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(modules));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('injector'.toJS, args));
  }

  /// Find `ng-app` roots under the provided element and bootstrap them. The first root uses this
  /// instance. Additional roots are bootstrapped as sub-applications and stored in subapps.
  ///
  /// Parameters:
  /// - [element]: Root element or document to scan.
  void init(Object? element) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('init'.toJS, args));
  }

  /// Find a scope by its registered `scopeName`.
  ///
  /// Parameters:
  /// - [name]: Scope name to search for.
  Object? getScopeByName(String name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getScopeByName'.toJS, args));
  }
}

/// Runtime metadata returned after defining a standalone custom element.
///
/// Raw JavaScript facade for `ng.AngularElementDefinition`.
base class GeneratedNgAngularElementDefinition extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AngularElementDefinition.
  const GeneratedNgAngularElementDefinition(super.raw);

  /// AngularTS runtime instance that owns the element injector.
  Object? get angular =>
      unsafe.jsToDart<Object?>(raw.getProperty('angular'.toJS));

  /// Custom runtime `ng` module.
  Object? get ngModule =>
      unsafe.jsToDart<Object?>(raw.getProperty('ngModule'.toJS));

  /// Application module that registered the element.
  Object? get elementModule =>
      unsafe.jsToDart<Object?>(raw.getProperty('elementModule'.toJS));

  /// Injector used by all instances of this custom element definition.
  Object? get injector =>
      unsafe.jsToDart<Object?>(raw.getProperty('injector'.toJS));

  /// Native custom element constructor registered with `customElements`.
  Object? get element =>
      unsafe.jsToDart<Object?>(raw.getProperty('element'.toJS));

  /// Registered custom element tag name.
  String get name => _toDartString(raw.getProperty('name'.toJS))!;

  /// Registered custom element tag name.
  set name(String value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }
}

/// Configuration for the application module that owns the custom element.
///
/// Raw JavaScript facade for `ng.AngularElementModuleOptions`.
base class GeneratedNgAngularElementModuleOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AngularElementModuleOptions.
  const GeneratedNgAngularElementModuleOptions(super.raw);

  /// Name of the element application module. Defaults to a name derived from the tag.
  String? get name => _toDartString(raw.getProperty('name'.toJS));

  /// Name of the element application module. Defaults to a name derived from the tag.
  set name(String? value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// Additional application modules required by the element module.
  Object? get requires =>
      unsafe.jsToDart<Object?>(raw.getProperty('requires'.toJS));

  /// Optional hook for adding services, filters, directives, or config to the element module.
  Object? get configure =>
      unsafe.jsToDart<Object?>(raw.getProperty('configure'.toJS));
}

/// Options for a standalone AngularTS-backed custom element runtime.
///
/// Raw JavaScript facade for `ng.AngularElementOptions`.
base class GeneratedNgAngularElementOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AngularElementOptions.
  const GeneratedNgAngularElementOptions(super.raw);

  /// Application module that registers the custom element.
  Object? get elementModule =>
      unsafe.jsToDart<Object?>(raw.getProperty('elementModule'.toJS));

  /// App component definition passed to `$webComponent.defineAppComponent`.
  Object? get component =>
      unsafe.jsToDart<Object?>(raw.getProperty('component'.toJS));

  /// Treat this runtime as a sub-application of the current host runtime.
  bool? get subapp => _toDartBool(raw.getProperty('subapp'.toJS));

  /// Treat this runtime as a sub-application of the current host runtime.
  set subapp(bool? value) {
    raw.setProperty('subapp'.toJS, unsafe.dartToJs(value));
  }

  /// Framework modules included by the composed `ng` module.
  Object? get modules =>
      unsafe.jsToDart<Object?>(raw.getProperty('modules'.toJS));

  /// Name of the composed root module. Defaults to `ng`.
  String? get name => _toDartString(raw.getProperty('name'.toJS));

  /// Name of the composed root module. Defaults to `ng`.
  set name(String? value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// Additional application modules required by the composed root module.
  Object? get requires =>
      unsafe.jsToDart<Object?>(raw.getProperty('requires'.toJS));

  /// Additional providers registered in the composed root module.
  Object? get providers =>
      unsafe.jsToDart<Object?>(raw.getProperty('providers'.toJS));

  /// Services registered in the composed root module.
  Object? get services =>
      unsafe.jsToDart<Object?>(raw.getProperty('services'.toJS));

  /// Filters registered in the composed root module.
  Object? get filters =>
      unsafe.jsToDart<Object?>(raw.getProperty('filters'.toJS));

  /// Directives registered in the composed root module.
  Object? get directives =>
      unsafe.jsToDart<Object?>(raw.getProperty('directives'.toJS));
}

/// Main AngularTS runtime entry point with the full built-in `ng` module configured by default.
///
/// Raw JavaScript facade for `ng.AngularService`.
base class GeneratedNgAngularService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AngularService.
  const GeneratedNgAngularService(super.raw);

  /// Base class for user-authored AngularTS custom elements.
  Object? get ScopeElement =>
      unsafe.jsToDart<Object?>(raw.getProperty('ScopeElement'.toJS));

  /// JSX-free real-DOM tag factories for programmatic component views.
  Object? get tags => unsafe.jsToDart<Object?>(raw.getProperty('tags'.toJS));

  /// Explicit programmatic-view binding and element helpers.
  Object? get view => unsafe.jsToDart<Object?>(raw.getProperty('view'.toJS));

  /// Sub-application instances created when multiple `ng-app` roots are initialized.
  Object? get subapps =>
      unsafe.jsToDart<Object?>(raw.getProperty('subapps'.toJS));

  /// Application-wide event bus, available after bootstrap providers are created.
  Object? get eventBus =>
      unsafe.jsToDart<Object?>(raw.getProperty('eventBus'.toJS));

  /// Application injector, available after `bootstrap()` or `injector()` completes.
  Object? get currentInjector =>
      unsafe.jsToDart<Object?>(raw.getProperty('currentInjector'.toJS));

  /// Root scope for the bootstrapped application.
  Object? get rootScope =>
      unsafe.jsToDart<Object?>(raw.getProperty('rootScope'.toJS));

  /// AngularTS version string replaced at build time.
  String get version => _toDartString(raw.getProperty('version'.toJS))!;

  /// AngularTS version string replaced at build time.
  set version(String value) {
    raw.setProperty('version'.toJS, unsafe.dartToJs(value));
  }

  /// Retrieve the controller instance cached on a compiled DOM element.
  Object? get getController =>
      unsafe.jsToDart<Object?>(raw.getProperty('getController'.toJS));

  /// Retrieve the injector cached on a bootstrapped DOM element.
  Object? get getInjector =>
      unsafe.jsToDart<Object?>(raw.getProperty('getInjector'.toJS));

  /// Retrieve the scope cached on a compiled DOM element.
  Object? get getScope =>
      unsafe.jsToDart<Object?>(raw.getProperty('getScope'.toJS));

  /// Read an element attribute by normalized directive-style name.
  Object? get getNormalizedAttr =>
      unsafe.jsToDart<Object?>(raw.getProperty('getNormalizedAttr'.toJS));

  /// Return the actual DOM attribute name for a normalized directive-style name.
  Object? get getNormalizedAttrName =>
      unsafe.jsToDart<Object?>(raw.getProperty('getNormalizedAttrName'.toJS));

  /// Return whether an element has an attribute matching a normalized name.
  Object? get hasNormalizedAttr =>
      unsafe.jsToDart<Object?>(raw.getProperty('hasNormalizedAttr'.toJS));

  /// Configure how values embedded in framework error messages are formatted.
  Object? get errorFormattingConfig =>
      unsafe.jsToDart<Object?>(raw.getProperty('errorFormattingConfig'.toJS));

  /// Public injection token names keyed by token value.
  Object? get tokens =>
      unsafe.jsToDart<Object?>(raw.getProperty('tokens'.toJS));

  /// Registers the configured built-in `ng` module for this runtime instance.
  Object? registerNgModule() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('registerNgModule'.toJS, const <JSAny?>[]));
  }

  /// The `angular.module` is a global place for creating, registering and retrieving AngularTS
  /// modules. All modules (AngularTS core or 3rd party) that should be available to an application
  /// must be registered using this mechanism. Passing one argument retrieves an existing ng.NgModule,
  /// whereas passing more than one argument creates a new ng.NgModule # Module A module is a
  /// collection of services, directives, controllers, filters, workers, WebAssembly modules, and
  /// configuration information. `angular.module` is used to configure the auto.$injector `$injector`.
  /// ```js // Create a new module let myModule = angular.module('myModule', []); // register a new
  /// service myModule.value('appName', 'MyCoolApp'); // configure built-in services with typed object
  /// config. myModule.config({ location: { hashPrefix: '!', }, }); ``` Then you can create an
  /// injector and load your modules like this: ```js let injector = angular.injector(['ng',
  /// 'myModule']) ``` However it's more likely that you'll use the `ng-app` directive or
  /// `bootstrap()` to simplify this process.
  ///
  /// Parameters:
  /// - [name]: The name of the module to create or retrieve.
  /// - [requires]: If specified then new module is being created. If unspecified then the module is being retrieved for further configuration.
  /// - [configFn]: Optional configuration function for the module that gets passed to `NgModule.config()`.
  Object? module(String name,
      [Object? requires = _undefinedArgument,
      Object? configFn = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(requires, _undefinedArgument)) {
      args.add(unsafe.dartToJs(requires));
    }
    if (!identical(configFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(configFn));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('module'.toJS, args));
  }

  /// Dispatches an invocation event to either an injectable service or a named scope. The event
  /// `type` identifies the target and the payload contains the expression to evaluate against that
  /// target.
  ///
  /// Parameters:
  /// - [event]: Value supplied for the event parameter.
  bool dispatchEvent(Event event) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(event));

    return _toDartBool(raw.callMethodVarArgs('dispatchEvent'.toJS, args))!;
  }

  /// Fire-and-forget. Accepts a single string: `"&lt;target&gt;.&lt;expression&gt;"`
  ///
  /// Parameters:
  /// - [input]: Value supplied for the input parameter.
  void emit(String input) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(input));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('emit'.toJS, args));
  }

  /// Await result. Accepts a single string: `"&lt;target&gt;.&lt;expression&gt;"`
  ///
  /// Parameters:
  /// - [input]: Value supplied for the input parameter.
  JSPromise<JSAny?> call(String input) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(input));

    return raw.callMethodVarArgs('call'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Use this function to manually start up AngularTS application. AngularTS will detect if it has
  /// been loaded into the browser more than once and only allow the first loaded script to be
  /// bootstrapped and will report a warning to the browser console for each of the subsequent
  /// scripts. This prevents strange results in applications, where otherwise multiple instances of
  /// AngularTS try to work on the DOM. **Note:** Do not bootstrap the app on an element with a
  /// directive that uses transclusion, such as `ng-if`, `ng-include`, or `ng-view`. Doing this
  /// misplaces the app root element and injector, causing animations to stop working and making the
  /// injector inaccessible from outside the app. ```html &lt;!doctype html&gt; &lt;html&gt; &lt;body&gt; &lt;div
  /// ng-controller="WelcomeController"&gt; {{greeting}} &lt;/div&gt; &lt;script src="angular.js"&gt;&lt;/script&gt;
  /// &lt;script&gt; let app = angular.module('demo', []) .controller('WelcomeController', ['$scope',
  /// function($scope) { $scope.greeting = 'Welcome!'; }]); angular.bootstrap(document, ['demo']);
  /// &lt;/script&gt; &lt;/body&gt; &lt;/html&gt; ```
  ///
  /// Parameters:
  /// - [element]: DOM element which is the root of AngularTS application.
  /// - [modules]: an array of modules to load into the application. Each item in the array should be the name of a predefined module or a (DI annotated) function that will be invoked by the injector as a `config` block. See `angular.module()`.
  Object? bootstrap(Object? element, [Object? modules = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    if (!identical(modules, _undefinedArgument)) {
      args.add(unsafe.dartToJs(modules));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('bootstrap'.toJS, args));
  }

  /// Create a standalone injector without bootstrapping the DOM.
  ///
  /// Parameters:
  /// - [modules]: Module names or config functions to load.
  Object? injector(Object? modules) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(modules));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('injector'.toJS, args));
  }

  /// Find `ng-app` roots under the provided element and bootstrap them. The first root uses this
  /// instance. Additional roots are bootstrapped as sub-applications and stored in subapps.
  ///
  /// Parameters:
  /// - [element]: Root element or document to scan.
  void init(Object? element) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('init'.toJS, args));
  }

  /// Find a scope by its registered `scopeName`.
  ///
  /// Parameters:
  /// - [name]: Scope name to search for.
  Object? getScopeByName(String name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getScopeByName'.toJS, args));
  }
}

/// Public AngularTS AnimateService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnimateService`.
base class GeneratedNgAnimateService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnimateService.
  const GeneratedNgAnimateService(super.raw);

  /// The cancel member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [handle]: Value supplied for the handle parameter.
  void cancel([Object? handle = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(handle, _undefinedArgument)) {
      args.add(unsafe.dartToJs(handle));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('cancel'.toJS, args));
  }

  /// The define member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [preset]: Value supplied for the preset parameter.
  void define(String name, Object? preset) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(preset));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('define'.toJS, args));
  }

  /// The enter member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [element]: Value supplied for the element parameter.
  /// - [parent]: Value supplied for the parent parameter.
  /// - [after]: Value supplied for the after parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? enter(Element element,
      [Object? parent = _undefinedArgument,
      Object? after = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    if (!identical(parent, _undefinedArgument)) {
      args.add(unsafe.dartToJs(parent));
    }
    if (!identical(after, _undefinedArgument)) {
      args.add(unsafe.dartToJs(after));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('enter'.toJS, args));
  }

  /// The move member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [element]: Value supplied for the element parameter.
  /// - [parent]: Value supplied for the parent parameter.
  /// - [after]: Value supplied for the after parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? move(Element element, Object? parent,
      [Object? after = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    args.add(unsafe.dartToJs(parent));
    if (!identical(after, _undefinedArgument)) {
      args.add(unsafe.dartToJs(after));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('move'.toJS, args));
  }

  /// The leave member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [element]: Value supplied for the element parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? leave(Element element, [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('leave'.toJS, args));
  }

  /// The addClass member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [element]: Value supplied for the element parameter.
  /// - [className]: Value supplied for the className parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? addClass(Element element, String className,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    args.add(unsafe.dartToJs(className));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('addClass'.toJS, args));
  }

  /// The removeClass member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [element]: Value supplied for the element parameter.
  /// - [className]: Value supplied for the className parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? removeClass(Element element, String className,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    args.add(unsafe.dartToJs(className));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('removeClass'.toJS, args));
  }

  /// The setClass member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [element]: Value supplied for the element parameter.
  /// - [add]: Value supplied for the add parameter.
  /// - [remove]: Value supplied for the remove parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? setClass(Element element, String add, String remove,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    args.add(unsafe.dartToJs(add));
    args.add(unsafe.dartToJs(remove));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('setClass'.toJS, args));
  }

  /// The animate member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [element]: Value supplied for the element parameter.
  /// - [from]: Value supplied for the from parameter.
  /// - [to]: Value supplied for the to parameter.
  /// - [className]: Value supplied for the className parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? animate(Element element, Object? from,
      [Object? to = _undefinedArgument,
      Object? className = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    args.add(unsafe.dartToJs(from));
    if (!identical(to, _undefinedArgument)) {
      args.add(unsafe.dartToJs(to));
    }
    if (!identical(className, _undefinedArgument)) {
      args.add(unsafe.dartToJs(className));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('animate'.toJS, args));
  }

  /// The transition member of ng.AnimateService.
  ///
  /// Parameters:
  /// - [update]: Value supplied for the update parameter.
  JSPromise<JSAny?> transition(JSFunction update) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(update));

    return raw.callMethodVarArgs('transition'.toJS, args) as JSPromise<JSAny?>;
  }
}

/// Public AngularTS AnimationContext contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnimationContext`.
base class GeneratedNgAnimationContext extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnimationContext.
  const GeneratedNgAnimationContext(super.raw);

  /// The signal member of ng.AnimationContext.
  AbortSignal get signal =>
      unsafe.jsToDart<AbortSignal>(raw.getProperty('signal'.toJS));

  /// The phase member of ng.AnimationContext.
  String get phase => _toDartString(raw.getProperty('phase'.toJS))!;

  /// The phase member of ng.AnimationContext.
  set phase(String value) {
    raw.setProperty('phase'.toJS, unsafe.dartToJs(value));
  }

  /// The className member of ng.AnimationContext.
  String? get className => _toDartString(raw.getProperty('className'.toJS));

  /// The className member of ng.AnimationContext.
  set className(String? value) {
    raw.setProperty('className'.toJS, unsafe.dartToJs(value));
  }

  /// The addClass member of ng.AnimationContext.
  String? get addClass => _toDartString(raw.getProperty('addClass'.toJS));

  /// The addClass member of ng.AnimationContext.
  set addClass(String? value) {
    raw.setProperty('addClass'.toJS, unsafe.dartToJs(value));
  }

  /// The removeClass member of ng.AnimationContext.
  String? get removeClass => _toDartString(raw.getProperty('removeClass'.toJS));

  /// The removeClass member of ng.AnimationContext.
  set removeClass(String? value) {
    raw.setProperty('removeClass'.toJS, unsafe.dartToJs(value));
  }

  /// The from member of ng.AnimationContext.
  Object? get from => unsafe.jsToDart<Object?>(raw.getProperty('from'.toJS));

  /// The to member of ng.AnimationContext.
  Object? get to => unsafe.jsToDart<Object?>(raw.getProperty('to'.toJS));
}

/// Public AngularTS AnimationHandle contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnimationHandle`.
base class GeneratedNgAnimationHandle extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnimationHandle.
  const GeneratedNgAnimationHandle(super.raw);

  /// The controller member of ng.AnimationHandle.
  Object? get controller =>
      unsafe.jsToDart<Object?>(raw.getProperty('controller'.toJS));

  /// The finished member of ng.AnimationHandle.
  JSPromise<JSAny?> get finished =>
      raw.getProperty('finished'.toJS) as JSPromise<JSAny?>;

  /// Attaches callbacks for the resolution and/or rejection of the Promise.
  ///
  /// Parameters:
  /// - [onfulfilled]: Value supplied for the onfulfilled parameter.
  /// - [onrejected]: Value supplied for the onrejected parameter.
  JSPromise<JSAny?> then(
      [Object? onfulfilled = _undefinedArgument,
      Object? onrejected = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(onfulfilled, _undefinedArgument)) {
      args.add(unsafe.dartToJs(onfulfilled));
    }
    if (!identical(onrejected, _undefinedArgument)) {
      args.add(unsafe.dartToJs(onrejected));
    }

    return raw.callMethodVarArgs('then'.toJS, args) as JSPromise<JSAny?>;
  }

  /// The catch member of ng.AnimationHandle.
  ///
  /// Parameters:
  /// - [onrejected]: Value supplied for the onrejected parameter.
  JSPromise<JSAny?> catchMember([Object? onrejected = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(onrejected, _undefinedArgument)) {
      args.add(unsafe.dartToJs(onrejected));
    }

    return raw.callMethodVarArgs('catch'.toJS, args) as JSPromise<JSAny?>;
  }

  /// The finally member of ng.AnimationHandle.
  ///
  /// Parameters:
  /// - [onfinally]: Value supplied for the onfinally parameter.
  JSPromise<JSAny?> finallyMember([Object? onfinally = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(onfinally, _undefinedArgument)) {
      args.add(unsafe.dartToJs(onfinally));
    }

    return raw.callMethodVarArgs('finally'.toJS, args) as JSPromise<JSAny?>;
  }

  /// The done member of ng.AnimationHandle.
  ///
  /// Parameters:
  /// - [callback]: Value supplied for the callback parameter.
  void done(JSFunction callback) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(callback));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('done'.toJS, args));
  }

  /// The cancel member of ng.AnimationHandle.
  void cancel() {
    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('cancel'.toJS, const <JSAny?>[]));
  }

  /// The finish member of ng.AnimationHandle.
  void finish() {
    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('finish'.toJS, const <JSAny?>[]));
  }

  /// The pause member of ng.AnimationHandle.
  void pause() {
    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('pause'.toJS, const <JSAny?>[]));
  }

  /// The play member of ng.AnimationHandle.
  void play() {
    unsafe.jsToDart<void>(raw.callMethodVarArgs('play'.toJS, const <JSAny?>[]));
  }

  /// The complete member of ng.AnimationHandle.
  ///
  /// Parameters:
  /// - [status]: Value supplied for the status parameter.
  void complete([Object? status = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(status, _undefinedArgument)) {
      args.add(unsafe.dartToJs(status));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('complete'.toJS, args));
  }
}

/// Public AngularTS AnimationLifecycleCallback contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnimationLifecycleCallback`.
base class GeneratedNgAnimationLifecycleCallback extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnimationLifecycleCallback.
  const GeneratedNgAnimationLifecycleCallback(super.raw);

  /// Calls the ng.AnimationLifecycleCallback function.
  ///
  /// Parameters:
  /// - [element]: Value supplied for the element parameter.
  /// - [context]: Value supplied for the context parameter.
  void call(Element element, Object? context) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    args.add(unsafe.dartToJs(context));

    unsafe.jsToDart<void>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS AnimationOptions contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnimationOptions`.
base class GeneratedNgAnimationOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnimationOptions.
  const GeneratedNgAnimationOptions(super.raw);

  /// The animation member of ng.AnimationOptions.
  String? get animation => _toDartString(raw.getProperty('animation'.toJS));

  /// The animation member of ng.AnimationOptions.
  set animation(String? value) {
    raw.setProperty('animation'.toJS, unsafe.dartToJs(value));
  }

  /// The keyframes member of ng.AnimationOptions.
  Object? get keyframes =>
      unsafe.jsToDart<Object?>(raw.getProperty('keyframes'.toJS));

  /// The enter member of ng.AnimationOptions.
  Object? get enter => unsafe.jsToDart<Object?>(raw.getProperty('enter'.toJS));

  /// The leave member of ng.AnimationOptions.
  Object? get leave => unsafe.jsToDart<Object?>(raw.getProperty('leave'.toJS));

  /// The move member of ng.AnimationOptions.
  Object? get move => unsafe.jsToDart<Object?>(raw.getProperty('move'.toJS));

  /// The addClass member of ng.AnimationOptions.
  String? get addClass => _toDartString(raw.getProperty('addClass'.toJS));

  /// The addClass member of ng.AnimationOptions.
  set addClass(String? value) {
    raw.setProperty('addClass'.toJS, unsafe.dartToJs(value));
  }

  /// The removeClass member of ng.AnimationOptions.
  String? get removeClass => _toDartString(raw.getProperty('removeClass'.toJS));

  /// The removeClass member of ng.AnimationOptions.
  set removeClass(String? value) {
    raw.setProperty('removeClass'.toJS, unsafe.dartToJs(value));
  }

  /// The from member of ng.AnimationOptions.
  Object? get from => unsafe.jsToDart<Object?>(raw.getProperty('from'.toJS));

  /// The to member of ng.AnimationOptions.
  Object? get to => unsafe.jsToDart<Object?>(raw.getProperty('to'.toJS));

  /// The tempClasses member of ng.AnimationOptions.
  Object? get tempClasses =>
      unsafe.jsToDart<Object?>(raw.getProperty('tempClasses'.toJS));

  /// The onStart member of ng.AnimationOptions.
  Object? get onStart =>
      unsafe.jsToDart<Object?>(raw.getProperty('onStart'.toJS));

  /// The onDone member of ng.AnimationOptions.
  Object? get onDone =>
      unsafe.jsToDart<Object?>(raw.getProperty('onDone'.toJS));

  /// The onCancel member of ng.AnimationOptions.
  Object? get onCancel =>
      unsafe.jsToDart<Object?>(raw.getProperty('onCancel'.toJS));
}

/// Public AngularTS AnimationPhase contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnimationPhase`.
base class GeneratedNgAnimationPhase extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnimationPhase.
  const GeneratedNgAnimationPhase(super.raw);
}

/// Public AngularTS AnimationPreset contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnimationPreset`.
base class GeneratedNgAnimationPreset extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnimationPreset.
  const GeneratedNgAnimationPreset(super.raw);

  /// The enter member of ng.AnimationPreset.
  Object? get enter => unsafe.jsToDart<Object?>(raw.getProperty('enter'.toJS));

  /// The leave member of ng.AnimationPreset.
  Object? get leave => unsafe.jsToDart<Object?>(raw.getProperty('leave'.toJS));

  /// The move member of ng.AnimationPreset.
  Object? get move => unsafe.jsToDart<Object?>(raw.getProperty('move'.toJS));

  /// The addClass member of ng.AnimationPreset.
  Object? get addClass =>
      unsafe.jsToDart<Object?>(raw.getProperty('addClass'.toJS));

  /// The removeClass member of ng.AnimationPreset.
  Object? get removeClass =>
      unsafe.jsToDart<Object?>(raw.getProperty('removeClass'.toJS));

  /// The setClass member of ng.AnimationPreset.
  Object? get setClass =>
      unsafe.jsToDart<Object?>(raw.getProperty('setClass'.toJS));

  /// The animate member of ng.AnimationPreset.
  Object? get animate =>
      unsafe.jsToDart<Object?>(raw.getProperty('animate'.toJS));

  /// The options member of ng.AnimationPreset.
  Object? get options =>
      unsafe.jsToDart<Object?>(raw.getProperty('options'.toJS));
}

/// Public AngularTS AnimationPresetHandler contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnimationPresetHandler`.
base class GeneratedNgAnimationPresetHandler extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnimationPresetHandler.
  const GeneratedNgAnimationPresetHandler(super.raw);

  /// Calls the ng.AnimationPresetHandler function.
  ///
  /// Parameters:
  /// - [element]: Value supplied for the element parameter.
  /// - [context]: Value supplied for the context parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? call(Element element, Object? context, Object? options) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(element));
    args.add(unsafe.dartToJs(context));
    args.add(unsafe.dartToJs(options));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS AnimationResult contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AnimationResult`.
base class GeneratedNgAnimationResult extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AnimationResult.
  const GeneratedNgAnimationResult(super.raw);
}

/// Public AngularTS AppComponentOptions contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AppComponentOptions`.
base class GeneratedNgAppComponentOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AppComponentOptions.
  const GeneratedNgAppComponentOptions(super.raw);

  /// Template compiled into the host or shadow root.
  String? get template => _toDartString(raw.getProperty('template'.toJS));

  /// Template compiled into the host or shadow root.
  set template(String? value) {
    raw.setProperty('template'.toJS, unsafe.dartToJs(value));
  }

  /// Enables shadow DOM, or passes ShadowRootInit options.
  Object? get shadow =>
      unsafe.jsToDart<Object?>(raw.getProperty('shadow'.toJS));

  /// Initial scope state, or a factory returning it.
  Object? get scope => unsafe.jsToDart<Object?>(raw.getProperty('scope'.toJS));

  /// Declared DOM attributes/properties that sync into the scope.
  Object? get inputs =>
      unsafe.jsToDart<Object?>(raw.getProperty('inputs'.toJS));

  /// Use an isolate child scope instead of inheriting parent properties.
  bool? get isolate => _toDartBool(raw.getProperty('isolate'.toJS));

  /// Use an isolate child scope instead of inheriting parent properties.
  set isolate(bool? value) {
    raw.setProperty('isolate'.toJS, unsafe.dartToJs(value));
  }

  /// Called after the scope exists and the template has been linked.
  Object? get connected =>
      unsafe.jsToDart<Object?>(raw.getProperty('connected'.toJS));

  /// Called before the scope is destroyed.
  Object? get disconnected =>
      unsafe.jsToDart<Object?>(raw.getProperty('disconnected'.toJS));

  /// Called after an observed input attribute changes.
  Object? get attributeChanged =>
      unsafe.jsToDart<Object?>(raw.getProperty('attributeChanged'.toJS));
}

/// Public AngularTS AriaConfig contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AriaConfig`.
base class GeneratedNgAriaConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AriaConfig.
  const GeneratedNgAriaConfig(super.raw);

  /// The ariaHidden member of ng.AriaConfig.
  bool get ariaHidden => _toDartBool(raw.getProperty('ariaHidden'.toJS))!;

  /// The ariaHidden member of ng.AriaConfig.
  set ariaHidden(bool value) {
    raw.setProperty('ariaHidden'.toJS, unsafe.dartToJs(value));
  }

  /// The ariaChecked member of ng.AriaConfig.
  bool get ariaChecked => _toDartBool(raw.getProperty('ariaChecked'.toJS))!;

  /// The ariaChecked member of ng.AriaConfig.
  set ariaChecked(bool value) {
    raw.setProperty('ariaChecked'.toJS, unsafe.dartToJs(value));
  }

  /// The ariaReadonly member of ng.AriaConfig.
  bool get ariaReadonly => _toDartBool(raw.getProperty('ariaReadonly'.toJS))!;

  /// The ariaReadonly member of ng.AriaConfig.
  set ariaReadonly(bool value) {
    raw.setProperty('ariaReadonly'.toJS, unsafe.dartToJs(value));
  }

  /// The ariaDisabled member of ng.AriaConfig.
  bool get ariaDisabled => _toDartBool(raw.getProperty('ariaDisabled'.toJS))!;

  /// The ariaDisabled member of ng.AriaConfig.
  set ariaDisabled(bool value) {
    raw.setProperty('ariaDisabled'.toJS, unsafe.dartToJs(value));
  }

  /// The ariaRequired member of ng.AriaConfig.
  bool get ariaRequired => _toDartBool(raw.getProperty('ariaRequired'.toJS))!;

  /// The ariaRequired member of ng.AriaConfig.
  set ariaRequired(bool value) {
    raw.setProperty('ariaRequired'.toJS, unsafe.dartToJs(value));
  }

  /// The ariaInvalid member of ng.AriaConfig.
  bool get ariaInvalid => _toDartBool(raw.getProperty('ariaInvalid'.toJS))!;

  /// The ariaInvalid member of ng.AriaConfig.
  set ariaInvalid(bool value) {
    raw.setProperty('ariaInvalid'.toJS, unsafe.dartToJs(value));
  }

  /// The ariaValue member of ng.AriaConfig.
  bool get ariaValue => _toDartBool(raw.getProperty('ariaValue'.toJS))!;

  /// The ariaValue member of ng.AriaConfig.
  set ariaValue(bool value) {
    raw.setProperty('ariaValue'.toJS, unsafe.dartToJs(value));
  }

  /// The ariaCurrent member of ng.AriaConfig.
  bool get ariaCurrent => _toDartBool(raw.getProperty('ariaCurrent'.toJS))!;

  /// The ariaCurrent member of ng.AriaConfig.
  set ariaCurrent(bool value) {
    raw.setProperty('ariaCurrent'.toJS, unsafe.dartToJs(value));
  }

  /// The ariaCurrentToken member of ng.AriaConfig.
  String get ariaCurrentToken =>
      _toDartString(raw.getProperty('ariaCurrentToken'.toJS))!;

  /// The ariaCurrentToken member of ng.AriaConfig.
  set ariaCurrentToken(String value) {
    raw.setProperty('ariaCurrentToken'.toJS, unsafe.dartToJs(value));
  }

  /// The tabindex member of ng.AriaConfig.
  bool get tabindex => _toDartBool(raw.getProperty('tabindex'.toJS))!;

  /// The tabindex member of ng.AriaConfig.
  set tabindex(bool value) {
    raw.setProperty('tabindex'.toJS, unsafe.dartToJs(value));
  }

  /// The bindKeydown member of ng.AriaConfig.
  bool get bindKeydown => _toDartBool(raw.getProperty('bindKeydown'.toJS))!;

  /// The bindKeydown member of ng.AriaConfig.
  set bindKeydown(bool value) {
    raw.setProperty('bindKeydown'.toJS, unsafe.dartToJs(value));
  }

  /// The bindRoleForClick member of ng.AriaConfig.
  bool get bindRoleForClick =>
      _toDartBool(raw.getProperty('bindRoleForClick'.toJS))!;

  /// The bindRoleForClick member of ng.AriaConfig.
  set bindRoleForClick(bool value) {
    raw.setProperty('bindRoleForClick'.toJS, unsafe.dartToJs(value));
  }

  /// The bindRoleForState member of ng.AriaConfig.
  bool get bindRoleForState =>
      _toDartBool(raw.getProperty('bindRoleForState'.toJS))!;

  /// The bindRoleForState member of ng.AriaConfig.
  set bindRoleForState(bool value) {
    raw.setProperty('bindRoleForState'.toJS, unsafe.dartToJs(value));
  }

  /// The diagnostics member of ng.AriaConfig.
  bool get diagnostics => _toDartBool(raw.getProperty('diagnostics'.toJS))!;

  /// The diagnostics member of ng.AriaConfig.
  set diagnostics(bool value) {
    raw.setProperty('diagnostics'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS AriaService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.AriaService`.
base class GeneratedNgAriaService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.AriaService.
  const GeneratedNgAriaService(super.raw);

  /// The config member of ng.AriaService.
  ///
  /// Parameters:
  /// - [key]: Value supplied for the key parameter.
  Object? config(Object? key) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('config'.toJS, args));
  }
}

/// Configuration for CachedRestBackend.
///
/// Raw JavaScript facade for `ng.CachedRestBackendOptions`.
base class GeneratedNgCachedRestBackendOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.CachedRestBackendOptions.
  const GeneratedNgCachedRestBackendOptions(super.raw);

  /// Backend used for authoritative network responses and writes.
  Object? get network =>
      unsafe.jsToDart<Object?>(raw.getProperty('network'.toJS));

  /// Async cache store, such as IndexedDB, Cache API, or memory.
  Object? get cache => unsafe.jsToDart<Object?>(raw.getProperty('cache'.toJS));

  /// Default read strategy used for cacheable GET requests.
  String? get strategy => _toDartString(raw.getProperty('strategy'.toJS));

  /// Default read strategy used for cacheable GET requests.
  set strategy(String? value) {
    raw.setProperty('strategy'.toJS, unsafe.dartToJs(value));
  }

  /// Runtime policy used to choose the read strategy for each cacheable request.
  Object? get policy =>
      unsafe.jsToDart<Object?>(raw.getProperty('policy'.toJS));

  /// Notified after a stale-while-revalidate refresh succeeds.
  Object? get onRevalidate =>
      unsafe.jsToDart<Object?>(raw.getProperty('onRevalidate'.toJS));
}

/// Public shape accepted by `ng-class` for class binding expressions.
///
/// Raw JavaScript facade for `ng.ClassValue`.
base class GeneratedNgClassValue extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ClassValue.
  const GeneratedNgClassValue(super.raw);
}

/// Entry point for the `$compile` service.
///
/// Raw JavaScript facade for `ng.CompileService`.
base class GeneratedNgCompileService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.CompileService.
  const GeneratedNgCompileService(super.raw);

  /// Calls the ng.CompileService function.
  ///
  /// Parameters:
  /// - [compileNode]: Value supplied for the compileNode parameter.
  /// - [transcludeFn]: Value supplied for the transcludeFn parameter.
  /// - [maxPriority]: Value supplied for the maxPriority parameter.
  /// - [ignoreDirective]: Value supplied for the ignoreDirective parameter.
  /// - [previousCompileContext]: Value supplied for the previousCompileContext parameter.
  Object? call(Object? compileNode,
      [Object? transcludeFn = _undefinedArgument,
      Object? maxPriority = _undefinedArgument,
      Object? ignoreDirective = _undefinedArgument,
      Object? previousCompileContext = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(compileNode));
    if (!identical(transcludeFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(transcludeFn));
    }
    if (!identical(maxPriority, _undefinedArgument)) {
      args.add(unsafe.dartToJs(maxPriority));
    }
    if (!identical(ignoreDirective, _undefinedArgument)) {
      args.add(unsafe.dartToJs(ignoreDirective));
    }
    if (!identical(previousCompileContext, _undefinedArgument)) {
      args.add(unsafe.dartToJs(previousCompileContext));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Defines a component's configuration object (a simplified directive definition object).
///
/// Raw JavaScript facade for `ng.Component`.
base class GeneratedNgComponent extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Component.
  const GeneratedNgComponent(super.raw);

  /// The controller member of ng.Component.
  Object? get controller =>
      unsafe.jsToDart<Object?>(raw.getProperty('controller'.toJS));

  /// An identifier name for a reference to the controller. If present, the controller will be
  /// published to its scope under the specified name. If not present, this will default to '$ctrl'.
  String? get controllerAs =>
      _toDartString(raw.getProperty('controllerAs'.toJS));

  /// An identifier name for a reference to the controller. If present, the controller will be
  /// published to its scope under the specified name. If not present, this will default to '$ctrl'.
  set controllerAs(String? value) {
    raw.setProperty('controllerAs'.toJS, unsafe.dartToJs(value));
  }

  /// html template as a string or a function that returns an html template as a string which should
  /// be used as the contents of this component. Empty string by default. If template is a function,
  /// then it is injected with the following locals: $element - Current element Use the array form to
  /// define dependencies.
  Object? get template =>
      unsafe.jsToDart<Object?>(raw.getProperty('template'.toJS));

  /// Path or function that returns a path to an html template that should be used as the contents of
  /// this component. If templateUrl is a function, then it is injected with the following locals:
  /// $element - Current element Use the array form to define dependencies.
  Object? get templateUrl =>
      unsafe.jsToDart<Object?>(raw.getProperty('templateUrl'.toJS));

  /// Programmatic real-DOM view factory. It runs during linking after controller bindings and
  /// `onInit`, and is mutually exclusive with template, templateUrl, and replace.
  Object? get view => unsafe.jsToDart<Object?>(raw.getProperty('view'.toJS));

  /// Define DOM attribute binding to component properties. Component properties are always bound to
  /// the component controller and not to the scope.
  Object? get bindings =>
      unsafe.jsToDart<Object?>(raw.getProperty('bindings'.toJS));

  /// Replaces the generated component host element with the component template.
  bool? get replace => _toDartBool(raw.getProperty('replace'.toJS));

  /// Replaces the generated component host element with the component template.
  set replace(bool? value) {
    raw.setProperty('replace'.toJS, unsafe.dartToJs(value));
  }

  /// Whether transclusion is enabled. Disabled by default.
  Object? get transclude =>
      unsafe.jsToDart<Object?>(raw.getProperty('transclude'.toJS));

  /// Requires the controllers of other directives and binds them to this component's controller. The
  /// object keys specify the property names under which the required controllers (object values) will
  /// be bound. Note that the required controllers will not be available during the instantiation of
  /// the controller, but they are guaranteed to be available just before the onInit method is
  /// executed!
  Object? get require =>
      unsafe.jsToDart<Object?>(raw.getProperty('require'.toJS));
}

/// Component registration accepted by `NgModule.component()`. Programmatic and template-based
/// rendering strategies are mutually exclusive.
///
/// Raw JavaScript facade for `ng.ComponentDefinition`.
base class GeneratedNgComponentDefinition extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ComponentDefinition.
  const GeneratedNgComponentDefinition(super.raw);

  /// The controller member of ng.ComponentDefinition.
  Object? get controller =>
      unsafe.jsToDart<Object?>(raw.getProperty('controller'.toJS));

  /// An identifier name for a reference to the controller. If present, the controller will be
  /// published to its scope under the specified name. If not present, this will default to '$ctrl'.
  String? get controllerAs =>
      _toDartString(raw.getProperty('controllerAs'.toJS));

  /// An identifier name for a reference to the controller. If present, the controller will be
  /// published to its scope under the specified name. If not present, this will default to '$ctrl'.
  set controllerAs(String? value) {
    raw.setProperty('controllerAs'.toJS, unsafe.dartToJs(value));
  }

  /// Define DOM attribute binding to component properties. Component properties are always bound to
  /// the component controller and not to the scope.
  Object? get bindings =>
      unsafe.jsToDart<Object?>(raw.getProperty('bindings'.toJS));

  /// Whether transclusion is enabled. Disabled by default.
  Object? get transclude =>
      unsafe.jsToDart<Object?>(raw.getProperty('transclude'.toJS));

  /// Requires the controllers of other directives and binds them to this component's controller. The
  /// object keys specify the property names under which the required controllers (object values) will
  /// be bound. Note that the required controllers will not be available during the instantiation of
  /// the controller, but they are guaranteed to be available just before the onInit method is
  /// executed!
  Object? get require =>
      unsafe.jsToDart<Object?>(raw.getProperty('require'.toJS));

  /// The view member of ng.ComponentDefinition.
  Object? get view => unsafe.jsToDart<Object?>(raw.getProperty('view'.toJS));

  /// The template member of ng.ComponentDefinition.
  Object? get template =>
      unsafe.jsToDart<Object?>(raw.getProperty('template'.toJS));

  /// The templateUrl member of ng.ComponentDefinition.
  Object? get templateUrl =>
      unsafe.jsToDart<Object?>(raw.getProperty('templateUrl'.toJS));

  /// The replace member of ng.ComponentDefinition.
  bool? get replace => _toDartBool(raw.getProperty('replace'.toJS));

  /// The replace member of ng.ComponentDefinition.
  set replace(bool? value) {
    raw.setProperty('replace'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS ConnectionConfig contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ConnectionConfig`.
base class GeneratedNgConnectionConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ConnectionConfig.
  const GeneratedNgConnectionConfig(super.raw);

  /// Called when the connection opens
  Object? get onOpen =>
      unsafe.jsToDart<Object?>(raw.getProperty('onOpen'.toJS));

  /// Called when a message is received
  Object? get onMessage =>
      unsafe.jsToDart<Object?>(raw.getProperty('onMessage'.toJS));

  /// Called with every registered connection message, including custom SSE event types
  Object? get onEvent =>
      unsafe.jsToDart<Object?>(raw.getProperty('onEvent'.toJS));

  /// Called when an error occurs
  Object? get onError =>
      unsafe.jsToDart<Object?>(raw.getProperty('onError'.toJS));

  /// Called when a WebSocket connection closes
  Object? get onClose =>
      unsafe.jsToDart<Object?>(raw.getProperty('onClose'.toJS));

  /// Called when a reconnect attempt happens
  Object? get onReconnect =>
      unsafe.jsToDart<Object?>(raw.getProperty('onReconnect'.toJS));

  /// Delay between reconnect attempts in milliseconds
  num? get retryDelay => _toDartNumber(raw.getProperty('retryDelay'.toJS));

  /// Delay between reconnect attempts in milliseconds
  set retryDelay(num? value) {
    raw.setProperty('retryDelay'.toJS, unsafe.dartToJs(value));
  }

  /// Maximum number of reconnect attempts
  num? get maxRetries => _toDartNumber(raw.getProperty('maxRetries'.toJS));

  /// Maximum number of reconnect attempts
  set maxRetries(num? value) {
    raw.setProperty('maxRetries'.toJS, unsafe.dartToJs(value));
  }

  /// Timeout in milliseconds to detect heartbeat inactivity
  num? get heartbeatTimeout =>
      _toDartNumber(raw.getProperty('heartbeatTimeout'.toJS));

  /// Timeout in milliseconds to detect heartbeat inactivity
  set heartbeatTimeout(num? value) {
    raw.setProperty('heartbeatTimeout'.toJS, unsafe.dartToJs(value));
  }

  /// Function to transform incoming messages
  Object? get transformMessage =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformMessage'.toJS));

  /// Additional EventSource event names to subscribe to
  Object? get eventTypes =>
      unsafe.jsToDart<Object?>(raw.getProperty('eventTypes'.toJS));
}

/// Public AngularTS ConnectionEvent contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ConnectionEvent`.
base class GeneratedNgConnectionEvent extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ConnectionEvent.
  const GeneratedNgConnectionEvent(super.raw);

  /// The type member of ng.ConnectionEvent.
  String get typeMember => _toDartString(raw.getProperty('type'.toJS))!;

  /// The type member of ng.ConnectionEvent.
  set typeMember(String value) {
    raw.setProperty('type'.toJS, unsafe.dartToJs(value));
  }

  /// The data member of ng.ConnectionEvent.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// The rawData member of ng.ConnectionEvent.
  Object? get rawData =>
      unsafe.jsToDart<Object?>(raw.getProperty('rawData'.toJS));

  /// The event member of ng.ConnectionEvent.
  Object? get event => unsafe.jsToDart<Object?>(raw.getProperty('event'.toJS));
}

/// AngularTS component lifecycle interface. Directive controllers have a well-defined lifecycle.
/// Each controller can implement "lifecycle hooks". These are methods that will be called by
/// Angular at certain points in the life cycle of the directive.
/// https://docs.angularjs.org/api/ng/service/$compile#life-cycle-hooks
/// https://docs.angularjs.org/guide/component
///
/// Raw JavaScript facade for `ng.Controller`.
base class GeneratedNgController extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Controller.
  const GeneratedNgController(super.raw);

  /// Optional controller name (used in debugging)
  String? get name => _toDartString(raw.getProperty('name'.toJS));

  /// Optional controller name (used in debugging)
  set name(String? value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// Called on each controller after all the controllers on an element have been constructed and had
  /// their bindings initialized (and before the pre & post linking functions for the directives on
  /// this element). This is a good place to put initialization code for your controller.
  Object? get onInit =>
      unsafe.jsToDart<Object?>(raw.getProperty('onInit'.toJS));

  /// Called whenever one-way bindings are updated. The onChangesObj is a hash whose keys are the
  /// names of the bound properties that have changed, and the values are a ChangesObject object of
  /// the form { currentValue, previousValue, isFirstChange() }. Use this hook to trigger updates
  /// within a component such as cloning the bound value to prevent accidental mutation of the outer
  /// value.
  Object? get onChanges =>
      unsafe.jsToDart<Object?>(raw.getProperty('onChanges'.toJS));

  /// Called on a controller when its containing scope is destroyed. Use this hook for releasing
  /// external resources, watches and event handlers.
  Object? get onDestroy =>
      unsafe.jsToDart<Object?>(raw.getProperty('onDestroy'.toJS));

  /// Called after this controller's element and its children have been linked. Similar to the
  /// post-link function this hook can be used to set up DOM event handlers and do direct DOM
  /// manipulation. Note that child elements that contain templateUrl directives will not have been
  /// compiled and linked since they are waiting for their template to load asynchronously and their
  /// own compilation and linking has been suspended until that occurs. This hook can be considered
  /// analogous to the ngAfterViewInit and ngAfterContentInit hooks in Angular 2. Since the
  /// compilation process is rather different in Angular 1 there is no direct mapping and care should
  /// be taken when upgrading.
  Object? get postLink =>
      unsafe.jsToDart<Object?>(raw.getProperty('postLink'.toJS));

  /// Called after this controller has been linked, AngularTS has applied DOM mutations for the
  /// current flush, and the browser has had one animation frame to settle layout. Multiple schedules
  /// for the same controller in one flush are coalesced into one call.
  Object? get afterRender =>
      unsafe.jsToDart<Object?>(raw.getProperty('afterRender'.toJS));
}

/// A controller constructor function used in AngularTS.
///
/// Raw JavaScript facade for `ng.ControllerConstructor`.
base class GeneratedNgControllerConstructor extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ControllerConstructor.
  const GeneratedNgControllerConstructor(super.raw);

  /// Calls the ng.ControllerConstructor function.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? call(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS ControllerService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ControllerService`.
base class GeneratedNgControllerService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ControllerService.
  const GeneratedNgControllerService(super.raw);

  /// Calls the ng.ControllerService function.
  ///
  /// Parameters:
  /// - [expression]: Value supplied for the expression parameter.
  /// - [locals]: Value supplied for the locals parameter.
  /// - [later]: Value supplied for the later parameter.
  /// - [ident]: Value supplied for the ident parameter.
  Object? call(Object? expression,
      [Object? locals = _undefinedArgument,
      Object? later = _undefinedArgument,
      Object? ident = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(expression));
    if (!identical(locals, _undefinedArgument)) {
      args.add(unsafe.dartToJs(locals));
    }
    if (!identical(later, _undefinedArgument)) {
      args.add(unsafe.dartToJs(later));
    }
    if (!identical(ident, _undefinedArgument)) {
      args.add(unsafe.dartToJs(ident));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS CookieOptions contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.CookieOptions`.
base class GeneratedNgCookieOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.CookieOptions.
  const GeneratedNgCookieOptions(super.raw);

  /// URL path scope for the cookie.
  String? get path => _toDartString(raw.getProperty('path'.toJS));

  /// URL path scope for the cookie.
  set path(String? value) {
    raw.setProperty('path'.toJS, unsafe.dartToJs(value));
  }

  /// Domain scope for the cookie.
  String? get domain => _toDartString(raw.getProperty('domain'.toJS));

  /// Domain scope for the cookie.
  set domain(String? value) {
    raw.setProperty('domain'.toJS, unsafe.dartToJs(value));
  }

  /// Expiration date, date string, or timestamp. Omit for a session cookie.
  Object? get expires =>
      unsafe.jsToDart<Object?>(raw.getProperty('expires'.toJS));

  /// Restrict the cookie to HTTPS connections.
  bool? get secure => _toDartBool(raw.getProperty('secure'.toJS));

  /// Restrict the cookie to HTTPS connections.
  set secure(bool? value) {
    raw.setProperty('secure'.toJS, unsafe.dartToJs(value));
  }

  /// SameSite policy applied by the browser.
  String? get samesite => _toDartString(raw.getProperty('samesite'.toJS));

  /// SameSite policy applied by the browser.
  set samesite(String? value) {
    raw.setProperty('samesite'.toJS, unsafe.dartToJs(value));
  }
}

/// High-level API for reading, writing, serializing, and removing browser cookies through the
/// injectable `$cookie` service.
///
/// Raw JavaScript facade for `ng.CookieService`.
base class GeneratedNgCookieService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.CookieService.
  const GeneratedNgCookieService(super.raw);

  /// Retrieves a raw cookie value.
  ///
  /// Parameters:
  /// - [key]: Cookie name to read.
  String? get(String key) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));

    return _toDartString(raw.callMethodVarArgs('get'.toJS, args));
  }

  /// Retrieves a cookie and deserializes its JSON content.
  ///
  /// Parameters:
  /// - [key]: Cookie name to read.
  Object? getObject(String key) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getObject'.toJS, args));
  }

  /// Returns an object containing all raw cookies.
  Object? getAll() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('getAll'.toJS, const <JSAny?>[]));
  }

  /// Sets a raw cookie value.
  ///
  /// Parameters:
  /// - [key]: Cookie name to write.
  /// - [value]: String value to write.
  /// - [options]: Cookie attributes for this write.
  void put(String key, String value, [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));
    args.add(unsafe.dartToJs(value));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('put'.toJS, args));
  }

  /// Serializes an object as JSON and stores it as a cookie.
  ///
  /// Parameters:
  /// - [key]: Cookie name to write.
  /// - [value]: JSON-serializable value.
  /// - [options]: Cookie attributes for this write.
  void putObject(String key, Object? value,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));
    args.add(unsafe.dartToJs(value));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('putObject'.toJS, args));
  }

  /// Removes a cookie by setting an expired date.
  ///
  /// Parameters:
  /// - [key]: Cookie name to remove.
  /// - [options]: Cookie attributes that must match the existing cookie.
  void remove(String key, [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('remove'.toJS, args));
  }
}

/// Serialization options for cookie-backed stores.
///
/// Raw JavaScript facade for `ng.CookieStoreOptions`.
base class GeneratedNgCookieStoreOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.CookieStoreOptions.
  const GeneratedNgCookieStoreOptions(super.raw);

  /// Convert values to strings before writing.
  Object? get serialize =>
      unsafe.jsToDart<Object?>(raw.getProperty('serialize'.toJS));

  /// Convert stored strings back to values after reading.
  Object? get deserialize =>
      unsafe.jsToDart<Object?>(raw.getProperty('deserialize'.toJS));

  /// Cookie attributes used for writes.
  Object? get cookie =>
      unsafe.jsToDart<Object?>(raw.getProperty('cookie'.toJS));
}

/// Defines the structure of an AngularTS directive.
///
/// Raw JavaScript facade for `ng.Directive`.
base class GeneratedNgDirective extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Directive.
  const GeneratedNgDirective(super.raw);

  /// Optional name (usually inferred)
  String? get name => _toDartString(raw.getProperty('name'.toJS));

  /// Optional name (usually inferred)
  set name(String? value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// Restrict option: 'A' and/or 'E'. Defaults to 'EA' if not defined
  String? get restrict => _toDartString(raw.getProperty('restrict'.toJS));

  /// Restrict option: 'A' and/or 'E'. Defaults to 'EA' if not defined
  set restrict(String? value) {
    raw.setProperty('restrict'.toJS, unsafe.dartToJs(value));
  }

  /// Compile function for the directive
  Object? get compile =>
      unsafe.jsToDart<Object?>(raw.getProperty('compile'.toJS));

  /// Real-DOM view factory. It is mutually exclusive with template, templateUrl, and replace and
  /// composes with compile/link.
  Object? get view => unsafe.jsToDart<Object?>(raw.getProperty('view'.toJS));

  /// Controller constructor or injectable string name
  Object? get controller =>
      unsafe.jsToDart<Object?>(raw.getProperty('controller'.toJS));

  /// Alias name for the controller in templates
  String? get controllerAs =>
      _toDartString(raw.getProperty('controllerAs'.toJS));

  /// Alias name for the controller in templates
  set controllerAs(String? value) {
    raw.setProperty('controllerAs'.toJS, unsafe.dartToJs(value));
  }

  /// Whether to bind scope to controller
  Object? get bindToController =>
      unsafe.jsToDart<Object?>(raw.getProperty('bindToController'.toJS));

  /// Link function(s) executed during linking
  Object? get link => unsafe.jsToDart<Object?>(raw.getProperty('link'.toJS));

  /// Priority of the directive
  num? get priority => _toDartNumber(raw.getProperty('priority'.toJS));

  /// Priority of the directive
  set priority(num? value) {
    raw.setProperty('priority'.toJS, unsafe.dartToJs(value));
  }

  /// Stops further directive processing if true
  bool? get terminal => _toDartBool(raw.getProperty('terminal'.toJS));

  /// Stops further directive processing if true
  set terminal(bool? value) {
    raw.setProperty('terminal'.toJS, unsafe.dartToJs(value));
  }

  /// Replaces the element with the template if true
  bool? get replace => _toDartBool(raw.getProperty('replace'.toJS));

  /// Replaces the element with the template if true
  set replace(bool? value) {
    raw.setProperty('replace'.toJS, unsafe.dartToJs(value));
  }

  /// Required controllers for the directive
  Object? get require =>
      unsafe.jsToDart<Object?>(raw.getProperty('require'.toJS));

  /// Scope configuration (`true`, `false`, or object for isolate scope)
  Object? get scope => unsafe.jsToDart<Object?>(raw.getProperty('scope'.toJS));

  /// Inline template
  Object? get template =>
      unsafe.jsToDart<Object?>(raw.getProperty('template'.toJS));

  /// Template namespace (e.g., SVG, HTML)
  String? get templateNamespace =>
      _toDartString(raw.getProperty('templateNamespace'.toJS));

  /// Template namespace (e.g., SVG, HTML)
  set templateNamespace(String? value) {
    raw.setProperty('templateNamespace'.toJS, unsafe.dartToJs(value));
  }

  /// Template URL for loading from server
  Object? get templateUrl =>
      unsafe.jsToDart<Object?>(raw.getProperty('templateUrl'.toJS));

  /// Enables transclusion or configures named slots
  Object? get transclude =>
      unsafe.jsToDart<Object?>(raw.getProperty('transclude'.toJS));

  /// Currently only used by view directive
  num? get count => _toDartNumber(raw.getProperty('count'.toJS));

  /// Currently only used by view directive
  set count(num? value) {
    raw.setProperty('count'.toJS, unsafe.dartToJs(value));
  }
}

/// A compile function used to prepare directives before linking.
///
/// Raw JavaScript facade for `ng.DirectiveCompileFn`.
base class GeneratedNgDirectiveCompileFn extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.DirectiveCompileFn.
  const GeneratedNgDirectiveCompileFn(super.raw);

  /// Calls the ng.DirectiveCompileFn function.
  ///
  /// Parameters:
  /// - [templateElement]: Value supplied for the templateElement parameter.
  /// - [transclude]: Value supplied for the transclude parameter.
  Object? call(HTMLElement templateElement,
      [Object? transclude = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(templateElement));
    if (!identical(transclude, _undefinedArgument)) {
      args.add(unsafe.dartToJs(transclude));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// A controller instance or object map used in directives.
///
/// Raw JavaScript facade for `ng.DirectiveController`.
base class GeneratedNgDirectiveController extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.DirectiveController.
  const GeneratedNgDirectiveController(super.raw);
}

/// Public AngularTS DirectiveFactory contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.DirectiveFactory`.
base class GeneratedNgDirectiveFactory extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.DirectiveFactory.
  const GeneratedNgDirectiveFactory(super.raw);

  /// Calls the ng.DirectiveFactory function.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? call(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS DirectiveFactoryFn contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.DirectiveFactoryFn`.
base class GeneratedNgDirectiveFactoryFn extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.DirectiveFactoryFn.
  const GeneratedNgDirectiveFactoryFn(super.raw);

  /// Calls the ng.DirectiveFactoryFn function.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? call(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS DirectiveLinkFn contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.DirectiveLinkFn`.
base class GeneratedNgDirectiveLinkFn extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.DirectiveLinkFn.
  const GeneratedNgDirectiveLinkFn(super.raw);

  /// Calls the ng.DirectiveLinkFn function.
  ///
  /// Parameters:
  /// - [scope]: Value supplied for the scope parameter.
  /// - [element]: Value supplied for the element parameter.
  /// - [arg2]: Value supplied for the arg2 parameter.
  /// - [transclude]: Value supplied for the transclude parameter.
  void call(
      [Object? scope = _undefinedArgument,
      Object? element = _undefinedArgument,
      Object? arg2 = _undefinedArgument,
      Object? transclude = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(scope, _undefinedArgument)) {
      args.add(unsafe.dartToJs(scope));
    }
    if (!identical(element, _undefinedArgument)) {
      args.add(unsafe.dartToJs(element));
    }
    if (!identical(arg2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(arg2));
    }
    if (!identical(transclude, _undefinedArgument)) {
      args.add(unsafe.dartToJs(transclude));
    }

    unsafe.jsToDart<void>(_callFunction(raw as JSFunction, args));
  }
}

/// Defines optional pre/post link functions in directive compile phase.
///
/// Raw JavaScript facade for `ng.DirectivePrePost`.
base class GeneratedNgDirectivePrePost extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.DirectivePrePost.
  const GeneratedNgDirectivePrePost(super.raw);

  /// The pre member of ng.DirectivePrePost.
  Object? get pre => unsafe.jsToDart<Object?>(raw.getProperty('pre'.toJS));

  /// The post member of ng.DirectivePrePost.
  Object? get post => unsafe.jsToDart<Object?>(raw.getProperty('post'.toJS));
}

/// Supported directive matching locations.
///
/// Raw JavaScript facade for `ng.DirectiveRestrict`.
base class GeneratedNgDirectiveRestrict extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.DirectiveRestrict.
  const GeneratedNgDirectiveRestrict(super.raw);
}

/// Public AngularTS ElementScopeOptions contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ElementScopeOptions`.
base class GeneratedNgElementScopeOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ElementScopeOptions.
  const GeneratedNgElementScopeOptions(super.raw);

  /// Explicit parent scope. Defaults to nearest inherited DOM scope.
  Object? get parentScope =>
      unsafe.jsToDart<Object?>(raw.getProperty('parentScope'.toJS));

  /// Use an isolate child scope.
  bool? get isolate => _toDartBool(raw.getProperty('isolate'.toJS));

  /// Use an isolate child scope.
  set isolate(bool? value) {
    raw.setProperty('isolate'.toJS, unsafe.dartToJs(value));
  }
}

/// Creates a new entity instance from raw response data.
///
/// Raw JavaScript facade for `ng.EntityClass`.
base class GeneratedNgEntityClass extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.EntityClass.
  const GeneratedNgEntityClass(super.raw);
}

/// Public AngularTS EntryFilterItem contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.EntryFilterItem`.
base class GeneratedNgEntryFilterItem extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.EntryFilterItem.
  const GeneratedNgEntryFilterItem(super.raw);

  /// The key member of ng.EntryFilterItem.
  Object? get key => unsafe.jsToDart<Object?>(raw.getProperty('key'.toJS));

  /// The value member of ng.EntryFilterItem.
  Object? get value => unsafe.jsToDart<Object?>(raw.getProperty('value'.toJS));
}

/// Controls how values embedded in framework error messages are formatted.
///
/// Raw JavaScript facade for `ng.ErrorFormattingConfig`.
base class GeneratedNgErrorFormattingConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ErrorFormattingConfig.
  const GeneratedNgErrorFormattingConfig(super.raw);

  /// The max depth for stringifying objects. Setting to a non-positive or non-numeric value removes
  /// the max depth limit. Default: 5.
  num? get objectMaxDepth =>
      _toDartNumber(raw.getProperty('objectMaxDepth'.toJS));

  /// The max depth for stringifying objects. Setting to a non-positive or non-numeric value removes
  /// the max depth limit. Default: 5.
  set objectMaxDepth(num? value) {
    raw.setProperty('objectMaxDepth'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS EventBusConfig contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.EventBusConfig`.
base class GeneratedNgEventBusConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.EventBusConfig.
  const GeneratedNgEventBusConfig(super.raw);

  /// The deliveryPolicy member of ng.EventBusConfig.
  Object? get deliveryPolicy =>
      unsafe.jsToDart<Object?>(raw.getProperty('deliveryPolicy'.toJS));
}

/// Application-wide asynchronous publish/subscribe utility. `EventBus` powers `$eventBus` for
/// cross-boundary domain events, browser callbacks, worker messages, realtime messages, and
/// non-Angular integrations. It is intentionally not a state store and should not replace scope
/// events for parent/child scope-tree communication.
///
/// Raw JavaScript facade for `ng.EventBusService`.
base class GeneratedNgEventBusService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.EventBusService.
  const GeneratedNgEventBusService(super.raw);

  /// Reset the bus to its initial state without disposing it. All topics and listeners are removed,
  /// and the instance can be reused.
  void reset() {
    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('reset'.toJS, const <JSAny?>[]));
  }

  /// Replace the runtime delivery policy used by future publications. The default policy delivers
  /// every active listener. Configured policies can drop deliveries for specific topics, scopes, or
  /// application metadata.
  ///
  /// Parameters:
  /// - [policy]: Value supplied for the policy parameter.
  void setDeliveryPolicy([Object? policy = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(policy, _undefinedArgument)) {
      args.add(unsafe.dartToJs(policy));
    }

    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('setDeliveryPolicy'.toJS, args));
  }

  /// Checks if instance has been disposed.
  bool isDisposed() {
    return _toDartBool(
        raw.callMethodVarArgs('isDisposed'.toJS, const <JSAny?>[]))!;
  }

  /// Dispose the instance, removing all topics and listeners.
  void dispose() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('dispose'.toJS, const <JSAny?>[]));
  }

  /// Subscribe a function to a topic. The returned function removes only this listener registration.
  /// When `context` is provided, it becomes the listener `this` binding. When `context` is an
  /// AngularTS scope proxy, the scope also owns the listener lifecycle: destroying the scope removes
  /// the listener and prevents queued delivery from reaching the destroyed scope.
  ///
  /// Parameters:
  /// - [topic]: Value supplied for the topic parameter.
  /// - [fn]: Value supplied for the fn parameter.
  /// - [context]: Value supplied for the context parameter.
  Object? subscribe(String topic, JSFunction fn,
      [Object? context = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(topic));
    args.add(unsafe.dartToJs(fn));
    if (!identical(context, _undefinedArgument)) {
      args.add(unsafe.dartToJs(context));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('subscribe'.toJS, args));
  }

  /// Subscribe a function to a topic only once. Listener is removed before the first invocation. When
  /// `context` is provided, it becomes the listener `this` binding. When `context` is an AngularTS
  /// scope proxy, scope destruction before first delivery removes the one-time listener.
  ///
  /// Parameters:
  /// - [topic]: Value supplied for the topic parameter.
  /// - [fn]: Value supplied for the fn parameter.
  /// - [context]: Value supplied for the context parameter.
  Object? subscribeOnce(String topic, JSFunction fn,
      [Object? context = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(topic));
    args.add(unsafe.dartToJs(fn));
    if (!identical(context, _undefinedArgument)) {
      args.add(unsafe.dartToJs(context));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('subscribeOnce'.toJS, args));
  }

  /// Unsubscribe a specific function from a topic. Matches by function reference and optional
  /// context.
  ///
  /// Parameters:
  /// - [topic]: Value supplied for the topic parameter.
  /// - [fn]: Value supplied for the fn parameter.
  /// - [context]: Value supplied for the context parameter.
  bool unsubscribe(String topic, JSFunction fn,
      [Object? context = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(topic));
    args.add(unsafe.dartToJs(fn));
    if (!identical(context, _undefinedArgument)) {
      args.add(unsafe.dartToJs(context));
    }

    return _toDartBool(raw.callMethodVarArgs('unsubscribe'.toJS, args))!;
  }

  /// Get the number of subscribers for a topic. This is the public diagnostic surface for
  /// `$eventBus`. It reports active registered listeners only; topic listings, leak reports, and
  /// reactive diagnostics are intentionally not exposed.
  ///
  /// Parameters:
  /// - [topic]: Topic name to inspect.
  int getCount(String topic) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(topic));

    return _toDartInt(raw.callMethodVarArgs('getCount'.toJS, args))!;
  }

  /// Publish a value to a topic asynchronously. Listeners are invoked in the order they were added
  /// until delivery finishes or `$exceptionHandler` terminates the publication. Delivery is scheduled
  /// with `queueMicrotask`. Scope-owned listeners are skipped if their scope is destroyed before the
  /// queued delivery runs.
  ///
  /// Parameters:
  /// - [topic]: The topic to publish.
  /// - [args1]: Arguments to pass to listeners.
  /// - [args2]: Arguments to pass to listeners.
  /// - [args3]: Arguments to pass to listeners.
  /// - [args4]: Arguments to pass to listeners.
  bool publish(String topic,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(topic));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return _toDartBool(raw.callMethodVarArgs('publish'.toJS, args))!;
  }
}

/// Public AngularTS EventDeliveryPolicy contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.EventDeliveryPolicy`.
base class GeneratedNgEventDeliveryPolicy extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.EventDeliveryPolicy.
  const GeneratedNgEventDeliveryPolicy(super.raw);

  /// Calls the ng.EventDeliveryPolicy function.
  ///
  /// Parameters:
  /// - [context]: Value supplied for the context parameter.
  Object? call(Object? context) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(context));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS EventDeliveryPolicyContext contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.EventDeliveryPolicyContext`.
base class GeneratedNgEventDeliveryPolicyContext extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.EventDeliveryPolicyContext.
  const GeneratedNgEventDeliveryPolicyContext(super.raw);

  /// The topic member of ng.EventDeliveryPolicyContext.
  String get topic => _toDartString(raw.getProperty('topic'.toJS))!;

  /// The args member of ng.EventDeliveryPolicyContext.
  Object? get args => unsafe.jsToDart<Object?>(raw.getProperty('args'.toJS));

  /// The listenerIndex member of ng.EventDeliveryPolicyContext.
  num get listenerIndex =>
      _toDartNumber(raw.getProperty('listenerIndex'.toJS))!;

  /// The scopeOwned member of ng.EventDeliveryPolicyContext.
  bool get scopeOwned => _toDartBool(raw.getProperty('scopeOwned'.toJS))!;

  /// The targetAlive member of ng.EventDeliveryPolicyContext.
  bool get targetAlive => _toDartBool(raw.getProperty('targetAlive'.toJS))!;

  /// The operation member of ng.EventDeliveryPolicyContext.
  String get operation => _toDartString(raw.getProperty('operation'.toJS))!;

  /// The meta member of ng.EventDeliveryPolicyContext.
  Object? get meta => unsafe.jsToDart<Object?>(raw.getProperty('meta'.toJS));
}

/// A callback type for handling errors.
///
/// Raw JavaScript facade for `ng.ExceptionHandlerService`.
base class GeneratedNgExceptionHandlerService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ExceptionHandlerService.
  const GeneratedNgExceptionHandlerService(super.raw);

  /// Calls the ng.ExceptionHandlerService function.
  ///
  /// Parameters:
  /// - [exception]: Value supplied for the exception parameter.
  Object? call(Object? exception) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(exception));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS FilterFactory contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.FilterFactory`.
base class GeneratedNgFilterFactory extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.FilterFactory.
  const GeneratedNgFilterFactory(super.raw);

  /// Calls the ng.FilterFactory function.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? call(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS FilterFn contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.FilterFn`.
base class GeneratedNgFilterFn extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.FilterFn.
  const GeneratedNgFilterFn(super.raw);

  /// Calls the ng.FilterFn function.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? call(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS FilterService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.FilterService`.
base class GeneratedNgFilterService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.FilterService.
  const GeneratedNgFilterService(super.raw);

  /// Calls the ng.FilterService function.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  Object? call(String name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Declarative config accepted by `NgModule.config({ $htmlCanvas: ... })`. The integration is
/// disabled by default and has no AngularTS fallback.
///
/// Raw JavaScript facade for `ng.HtmlCanvasConfig`.
base class GeneratedNgHtmlCanvasConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HtmlCanvasConfig.
  const GeneratedNgHtmlCanvasConfig(super.raw);

  /// The enabled member of ng.HtmlCanvasConfig.
  Object? get enabled =>
      unsafe.jsToDart<Object?>(raw.getProperty('enabled'.toJS));

  /// Throw when HTML-in-Canvas is enabled on a runtime that does not support the native browser
  /// feature. AngularTS does not provide a fallback renderer.
  bool? get throwOnUnsupported =>
      _toDartBool(raw.getProperty('throwOnUnsupported'.toJS));

  /// Throw when HTML-in-Canvas is enabled on a runtime that does not support the native browser
  /// feature. AngularTS does not provide a fallback renderer.
  set throwOnUnsupported(bool? value) {
    raw.setProperty('throwOnUnsupported'.toJS, unsafe.dartToJs(value));
  }

  /// Default invalidation scheduler for canvas-backed HTML layers.
  String? get defaultScheduler =>
      _toDartString(raw.getProperty('defaultScheduler'.toJS));

  /// Default invalidation scheduler for canvas-backed HTML layers.
  set defaultScheduler(String? value) {
    raw.setProperty('defaultScheduler'.toJS, unsafe.dartToJs(value));
  }

  /// Default canvas rendering target for directives that do not specify one.
  String? get defaultMode => _toDartString(raw.getProperty('defaultMode'.toJS));

  /// Default canvas rendering target for directives that do not specify one.
  set defaultMode(String? value) {
    raw.setProperty('defaultMode'.toJS, unsafe.dartToJs(value));
  }

  /// Require an explicit browser/engine feature flag before activation. This stays strict by default
  /// while the browser API is experimental.
  bool? get requireFlag => _toDartBool(raw.getProperty('requireFlag'.toJS));

  /// Require an explicit browser/engine feature flag before activation. This stays strict by default
  /// while the browser API is experimental.
  set requireFlag(bool? value) {
    raw.setProperty('requireFlag'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS HtmlCanvasRuntimeSupport contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.HtmlCanvasRuntimeSupport`.
base class GeneratedNgHtmlCanvasRuntimeSupport extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HtmlCanvasRuntimeSupport.
  const GeneratedNgHtmlCanvasRuntimeSupport(super.raw);

  /// Native layout-subtree support or an implied drawing primitive.
  bool get layoutSubtree => _toDartBool(raw.getProperty('layoutSubtree'.toJS))!;

  /// Native layout-subtree support or an implied drawing primitive.
  set layoutSubtree(bool value) {
    raw.setProperty('layoutSubtree'.toJS, unsafe.dartToJs(value));
  }

  /// Native canvas `paint` event support.
  bool get paintEvent => _toDartBool(raw.getProperty('paintEvent'.toJS))!;

  /// Native canvas `paint` event support.
  set paintEvent(bool value) {
    raw.setProperty('paintEvent'.toJS, unsafe.dartToJs(value));
  }

  /// Native canvas `requestPaint()` support.
  bool get requestPaint => _toDartBool(raw.getProperty('requestPaint'.toJS))!;

  /// Native canvas `requestPaint()` support.
  set requestPaint(bool value) {
    raw.setProperty('requestPaint'.toJS, unsafe.dartToJs(value));
  }

  /// Native 2D `drawElementImage(...)` support.
  bool get drawElementImage =>
      _toDartBool(raw.getProperty('drawElementImage'.toJS))!;

  /// Native 2D `drawElementImage(...)` support.
  set drawElementImage(bool value) {
    raw.setProperty('drawElementImage'.toJS, unsafe.dartToJs(value));
  }

  /// Native WebGL `texElementImage2D(...)` support.
  bool get texElementImage2D =>
      _toDartBool(raw.getProperty('texElementImage2D'.toJS))!;

  /// Native WebGL `texElementImage2D(...)` support.
  set texElementImage2D(bool value) {
    raw.setProperty('texElementImage2D'.toJS, unsafe.dartToJs(value));
  }

  /// Native WebGPU `copyElementImageToTexture(...)` support.
  bool get copyElementImageToTexture =>
      _toDartBool(raw.getProperty('copyElementImageToTexture'.toJS))!;

  /// Native WebGPU `copyElementImageToTexture(...)` support.
  set copyElementImageToTexture(bool value) {
    raw.setProperty('copyElementImageToTexture'.toJS, unsafe.dartToJs(value));
  }

  /// Supported rendering modes for the current runtime.
  Object? get modes => unsafe.jsToDart<Object?>(raw.getProperty('modes'.toJS));

  /// Whether any native HTML-in-Canvas rendering mode is available.
  bool get supported => _toDartBool(raw.getProperty('supported'.toJS))!;

  /// Whether any native HTML-in-Canvas rendering mode is available.
  set supported(bool value) {
    raw.setProperty('supported'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS HtmlCanvasService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.HtmlCanvasService`.
base class GeneratedNgHtmlCanvasService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HtmlCanvasService.
  const GeneratedNgHtmlCanvasService(super.raw);

  /// The config member of ng.HtmlCanvasService.
  Object? get config =>
      unsafe.jsToDart<Object?>(raw.getProperty('config'.toJS));

  /// The support member of ng.HtmlCanvasService.
  Object? get support =>
      unsafe.jsToDart<Object?>(raw.getProperty('support'.toJS));

  /// The enabled member of ng.HtmlCanvasService.
  bool get enabled => _toDartBool(raw.getProperty('enabled'.toJS))!;

  /// The supported member of ng.HtmlCanvasService.
  bool get supported => _toDartBool(raw.getProperty('supported'.toJS))!;

  /// The registerRoot member of ng.HtmlCanvasService.
  ///
  /// Parameters:
  /// - [canvas]: Value supplied for the canvas parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? registerRoot(Object? canvas, [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(canvas));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('registerRoot'.toJS, args));
  }

  /// The registerSource member of ng.HtmlCanvasService.
  ///
  /// Parameters:
  /// - [canvas]: Value supplied for the canvas parameter.
  /// - [source]: Value supplied for the source parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? registerSource(Object? canvas, Element source,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(canvas));
    args.add(unsafe.dartToJs(source));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('registerSource'.toJS, args));
  }

  /// The invalidate member of ng.HtmlCanvasService.
  ///
  /// Parameters:
  /// - [canvas]: Value supplied for the canvas parameter.
  void invalidate(Object? canvas) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(canvas));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('invalidate'.toJS, args));
  }

  /// The requestPaint member of ng.HtmlCanvasService.
  ///
  /// Parameters:
  /// - [canvas]: Value supplied for the canvas parameter.
  void requestPaint(Object? canvas) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(canvas));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('requestPaint'.toJS, args));
  }
}

/// Default request settings configured through `app.config({ $http })` and exposed at runtime
/// through `$http.defaults`. Not every `HttpRequestOptions` field is supported here; this shape
/// only includes the fields that the runtime reads from provider-level defaults.
/// https://docs.angularjs.org/api/ng/service/$http#defaults
/// https://docs.angularjs.org/api/ng/service/$http#usage
///
/// Raw JavaScript facade for `ng.HttpDefaults`.
base class GeneratedNgHttpDefaults extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HttpDefaults.
  const GeneratedNgHttpDefaults(super.raw);

  /// Cache used for cacheable requests. `true` enables the default cache.
  Object? get cache => unsafe.jsToDart<Object?>(raw.getProperty('cache'.toJS));

  /// Request body transform pipeline.
  Object? get transformRequest =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformRequest'.toJS));

  /// Response body transform pipeline.
  Object? get transformResponse =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformResponse'.toJS));

  /// Default headers merged into each request.
  Object? get headers =>
      unsafe.jsToDart<Object?>(raw.getProperty('headers'.toJS));

  /// Header name used when sending the XSRF token.
  String? get xsrfHeaderName =>
      _toDartString(raw.getProperty('xsrfHeaderName'.toJS));

  /// Header name used when sending the XSRF token.
  set xsrfHeaderName(String? value) {
    raw.setProperty('xsrfHeaderName'.toJS, unsafe.dartToJs(value));
  }

  /// Cookie name used when reading the XSRF token.
  String? get xsrfCookieName =>
      _toDartString(raw.getProperty('xsrfCookieName'.toJS));

  /// Cookie name used when reading the XSRF token.
  set xsrfCookieName(String? value) {
    raw.setProperty('xsrfCookieName'.toJS, unsafe.dartToJs(value));
  }

  /// Whether cross-site requests should include credentials by default.
  bool? get withCredentials =>
      _toDartBool(raw.getProperty('withCredentials'.toJS));

  /// Whether cross-site requests should include credentials by default.
  set withCredentials(bool? value) {
    raw.setProperty('withCredentials'.toJS, unsafe.dartToJs(value));
  }

  /// Query parameter serializer token or function.
  Object? get paramSerializer =>
      unsafe.jsToDart<Object?>(raw.getProperty('paramSerializer'.toJS));
}

/// HTTP method accepted by HttpRequestConfig.method.
///
/// Raw JavaScript facade for `ng.HttpMethod`.
base class GeneratedNgHttpMethod extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HttpMethod.
  const GeneratedNgHttpMethod(super.raw);
}

/// Function that serializes query params into a URL-encoded string.
///
/// Raw JavaScript facade for `ng.HttpParamSerializerService`.
base class GeneratedNgHttpParamSerializerService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HttpParamSerializerService.
  const GeneratedNgHttpParamSerializerService(super.raw);

  /// Calls the ng.HttpParamSerializerService function.
  ///
  /// Parameters:
  /// - [params]: Value supplied for the params parameter.
  String call([Object? params = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(params, _undefinedArgument)) {
      args.add(unsafe.dartToJs(params));
    }

    return _toDartString(_callFunction(raw as JSFunction, args))!;
  }
}

/// Full request configuration accepted by `$http(...)`. See
/// http://docs.angularjs.org/api/ng/service/$http#usage
///
/// Raw JavaScript facade for `ng.HttpRequestConfig`.
base class GeneratedNgHttpRequestConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HttpRequestConfig.
  const GeneratedNgHttpRequestConfig(super.raw);

  /// HTTP verb to use for the request.
  String get method => _toDartString(raw.getProperty('method'.toJS))!;

  /// HTTP verb to use for the request.
  set method(String value) {
    raw.setProperty('method'.toJS, unsafe.dartToJs(value));
  }

  /// Request URL. Query parameters from `params` are appended to this URL.
  String get url => _toDartString(raw.getProperty('url'.toJS))!;

  /// Request URL. Query parameters from `params` are appended to this URL.
  set url(String value) {
    raw.setProperty('url'.toJS, unsafe.dartToJs(value));
  }

  /// Event handlers notified by the underlying transport.
  Object? get eventHandlers =>
      unsafe.jsToDart<Object?>(raw.getProperty('eventHandlers'.toJS));

  /// Upload event handlers. Not used by the fetch transport.
  Object? get uploadEventHandlers =>
      unsafe.jsToDart<Object?>(raw.getProperty('uploadEventHandlers'.toJS));

  /// Query parameters appended to the request URL.
  Object? get params =>
      unsafe.jsToDart<Object?>(raw.getProperty('params'.toJS));

  /// Request body. Shorthand methods with explicit data set this automatically.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// Millisecond timeout, or a promise whose resolution aborts the request.
  Object? get timeout =>
      unsafe.jsToDart<Object?>(raw.getProperty('timeout'.toJS));

  /// Native fetch response body reader hint.
  String? get responseType =>
      _toDartString(raw.getProperty('responseType'.toJS));

  /// Native fetch response body reader hint.
  set responseType(String? value) {
    raw.setProperty('responseType'.toJS, unsafe.dartToJs(value));
  }

  /// Cache used for cacheable requests. `true` enables the default cache.
  Object? get cache => unsafe.jsToDart<Object?>(raw.getProperty('cache'.toJS));

  /// Request body transform pipeline.
  Object? get transformRequest =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformRequest'.toJS));

  /// Response body transform pipeline.
  Object? get transformResponse =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformResponse'.toJS));

  /// Default headers merged into each request.
  Object? get headers =>
      unsafe.jsToDart<Object?>(raw.getProperty('headers'.toJS));

  /// Header name used when sending the XSRF token.
  String? get xsrfHeaderName =>
      _toDartString(raw.getProperty('xsrfHeaderName'.toJS));

  /// Header name used when sending the XSRF token.
  set xsrfHeaderName(String? value) {
    raw.setProperty('xsrfHeaderName'.toJS, unsafe.dartToJs(value));
  }

  /// Cookie name used when reading the XSRF token.
  String? get xsrfCookieName =>
      _toDartString(raw.getProperty('xsrfCookieName'.toJS));

  /// Cookie name used when reading the XSRF token.
  set xsrfCookieName(String? value) {
    raw.setProperty('xsrfCookieName'.toJS, unsafe.dartToJs(value));
  }

  /// Whether cross-site requests should include credentials by default.
  bool? get withCredentials =>
      _toDartBool(raw.getProperty('withCredentials'.toJS));

  /// Whether cross-site requests should include credentials by default.
  set withCredentials(bool? value) {
    raw.setProperty('withCredentials'.toJS, unsafe.dartToJs(value));
  }

  /// Query parameter serializer token or function.
  Object? get paramSerializer =>
      unsafe.jsToDart<Object?>(raw.getProperty('paramSerializer'.toJS));
}

/// Request options shared by the `$http` shortcut methods. See
/// http://docs.angularjs.org/api/ng/service/$http#usage
///
/// Raw JavaScript facade for `ng.HttpRequestOptions`.
base class GeneratedNgHttpRequestOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HttpRequestOptions.
  const GeneratedNgHttpRequestOptions(super.raw);

  /// Query parameters appended to the request URL.
  Object? get params =>
      unsafe.jsToDart<Object?>(raw.getProperty('params'.toJS));

  /// Request body. Shorthand methods with explicit data set this automatically.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// Millisecond timeout, or a promise whose resolution aborts the request.
  Object? get timeout =>
      unsafe.jsToDart<Object?>(raw.getProperty('timeout'.toJS));

  /// Native fetch response body reader hint.
  String? get responseType =>
      _toDartString(raw.getProperty('responseType'.toJS));

  /// Native fetch response body reader hint.
  set responseType(String? value) {
    raw.setProperty('responseType'.toJS, unsafe.dartToJs(value));
  }

  /// Cache used for cacheable requests. `true` enables the default cache.
  Object? get cache => unsafe.jsToDart<Object?>(raw.getProperty('cache'.toJS));

  /// Request body transform pipeline.
  Object? get transformRequest =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformRequest'.toJS));

  /// Response body transform pipeline.
  Object? get transformResponse =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformResponse'.toJS));

  /// Default headers merged into each request.
  Object? get headers =>
      unsafe.jsToDart<Object?>(raw.getProperty('headers'.toJS));

  /// Header name used when sending the XSRF token.
  String? get xsrfHeaderName =>
      _toDartString(raw.getProperty('xsrfHeaderName'.toJS));

  /// Header name used when sending the XSRF token.
  set xsrfHeaderName(String? value) {
    raw.setProperty('xsrfHeaderName'.toJS, unsafe.dartToJs(value));
  }

  /// Cookie name used when reading the XSRF token.
  String? get xsrfCookieName =>
      _toDartString(raw.getProperty('xsrfCookieName'.toJS));

  /// Cookie name used when reading the XSRF token.
  set xsrfCookieName(String? value) {
    raw.setProperty('xsrfCookieName'.toJS, unsafe.dartToJs(value));
  }

  /// Whether cross-site requests should include credentials by default.
  bool? get withCredentials =>
      _toDartBool(raw.getProperty('withCredentials'.toJS));

  /// Whether cross-site requests should include credentials by default.
  set withCredentials(bool? value) {
    raw.setProperty('withCredentials'.toJS, unsafe.dartToJs(value));
  }

  /// Query parameter serializer token or function.
  Object? get paramSerializer =>
      unsafe.jsToDart<Object?>(raw.getProperty('paramSerializer'.toJS));
}

/// Response object returned by `$http` requests.
///
/// Raw JavaScript facade for `ng.HttpResponse`.
base class GeneratedNgHttpResponse extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HttpResponse.
  const GeneratedNgHttpResponse(super.raw);

  /// Parsed response body.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// Numeric HTTP status code. Non-2xx statuses reject the promise.
  num get status => _toDartNumber(raw.getProperty('status'.toJS))!;

  /// Numeric HTTP status code. Non-2xx statuses reject the promise.
  set status(num value) {
    raw.setProperty('status'.toJS, unsafe.dartToJs(value));
  }

  /// Lazy response header reader.
  Object? get headers =>
      unsafe.jsToDart<Object?>(raw.getProperty('headers'.toJS));

  /// Request configuration that produced this response.
  Object? get config =>
      unsafe.jsToDart<Object?>(raw.getProperty('config'.toJS));

  /// Native status text such as `OK` or `Not Found`.
  String get statusText => _toDartString(raw.getProperty('statusText'.toJS))!;

  /// Native status text such as `OK` or `Not Found`.
  set statusText(String value) {
    raw.setProperty('statusText'.toJS, unsafe.dartToJs(value));
  }

  /// Transport completion status. Useful for distinguishing timeout, abort, and network errors.
  String get xhrStatus => _toDartString(raw.getProperty('xhrStatus'.toJS))!;

  /// Transport completion status. Useful for distinguishing timeout, abort, and network errors.
  set xhrStatus(String value) {
    raw.setProperty('xhrStatus'.toJS, unsafe.dartToJs(value));
  }
}

/// Final transport status reported by transport completion handlers.
///
/// Raw JavaScript facade for `ng.HttpResponseStatus`.
base class GeneratedNgHttpResponseStatus extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HttpResponseStatus.
  const GeneratedNgHttpResponseStatus(super.raw);
}

/// Runtime surface of the `$http` service. Call the service directly with a full HttpRequestConfig,
/// or use a shorthand method for common HTTP verbs. All methods return an
/// `Promise&lt;HttpResponse&lt;T&gt;&gt;` that resolves for successful 2xx responses and rejects with HttpError
/// for errors.
///
/// Raw JavaScript facade for `ng.HttpService`.
base class GeneratedNgHttpService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.HttpService.
  const GeneratedNgHttpService(super.raw);

  /// Send a request using the full configuration object.
  ///
  /// Parameters:
  /// - [config]: Value supplied for the config parameter.
  JSPromise<JSAny?> call(Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(config));

    return _callFunction(raw as JSFunction, args) as JSPromise<JSAny?>;
  }

  /// Send a `GET` request.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [config]: Value supplied for the config parameter.
  JSPromise<JSAny?> get(String url, [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return raw.callMethodVarArgs('get'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Send a `DELETE` request.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [config]: Value supplied for the config parameter.
  JSPromise<JSAny?> delete(String url, [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return raw.callMethodVarArgs('delete'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Send a `HEAD` request.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [config]: Value supplied for the config parameter.
  JSPromise<JSAny?> head(String url, [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return raw.callMethodVarArgs('head'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Send a `POST` request with a request body.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [data]: Value supplied for the data parameter.
  /// - [config]: Value supplied for the config parameter.
  JSPromise<JSAny?> post(String url, Object? data,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    args.add(unsafe.dartToJs(data));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return raw.callMethodVarArgs('post'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Send a `PUT` request with a request body.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [data]: Value supplied for the data parameter.
  /// - [config]: Value supplied for the config parameter.
  JSPromise<JSAny?> put(String url, Object? data,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    args.add(unsafe.dartToJs(data));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return raw.callMethodVarArgs('put'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Send a `PATCH` request with a request body.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [data]: Value supplied for the data parameter.
  /// - [config]: Value supplied for the config parameter.
  JSPromise<JSAny?> patch(String url, Object? data,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    args.add(unsafe.dartToJs(data));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return raw.callMethodVarArgs('patch'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Live runtime defaults initialized from app-level `$http` configuration.
  Object? get defaults =>
      unsafe.jsToDart<Object?>(raw.getProperty('defaults'.toJS));

  /// Requests currently in flight.
  Object? get pendingRequests =>
      unsafe.jsToDart<Object?>(raw.getProperty('pendingRequests'.toJS));
}

/// Public AngularTS Injectable contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.Injectable`.
base class GeneratedNgInjectable extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Injectable.
  const GeneratedNgInjectable(super.raw);
}

/// Public injectable contracts keyed by their canonical runtime token. Every single-dollar token
/// exposed by [[PublicInjectionTokens]] must map to a named, documented contract here.
/// Double-dollar framework internals are intentionally excluded.
///
/// Raw JavaScript facade for `ng.InjectionTokenMap`.
base class GeneratedNgInjectionTokenMap extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.InjectionTokenMap.
  const GeneratedNgInjectionTokenMap(super.raw);

  /// The $angular member of ng.InjectionTokenMap.
  Object? get $angular =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$angular'.toJS));

  /// The $scope member of ng.InjectionTokenMap.
  Object? get $scope =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$scope'.toJS));

  /// The $element member of ng.InjectionTokenMap.
  Element get $element =>
      unsafe.jsToDart<Element>(raw.getProperty('\$element'.toJS));

  /// The $anchorScroll member of ng.InjectionTokenMap.
  Object? get $anchorScroll =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$anchorScroll'.toJS));

  /// The $animate member of ng.InjectionTokenMap.
  Object? get $animate =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$animate'.toJS));

  /// The $aria member of ng.InjectionTokenMap.
  Object? get $aria => unsafe.jsToDart<Object?>(raw.getProperty('\$aria'.toJS));

  /// The $compile member of ng.InjectionTokenMap.
  Object? get $compile =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$compile'.toJS));

  /// The $controller member of ng.InjectionTokenMap.
  Object? get $controller =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$controller'.toJS));

  /// The $cookie member of ng.InjectionTokenMap.
  Object? get $cookie =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$cookie'.toJS));

  /// The $document member of ng.InjectionTokenMap.
  Object? get $document =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$document'.toJS));

  /// The $eventBus member of ng.InjectionTokenMap.
  Object? get $eventBus =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$eventBus'.toJS));

  /// The $exceptionHandler member of ng.InjectionTokenMap.
  Object? get $exceptionHandler =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$exceptionHandler'.toJS));

  /// The $filter member of ng.InjectionTokenMap.
  Object? get $filter =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$filter'.toJS));

  /// The $htmlCanvas member of ng.InjectionTokenMap.
  Object? get $htmlCanvas =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$htmlCanvas'.toJS));

  /// The $http member of ng.InjectionTokenMap.
  Object? get $http => unsafe.jsToDart<Object?>(raw.getProperty('\$http'.toJS));

  /// The $httpParamSerializer member of ng.InjectionTokenMap.
  Object? get $httpParamSerializer =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$httpParamSerializer'.toJS));

  /// The $injector member of ng.InjectionTokenMap.
  Object? get $injector =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$injector'.toJS));

  /// The $interpolate member of ng.InjectionTokenMap.
  Object? get $interpolate =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$interpolate'.toJS));

  /// The $location member of ng.InjectionTokenMap.
  Object? get $location =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$location'.toJS));

  /// The $log member of ng.InjectionTokenMap.
  Object? get $log => unsafe.jsToDart<Object?>(raw.getProperty('\$log'.toJS));

  /// The $machine member of ng.InjectionTokenMap.
  Object? get $machine =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$machine'.toJS));

  /// The $parse member of ng.InjectionTokenMap.
  Object? get $parse =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$parse'.toJS));

  /// The $rest member of ng.InjectionTokenMap.
  Object? get $rest => unsafe.jsToDart<Object?>(raw.getProperty('\$rest'.toJS));

  /// The $rootElement member of ng.InjectionTokenMap.
  HTMLElement get $rootElement =>
      unsafe.jsToDart<HTMLElement>(raw.getProperty('\$rootElement'.toJS));

  /// The $rootScope member of ng.InjectionTokenMap.
  Object? get $rootScope =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$rootScope'.toJS));

  /// The $sce member of ng.InjectionTokenMap.
  Object? get $sce => unsafe.jsToDart<Object?>(raw.getProperty('\$sce'.toJS));

  /// The $sceDelegate member of ng.InjectionTokenMap.
  Object? get $sceDelegate =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$sceDelegate'.toJS));

  /// The $security member of ng.InjectionTokenMap.
  Object? get $security =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$security'.toJS));

  /// The $serviceWorker member of ng.InjectionTokenMap.
  Object? get $serviceWorker =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$serviceWorker'.toJS));

  /// The $sse member of ng.InjectionTokenMap.
  Object? get $sse => unsafe.jsToDart<Object?>(raw.getProperty('\$sse'.toJS));

  /// The $state member of ng.InjectionTokenMap.
  Object? get $state =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$state'.toJS));

  /// The $stateRegistry member of ng.InjectionTokenMap.
  Object? get $stateRegistry =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$stateRegistry'.toJS));

  /// The $storage member of ng.InjectionTokenMap.
  Object? get $storage =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$storage'.toJS));

  /// The $stream member of ng.InjectionTokenMap.
  Object? get $stream =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$stream'.toJS));

  /// The $templateCache member of ng.InjectionTokenMap.
  Object? get $templateCache =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$templateCache'.toJS));

  /// The $templateRequest member of ng.InjectionTokenMap.
  Object? get $templateRequest =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$templateRequest'.toJS));

  /// The $transitions member of ng.InjectionTokenMap.
  Object? get $transitions =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$transitions'.toJS));

  /// The $wasm member of ng.InjectionTokenMap.
  Object? get $wasm => unsafe.jsToDart<Object?>(raw.getProperty('\$wasm'.toJS));

  /// The $webComponent member of ng.InjectionTokenMap.
  Object? get $webComponent =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$webComponent'.toJS));

  /// The $websocket member of ng.InjectionTokenMap.
  Object? get $websocket =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$websocket'.toJS));

  /// The $webTransport member of ng.InjectionTokenMap.
  Object? get $webTransport =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$webTransport'.toJS));

  /// The $window member of ng.InjectionTokenMap.
  Object? get $window =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$window'.toJS));

  /// The $workflow member of ng.InjectionTokenMap.
  Object? get $workflow =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$workflow'.toJS));

  /// The $workflowSupervisor member of ng.InjectionTokenMap.
  Object? get $workflowSupervisor =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$workflowSupervisor'.toJS));

  /// The $worker member of ng.InjectionTokenMap.
  Object? get $worker =>
      unsafe.jsToDart<Object?>(raw.getProperty('\$worker'.toJS));
}

/// Injector for factories and services
///
/// Raw JavaScript facade for `ng.InjectorService`.
base class GeneratedNgInjectorService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.InjectorService.
  const GeneratedNgInjectorService(super.raw);

  /// The loadNewModules member of ng.InjectorService.
  ///
  /// Parameters:
  /// - [mods]: Value supplied for the mods parameter.
  void loadNewModules(Object? mods) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(mods));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('loadNewModules'.toJS, args));
  }

  /// The has member of ng.InjectorService.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  bool has(String name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return _toDartBool(raw.callMethodVarArgs('has'.toJS, args))!;
  }

  /// Get a service by name.
  ///
  /// Parameters:
  /// - [serviceName]: Value supplied for the serviceName parameter.
  Object? get(Object? serviceName) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(serviceName));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('get'.toJS, args));
  }

  /// Invoke a function with optional context and locals.
  ///
  /// Parameters:
  /// - [fn]: Value supplied for the fn parameter.
  /// - [self]: Value supplied for the self parameter.
  /// - [locals]: Value supplied for the locals parameter.
  /// - [serviceName]: Value supplied for the serviceName parameter.
  Object? invoke(Object? fn,
      [Object? self = _undefinedArgument,
      Object? locals = _undefinedArgument,
      Object? serviceName = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(fn));
    if (!identical(self, _undefinedArgument)) {
      args.add(unsafe.dartToJs(self));
    }
    if (!identical(locals, _undefinedArgument)) {
      args.add(unsafe.dartToJs(locals));
    }
    if (!identical(serviceName, _undefinedArgument)) {
      args.add(unsafe.dartToJs(serviceName));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('invoke'.toJS, args));
  }

  /// Instantiate a type constructor with optional locals.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [locals]: Value supplied for the locals parameter.
  /// - [serviceName]: Value supplied for the serviceName parameter.
  Object? instantiate(Object? typeValue,
      [Object? locals = _undefinedArgument,
      Object? serviceName = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    if (!identical(locals, _undefinedArgument)) {
      args.add(unsafe.dartToJs(locals));
    }
    if (!identical(serviceName, _undefinedArgument)) {
      args.add(unsafe.dartToJs(serviceName));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('instantiate'.toJS, args));
  }
}

/// Delimiter configuration accepted by `NgModule.config()`.
///
/// Raw JavaScript facade for `ng.InterpolateConfig`.
base class GeneratedNgInterpolateConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.InterpolateConfig.
  const GeneratedNgInterpolateConfig(super.raw);

  /// Opening delimiter. Defaults to `{{`.
  String? get startSymbol => _toDartString(raw.getProperty('startSymbol'.toJS));

  /// Opening delimiter. Defaults to `{{`.
  set startSymbol(String? value) {
    raw.setProperty('startSymbol'.toJS, unsafe.dartToJs(value));
  }

  /// Closing delimiter. Defaults to `}}`.
  String? get endSymbol => _toDartString(raw.getProperty('endSymbol'.toJS));

  /// Closing delimiter. Defaults to `}}`.
  set endSymbol(String? value) {
    raw.setProperty('endSymbol'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS InterpolateService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.InterpolateService`.
base class GeneratedNgInterpolateService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.InterpolateService.
  const GeneratedNgInterpolateService(super.raw);

  /// Calls the ng.InterpolateService function.
  ///
  /// Parameters:
  /// - [text]: Value supplied for the text parameter.
  /// - [mustHaveExpression]: Value supplied for the mustHaveExpression parameter.
  /// - [trustedContext]: Value supplied for the trustedContext parameter.
  /// - [allOrNothing]: Value supplied for the allOrNothing parameter.
  Object? call(String text,
      [Object? mustHaveExpression = _undefinedArgument,
      Object? trustedContext = _undefinedArgument,
      Object? allOrNothing = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(text));
    if (!identical(mustHaveExpression, _undefinedArgument)) {
      args.add(unsafe.dartToJs(mustHaveExpression));
    }
    if (!identical(trustedContext, _undefinedArgument)) {
      args.add(unsafe.dartToJs(trustedContext));
    }
    if (!identical(allOrNothing, _undefinedArgument)) {
      args.add(unsafe.dartToJs(allOrNothing));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }

  /// Return the configured interpolation end delimiter.
  String endSymbol() {
    return _toDartString(
        raw.callMethodVarArgs('endSymbol'.toJS, const <JSAny?>[]))!;
  }

  /// Return the configured interpolation start delimiter.
  String startSymbol() {
    return _toDartString(
        raw.callMethodVarArgs('startSymbol'.toJS, const <JSAny?>[]))!;
  }
}

/// Public AngularTS InterpolationFunction contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.InterpolationFunction`.
base class GeneratedNgInterpolationFunction extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.InterpolationFunction.
  const GeneratedNgInterpolationFunction(super.raw);

  /// Evaluate the interpolation.
  ///
  /// Parameters:
  /// - [context]: The scope/context
  /// - [cb]: Optional callback when expressions change
  Object? call(Object? context, [Object? cb = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(context));
    if (!identical(cb, _undefinedArgument)) {
      args.add(unsafe.dartToJs(cb));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }

  /// Expressions extracted from the interpolation text.
  Object? get expressions =>
      unsafe.jsToDart<Object?>(raw.getProperty('expressions'.toJS));

  /// Original interpolation text.
  String get exp => _toDartString(raw.getProperty('exp'.toJS))!;

  /// Original interpolation text.
  set exp(String value) {
    raw.setProperty('exp'.toJS, unsafe.dartToJs(value));
  }
}

/// A function returned by the `$compile` service that links a compiled template to a scope.
///
/// Raw JavaScript facade for `ng.LinkFn`.
base class GeneratedNgLinkFn extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.LinkFn.
  const GeneratedNgLinkFn(super.raw);

  /// Calls the ng.LinkFn function.
  ///
  /// Parameters:
  /// - [scope]: Value supplied for the scope parameter.
  /// - [cloneAttachFn]: Value supplied for the cloneAttachFn parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? call(Scope<Object?> scope,
      [Object? cloneAttachFn = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(scope));
    if (!identical(cloneAttachFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(cloneAttachFn));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }

  /// The pre member of ng.LinkFn.
  Object? get pre => unsafe.jsToDart<Object?>(raw.getProperty('pre'.toJS));

  /// The post member of ng.LinkFn.
  Object? get post => unsafe.jsToDart<Object?>(raw.getProperty('post'.toJS));
}

/// Public watcher callback shape. The first argument is the resolved watched value. The second
/// argument is the original target object used when the watcher was registered.
///
/// Raw JavaScript facade for `ng.ListenerFn`.
base class GeneratedNgListenerFn extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ListenerFn.
  const GeneratedNgListenerFn(super.raw);

  /// Calls the ng.ListenerFn function.
  ///
  /// Parameters:
  /// - [newValue]: Value supplied for the newValue parameter.
  /// - [originalTarget]: Value supplied for the originalTarget parameter.
  void call(
      [Object? newValue = _undefinedArgument,
      Object? originalTarget = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(newValue, _undefinedArgument)) {
      args.add(unsafe.dartToJs(newValue));
    }
    if (!identical(originalTarget, _undefinedArgument)) {
      args.add(unsafe.dartToJs(originalTarget));
    }

    unsafe.jsToDart<void>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS LocationService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.LocationService`.
base class GeneratedNgLocationService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.LocationService.
  const GeneratedNgLocationService(super.raw);

  /// The appBase member of ng.LocationService.
  String get appBase => _toDartString(raw.getProperty('appBase'.toJS))!;

  /// The appBase member of ng.LocationService.
  set appBase(String value) {
    raw.setProperty('appBase'.toJS, unsafe.dartToJs(value));
  }

  /// The appBaseNoFile member of ng.LocationService.
  String get appBaseNoFile =>
      _toDartString(raw.getProperty('appBaseNoFile'.toJS))!;

  /// The appBaseNoFile member of ng.LocationService.
  set appBaseNoFile(String value) {
    raw.setProperty('appBaseNoFile'.toJS, unsafe.dartToJs(value));
  }

  /// The html5 member of ng.LocationService.
  bool get html5 => _toDartBool(raw.getProperty('html5'.toJS))!;

  /// The html5 member of ng.LocationService.
  set html5(bool value) {
    raw.setProperty('html5'.toJS, unsafe.dartToJs(value));
  }

  /// The basePrefix member of ng.LocationService.
  String? get basePrefix => _toDartString(raw.getProperty('basePrefix'.toJS));

  /// The basePrefix member of ng.LocationService.
  set basePrefix(String? value) {
    raw.setProperty('basePrefix'.toJS, unsafe.dartToJs(value));
  }

  /// The hashPrefix member of ng.LocationService.
  String? get hashPrefix => _toDartString(raw.getProperty('hashPrefix'.toJS));

  /// The hashPrefix member of ng.LocationService.
  set hashPrefix(String? value) {
    raw.setProperty('hashPrefix'.toJS, unsafe.dartToJs(value));
  }

  /// The absUrl member of ng.LocationService.
  String get absUrl => _toDartString(raw.getProperty('absUrl'.toJS))!;

  /// The absUrl member of ng.LocationService.
  set absUrl(String value) {
    raw.setProperty('absUrl'.toJS, unsafe.dartToJs(value));
  }

  /// Set the path, search, and hash, then return `$location`.
  ///
  /// Parameters:
  /// - [url]: New URL without base prefix (e.g. `/path?a=b#hash`).
  GeneratedNgLocationService setUrl(String url) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));

    raw.callMethodVarArgs('setUrl'.toJS, args);
    return this;
  }

  /// Return the current URL (for example, `/path?a=b#hash`).
  String getUrl() {
    return _toDartString(
        raw.callMethodVarArgs('getUrl'.toJS, const <JSAny?>[]))!;
  }

  /// Changes the path parameter and returns `$location`.
  ///
  /// Parameters:
  /// - [path]: New path.
  GeneratedNgLocationService setPath(Object? path) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(path));

    raw.callMethodVarArgs('setPath'.toJS, args);
    return this;
  }

  /// Returns the path of the current URL.
  String getPath() {
    return _toDartString(
        raw.callMethodVarArgs('getPath'.toJS, const <JSAny?>[]))!;
  }

  /// Set the hash fragment and return `$location`.
  ///
  /// Parameters:
  /// - [hash]: New hash fragment.
  GeneratedNgLocationService setHash(Object? hash) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(hash));

    raw.callMethodVarArgs('setHash'.toJS, args);
    return this;
  }

  /// Return the current hash fragment.
  String getHash() {
    return _toDartString(
        raw.callMethodVarArgs('getHash'.toJS, const <JSAny?>[]))!;
  }

  /// Sets the search part of the current URL as an object.
  ///
  /// Parameters:
  /// - [search]: New search params as a string or object.
  /// - [paramValue]: If `search` is a string or number, overrides only a single search property.
  GeneratedNgLocationService setSearch(Object? search,
      [Object? paramValue = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(search));
    if (!identical(paramValue, _undefinedArgument)) {
      args.add(unsafe.dartToJs(paramValue));
    }

    raw.callMethodVarArgs('setSearch'.toJS, args);
    return this;
  }

  /// Returns the search part of the current URL as an object.
  Object? getSearch() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('getSearch'.toJS, const <JSAny?>[]));
  }

  /// Set the history state object and return `$location`. The state object is later passed to
  /// `pushState` or `replaceState`. See {@link
  /// https://developer.mozilla.org/en-US/docs/Web/API/History/pushState#state History.state} NOTE:
  /// This method is supported only in HTML5 mode and only in browsers supporting the HTML5 History
  /// API (i.e. methods `pushState` and `replaceState`). If you need to support older browsers (like
  /// IE9 or Android &lt; 4.0), don't use this method.
  ///
  /// Parameters:
  /// - [state]: Value supplied for the state parameter.
  GeneratedNgLocationService setState(Object? state) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(state));

    raw.callMethodVarArgs('setState'.toJS, args);
    return this;
  }

  /// Returns the current history state object.
  Object? getState() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('getState'.toJS, const <JSAny?>[]));
  }

  /// Attempts to parse a clicked link into an app-relative URL update.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [relHref]: Value supplied for the relHref parameter.
  bool parseLinkUrl(String url, String? relHref) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    args.add(unsafe.dartToJs(relHref));

    return _toDartBool(raw.callMethodVarArgs('parseLinkUrl'.toJS, args))!;
  }

  /// Parse given HTML5 (regular) URL string into properties
  ///
  /// Parameters:
  /// - [url]: HTML5 URL
  void parse(String url) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('parse'.toJS, args));
  }
}

/// Declarative remote logging configuration for `navigator.sendBeacon()`.
///
/// Raw JavaScript facade for `ng.LogBeaconConfig`.
base class GeneratedNgLogBeaconConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.LogBeaconConfig.
  const GeneratedNgLogBeaconConfig(super.raw);

  /// Action taken when serialization or Beacon queueing fails.
  String? get failure => _toDartString(raw.getProperty('failure'.toJS));

  /// Action taken when serialization or Beacon queueing fails.
  set failure(String? value) {
    raw.setProperty('failure'.toJS, unsafe.dartToJs(value));
  }

  /// Levels delivered remotely. Defaults to `error` only.
  Object? get levels =>
      unsafe.jsToDart<Object?>(raw.getProperty('levels'.toJS));

  /// Name of an injectable LogBeaconSerializer.
  String? get serializer => _toDartString(raw.getProperty('serializer'.toJS));

  /// Name of an injectable LogBeaconSerializer.
  set serializer(String? value) {
    raw.setProperty('serializer'.toJS, unsafe.dartToJs(value));
  }

  /// Beacon endpoint URL.
  String get url => _toDartString(raw.getProperty('url'.toJS))!;

  /// Beacon endpoint URL.
  set url(String value) {
    raw.setProperty('url'.toJS, unsafe.dartToJs(value));
  }
}

/// Converts a structured log entry into a Beacon-compatible request body.
///
/// Raw JavaScript facade for `ng.LogBeaconSerializer`.
base class GeneratedNgLogBeaconSerializer extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.LogBeaconSerializer.
  const GeneratedNgLogBeaconSerializer(super.raw);

  /// Calls the ng.LogBeaconSerializer function.
  ///
  /// Parameters:
  /// - [entry]: Value supplied for the entry parameter.
  Object? call(Object? entry) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(entry));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Structured record passed to a configured Beacon serializer.
///
/// Raw JavaScript facade for `ng.LogEntry`.
base class GeneratedNgLogEntry extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.LogEntry.
  const GeneratedNgLogEntry(super.raw);

  /// Arguments originally passed to the logging method.
  Object? get args => unsafe.jsToDart<Object?>(raw.getProperty('args'.toJS));

  /// Logging method that produced this entry.
  String get level => _toDartString(raw.getProperty('level'.toJS))!;

  /// ISO-8601 timestamp captured when the logging method was called.
  String get timestamp => _toDartString(raw.getProperty('timestamp'.toJS))!;
}

/// Logging severity attached to a structured remote log entry.
///
/// Raw JavaScript facade for `ng.LogLevel`.
base class GeneratedNgLogLevel extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.LogLevel.
  const GeneratedNgLogLevel(super.raw);
}

/// Service for logging messages at various levels.
///
/// Raw JavaScript facade for `ng.LogService`.
base class GeneratedNgLogService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.LogService.
  const GeneratedNgLogService(super.raw);

  /// Log a debug message.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  void debug(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('debug'.toJS, args));
  }

  /// Log an error message.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  void error(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('error'.toJS, args));
  }

  /// Log an info message.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  void info(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('info'.toJS, args));
  }

  /// Log a general message.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  void log(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('log'.toJS, args));
  }

  /// Log a warning message.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  void warn(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('warn'.toJS, args));
  }
}

/// Public AngularTS Machine contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.Machine`.
base class GeneratedNgMachine extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Machine.
  const GeneratedNgMachine(super.raw);

  /// The state member of ng.Machine.
  Object? get state => unsafe.jsToDart<Object?>(raw.getProperty('state'.toJS));

  /// The data member of ng.Machine.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// The send member of ng.Machine.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [payload1]: Value supplied for the payload parameter.
  /// - [payload2]: Value supplied for the payload parameter.
  /// - [payload3]: Value supplied for the payload parameter.
  /// - [payload4]: Value supplied for the payload parameter.
  Object? send(Object? typeValue,
      [Object? payload1 = _undefinedArgument,
      Object? payload2 = _undefinedArgument,
      Object? payload3 = _undefinedArgument,
      Object? payload4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    if (!identical(payload1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(payload1));
    }
    if (!identical(payload2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(payload2));
    }
    if (!identical(payload3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(payload3));
    }
    if (!identical(payload4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(payload4));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('send'.toJS, args));
  }

  /// The can member of ng.Machine.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [payload1]: Value supplied for the payload parameter.
  /// - [payload2]: Value supplied for the payload parameter.
  /// - [payload3]: Value supplied for the payload parameter.
  /// - [payload4]: Value supplied for the payload parameter.
  bool can(Object? typeValue,
      [Object? payload1 = _undefinedArgument,
      Object? payload2 = _undefinedArgument,
      Object? payload3 = _undefinedArgument,
      Object? payload4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    if (!identical(payload1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(payload1));
    }
    if (!identical(payload2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(payload2));
    }
    if (!identical(payload3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(payload3));
    }
    if (!identical(payload4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(payload4));
    }

    return _toDartBool(raw.callMethodVarArgs('can'.toJS, args))!;
  }

  /// The matches member of ng.Machine.
  ///
  /// Parameters:
  /// - [state]: Value supplied for the state parameter.
  bool matches(Object? state) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(state));

    return _toDartBool(raw.callMethodVarArgs('matches'.toJS, args))!;
  }

  /// The snapshot member of ng.Machine.
  Object? snapshot() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('snapshot'.toJS, const <JSAny?>[]));
  }

  /// The restore member of ng.Machine.
  ///
  /// Parameters:
  /// - [snapshot]: Value supplied for the snapshot parameter.
  void restore(Object? snapshot) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(snapshot));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('restore'.toJS, args));
  }
}

/// Public AngularTS MachineConfig contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.MachineConfig`.
base class GeneratedNgMachineConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.MachineConfig.
  const GeneratedNgMachineConfig(super.raw);

  /// The id member of ng.MachineConfig.
  String? get id => _toDartString(raw.getProperty('id'.toJS));

  /// The id member of ng.MachineConfig.
  set id(String? value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The initial member of ng.MachineConfig.
  Object? get initial =>
      unsafe.jsToDart<Object?>(raw.getProperty('initial'.toJS));

  /// The states member of ng.MachineConfig.
  Object? get states =>
      unsafe.jsToDart<Object?>(raw.getProperty('states'.toJS));

  /// The hooks member of ng.MachineConfig.
  Object? get hooks => unsafe.jsToDart<Object?>(raw.getProperty('hooks'.toJS));

  /// The policy member of ng.MachineConfig.
  Object? get policy =>
      unsafe.jsToDart<Object?>(raw.getProperty('policy'.toJS));

  /// The meta member of ng.MachineConfig.
  Object? get meta => unsafe.jsToDart<Object?>(raw.getProperty('meta'.toJS));

  /// The data member of ng.MachineConfig.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));
}

/// Labeled type contract carried by a machine definition and instance.
///
/// Raw JavaScript facade for `ng.MachineContract`.
base class GeneratedNgMachineContract extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.MachineContract.
  const GeneratedNgMachineContract(super.raw);

  /// The data member of ng.MachineContract.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// The events member of ng.MachineContract.
  Object? get events =>
      unsafe.jsToDart<Object?>(raw.getProperty('events'.toJS));

  /// The state member of ng.MachineContract.
  String get state => _toDartString(raw.getProperty('state'.toJS))!;

  /// The state member of ng.MachineContract.
  set state(String value) {
    raw.setProperty('state'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS MachineSendResult contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.MachineSendResult`.
base class GeneratedNgMachineSendResult extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.MachineSendResult.
  const GeneratedNgMachineSendResult(super.raw);

  /// The type member of ng.MachineSendResult.
  String get typeMember => _toDartString(raw.getProperty('type'.toJS))!;

  /// The from member of ng.MachineSendResult.
  Object? get from => unsafe.jsToDart<Object?>(raw.getProperty('from'.toJS));

  /// The to member of ng.MachineSendResult.
  Object? get to => unsafe.jsToDart<Object?>(raw.getProperty('to'.toJS));

  /// The ok member of ng.MachineSendResult.
  bool get ok => _toDartBool(raw.getProperty('ok'.toJS))!;

  /// The status member of ng.MachineSendResult.
  String get status => _toDartString(raw.getProperty('status'.toJS))!;
}

/// Public AngularTS MachineSendStatus contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.MachineSendStatus`.
base class GeneratedNgMachineSendStatus extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.MachineSendStatus.
  const GeneratedNgMachineSendStatus(super.raw);
}

/// Public AngularTS MachineService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.MachineService`.
base class GeneratedNgMachineService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.MachineService.
  const GeneratedNgMachineService(super.raw);

  /// Calls the ng.MachineService function.
  ///
  /// Parameters:
  /// - [config]: Value supplied for the config parameter.
  Object? call(Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(config));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS MachineSnapshot contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.MachineSnapshot`.
base class GeneratedNgMachineSnapshot extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.MachineSnapshot.
  const GeneratedNgMachineSnapshot(super.raw);

  /// The state member of ng.MachineSnapshot.
  Object? get state => unsafe.jsToDart<Object?>(raw.getProperty('state'.toJS));

  /// The data member of ng.MachineSnapshot.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));
}

/// Public AngularTS Model contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.Model`.
base class GeneratedNgModel extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Model.
  const GeneratedNgModel(super.raw);

  /// The id member of ng.Model.
  num get id => _toDartNumber(raw.getProperty('id'.toJS))!;

  /// The id member of ng.Model.
  set id(num value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The root member of ng.Model.
  Object? get root => unsafe.jsToDart<Object?>(raw.getProperty('root'.toJS));

  /// The parent member of ng.Model.
  Object? get parent =>
      unsafe.jsToDart<Object?>(raw.getProperty('parent'.toJS));

  /// The scopeName member of ng.Model.
  String? get scopeName => _toDartString(raw.getProperty('scopeName'.toJS));

  /// The scopeName member of ng.Model.
  set scopeName(String? value) {
    raw.setProperty('scopeName'.toJS, unsafe.dartToJs(value));
  }

  /// Intercepts and handles property assignments on the target object. Scopeable objects are stored
  /// as raw model values and proxied lazily when read.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being set.
  /// - [value]: The new value being assigned to the property.
  /// - [proxy]: The proxy intercepting property access.
  bool setMember(
      Object? target, String property, Object? value, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(value));
    args.add(unsafe.dartToJs(proxy));

    return _toDartBool(raw.callMethodVarArgs('set'.toJS, args))!;
  }

  /// Intercepts property access on the target object. It checks for specific properties (`watch` and
  /// `sync`) and binds their methods. For other properties, it returns the value directly.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being accessed.
  /// - [proxy]: The proxy object being invoked.
  Object? getMember(Object? target, Object? property, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(proxy));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('get'.toJS, args));
  }

  /// The deleteProperty member of ng.Model.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being deleted.
  bool deleteProperty(Object? target, Object? property) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));

    return _toDartBool(raw.callMethodVarArgs('deleteProperty'.toJS, args))!;
  }

  /// Runs synchronous scope mutations as one batch. Listener notifications are queued while the
  /// callback runs and flushed once after the outermost batch exits. Mutations are not rolled back if
  /// the callback throws.
  ///
  /// Parameters:
  /// - [fn]: Value supplied for the fn parameter.
  Object? batch(JSFunction fn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(fn));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('batch'.toJS, args));
  }

  /// Registers a watcher for a property along with a listener function. The listener function is
  /// invoked when changes to that property are detected.
  ///
  /// Parameters:
  /// - [watchProp]: An expression to be watched in the context of this model.
  /// - [listenerFn]: A function to execute when changes are detected on watched context.
  /// - [lazy]: A flag to indicate if the listener should be invoked immediately. Defaults to false.
  /// - [directLeaf]: Value supplied for the directLeaf parameter.
  Object? watch(String watchProp,
      [Object? listenerFn = _undefinedArgument,
      Object? lazy = _undefinedArgument,
      Object? directLeaf = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(watchProp));
    if (!identical(listenerFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(listenerFn));
    }
    if (!identical(lazy, _undefinedArgument)) {
      args.add(unsafe.dartToJs(lazy));
    }
    if (!identical(directLeaf, _undefinedArgument)) {
      args.add(unsafe.dartToJs(directLeaf));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('watch'.toJS, args));
  }

  /// Creates a prototypically inherited child scope.
  ///
  /// Parameters:
  /// - [childInstance]: Value supplied for the childInstance parameter.
  Object? newMember([Object? childInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(childInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(childInstance));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('new'.toJS, args));
  }

  /// Creates an isolate child scope that does not inherit watchable properties directly.
  ///
  /// Parameters:
  /// - [instance]: Value supplied for the instance parameter.
  Object? newIsolate([Object? instance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(instance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(instance));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('newIsolate'.toJS, args));
  }

  /// Creates a transcluded child scope linked to this scope and an optional parent instance.
  ///
  /// Parameters:
  /// - [parentInstance]: Value supplied for the parentInstance parameter.
  Object? transcluded([Object? parentInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(parentInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(parentInstance));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('transcluded'.toJS, args));
  }

  /// Merges enumerable properties from the provided object into the current scope target.
  ///
  /// Parameters:
  /// - [newTarget]: Value supplied for the newTarget parameter.
  void merge(Object? newTarget) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(newTarget));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('merge'.toJS, args));
  }

  /// Registers an event listener on this scope and returns a deregistration function.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [listener]: Value supplied for the listener parameter.
  Object? onMember(String name, JSFunction listener) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(listener));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('on'.toJS, args));
  }

  /// Emits an event upward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? emit(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('emit'.toJS, args));
  }

  /// Broadcasts an event downward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? broadcast(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('broadcast'.toJS, args));
  }

  /// Destroys this scope and its descendants.
  void destroy() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('destroy'.toJS, const <JSAny?>[]));
  }

  /// Searches this scope tree for a scope with the given id.
  ///
  /// Parameters:
  /// - [id]: Value supplied for the id parameter.
  Object? getById(Object? id) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getById'.toJS, args));
  }

  /// Searches the scope tree for a scope registered under the provided name.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  Object? searchByName(String name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('searchByName'.toJS, args));
  }

  /// The snapshot member of ng.Model.
  Object? snapshot() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('snapshot'.toJS, const <JSAny?>[]));
  }

  /// The restore member of ng.Model.
  ///
  /// Parameters:
  /// - [snapshot]: Value supplied for the snapshot parameter.
  /// - [options]: Value supplied for the options parameter.
  void restore(Object? snapshot, [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(snapshot));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('restore'.toJS, args));
  }

  /// The sync member of ng.Model.
  ///
  /// Parameters:
  /// - [target]: Value supplied for the target parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? syncMember(Object? target, [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('sync'.toJS, args));
  }
}

/// Public AngularTS ModelChange contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ModelChange`.
base class GeneratedNgModelChange extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ModelChange.
  const GeneratedNgModelChange(super.raw);

  /// The origin member of ng.ModelChange.
  String? get origin => _toDartString(raw.getProperty('origin'.toJS));

  /// The origin member of ng.ModelChange.
  set origin(String? value) {
    raw.setProperty('origin'.toJS, unsafe.dartToJs(value));
  }

  /// The keys member of ng.ModelChange.
  Object? get keys => unsafe.jsToDart<Object?>(raw.getProperty('keys'.toJS));

  /// The snapshotVersion member of ng.ModelChange.
  num get snapshotVersion =>
      _toDartNumber(raw.getProperty('snapshotVersion'.toJS))!;

  /// The snapshotVersion member of ng.ModelChange.
  set snapshotVersion(num value) {
    raw.setProperty('snapshotVersion'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS ModelRestoreOptions contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ModelRestoreOptions`.
base class GeneratedNgModelRestoreOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ModelRestoreOptions.
  const GeneratedNgModelRestoreOptions(super.raw);

  /// The origin member of ng.ModelRestoreOptions.
  String? get origin => _toDartString(raw.getProperty('origin'.toJS));

  /// The origin member of ng.ModelRestoreOptions.
  set origin(String? value) {
    raw.setProperty('origin'.toJS, unsafe.dartToJs(value));
  }

  /// The mode member of ng.ModelRestoreOptions.
  String? get mode => _toDartString(raw.getProperty('mode'.toJS));

  /// The mode member of ng.ModelRestoreOptions.
  set mode(String? value) {
    raw.setProperty('mode'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS ModelSyncFailureMode contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ModelSyncFailureMode`.
base class GeneratedNgModelSyncFailureMode extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ModelSyncFailureMode.
  const GeneratedNgModelSyncFailureMode(super.raw);
}

/// Public AngularTS ModelSyncOptions contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ModelSyncOptions`.
base class GeneratedNgModelSyncOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ModelSyncOptions.
  const GeneratedNgModelSyncOptions(super.raw);

  /// The failure member of ng.ModelSyncOptions.
  String? get failure => _toDartString(raw.getProperty('failure'.toJS));

  /// The failure member of ng.ModelSyncOptions.
  set failure(String? value) {
    raw.setProperty('failure'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS ModelSyncTarget contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ModelSyncTarget`.
base class GeneratedNgModelSyncTarget extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ModelSyncTarget.
  const GeneratedNgModelSyncTarget(super.raw);

  /// The restore member of ng.ModelSyncTarget.
  Object? restore() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('restore'.toJS, const <JSAny?>[]));
  }

  /// The write member of ng.ModelSyncTarget.
  ///
  /// Parameters:
  /// - [snapshot]: Value supplied for the snapshot parameter.
  /// - [change]: Value supplied for the change parameter.
  JSPromise<JSAny?> write(Object? snapshot, Object? change) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(snapshot));
    args.add(unsafe.dartToJs(change));

    return raw.callMethodVarArgs('write'.toJS, args) as JSPromise<JSAny?>;
  }

  /// The receive member of ng.ModelSyncTarget.
  ///
  /// Parameters:
  /// - [apply]: Value supplied for the apply parameter.
  Object? receive(JSFunction apply) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(apply));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('receive'.toJS, args));
  }

  /// The dispose member of ng.ModelSyncTarget.
  void dispose() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('dispose'.toJS, const <JSAny?>[]));
  }
}

/// Public AngularTS NgModelController contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.NgModelController`.
base class GeneratedNgNgModelController extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.NgModelController.
  const GeneratedNgNgModelController(super.raw);

  /// The viewValue member of ng.NgModelController.
  Object? get viewValue =>
      unsafe.jsToDart<Object?>(raw.getProperty('viewValue'.toJS));

  /// The modelValue member of ng.NgModelController.
  Object? get modelValue =>
      unsafe.jsToDart<Object?>(raw.getProperty('modelValue'.toJS));

  /// The validators member of ng.NgModelController.
  Object? get validators =>
      unsafe.jsToDart<Object?>(raw.getProperty('validators'.toJS));

  /// The asyncValidators member of ng.NgModelController.
  Object? get asyncValidators =>
      unsafe.jsToDart<Object?>(raw.getProperty('asyncValidators'.toJS));

  /// The parsers member of ng.NgModelController.
  Object? get parsers =>
      unsafe.jsToDart<Object?>(raw.getProperty('parsers'.toJS));

  /// The formatters member of ng.NgModelController.
  Object? get formatters =>
      unsafe.jsToDart<Object?>(raw.getProperty('formatters'.toJS));

  /// The viewChangeListeners member of ng.NgModelController.
  Object? get viewChangeListeners =>
      unsafe.jsToDart<Object?>(raw.getProperty('viewChangeListeners'.toJS));

  /// The untouched member of ng.NgModelController.
  bool get untouched => _toDartBool(raw.getProperty('untouched'.toJS))!;

  /// The untouched member of ng.NgModelController.
  set untouched(bool value) {
    raw.setProperty('untouched'.toJS, unsafe.dartToJs(value));
  }

  /// The touched member of ng.NgModelController.
  bool get touched => _toDartBool(raw.getProperty('touched'.toJS))!;

  /// The touched member of ng.NgModelController.
  set touched(bool value) {
    raw.setProperty('touched'.toJS, unsafe.dartToJs(value));
  }

  /// The pristine member of ng.NgModelController.
  bool get pristine => _toDartBool(raw.getProperty('pristine'.toJS))!;

  /// The pristine member of ng.NgModelController.
  set pristine(bool value) {
    raw.setProperty('pristine'.toJS, unsafe.dartToJs(value));
  }

  /// The dirty member of ng.NgModelController.
  bool get dirty => _toDartBool(raw.getProperty('dirty'.toJS))!;

  /// The dirty member of ng.NgModelController.
  set dirty(bool value) {
    raw.setProperty('dirty'.toJS, unsafe.dartToJs(value));
  }

  /// The valid member of ng.NgModelController.
  bool? get valid => _toDartBool(raw.getProperty('valid'.toJS));

  /// The valid member of ng.NgModelController.
  set valid(bool? value) {
    raw.setProperty('valid'.toJS, unsafe.dartToJs(value));
  }

  /// The invalid member of ng.NgModelController.
  bool? get invalid => _toDartBool(raw.getProperty('invalid'.toJS));

  /// The invalid member of ng.NgModelController.
  set invalid(bool? value) {
    raw.setProperty('invalid'.toJS, unsafe.dartToJs(value));
  }

  /// The validity member of ng.NgModelController.
  Object? get validity =>
      unsafe.jsToDart<Object?>(raw.getProperty('validity'.toJS));

  /// The validationMessage member of ng.NgModelController.
  String get validationMessage =>
      _toDartString(raw.getProperty('validationMessage'.toJS))!;

  /// The validationMessage member of ng.NgModelController.
  set validationMessage(String value) {
    raw.setProperty('validationMessage'.toJS, unsafe.dartToJs(value));
  }

  /// The error member of ng.NgModelController.
  Object? get error => unsafe.jsToDart<Object?>(raw.getProperty('error'.toJS));

  /// The pending member of ng.NgModelController.
  Object? get pending =>
      unsafe.jsToDart<Object?>(raw.getProperty('pending'.toJS));

  /// The controlName member of ng.NgModelController.
  Object? get controlName =>
      unsafe.jsToDart<Object?>(raw.getProperty('controlName'.toJS));

  /// The options member of ng.NgModelController.
  Object? get options =>
      unsafe.jsToDart<Object?>(raw.getProperty('options'.toJS));

  /// Updates the validation state of the control and propagates it to the parent form.
  ///
  /// Parameters:
  /// - [validationErrorKey]: Value supplied for the validationErrorKey parameter.
  /// - [state]: Value supplied for the state parameter.
  void setValidity(String validationErrorKey, bool? state) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(validationErrorKey));
    args.add(unsafe.dartToJs(state));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('setValidity'.toJS, args));
  }

  /// The setNativeValidity member of ng.NgModelController.
  ///
  /// Parameters:
  /// - [state]: Value supplied for the state parameter.
  void setNativeValidity(bool? state) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(state));

    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('setNativeValidity'.toJS, args));
  }

  /// Sets the control's single native custom-validity message. Native controls expose this through
  /// `ValidityState.customError`; an empty message clears the custom error.
  ///
  /// Parameters:
  /// - [message]: Value supplied for the message parameter.
  void setCustomValidity(String message) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(message));

    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('setCustomValidity'.toJS, args));
  }

  /// Called when the view needs to be updated. It is expected that the user of the ng-model directive
  /// will implement this method. The `render()` method is invoked in the following situations: *
  /// `rollbackViewValue()` is called. If we are rolling back the view value to the last committed
  /// value then `render()` is called to update the input control. * The value referenced by
  /// `ng-model` is changed programmatically and both the `modelValue` and the `viewValue` are
  /// different from last time. Since `ng-model` does not do a deep watch, `render()` is only invoked
  /// if the values of `modelValue` and `viewValue` are actually different from their previous values.
  /// If `modelValue` or `viewValue` are objects (rather than a string or number) then `render()` will
  /// not be invoked if you only change a property on the objects.
  void render() {
    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('render'.toJS, const <JSAny?>[]));
  }

  /// This is called when we need to determine if the value of an input is empty. For instance, the
  /// required directive does this to work out if the input has data or not. The default `isEmpty`
  /// function checks whether the value is `undefined`, `''`, `null` or `NaN`. You can override this
  /// for input directives whose concept of being empty is different from the default. The
  /// `checkboxInputType` directive does this because in its case a value of `false` implies empty.
  ///
  /// Parameters:
  /// - [value]: The value of the input to check for emptiness.
  bool isEmpty(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return _toDartBool(raw.callMethodVarArgs('isEmpty'.toJS, args))!;
  }

  /// Sets the control to its pristine state. This method can be called to remove the `ng-dirty` class
  /// and set the control to its pristine state (`ng-pristine` class). A model is considered to be
  /// pristine when the control has not been changed from when first compiled.
  void setPristine() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('setPristine'.toJS, const <JSAny?>[]));
  }

  /// Sets the control to its dirty state. This method can be called to remove the `ng-pristine` class
  /// and set the control to its dirty state (`ng-dirty` class). A model is considered to be dirty
  /// when the control has been changed from when first compiled.
  void setDirty() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('setDirty'.toJS, const <JSAny?>[]));
  }

  /// Sets the control to its untouched state. This method can be called to remove the `ng-touched`
  /// class and set the control to its untouched state (`ng-untouched` class). Upon compilation, a
  /// model is set as untouched by default, however this function can be used to restore that state if
  /// the model has already been touched by the user.
  void setUntouched() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('setUntouched'.toJS, const <JSAny?>[]));
  }

  /// Sets the control to its touched state. This method can be called to remove the `ng-untouched`
  /// class and set the control to its touched state (`ng-touched` class). A model is considered to be
  /// touched when the user has first focused the control element and then shifted focus away from the
  /// control (blur event).
  void setTouched() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('setTouched'.toJS, const <JSAny?>[]));
  }

  /// Cancel an update and reset the input element's value to prevent an update to the `modelValue`,
  /// which may be caused by a pending debounced event or because the input is waiting for some future
  /// event. If you have an input that uses `ng-model-options` to set up debounced updates or updates
  /// that depend on special events such as `blur`, there can be a period when the `viewValue` is out
  /// of sync with the ngModel's `modelValue`. In this case, you can use `rollbackViewValue()` to
  /// manually cancel the debounced / future update and reset the input to the last committed view
  /// value. It is also possible that you run into difficulties if you try to update the ngModel's
  /// `modelValue` programmatically before these debounced/future events have resolved/occurred,
  /// because AngularTS's dirty checking mechanism is not able to tell whether the model has actually
  /// changed or not. The `rollbackViewValue()` method should be called before programmatically
  /// changing the model of an input which may have such events pending. This is important in order to
  /// make sure that the input field will be updated with the new model value and any pending
  /// operations are cancelled.
  void rollbackViewValue() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('rollbackViewValue'.toJS, const <JSAny?>[]));
  }

  /// Runs each of the registered validators (first synchronous validators and then asynchronous
  /// validators). If the validity changes to invalid, the model will be set to `undefined`, unless
  /// `ngModelOptions.allowInvalid` is `true`. If the validity changes to valid, it will set the model
  /// to the last available valid `modelValue`, i.e. either the last parsed value or the last value
  /// set from the scope.
  void validate() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('validate'.toJS, const <JSAny?>[]));
  }

  /// Commit a pending update to the `modelValue`. Updates may be pending by a debounced event or
  /// because the input is waiting for a some future event defined in `ng-model-options`. this method
  /// is rarely needed as `NgModelController` usually handles calling this in response to input
  /// events.
  void commitViewValue() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('commitViewValue'.toJS, const <JSAny?>[]));
  }

  /// Update the view value. This method should be called when a control wants to change the view
  /// value; typically, this is done from within a DOM event handler. For example, the `input`
  /// directive calls it when the value of the input changes and `select` calls it when an option is
  /// selected. When `setViewValue` is called, the new `value` will be staged for committing through
  /// the `parsers` and `validators` pipelines. If there are no special `ngModelOptions` settings
  /// specified then the staged value is sent directly for processing through the `parsers` pipeline.
  /// After this, the `validators` and `asyncValidators` are called and the value is applied to
  /// `modelValue`. Finally, the value is set to the **expression** specified in the `ng-model`
  /// attribute and all the registered change listeners, in the `viewChangeListeners` list are called.
  /// In case the `ngModelOptions` directive is used with `updateOn` and the `default` trigger is not
  /// listed, all those actions will remain pending until one of the `updateOn` events is triggered on
  /// the DOM element. All these actions will be debounced if the `ngModelOptions` directive is used
  /// with a custom debounce for this particular event. The model commit and its reactive
  /// notifications run when an `updateOn` event fires, or after the configured debounce timer
  /// expires. Standard native inputs pass through browser-native values, such as strings from
  /// text-like controls, booleans from checkboxes, and `FileList | null` from file inputs. However,
  /// custom controls might also pass objects to this method. In this case, we should make a copy of
  /// the object before passing it to `setViewValue`. This is because `ngModel` does not perform a
  /// deep watch of objects, it only looks for a change of identity. If you only change the property
  /// of the object then ngModel will not realize that the object has changed and will not invoke the
  /// `parsers` and `validators` pipelines. For this reason, you should not change properties of the
  /// copy once it has been passed to `setViewValue`. Otherwise you may cause the model value on the
  /// scope to change incorrectly. &lt;div class="alert alert-info"&gt; In any case, the value passed to the
  /// method should always reflect the current value of the control. For example, if you are calling
  /// `setViewValue` for an input element, you should pass the input DOM value. Otherwise, the control
  /// and the scope model become out of sync. It's also important to note that `setViewValue` does not
  /// call `render` or change the control's DOM value in any way. If we want to change the control's
  /// DOM value programmatically, we should update the `ngModel` scope expression. Its new value will
  /// be picked up by the model controller, which will run it through the `formatters`, `render` it to
  /// update the DOM, and finally call `validate` on it. &lt;/div&gt;
  ///
  /// Parameters:
  /// - [value]: value from the view.
  /// - [trigger]: Event that triggered the update.
  void setViewValue(Object? value, [Object? trigger = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));
    if (!identical(trigger, _undefinedArgument)) {
      args.add(unsafe.dartToJs(trigger));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('setViewValue'.toJS, args));
  }

  /// Override the current model options settings programmatically. The previous `ModelOptions` value
  /// will not be modified. Instead, a new `ModelOptions` object will inherit from the previous one
  /// overriding or inheriting settings that are defined in the given parameter. See `ngModelOptions`
  /// for information about what options can be specified and how model option inheritance works. &lt;div
  /// class="alert alert-warning"&gt; **Note:** this function only affects the options set on the
  /// `ngModelController`, and not the options on the `ngModelOptions` directive from which they might
  /// have been obtained initially. &lt;/div&gt; &lt;div class="alert alert-danger"&gt; **Note:** it is not
  /// possible to override the `getterSetter` option. &lt;/div&gt;
  ///
  /// Parameters:
  /// - [options]: a hash of settings to override the previous options
  void overrideModelOptions(Object? options) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(options));

    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('overrideModelOptions'.toJS, args));
  }

  /// Runs the model -&gt; view pipeline on the current NgModelController.modelValuemodelValue. The
  /// following actions are performed by this method: - the `modelValue` is run through the
  /// NgModelController.formattersformatters and the result is set to the
  /// NgModelController.viewValueviewValue - the `ng-empty` or `ng-not-empty` class is set on the
  /// element - if the `viewValue` has changed: - NgModelController.renderrender is called on the
  /// control - the NgModelController.validatorsvalidators are run and the validation status is set.
  /// This method is called by ngModel internally when the bound scope value changes. Application
  /// developers usually do not have to call this function themselves. This function can be used when
  /// the `viewValue` or the rendered DOM value are not correctly formatted and the `modelValue` must
  /// be run through the `formatters` again.
  void processModelValue() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('processModelValue'.toJS, const <JSAny?>[]));
  }
}

/// Modules are collections of application configuration information for components: controllers,
/// directives, filters, etc. They provide recipes for the injector to do the actual instantiation.
/// A module itself has no behaviour but only state. A such, it acts as a data structure between the
/// Angular instance and the injector service.
///
/// Raw JavaScript facade for `ng.NgModule`.
base class GeneratedNgNgModule extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.NgModule.
  const GeneratedNgNgModule(super.raw);

  /// The name member of ng.NgModule.
  String get name => _toDartString(raw.getProperty('name'.toJS))!;

  /// The name member of ng.NgModule.
  set name(String value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// The value member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [object]: Allows undefined
  GeneratedNgNgModule rawValue(String name, Object? object) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(object));

    raw.callMethodVarArgs('value'.toJS, args);
    return this;
  }

  /// The constant member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [object]: Value supplied for the object parameter.
  GeneratedNgNgModule rawConstant(String name, Object? object) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(object));

    raw.callMethodVarArgs('constant'.toJS, args);
    return this;
  }

  /// Declare built-in AngularTS service configuration during the config phase.
  ///
  /// Parameters:
  /// - [config]: Built-in config map.
  GeneratedNgNgModule rawConfig(Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('config'.toJS, args);
    return this;
  }

  /// The run member of ng.NgModule.
  ///
  /// Parameters:
  /// - [block]: Value supplied for the block parameter.
  GeneratedNgNgModule rawRun(Object? block) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(block));

    raw.callMethodVarArgs('run'.toJS, args);
    return this;
  }

  /// The component member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [options]: Value supplied for the options parameter.
  GeneratedNgNgModule rawComponent(String name, Object? options) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(options));

    raw.callMethodVarArgs('component'.toJS, args);
    return this;
  }

  /// The factory member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [providerFunction]: Value supplied for the providerFunction parameter.
  GeneratedNgNgModule rawFactory(String name, Object? providerFunction) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(providerFunction));

    raw.callMethodVarArgs('factory'.toJS, args);
    return this;
  }

  /// The service member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [serviceFunction]: Value supplied for the serviceFunction parameter.
  GeneratedNgNgModule rawService(String name, Object? serviceFunction) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(serviceFunction));

    raw.callMethodVarArgs('service'.toJS, args);
    return this;
  }

  /// The provider member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [providerType]: Value supplied for the providerType parameter.
  GeneratedNgNgModule rawProvider(String name, Object? providerType) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(providerType));

    raw.callMethodVarArgs('provider'.toJS, args);
    return this;
  }

  /// The decorator member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [decorFn]: Value supplied for the decorFn parameter.
  GeneratedNgNgModule rawDecorator(String name, Object? decorFn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(decorFn));

    raw.callMethodVarArgs('decorator'.toJS, args);
    return this;
  }

  /// The directive member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [directiveFactory]: Value supplied for the directiveFactory parameter.
  GeneratedNgNgModule rawDirective(String name, Object? directiveFactory) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(directiveFactory));

    raw.callMethodVarArgs('directive'.toJS, args);
    return this;
  }

  /// The animation member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [animationFactory]: Value supplied for the animationFactory parameter.
  GeneratedNgNgModule rawAnimation(String name, Object? animationFactory) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(animationFactory));

    raw.callMethodVarArgs('animation'.toJS, args);
    return this;
  }

  /// The filter member of ng.NgModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [filterFn]: Value supplied for the filterFn parameter.
  GeneratedNgNgModule rawFilter(String name, Object? filterFn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(filterFn));

    raw.callMethodVarArgs('filter'.toJS, args);
    return this;
  }

  /// The $controller service is used by Angular to create new controllers. Named controllers are
  /// stored in the owning runtime's controller registry.
  ///
  /// Parameters:
  /// - [name]: Controller name
  /// - [ctlFn]: Controller constructor fn (optionally decorated with DI annotations in the array notation)
  GeneratedNgNgModule rawController(String name, Object? ctlFn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(ctlFn));

    raw.callMethodVarArgs('controller'.toJS, args);
    return this;
  }

  /// Register a named reactive model as an injectable app-owned service. The model is created lazily
  /// by the owning `AppContext` when the service is first injected. Models are shared across every
  /// root scope managed by the same `AppContext`; they are not children of `$rootScope`. Assign an
  /// injected model to a controller or scope property to bind it in a template. DOM interpolation,
  /// `ng-bind`, directive expressions, nested object reads, and array length reads update when the
  /// app model changes. Mutating the model proxy schedules every affected observer. The injected
  /// `Model&lt;T&gt;` value is proxy-backed. It exposes scope-proxy methods such as `watch`, `batch`,
  /// `merge`, `on`, `emit`, `broadcast`, and `destroy`, plus `snapshot`, `restore`, and `sync` for
  /// model lifecycle and synchronization. Prefer the factory form for nontrivial initial state: ```ts
  /// app.model("user", () =&gt; ({ name: "John", authenticated: false })); ```
  ///
  /// Parameters:
  /// - [name]: Injectable model name.
  /// - [initial]: Plain object state or an injectable factory that returns plain object state.
  GeneratedNgNgModule model(String name, Object? initial) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(initial));

    raw.callMethodVarArgs('model'.toJS, args);
    return this;
  }

  /// Register a named reactive state machine as an injectable service. The machine is created by
  /// `$machine` when the named service is requested. The returned instance is not tied to any one
  /// scope lifetime; it registers with AngularTS scope proxies when assigned to a controller or
  /// scope.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [config]: Machine configuration or a resolvable config factory.
  GeneratedNgNgModule machine(String name, Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('machine'.toJS, args);
    return this;
  }

  /// Register a named workflow as an injectable service. The workflow is created by `$workflow` when
  /// the named service is requested. Workflow behavior remains local to its `WorkflowConfig`; the
  /// provider does not apply global workflow defaults.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [config]: Workflow configuration or a resolvable config factory.
  GeneratedNgNgModule workflow(String name, Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('workflow'.toJS, args);
    return this;
  }

  /// Register a named workflow supervisor as an injectable service. The supervisor is created when
  /// the named service is requested. It composes existing workflow configs or workflow instances and
  /// keeps persistence and recovery policy local to the supervisor config.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [config]: Supervisor configuration or a resolvable config factory.
  GeneratedNgNgModule workflowSupervisor(String name, Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('workflowSupervisor'.toJS, args);
    return this;
  }

  /// Register a module-owned router state tree during module configuration. Child state names are
  /// relative to their parent unless they contain a dot. Each route is queued for the composed router
  /// runtime, so module router trees compose with `lazyState(...)` and inherited route policies.
  ///
  /// Parameters:
  /// - [declaration]: Router tree root declaration.
  GeneratedNgNgModule rawRouter(Object? declaration) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(declaration));

    raw.callMethodVarArgs('router'.toJS, args);
    return this;
  }

  /// Register a lazy router state namespace during module configuration. Lazy route declarations use
  /// the same composed router runtime as static module routes.
  ///
  /// Parameters:
  /// - [prefix]: State name prefix to load on demand.
  /// - [loader]: Loader invoked by the router when a transition targets the prefix.
  GeneratedNgNgModule lazyState(String prefix, Object? loader) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(prefix));
    args.add(unsafe.dartToJs(loader));

    raw.callMethodVarArgs('lazyState'.toJS, args);
    return this;
  }

  /// Register a named WebAssembly module as an injectable resource. The actual loading is delegated
  /// to the `$wasm` service, so custom runtimes can decide whether WebAssembly support is included.
  ///
  /// Parameters:
  /// - [name]: Injectable name used to access the resource.
  /// - [config]: Module source and imports, optionally produced by DI.
  GeneratedNgNgModule rawWasm(String name, Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('wasm'.toJS, args);
    return this;
  }

  /// Register a named Web Worker connection as an injectable service. The actual connection is
  /// delegated to the `$worker` provider, so worker support remains provider-driven instead of
  /// directive-driven.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [scriptPath]: Worker script URL, optionally produced by DI.
  /// - [config]: Worker connection options, optionally produced by DI.
  GeneratedNgNgModule rawWorker(String name, Object? scriptPath,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(scriptPath));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('worker'.toJS, args);
    return this;
  }

  /// Configure the singleton `$serviceWorker` for this application.
  ///
  /// Parameters:
  /// - [scriptUrl]: Value supplied for the scriptUrl parameter.
  /// - [config]: Value supplied for the config parameter.
  GeneratedNgNgModule serviceWorker(Object? scriptUrl,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(scriptUrl));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('serviceWorker'.toJS, args);
    return this;
  }

  /// Register a persistent object store as an injectable service. Store construction is delegated to
  /// the internal provider registry, which creates the service through the injector and persists it
  /// through the selected backend.
  ///
  /// Parameters:
  /// - [name]: Service name.
  /// - [ctor]: Constructor, factory, or object to persist.
  /// - [typeValue]: Storage backend type.
  /// - [backendOrConfig]: Custom backend or persistence options.
  GeneratedNgNgModule rawStore(String name, Object? ctor, String typeValue,
      [Object? backendOrConfig = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(ctor));
    args.add(unsafe.dartToJs(typeValue));
    if (!identical(backendOrConfig, _undefinedArgument)) {
      args.add(unsafe.dartToJs(backendOrConfig));
    }

    raw.callMethodVarArgs('store'.toJS, args);
    return this;
  }

  /// Register a REST resource as an injectable service. The resource factory is delegated to the
  /// injected `$rest` service, keeping REST support configurable by custom runtimes.
  ///
  /// Parameters:
  /// - [name]: Service name.
  /// - [url]: Base URL or URI template.
  /// - [entityClass]: Optional constructor for mapping JSON.
  /// - [options]: Optional RestService options, optionally produced by DI.
  GeneratedNgNgModule rawRest(String name, String url,
      [Object? entityClass = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(url));
    if (!identical(entityClass, _undefinedArgument)) {
      args.add(unsafe.dartToJs(entityClass));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    raw.callMethodVarArgs('rest'.toJS, args);
    return this;
  }

  /// Register a pre-configured SSE connection as an injectable service. The connection is created by
  /// `$sse` when the named service is requested.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [url]: SSE endpoint.
  /// - [config]: SSE connection options, optionally produced by DI.
  GeneratedNgNgModule rawSse(String name, String url,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('sse'.toJS, args);
    return this;
  }

  /// Register a pre-configured WebSocket connection as an injectable service. The connection is
  /// created by `$websocket` when the named service is requested.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [url]: WebSocket endpoint.
  /// - [config]: WebSocket connection options, optionally produced by DI.
  GeneratedNgNgModule rawWebsocket(String name, String url,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('websocket'.toJS, args);
    return this;
  }

  /// Register a pre-configured WebTransport connection as an injectable service. The connection is
  /// created by `$webTransport` when the named service is requested.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [url]: WebTransport endpoint.
  /// - [config]: WebTransport connection options, optionally produced by DI.
  GeneratedNgNgModule rawWebTransport(String name, String url,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('webTransport'.toJS, args);
    return this;
  }

  /// Register an options-backed application host custom element. The definition is installed when the
  /// module runs. The host element is a native custom element backed by an AngularTS child scope.
  ///
  /// Parameters:
  /// - [name]: Custom element tag name.
  /// - [options]: App component options.
  GeneratedNgNgModule rawAppComponent(String name, Object? options) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(options));

    raw.callMethodVarArgs('appComponent'.toJS, args);
    return this;
  }

  /// Register a user-authored native custom element backed by an AngularTS scope. The element class
  /// must extend `ScopeElement`. Its static template, shadow, scope, inputs, and isolate properties
  /// configure the AngularTS wiring.
  ///
  /// Parameters:
  /// - [name]: Custom element tag name.
  /// - [elementClass]: Custom element class.
  GeneratedNgNgModule rawWebComponent(String name, Object? elementClass) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(elementClass));

    raw.callMethodVarArgs('webComponent'.toJS, args);
    return this;
  }
}

/// Params declared by one route in a public route map.
///
/// Raw JavaScript facade for `ng.ParamsOf`.
base class GeneratedNgParamsOf extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ParamsOf.
  const GeneratedNgParamsOf(super.raw);
}

/// Parses a string or expression function into a compiled expression.
///
/// Raw JavaScript facade for `ng.ParseService`.
base class GeneratedNgParseService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ParseService.
  const GeneratedNgParseService(super.raw);

  /// Calls the ng.ParseService function.
  ///
  /// Parameters:
  /// - [expression]: Value supplied for the expression parameter.
  /// - [interceptorFn]: Value supplied for the interceptorFn parameter.
  Object? call(String expression,
      [Object? interceptorFn = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(expression));
    if (!identical(interceptorFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(interceptorFn));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS Policy contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.Policy`.
base class GeneratedNgPolicy extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Policy.
  const GeneratedNgPolicy(super.raw);

  /// Calls the ng.Policy function.
  ///
  /// Parameters:
  /// - [context]: Value supplied for the context parameter.
  Object? call(Object? context) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(context));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS PolicyContext contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.PolicyContext`.
base class GeneratedNgPolicyContext extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.PolicyContext.
  const GeneratedNgPolicyContext(super.raw);

  /// The operation member of ng.PolicyContext.
  Object? get operation =>
      unsafe.jsToDart<Object?>(raw.getProperty('operation'.toJS));

  /// The meta member of ng.PolicyContext.
  Object? get meta => unsafe.jsToDart<Object?>(raw.getProperty('meta'.toJS));
}

/// Public AngularTS PolicyDecision contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.PolicyDecision`.
base class GeneratedNgPolicyDecision extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.PolicyDecision.
  const GeneratedNgPolicyDecision(super.raw);

  /// The type member of ng.PolicyDecision.
  Object? get typeMember =>
      unsafe.jsToDart<Object?>(raw.getProperty('type'.toJS));

  /// The reason member of ng.PolicyDecision.
  String? get reason => _toDartString(raw.getProperty('reason'.toJS));

  /// The meta member of ng.PolicyDecision.
  Object? get meta => unsafe.jsToDart<Object?>(raw.getProperty('meta'.toJS));
}

/// Programmatic real-DOM factory used instead of a template.
///
/// Raw JavaScript facade for `ng.ProgrammaticView`.
base class GeneratedNgProgrammaticView extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ProgrammaticView.
  const GeneratedNgProgrammaticView(super.raw);

  /// Calls the ng.ProgrammaticView function.
  ///
  /// Parameters:
  /// - [context]: Value supplied for the context parameter.
  Object? call(Object? context) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(context));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// DOM content accepted from a programmatic view. Readers are reactive, arrays are flattened
/// recursively, and existing nodes are moved rather than cloned.
///
/// Raw JavaScript facade for `ng.ProgrammaticViewChild`.
base class GeneratedNgProgrammaticViewChild extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ProgrammaticViewChild.
  const GeneratedNgProgrammaticViewChild(super.raw);

  /// Calls the ng.ProgrammaticViewChild function.
  Object? call() {
    return unsafe
        .jsToDart<Object?>(_callFunction(raw as JSFunction, const <JSAny?>[]));
  }
}

/// Runtime context passed to a programmatic component or directive view.
///
/// Raw JavaScript facade for `ng.ProgrammaticViewContext`.
base class GeneratedNgProgrammaticViewContext extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ProgrammaticViewContext.
  const GeneratedNgProgrammaticViewContext(super.raw);

  /// Controller associated with the view.
  Object? get controller =>
      unsafe.jsToDart<Object?>(raw.getProperty('controller'.toJS));

  /// Controllers resolved through a directive's require declaration.
  Object? get requiredMember =>
      unsafe.jsToDart<Object?>(raw.getProperty('required'.toJS));

  /// Scope that owns the generated DOM and reactive readers.
  Object? get scope => unsafe.jsToDart<Object?>(raw.getProperty('scope'.toJS));

  /// Native host element matched by the component or directive.
  Object? get host => unsafe.jsToDart<Object?>(raw.getProperty('host'.toJS));

  /// Transclusion function, when transclusion is enabled.
  Object? get transclude =>
      unsafe.jsToDart<Object?>(raw.getProperty('transclude'.toJS));

  /// Registers cleanup and returns a cancellation function.
  Object? get onDestroy =>
      unsafe.jsToDart<Object?>(raw.getProperty('onDestroy'.toJS));
}

/// Primitive accepted as a programmatic view child. Booleans render no content.
///
/// Raw JavaScript facade for `ng.ProgrammaticViewPrimitive`.
base class GeneratedNgProgrammaticViewPrimitive extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ProgrammaticViewPrimitive.
  const GeneratedNgProgrammaticViewPrimitive(super.raw);
}

/// Typed DOM properties plus arbitrary attribute and custom-element values.
///
/// Raw JavaScript facade for `ng.ProgrammaticViewProperties`.
base class GeneratedNgProgrammaticViewProperties extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ProgrammaticViewProperties.
  const GeneratedNgProgrammaticViewProperties(super.raw);

  /// [MDN Reference](https://developer.mozilla.org/docs/Web/API/Element/role)
  Object? get role => unsafe.jsToDart<Object?>(raw.getProperty('role'.toJS));

  /// The onabort member of ng.ProgrammaticViewProperties.
  Object? get onabort =>
      unsafe.jsToDart<Object?>(raw.getProperty('onabort'.toJS));

  /// The onanimationcancel member of ng.ProgrammaticViewProperties.
  Object? get onanimationcancel =>
      unsafe.jsToDart<Object?>(raw.getProperty('onanimationcancel'.toJS));

  /// The onanimationend member of ng.ProgrammaticViewProperties.
  Object? get onanimationend =>
      unsafe.jsToDart<Object?>(raw.getProperty('onanimationend'.toJS));

  /// The onanimationiteration member of ng.ProgrammaticViewProperties.
  Object? get onanimationiteration =>
      unsafe.jsToDart<Object?>(raw.getProperty('onanimationiteration'.toJS));

  /// The onanimationstart member of ng.ProgrammaticViewProperties.
  Object? get onanimationstart =>
      unsafe.jsToDart<Object?>(raw.getProperty('onanimationstart'.toJS));

  /// The onauxclick member of ng.ProgrammaticViewProperties.
  Object? get onauxclick =>
      unsafe.jsToDart<Object?>(raw.getProperty('onauxclick'.toJS));

  /// The onbeforeinput member of ng.ProgrammaticViewProperties.
  Object? get onbeforeinput =>
      unsafe.jsToDart<Object?>(raw.getProperty('onbeforeinput'.toJS));

  /// The onbeforematch member of ng.ProgrammaticViewProperties.
  Object? get onbeforematch =>
      unsafe.jsToDart<Object?>(raw.getProperty('onbeforematch'.toJS));

  /// The onbeforetoggle member of ng.ProgrammaticViewProperties.
  Object? get onbeforetoggle =>
      unsafe.jsToDart<Object?>(raw.getProperty('onbeforetoggle'.toJS));

  /// The onblur member of ng.ProgrammaticViewProperties.
  Object? get onblur =>
      unsafe.jsToDart<Object?>(raw.getProperty('onblur'.toJS));

  /// The oncancel member of ng.ProgrammaticViewProperties.
  Object? get oncancel =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncancel'.toJS));

  /// The oncanplay member of ng.ProgrammaticViewProperties.
  Object? get oncanplay =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncanplay'.toJS));

  /// The oncanplaythrough member of ng.ProgrammaticViewProperties.
  Object? get oncanplaythrough =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncanplaythrough'.toJS));

  /// The onchange member of ng.ProgrammaticViewProperties.
  Object? get onchange =>
      unsafe.jsToDart<Object?>(raw.getProperty('onchange'.toJS));

  /// The onclick member of ng.ProgrammaticViewProperties.
  Object? get onclick =>
      unsafe.jsToDart<Object?>(raw.getProperty('onclick'.toJS));

  /// The onclose member of ng.ProgrammaticViewProperties.
  Object? get onclose =>
      unsafe.jsToDart<Object?>(raw.getProperty('onclose'.toJS));

  /// The oncompositionend member of ng.ProgrammaticViewProperties.
  Object? get oncompositionend =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncompositionend'.toJS));

  /// The oncompositionstart member of ng.ProgrammaticViewProperties.
  Object? get oncompositionstart =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncompositionstart'.toJS));

  /// The oncompositionupdate member of ng.ProgrammaticViewProperties.
  Object? get oncompositionupdate =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncompositionupdate'.toJS));

  /// The oncontextlost member of ng.ProgrammaticViewProperties.
  Object? get oncontextlost =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncontextlost'.toJS));

  /// The oncontextmenu member of ng.ProgrammaticViewProperties.
  Object? get oncontextmenu =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncontextmenu'.toJS));

  /// The oncontextrestored member of ng.ProgrammaticViewProperties.
  Object? get oncontextrestored =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncontextrestored'.toJS));

  /// The oncopy member of ng.ProgrammaticViewProperties.
  Object? get oncopy =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncopy'.toJS));

  /// The oncuechange member of ng.ProgrammaticViewProperties.
  Object? get oncuechange =>
      unsafe.jsToDart<Object?>(raw.getProperty('oncuechange'.toJS));

  /// The oncut member of ng.ProgrammaticViewProperties.
  Object? get oncut => unsafe.jsToDart<Object?>(raw.getProperty('oncut'.toJS));

  /// The ondblclick member of ng.ProgrammaticViewProperties.
  Object? get ondblclick =>
      unsafe.jsToDart<Object?>(raw.getProperty('ondblclick'.toJS));

  /// The ondrag member of ng.ProgrammaticViewProperties.
  Object? get ondrag =>
      unsafe.jsToDart<Object?>(raw.getProperty('ondrag'.toJS));

  /// The ondragend member of ng.ProgrammaticViewProperties.
  Object? get ondragend =>
      unsafe.jsToDart<Object?>(raw.getProperty('ondragend'.toJS));

  /// The ondragenter member of ng.ProgrammaticViewProperties.
  Object? get ondragenter =>
      unsafe.jsToDart<Object?>(raw.getProperty('ondragenter'.toJS));

  /// The ondragleave member of ng.ProgrammaticViewProperties.
  Object? get ondragleave =>
      unsafe.jsToDart<Object?>(raw.getProperty('ondragleave'.toJS));

  /// The ondragover member of ng.ProgrammaticViewProperties.
  Object? get ondragover =>
      unsafe.jsToDart<Object?>(raw.getProperty('ondragover'.toJS));

  /// The ondragstart member of ng.ProgrammaticViewProperties.
  Object? get ondragstart =>
      unsafe.jsToDart<Object?>(raw.getProperty('ondragstart'.toJS));

  /// The ondrop member of ng.ProgrammaticViewProperties.
  Object? get ondrop =>
      unsafe.jsToDart<Object?>(raw.getProperty('ondrop'.toJS));

  /// The ondurationchange member of ng.ProgrammaticViewProperties.
  Object? get ondurationchange =>
      unsafe.jsToDart<Object?>(raw.getProperty('ondurationchange'.toJS));

  /// The onemptied member of ng.ProgrammaticViewProperties.
  Object? get onemptied =>
      unsafe.jsToDart<Object?>(raw.getProperty('onemptied'.toJS));

  /// The onended member of ng.ProgrammaticViewProperties.
  Object? get onended =>
      unsafe.jsToDart<Object?>(raw.getProperty('onended'.toJS));

  /// The onerror member of ng.ProgrammaticViewProperties.
  Object? get onerror =>
      unsafe.jsToDart<Object?>(raw.getProperty('onerror'.toJS));

  /// The onfocus member of ng.ProgrammaticViewProperties.
  Object? get onfocus =>
      unsafe.jsToDart<Object?>(raw.getProperty('onfocus'.toJS));

  /// The onfocusin member of ng.ProgrammaticViewProperties.
  Object? get onfocusin =>
      unsafe.jsToDart<Object?>(raw.getProperty('onfocusin'.toJS));

  /// The onfocusout member of ng.ProgrammaticViewProperties.
  Object? get onfocusout =>
      unsafe.jsToDart<Object?>(raw.getProperty('onfocusout'.toJS));

  /// The onformdata member of ng.ProgrammaticViewProperties.
  Object? get onformdata =>
      unsafe.jsToDart<Object?>(raw.getProperty('onformdata'.toJS));

  /// The ongotpointercapture member of ng.ProgrammaticViewProperties.
  Object? get ongotpointercapture =>
      unsafe.jsToDart<Object?>(raw.getProperty('ongotpointercapture'.toJS));

  /// The oninput member of ng.ProgrammaticViewProperties.
  Object? get oninput =>
      unsafe.jsToDart<Object?>(raw.getProperty('oninput'.toJS));

  /// The oninvalid member of ng.ProgrammaticViewProperties.
  Object? get oninvalid =>
      unsafe.jsToDart<Object?>(raw.getProperty('oninvalid'.toJS));

  /// The onkeydown member of ng.ProgrammaticViewProperties.
  Object? get onkeydown =>
      unsafe.jsToDart<Object?>(raw.getProperty('onkeydown'.toJS));

  /// The onkeypress member of ng.ProgrammaticViewProperties.
  Object? get onkeypress =>
      unsafe.jsToDart<Object?>(raw.getProperty('onkeypress'.toJS));

  /// The onkeyup member of ng.ProgrammaticViewProperties.
  Object? get onkeyup =>
      unsafe.jsToDart<Object?>(raw.getProperty('onkeyup'.toJS));

  /// The onload member of ng.ProgrammaticViewProperties.
  Object? get onload =>
      unsafe.jsToDart<Object?>(raw.getProperty('onload'.toJS));

  /// The onloadeddata member of ng.ProgrammaticViewProperties.
  Object? get onloadeddata =>
      unsafe.jsToDart<Object?>(raw.getProperty('onloadeddata'.toJS));

  /// The onloadedmetadata member of ng.ProgrammaticViewProperties.
  Object? get onloadedmetadata =>
      unsafe.jsToDart<Object?>(raw.getProperty('onloadedmetadata'.toJS));

  /// The onloadstart member of ng.ProgrammaticViewProperties.
  Object? get onloadstart =>
      unsafe.jsToDart<Object?>(raw.getProperty('onloadstart'.toJS));

  /// The onlostpointercapture member of ng.ProgrammaticViewProperties.
  Object? get onlostpointercapture =>
      unsafe.jsToDart<Object?>(raw.getProperty('onlostpointercapture'.toJS));

  /// The onmousedown member of ng.ProgrammaticViewProperties.
  Object? get onmousedown =>
      unsafe.jsToDart<Object?>(raw.getProperty('onmousedown'.toJS));

  /// The onmouseenter member of ng.ProgrammaticViewProperties.
  Object? get onmouseenter =>
      unsafe.jsToDart<Object?>(raw.getProperty('onmouseenter'.toJS));

  /// The onmouseleave member of ng.ProgrammaticViewProperties.
  Object? get onmouseleave =>
      unsafe.jsToDart<Object?>(raw.getProperty('onmouseleave'.toJS));

  /// The onmousemove member of ng.ProgrammaticViewProperties.
  Object? get onmousemove =>
      unsafe.jsToDart<Object?>(raw.getProperty('onmousemove'.toJS));

  /// The onmouseout member of ng.ProgrammaticViewProperties.
  Object? get onmouseout =>
      unsafe.jsToDart<Object?>(raw.getProperty('onmouseout'.toJS));

  /// The onmouseover member of ng.ProgrammaticViewProperties.
  Object? get onmouseover =>
      unsafe.jsToDart<Object?>(raw.getProperty('onmouseover'.toJS));

  /// The onmouseup member of ng.ProgrammaticViewProperties.
  Object? get onmouseup =>
      unsafe.jsToDart<Object?>(raw.getProperty('onmouseup'.toJS));

  /// The onpaste member of ng.ProgrammaticViewProperties.
  Object? get onpaste =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpaste'.toJS));

  /// The onpause member of ng.ProgrammaticViewProperties.
  Object? get onpause =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpause'.toJS));

  /// The onplay member of ng.ProgrammaticViewProperties.
  Object? get onplay =>
      unsafe.jsToDart<Object?>(raw.getProperty('onplay'.toJS));

  /// The onplaying member of ng.ProgrammaticViewProperties.
  Object? get onplaying =>
      unsafe.jsToDart<Object?>(raw.getProperty('onplaying'.toJS));

  /// The onpointercancel member of ng.ProgrammaticViewProperties.
  Object? get onpointercancel =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpointercancel'.toJS));

  /// The onpointerdown member of ng.ProgrammaticViewProperties.
  Object? get onpointerdown =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpointerdown'.toJS));

  /// The onpointerenter member of ng.ProgrammaticViewProperties.
  Object? get onpointerenter =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpointerenter'.toJS));

  /// The onpointerleave member of ng.ProgrammaticViewProperties.
  Object? get onpointerleave =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpointerleave'.toJS));

  /// The onpointermove member of ng.ProgrammaticViewProperties.
  Object? get onpointermove =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpointermove'.toJS));

  /// The onpointerout member of ng.ProgrammaticViewProperties.
  Object? get onpointerout =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpointerout'.toJS));

  /// The onpointerover member of ng.ProgrammaticViewProperties.
  Object? get onpointerover =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpointerover'.toJS));

  /// The onpointerrawupdate member of ng.ProgrammaticViewProperties.
  Object? get onpointerrawupdate =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpointerrawupdate'.toJS));

  /// The onpointerup member of ng.ProgrammaticViewProperties.
  Object? get onpointerup =>
      unsafe.jsToDart<Object?>(raw.getProperty('onpointerup'.toJS));

  /// The onprogress member of ng.ProgrammaticViewProperties.
  Object? get onprogress =>
      unsafe.jsToDart<Object?>(raw.getProperty('onprogress'.toJS));

  /// The onratechange member of ng.ProgrammaticViewProperties.
  Object? get onratechange =>
      unsafe.jsToDart<Object?>(raw.getProperty('onratechange'.toJS));

  /// The onreset member of ng.ProgrammaticViewProperties.
  Object? get onreset =>
      unsafe.jsToDart<Object?>(raw.getProperty('onreset'.toJS));

  /// The onresize member of ng.ProgrammaticViewProperties.
  Object? get onresize =>
      unsafe.jsToDart<Object?>(raw.getProperty('onresize'.toJS));

  /// The onscroll member of ng.ProgrammaticViewProperties.
  Object? get onscroll =>
      unsafe.jsToDart<Object?>(raw.getProperty('onscroll'.toJS));

  /// The onscrollend member of ng.ProgrammaticViewProperties.
  Object? get onscrollend =>
      unsafe.jsToDart<Object?>(raw.getProperty('onscrollend'.toJS));

  /// The onsecuritypolicyviolation member of ng.ProgrammaticViewProperties.
  Object? get onsecuritypolicyviolation => unsafe
      .jsToDart<Object?>(raw.getProperty('onsecuritypolicyviolation'.toJS));

  /// The onseeked member of ng.ProgrammaticViewProperties.
  Object? get onseeked =>
      unsafe.jsToDart<Object?>(raw.getProperty('onseeked'.toJS));

  /// The onseeking member of ng.ProgrammaticViewProperties.
  Object? get onseeking =>
      unsafe.jsToDart<Object?>(raw.getProperty('onseeking'.toJS));

  /// The onselect member of ng.ProgrammaticViewProperties.
  Object? get onselect =>
      unsafe.jsToDart<Object?>(raw.getProperty('onselect'.toJS));

  /// The onselectionchange member of ng.ProgrammaticViewProperties.
  Object? get onselectionchange =>
      unsafe.jsToDart<Object?>(raw.getProperty('onselectionchange'.toJS));

  /// The onselectstart member of ng.ProgrammaticViewProperties.
  Object? get onselectstart =>
      unsafe.jsToDart<Object?>(raw.getProperty('onselectstart'.toJS));

  /// The onslotchange member of ng.ProgrammaticViewProperties.
  Object? get onslotchange =>
      unsafe.jsToDart<Object?>(raw.getProperty('onslotchange'.toJS));

  /// The onstalled member of ng.ProgrammaticViewProperties.
  Object? get onstalled =>
      unsafe.jsToDart<Object?>(raw.getProperty('onstalled'.toJS));

  /// The onsubmit member of ng.ProgrammaticViewProperties.
  Object? get onsubmit =>
      unsafe.jsToDart<Object?>(raw.getProperty('onsubmit'.toJS));

  /// The onsuspend member of ng.ProgrammaticViewProperties.
  Object? get onsuspend =>
      unsafe.jsToDart<Object?>(raw.getProperty('onsuspend'.toJS));

  /// The ontimeupdate member of ng.ProgrammaticViewProperties.
  Object? get ontimeupdate =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontimeupdate'.toJS));

  /// The ontoggle member of ng.ProgrammaticViewProperties.
  Object? get ontoggle =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontoggle'.toJS));

  /// The ontouchcancel member of ng.ProgrammaticViewProperties.
  Object? get ontouchcancel =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontouchcancel'.toJS));

  /// The ontouchend member of ng.ProgrammaticViewProperties.
  Object? get ontouchend =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontouchend'.toJS));

  /// The ontouchmove member of ng.ProgrammaticViewProperties.
  Object? get ontouchmove =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontouchmove'.toJS));

  /// The ontouchstart member of ng.ProgrammaticViewProperties.
  Object? get ontouchstart =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontouchstart'.toJS));

  /// The ontransitioncancel member of ng.ProgrammaticViewProperties.
  Object? get ontransitioncancel =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontransitioncancel'.toJS));

  /// The ontransitionend member of ng.ProgrammaticViewProperties.
  Object? get ontransitionend =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontransitionend'.toJS));

  /// The ontransitionrun member of ng.ProgrammaticViewProperties.
  Object? get ontransitionrun =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontransitionrun'.toJS));

  /// The ontransitionstart member of ng.ProgrammaticViewProperties.
  Object? get ontransitionstart =>
      unsafe.jsToDart<Object?>(raw.getProperty('ontransitionstart'.toJS));

  /// The onvolumechange member of ng.ProgrammaticViewProperties.
  Object? get onvolumechange =>
      unsafe.jsToDart<Object?>(raw.getProperty('onvolumechange'.toJS));

  /// The onwaiting member of ng.ProgrammaticViewProperties.
  Object? get onwaiting =>
      unsafe.jsToDart<Object?>(raw.getProperty('onwaiting'.toJS));

  /// The onwebkitanimationend member of ng.ProgrammaticViewProperties.
  Object? get onwebkitanimationend =>
      unsafe.jsToDart<Object?>(raw.getProperty('onwebkitanimationend'.toJS));

  /// The onwebkitanimationiteration member of ng.ProgrammaticViewProperties.
  Object? get onwebkitanimationiteration => unsafe
      .jsToDart<Object?>(raw.getProperty('onwebkitanimationiteration'.toJS));

  /// The onwebkitanimationstart member of ng.ProgrammaticViewProperties.
  Object? get onwebkitanimationstart =>
      unsafe.jsToDart<Object?>(raw.getProperty('onwebkitanimationstart'.toJS));

  /// The onwebkittransitionend member of ng.ProgrammaticViewProperties.
  Object? get onwebkittransitionend =>
      unsafe.jsToDart<Object?>(raw.getProperty('onwebkittransitionend'.toJS));

  /// The onwheel member of ng.ProgrammaticViewProperties.
  Object? get onwheel =>
      unsafe.jsToDart<Object?>(raw.getProperty('onwheel'.toJS));

  /// The class member of ng.ProgrammaticViewProperties.
  Object? get classMember =>
      unsafe.jsToDart<Object?>(raw.getProperty('class'.toJS));

  /// The is member of ng.ProgrammaticViewProperties.
  String? get isMember => _toDartString(raw.getProperty('is'.toJS));

  /// The is member of ng.ProgrammaticViewProperties.
  set isMember(String? value) {
    raw.setProperty('is'.toJS, unsafe.dartToJs(value));
  }
}

/// Static value or reactive reader accepted by a programmatic view property.
///
/// Raw JavaScript facade for `ng.ProgrammaticViewPropertyValue`.
base class GeneratedNgProgrammaticViewPropertyValue extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ProgrammaticViewPropertyValue.
  const GeneratedNgProgrammaticViewPropertyValue(super.raw);
}

/// Factory that creates one real DOM element without parsing HTML.
///
/// Raw JavaScript facade for `ng.ProgrammaticViewTag`.
base class GeneratedNgProgrammaticViewTag extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ProgrammaticViewTag.
  const GeneratedNgProgrammaticViewTag(super.raw);

  /// Calls the ng.ProgrammaticViewTag function.
  ///
  /// Parameters:
  /// - [first]: Value supplied for the first parameter.
  /// - [children1]: Value supplied for the children parameter.
  /// - [children2]: Value supplied for the children parameter.
  /// - [children3]: Value supplied for the children parameter.
  /// - [children4]: Value supplied for the children parameter.
  Object? call(
      [Object? first = _undefinedArgument,
      Object? children1 = _undefinedArgument,
      Object? children2 = _undefinedArgument,
      Object? children3 = _undefinedArgument,
      Object? children4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(first, _undefinedArgument)) {
      args.add(unsafe.dartToJs(first));
    }
    if (!identical(children1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(children1));
    }
    if (!identical(children2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(children2));
    }
    if (!identical(children3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(children3));
    }
    if (!identical(children4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(children4));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Typed HTML tag factories. Calling the object with a namespace URI returns factories for
/// namespaced elements such as SVG and MathML.
///
/// Raw JavaScript facade for `ng.ProgrammaticViewTags`.
base class GeneratedNgProgrammaticViewTags extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ProgrammaticViewTags.
  const GeneratedNgProgrammaticViewTags(super.raw);

  /// Calls the ng.ProgrammaticViewTags function.
  ///
  /// Parameters:
  /// - [namespaceUri]: Value supplied for the namespaceUri parameter.
  Object? call(String namespaceUri) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(namespaceUri));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// A user-defined service recipe accepted by ng.NgModule.provider. Object recipes define an
/// injectable `get` factory directly. Injectable functions and classes are instantiated first and
/// must produce an object with an injectable `get` factory.
///
/// Raw JavaScript facade for `ng.ProviderDefinition`.
base class GeneratedNgProviderDefinition extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ProviderDefinition.
  const GeneratedNgProviderDefinition(super.raw);

  /// Calls the ng.ProviderDefinition function.
  ///
  /// Parameters:
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? call(
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS RealtimeProtocolEventDetail contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.RealtimeProtocolEventDetail`.
base class GeneratedNgRealtimeProtocolEventDetail extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RealtimeProtocolEventDetail.
  const GeneratedNgRealtimeProtocolEventDetail(super.raw);

  /// The data member of ng.RealtimeProtocolEventDetail.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// The event member of ng.RealtimeProtocolEventDetail.
  Object? get event => unsafe.jsToDart<Object?>(raw.getProperty('event'.toJS));

  /// The source member of ng.RealtimeProtocolEventDetail.
  Object? get source =>
      unsafe.jsToDart<Object?>(raw.getProperty('source'.toJS));

  /// The url member of ng.RealtimeProtocolEventDetail.
  String? get url => _toDartString(raw.getProperty('url'.toJS));

  /// The url member of ng.RealtimeProtocolEventDetail.
  set url(String? value) {
    raw.setProperty('url'.toJS, unsafe.dartToJs(value));
  }

  /// The error member of ng.RealtimeProtocolEventDetail.
  Object? get error => unsafe.jsToDart<Object?>(raw.getProperty('error'.toJS));
}

/// Public AngularTS RealtimeProtocolMessage contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.RealtimeProtocolMessage`.
base class GeneratedNgRealtimeProtocolMessage extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RealtimeProtocolMessage.
  const GeneratedNgRealtimeProtocolMessage(super.raw);

  /// Plain value used as swap content when `html` is omitted.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// HTML or text payload to apply with the configured swap mode.
  Object? get html => unsafe.jsToDart<Object?>(raw.getProperty('html'.toJS));

  /// Optional CSS selector that overrides the directive target for this message.
  String? get target => _toDartString(raw.getProperty('target'.toJS));

  /// Optional CSS selector that overrides the directive target for this message.
  set target(String? value) {
    raw.setProperty('target'.toJS, unsafe.dartToJs(value));
  }

  /// Optional swap mode that overrides the directive swap mode for this message.
  String? get swap => _toDartString(raw.getProperty('swap'.toJS));

  /// Optional swap mode that overrides the directive swap mode for this message.
  set swap(String? value) {
    raw.setProperty('swap'.toJS, unsafe.dartToJs(value));
  }
}

/// Resolve values declared by one route in a public route map.
///
/// Raw JavaScript facade for `ng.ResolvesOf`.
base class GeneratedNgResolvesOf extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ResolvesOf.
  const GeneratedNgResolvesOf(super.raw);
}

/// Backend abstraction used by RestService. Implement this interface to route REST operations
/// through `$http`, IndexedDB, the Cache API, a test double, or a composed backend such as
/// CachedRestBackend.
///
/// Raw JavaScript facade for `ng.RestBackend`.
base class GeneratedNgRestBackend extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestBackend.
  const GeneratedNgRestBackend(super.raw);

  /// Execute one normalized REST request.
  ///
  /// Parameters:
  /// - [request]: Request produced by `RestService`.
  JSPromise<JSAny?> request(Object? request) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(request));

    return raw.callMethodVarArgs('request'.toJS, args) as JSPromise<JSAny?>;
  }
}

/// Public AngularTS RestCachePolicy contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.RestCachePolicy`.
base class GeneratedNgRestCachePolicy extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestCachePolicy.
  const GeneratedNgRestCachePolicy(super.raw);

  /// Calls the ng.RestCachePolicy function.
  ///
  /// Parameters:
  /// - [context]: Value supplied for the context parameter.
  Object? call(Object? context) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(context));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS RestCachePolicyContext contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.RestCachePolicyContext`.
base class GeneratedNgRestCachePolicyContext extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestCachePolicyContext.
  const GeneratedNgRestCachePolicyContext(super.raw);

  /// The method member of ng.RestCachePolicyContext.
  String get method => _toDartString(raw.getProperty('method'.toJS))!;

  /// The url member of ng.RestCachePolicyContext.
  String get url => _toDartString(raw.getProperty('url'.toJS))!;

  /// The collectionUrl member of ng.RestCachePolicyContext.
  String? get collectionUrl =>
      _toDartString(raw.getProperty('collectionUrl'.toJS));

  /// The id member of ng.RestCachePolicyContext.
  Object? get id => unsafe.jsToDart<Object?>(raw.getProperty('id'.toJS));

  /// The params member of ng.RestCachePolicyContext.
  Object? get params =>
      unsafe.jsToDart<Object?>(raw.getProperty('params'.toJS));

  /// The options member of ng.RestCachePolicyContext.
  Object? get options =>
      unsafe.jsToDart<Object?>(raw.getProperty('options'.toJS));

  /// The cacheKey member of ng.RestCachePolicyContext.
  String get cacheKey => _toDartString(raw.getProperty('cacheKey'.toJS))!;

  /// The operation member of ng.RestCachePolicyContext.
  String get operation => _toDartString(raw.getProperty('operation'.toJS))!;

  /// The meta member of ng.RestCachePolicyContext.
  Object? get meta => unsafe.jsToDart<Object?>(raw.getProperty('meta'.toJS));
}

/// Async cache store used by CachedRestBackend. The interface is deliberately small so
/// implementations can be backed by IndexedDB, the browser Cache API, local storage, memory, or
/// test fixtures.
///
/// Raw JavaScript facade for `ng.RestCacheStore`.
base class GeneratedNgRestCacheStore extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestCacheStore.
  const GeneratedNgRestCacheStore(super.raw);

  /// Read a cached REST response by deterministic key.
  ///
  /// Parameters:
  /// - [key]: Opaque cache key supplied by CachedRestBackend.
  JSPromise<JSAny?> getMember(String key) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));

    return raw.callMethodVarArgs('get'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Store a REST response by deterministic key.
  ///
  /// Parameters:
  /// - [key]: Opaque cache key supplied by CachedRestBackend.
  /// - [response]: Response to persist.
  JSPromise<JSAny?> setMember(String key, Object? response) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));
    args.add(unsafe.dartToJs(response));

    return raw.callMethodVarArgs('set'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Delete one cached REST response.
  ///
  /// Parameters:
  /// - [key]: Exact cache key to remove.
  JSPromise<JSAny?> delete(String key) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));

    return raw.callMethodVarArgs('delete'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Delete cached REST responses whose keys start with the prefix. `CachedRestBackend` uses prefixes
  /// such as `GET /api/users` to invalidate collection and entity entries after successful writes.
  ///
  /// Parameters:
  /// - [prefix]: Value supplied for the prefix parameter.
  JSPromise<JSAny?> deletePrefix(String prefix) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(prefix));

    return raw.callMethodVarArgs('deletePrefix'.toJS, args)
        as JSPromise<JSAny?>;
  }
}

/// Cache read strategies shared by HTTP, REST, and router transports.
///
/// Raw JavaScript facade for `ng.RestCacheStrategy`.
base class GeneratedNgRestCacheStrategy extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestCacheStrategy.
  const GeneratedNgRestCacheStrategy(super.raw);
}

/// Factory service exposed as `$rest`. Creates a typed RestService for a base URL, optional entity
/// mapper, and optional backend request defaults.
///
/// Raw JavaScript facade for `ng.RestFactory`.
base class GeneratedNgRestFactory extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestFactory.
  const GeneratedNgRestFactory(super.raw);

  /// Calls the ng.RestFactory function.
  ///
  /// Parameters:
  /// - [baseUrl]: Value supplied for the baseUrl parameter.
  /// - [entityClass]: Value supplied for the entityClass parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? call(String baseUrl,
      [Object? entityClass = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(baseUrl));
    if (!identical(entityClass, _undefinedArgument)) {
      args.add(unsafe.dartToJs(entityClass));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Extra backend options merged into requests made by a REST resource.
///
/// Raw JavaScript facade for `ng.RestOptions`.
base class GeneratedNgRestOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestOptions.
  const GeneratedNgRestOptions(super.raw);

  /// Optional backend used instead of the default HTTP backend.
  Object? get backend =>
      unsafe.jsToDart<Object?>(raw.getProperty('backend'.toJS));
}

/// Normalized request object passed from RestService to a RestBackend. Backends receive expanded
/// URLs and already-separated request options, so they can focus on transport, persistence, or
/// cache policy.
///
/// Raw JavaScript facade for `ng.RestRequest`.
base class GeneratedNgRestRequest extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestRequest.
  const GeneratedNgRestRequest(super.raw);

  /// Resource operation method.
  String get method => _toDartString(raw.getProperty('method'.toJS))!;

  /// Resource operation method.
  set method(String value) {
    raw.setProperty('method'.toJS, unsafe.dartToJs(value));
  }

  /// Expanded request URL.
  String get url => _toDartString(raw.getProperty('url'.toJS))!;

  /// Expanded request URL.
  set url(String value) {
    raw.setProperty('url'.toJS, unsafe.dartToJs(value));
  }

  /// Collection URL used for broad cache invalidation.
  String? get collectionUrl =>
      _toDartString(raw.getProperty('collectionUrl'.toJS));

  /// Collection URL used for broad cache invalidation.
  set collectionUrl(String? value) {
    raw.setProperty('collectionUrl'.toJS, unsafe.dartToJs(value));
  }

  /// Resource identifier for entity operations.
  Object? get id => unsafe.jsToDart<Object?>(raw.getProperty('id'.toJS));

  /// Request body for write operations.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// URI template and query parameters.
  Object? get params =>
      unsafe.jsToDart<Object?>(raw.getProperty('params'.toJS));

  /// Backend-specific request options.
  Object? get options =>
      unsafe.jsToDart<Object?>(raw.getProperty('options'.toJS));
}

/// Response shape returned by RestBackend implementations. HTTP-backed responses may include the
/// usual HttpResponse metadata, while local or cached backends can return only `data` plus optional
/// cache source metadata.
///
/// Raw JavaScript facade for `ng.RestResponse`.
base class GeneratedNgRestResponse extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestResponse.
  const GeneratedNgRestResponse(super.raw);

  /// Response payload.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// Backend that produced the response.
  String? get source => _toDartString(raw.getProperty('source'.toJS));

  /// Backend that produced the response.
  set source(String? value) {
    raw.setProperty('source'.toJS, unsafe.dartToJs(value));
  }

  /// Whether the returned cached value may be older than the remote source.
  bool? get stale => _toDartBool(raw.getProperty('stale'.toJS));

  /// Whether the returned cached value may be older than the remote source.
  set stale(bool? value) {
    raw.setProperty('stale'.toJS, unsafe.dartToJs(value));
  }

  /// Numeric HTTP status code. Non-2xx statuses reject the promise.
  num? get status => _toDartNumber(raw.getProperty('status'.toJS));

  /// Numeric HTTP status code. Non-2xx statuses reject the promise.
  set status(num? value) {
    raw.setProperty('status'.toJS, unsafe.dartToJs(value));
  }

  /// Lazy response header reader.
  Object? get headers =>
      unsafe.jsToDart<Object?>(raw.getProperty('headers'.toJS));

  /// Request configuration that produced this response.
  Object? get config =>
      unsafe.jsToDart<Object?>(raw.getProperty('config'.toJS));

  /// Native status text such as `OK` or `Not Found`.
  String? get statusText => _toDartString(raw.getProperty('statusText'.toJS));

  /// Native status text such as `OK` or `Not Found`.
  set statusText(String? value) {
    raw.setProperty('statusText'.toJS, unsafe.dartToJs(value));
  }

  /// Transport completion status. Useful for distinguishing timeout, abort, and network errors.
  String? get xhrStatus => _toDartString(raw.getProperty('xhrStatus'.toJS));

  /// Transport completion status. Useful for distinguishing timeout, abort, and network errors.
  set xhrStatus(String? value) {
    raw.setProperty('xhrStatus'.toJS, unsafe.dartToJs(value));
  }
}

/// Event emitted after a stale-while-revalidate background refresh succeeds.
///
/// Raw JavaScript facade for `ng.RestRevalidateEvent`.
base class GeneratedNgRestRevalidateEvent extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestRevalidateEvent.
  const GeneratedNgRestRevalidateEvent(super.raw);

  /// Cache key that was refreshed.
  String get key => _toDartString(raw.getProperty('key'.toJS))!;

  /// Cache key that was refreshed.
  set key(String value) {
    raw.setProperty('key'.toJS, unsafe.dartToJs(value));
  }

  /// Original request.
  Object? get request =>
      unsafe.jsToDart<Object?>(raw.getProperty('request'.toJS));

  /// Fresh network response.
  Object? get response =>
      unsafe.jsToDart<Object?>(raw.getProperty('response'.toJS));
}

/// Typed REST resource client backed by RestBackend. A `RestService` is usually created by
/// injecting `$rest` and calling it with a base URL, optional EntityClass, and optional backend
/// request defaults.
///
/// Raw JavaScript facade for `ng.RestService`.
base class GeneratedNgRestService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RestService.
  const GeneratedNgRestService(super.raw);

  /// Fetch a collection. Parameters are used for URI template expansion and are also forwarded to
  /// `$http` as query params. Non-array responses resolve to an empty array.
  ///
  /// Parameters:
  /// - [params]: Value supplied for the params parameter.
  JSPromise<JSAny?> list([Object? params = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(params, _undefinedArgument)) {
      args.add(unsafe.dartToJs(params));
    }

    return raw.callMethodVarArgs('list'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Fetch one resource by ID using `GET`.
  ///
  /// Parameters:
  /// - [id]: Resource identifier appended to the base URL.
  /// - [params]: Additional URI template or query parameters.
  JSPromise<JSAny?> getMember(Object? id,
      [Object? params = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));
    if (!identical(params, _undefinedArgument)) {
      args.add(unsafe.dartToJs(params));
    }

    return raw.callMethodVarArgs('get'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Create a resource using `POST`.
  ///
  /// Parameters:
  /// - [item]: Request body to create.
  JSPromise<JSAny?> create(Object? item) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(item));

    return raw.callMethodVarArgs('create'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Update a resource using `PUT`.
  ///
  /// Parameters:
  /// - [id]: Resource identifier appended to the base URL.
  /// - [item]: Request body to send.
  JSPromise<JSAny?> update(Object? id, Object? item) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));
    args.add(unsafe.dartToJs(item));

    return raw.callMethodVarArgs('update'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Delete a resource by ID.
  ///
  /// Parameters:
  /// - [id]: Resource identifier appended to the base URL.
  JSPromise<JSAny?> delete(Object? id) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));

    return raw.callMethodVarArgs('delete'.toJS, args) as JSPromise<JSAny?>;
  }
}

/// Scope class for the Proxy. It intercepts operations like property access (get) and property
/// setting (set), and adds support for deep change tracking and observer-like behavior.
///
/// Raw JavaScript facade for `ng.RootScopeService`.
base class GeneratedNgRootScopeService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RootScopeService.
  const GeneratedNgRootScopeService(super.raw);

  /// The id member of ng.RootScopeService.
  num get id => _toDartNumber(raw.getProperty('id'.toJS))!;

  /// The id member of ng.RootScopeService.
  set id(num value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The root member of ng.RootScopeService.
  Object? get root => unsafe.jsToDart<Object?>(raw.getProperty('root'.toJS));

  /// The parent member of ng.RootScopeService.
  Object? get parent =>
      unsafe.jsToDart<Object?>(raw.getProperty('parent'.toJS));

  /// The scopeName member of ng.RootScopeService.
  String? get scopeName => _toDartString(raw.getProperty('scopeName'.toJS));

  /// The scopeName member of ng.RootScopeService.
  set scopeName(String? value) {
    raw.setProperty('scopeName'.toJS, unsafe.dartToJs(value));
  }

  /// Intercepts and handles property assignments on the target object. Scopeable objects are stored
  /// as raw model values and proxied lazily when read.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being set.
  /// - [value]: The new value being assigned to the property.
  /// - [proxy]: The proxy intercepting property access.
  bool setMember(
      Object? target, String property, Object? value, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(value));
    args.add(unsafe.dartToJs(proxy));

    return _toDartBool(raw.callMethodVarArgs('set'.toJS, args))!;
  }

  /// Intercepts property access on the target object. It checks for specific properties (`watch` and
  /// `sync`) and binds their methods. For other properties, it returns the value directly.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being accessed.
  /// - [proxy]: The proxy object being invoked.
  Object? getMember(Object? target, Object? property, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(proxy));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('get'.toJS, args));
  }

  /// The deleteProperty member of ng.RootScopeService.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being deleted.
  bool deleteProperty(Object? target, Object? property) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));

    return _toDartBool(raw.callMethodVarArgs('deleteProperty'.toJS, args))!;
  }

  /// Runs synchronous scope mutations as one batch. Listener notifications are queued while the
  /// callback runs and flushed once after the outermost batch exits. Mutations are not rolled back if
  /// the callback throws.
  ///
  /// Parameters:
  /// - [fn]: Value supplied for the fn parameter.
  Object? batch(JSFunction fn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(fn));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('batch'.toJS, args));
  }

  /// Registers a watcher for a property along with a listener function. The listener function is
  /// invoked when changes to that property are detected.
  ///
  /// Parameters:
  /// - [watchProp]: An expression to be watched in the context of this model.
  /// - [listenerFn]: A function to execute when changes are detected on watched context.
  /// - [lazy]: A flag to indicate if the listener should be invoked immediately. Defaults to false.
  /// - [directLeaf]: Value supplied for the directLeaf parameter.
  Object? watch(String watchProp,
      [Object? listenerFn = _undefinedArgument,
      Object? lazy = _undefinedArgument,
      Object? directLeaf = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(watchProp));
    if (!identical(listenerFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(listenerFn));
    }
    if (!identical(lazy, _undefinedArgument)) {
      args.add(unsafe.dartToJs(lazy));
    }
    if (!identical(directLeaf, _undefinedArgument)) {
      args.add(unsafe.dartToJs(directLeaf));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('watch'.toJS, args));
  }

  /// Creates a prototypically inherited child scope.
  ///
  /// Parameters:
  /// - [childInstance]: Value supplied for the childInstance parameter.
  GeneratedNgRootScopeService newMember(
      [Object? childInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(childInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(childInstance));
    }

    raw.callMethodVarArgs('new'.toJS, args);
    return this;
  }

  /// Creates an isolate child scope that does not inherit watchable properties directly.
  ///
  /// Parameters:
  /// - [instance]: Value supplied for the instance parameter.
  GeneratedNgRootScopeService newIsolate(
      [Object? instance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(instance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(instance));
    }

    raw.callMethodVarArgs('newIsolate'.toJS, args);
    return this;
  }

  /// Creates a transcluded child scope linked to this scope and an optional parent instance.
  ///
  /// Parameters:
  /// - [parentInstance]: Value supplied for the parentInstance parameter.
  GeneratedNgRootScopeService transcluded(
      [Object? parentInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(parentInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(parentInstance));
    }

    raw.callMethodVarArgs('transcluded'.toJS, args);
    return this;
  }

  /// Merges enumerable properties from the provided object into the current scope target.
  ///
  /// Parameters:
  /// - [newTarget]: Value supplied for the newTarget parameter.
  void merge(Object? newTarget) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(newTarget));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('merge'.toJS, args));
  }

  /// Registers an event listener on this scope and returns a deregistration function.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [listener]: Value supplied for the listener parameter.
  Object? onMember(String name, JSFunction listener) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(listener));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('on'.toJS, args));
  }

  /// Emits an event upward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? emit(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('emit'.toJS, args));
  }

  /// Broadcasts an event downward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? broadcast(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('broadcast'.toJS, args));
  }

  /// Destroys this scope and its descendants.
  void destroy() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('destroy'.toJS, const <JSAny?>[]));
  }

  /// Searches this scope tree for a scope with the given id.
  ///
  /// Parameters:
  /// - [id]: Value supplied for the id parameter.
  Object? getById(Object? id) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getById'.toJS, args));
  }

  /// Searches the scope tree for a scope registered under the provided name.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  Object? searchByName(String name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('searchByName'.toJS, args));
  }
}

/// Public route contract entry used by router helper types. This is an author-written TypeScript
/// shape. It is intentionally separate from built router state records so generated docs and
/// language bindings do not expose internal state/runtime implementation details.
///
/// Raw JavaScript facade for `ng.RouteContract`.
base class GeneratedNgRouteContract extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RouteContract.
  const GeneratedNgRouteContract(super.raw);

  /// The params member of ng.RouteContract.
  Object? get params =>
      unsafe.jsToDart<Object?>(raw.getProperty('params'.toJS));

  /// The resolves member of ng.RouteContract.
  Object? get resolves =>
      unsafe.jsToDart<Object?>(raw.getProperty('resolves'.toJS));
}

/// Public AngularTS RouterConfig contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.RouterConfig`.
base class GeneratedNgRouterConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RouterConfig.
  const GeneratedNgRouterConfig(super.raw);

  /// The strict member of ng.RouterConfig.
  bool? get strict => _toDartBool(raw.getProperty('strict'.toJS));

  /// The strict member of ng.RouterConfig.
  set strict(bool? value) {
    raw.setProperty('strict'.toJS, unsafe.dartToJs(value));
  }

  /// The caseInsensitive member of ng.RouterConfig.
  bool? get caseInsensitive =>
      _toDartBool(raw.getProperty('caseInsensitive'.toJS));

  /// The caseInsensitive member of ng.RouterConfig.
  set caseInsensitive(bool? value) {
    raw.setProperty('caseInsensitive'.toJS, unsafe.dartToJs(value));
  }

  /// The defaultSquash member of ng.RouterConfig.
  Object? get defaultSquash =>
      unsafe.jsToDart<Object?>(raw.getProperty('defaultSquash'.toJS));

  /// The paramTypes member of ng.RouterConfig.
  Object? get paramTypes =>
      unsafe.jsToDart<Object?>(raw.getProperty('paramTypes'.toJS));

  /// The scroll member of ng.RouterConfig.
  Object? get scroll =>
      unsafe.jsToDart<Object?>(raw.getProperty('scroll'.toJS));

  /// The focus member of ng.RouterConfig.
  Object? get focus => unsafe.jsToDart<Object?>(raw.getProperty('focus'.toJS));

  /// The prefetch member of ng.RouterConfig.
  bool? get prefetch => _toDartBool(raw.getProperty('prefetch'.toJS));

  /// The prefetch member of ng.RouterConfig.
  set prefetch(bool? value) {
    raw.setProperty('prefetch'.toJS, unsafe.dartToJs(value));
  }

  /// The prefetchDelay member of ng.RouterConfig.
  num? get prefetchDelay =>
      _toDartNumber(raw.getProperty('prefetchDelay'.toJS));

  /// The prefetchDelay member of ng.RouterConfig.
  set prefetchDelay(num? value) {
    raw.setProperty('prefetchDelay'.toJS, unsafe.dartToJs(value));
  }

  /// The relay member of ng.RouterConfig.
  Object? get relay => unsafe.jsToDart<Object?>(raw.getProperty('relay'.toJS));

  /// The viewTransitions member of ng.RouterConfig.
  bool? get viewTransitions =>
      _toDartBool(raw.getProperty('viewTransitions'.toJS));

  /// The viewTransitions member of ng.RouterConfig.
  set viewTransitions(bool? value) {
    raw.setProperty('viewTransitions'.toJS, unsafe.dartToJs(value));
  }

  /// The loading member of ng.RouterConfig.
  Object? get loading =>
      unsafe.jsToDart<Object?>(raw.getProperty('loading'.toJS));

  /// The retry member of ng.RouterConfig.
  Object? get retry => unsafe.jsToDart<Object?>(raw.getProperty('retry'.toJS));

  /// The fallbackTo member of ng.RouterConfig.
  Object? get fallbackTo =>
      unsafe.jsToDart<Object?>(raw.getProperty('fallbackTo'.toJS));

  /// The errorBoundary member of ng.RouterConfig.
  Object? get errorBoundary =>
      unsafe.jsToDart<Object?>(raw.getProperty('errorBoundary'.toJS));

  /// The retention member of ng.RouterConfig.
  Object? get retention =>
      unsafe.jsToDart<Object?>(raw.getProperty('retention'.toJS));
}

/// Public AngularTS RouterModule contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.RouterModule`.
base class GeneratedNgRouterModule extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RouterModule.
  const GeneratedNgRouterModule(super.raw);

  /// The $controller service is used by Angular to create new controllers. Named controllers are
  /// stored in the owning runtime's controller registry.
  ///
  /// Parameters:
  /// - [name]: Controller name
  /// - [ctlFn]: Controller constructor fn (optionally decorated with DI annotations in the array notation)
  GeneratedNgRouterModule controller(String name, Object? ctlFn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(ctlFn));

    raw.callMethodVarArgs('controller'.toJS, args);
    return this;
  }

  /// The name member of ng.RouterModule.
  String get name => _toDartString(raw.getProperty('name'.toJS))!;

  /// The name member of ng.RouterModule.
  set name(String value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// The filter member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [filterFn]: Value supplied for the filterFn parameter.
  GeneratedNgRouterModule filter(String name, Object? filterFn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(filterFn));

    raw.callMethodVarArgs('filter'.toJS, args);
    return this;
  }

  /// Declare built-in AngularTS service configuration during the config phase.
  ///
  /// Parameters:
  /// - [config]: Built-in config map.
  GeneratedNgRouterModule config(Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('config'.toJS, args);
    return this;
  }

  /// The value member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [object]: Allows undefined
  GeneratedNgRouterModule value(String name, Object? object) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(object));

    raw.callMethodVarArgs('value'.toJS, args);
    return this;
  }

  /// The constant member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [object]: Value supplied for the object parameter.
  GeneratedNgRouterModule constant(String name, Object? object) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(object));

    raw.callMethodVarArgs('constant'.toJS, args);
    return this;
  }

  /// The run member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [block]: Value supplied for the block parameter.
  GeneratedNgRouterModule run(Object? block) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(block));

    raw.callMethodVarArgs('run'.toJS, args);
    return this;
  }

  /// The component member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [options]: Value supplied for the options parameter.
  GeneratedNgRouterModule component(String name, Object? options) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(options));

    raw.callMethodVarArgs('component'.toJS, args);
    return this;
  }

  /// The factory member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [providerFunction]: Value supplied for the providerFunction parameter.
  GeneratedNgRouterModule factory(String name, Object? providerFunction) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(providerFunction));

    raw.callMethodVarArgs('factory'.toJS, args);
    return this;
  }

  /// The service member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [serviceFunction]: Value supplied for the serviceFunction parameter.
  GeneratedNgRouterModule service(String name, Object? serviceFunction) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(serviceFunction));

    raw.callMethodVarArgs('service'.toJS, args);
    return this;
  }

  /// The provider member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [providerType]: Value supplied for the providerType parameter.
  GeneratedNgRouterModule provider(String name, Object? providerType) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(providerType));

    raw.callMethodVarArgs('provider'.toJS, args);
    return this;
  }

  /// The decorator member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [decorFn]: Value supplied for the decorFn parameter.
  GeneratedNgRouterModule decorator(String name, Object? decorFn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(decorFn));

    raw.callMethodVarArgs('decorator'.toJS, args);
    return this;
  }

  /// The directive member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [directiveFactory]: Value supplied for the directiveFactory parameter.
  GeneratedNgRouterModule directive(String name, Object? directiveFactory) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(directiveFactory));

    raw.callMethodVarArgs('directive'.toJS, args);
    return this;
  }

  /// The animation member of ng.RouterModule.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [animationFactory]: Value supplied for the animationFactory parameter.
  GeneratedNgRouterModule animation(String name, Object? animationFactory) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(animationFactory));

    raw.callMethodVarArgs('animation'.toJS, args);
    return this;
  }

  /// Register a named reactive model as an injectable app-owned service. The model is created lazily
  /// by the owning `AppContext` when the service is first injected. Models are shared across every
  /// root scope managed by the same `AppContext`; they are not children of `$rootScope`. Assign an
  /// injected model to a controller or scope property to bind it in a template. DOM interpolation,
  /// `ng-bind`, directive expressions, nested object reads, and array length reads update when the
  /// app model changes. Mutating the model proxy schedules every affected observer. The injected
  /// `Model&lt;T&gt;` value is proxy-backed. It exposes scope-proxy methods such as `watch`, `batch`,
  /// `merge`, `on`, `emit`, `broadcast`, and `destroy`, plus `snapshot`, `restore`, and `sync` for
  /// model lifecycle and synchronization. Prefer the factory form for nontrivial initial state: ```ts
  /// app.model("user", () =&gt; ({ name: "John", authenticated: false })); ```
  ///
  /// Parameters:
  /// - [name]: Injectable model name.
  /// - [initial]: Plain object state or an injectable factory that returns plain object state.
  GeneratedNgRouterModule model(String name, Object? initial) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(initial));

    raw.callMethodVarArgs('model'.toJS, args);
    return this;
  }

  /// Register a named reactive state machine as an injectable service. The machine is created by
  /// `$machine` when the named service is requested. The returned instance is not tied to any one
  /// scope lifetime; it registers with AngularTS scope proxies when assigned to a controller or
  /// scope.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [config]: Machine configuration or a resolvable config factory.
  GeneratedNgRouterModule machine(String name, Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('machine'.toJS, args);
    return this;
  }

  /// Register a named workflow as an injectable service. The workflow is created by `$workflow` when
  /// the named service is requested. Workflow behavior remains local to its `WorkflowConfig`; the
  /// provider does not apply global workflow defaults.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [config]: Workflow configuration or a resolvable config factory.
  GeneratedNgRouterModule workflow(String name, Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('workflow'.toJS, args);
    return this;
  }

  /// Register a named workflow supervisor as an injectable service. The supervisor is created when
  /// the named service is requested. It composes existing workflow configs or workflow instances and
  /// keeps persistence and recovery policy local to the supervisor config.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [config]: Supervisor configuration or a resolvable config factory.
  GeneratedNgRouterModule workflowSupervisor(String name, Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('workflowSupervisor'.toJS, args);
    return this;
  }

  /// Register a named WebAssembly module as an injectable resource. The actual loading is delegated
  /// to the `$wasm` service, so custom runtimes can decide whether WebAssembly support is included.
  ///
  /// Parameters:
  /// - [name]: Injectable name used to access the resource.
  /// - [config]: Module source and imports, optionally produced by DI.
  GeneratedNgRouterModule wasm(String name, Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(config));

    raw.callMethodVarArgs('wasm'.toJS, args);
    return this;
  }

  /// Register a named Web Worker connection as an injectable service. The actual connection is
  /// delegated to the `$worker` provider, so worker support remains provider-driven instead of
  /// directive-driven.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [scriptPath]: Worker script URL, optionally produced by DI.
  /// - [config]: Worker connection options, optionally produced by DI.
  GeneratedNgRouterModule worker(String name, Object? scriptPath,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(scriptPath));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('worker'.toJS, args);
    return this;
  }

  /// Configure the singleton `$serviceWorker` for this application.
  ///
  /// Parameters:
  /// - [scriptUrl]: Value supplied for the scriptUrl parameter.
  /// - [config]: Value supplied for the config parameter.
  GeneratedNgRouterModule serviceWorker(Object? scriptUrl,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(scriptUrl));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('serviceWorker'.toJS, args);
    return this;
  }

  /// Register a persistent object store as an injectable service. Store construction is delegated to
  /// the internal provider registry, which creates the service through the injector and persists it
  /// through the selected backend.
  ///
  /// Parameters:
  /// - [name]: Service name.
  /// - [ctor]: Constructor, factory, or object to persist.
  /// - [typeValue]: Storage backend type.
  /// - [backendOrConfig]: Custom backend or persistence options.
  GeneratedNgRouterModule store(String name, Object? ctor, String typeValue,
      [Object? backendOrConfig = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(ctor));
    args.add(unsafe.dartToJs(typeValue));
    if (!identical(backendOrConfig, _undefinedArgument)) {
      args.add(unsafe.dartToJs(backendOrConfig));
    }

    raw.callMethodVarArgs('store'.toJS, args);
    return this;
  }

  /// Register a REST resource as an injectable service. The resource factory is delegated to the
  /// injected `$rest` service, keeping REST support configurable by custom runtimes.
  ///
  /// Parameters:
  /// - [name]: Service name.
  /// - [url]: Base URL or URI template.
  /// - [entityClass]: Optional constructor for mapping JSON.
  /// - [options]: Optional RestService options, optionally produced by DI.
  GeneratedNgRouterModule rest(String name, String url,
      [Object? entityClass = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(url));
    if (!identical(entityClass, _undefinedArgument)) {
      args.add(unsafe.dartToJs(entityClass));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    raw.callMethodVarArgs('rest'.toJS, args);
    return this;
  }

  /// Register a pre-configured SSE connection as an injectable service. The connection is created by
  /// `$sse` when the named service is requested.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [url]: SSE endpoint.
  /// - [config]: SSE connection options, optionally produced by DI.
  GeneratedNgRouterModule sse(String name, String url,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('sse'.toJS, args);
    return this;
  }

  /// Register a pre-configured WebSocket connection as an injectable service. The connection is
  /// created by `$websocket` when the named service is requested.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [url]: WebSocket endpoint.
  /// - [config]: WebSocket connection options, optionally produced by DI.
  GeneratedNgRouterModule websocket(String name, String url,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('websocket'.toJS, args);
    return this;
  }

  /// Register a pre-configured WebTransport connection as an injectable service. The connection is
  /// created by `$webTransport` when the named service is requested.
  ///
  /// Parameters:
  /// - [name]: Injectable name.
  /// - [url]: WebTransport endpoint.
  /// - [config]: WebTransport connection options, optionally produced by DI.
  GeneratedNgRouterModule webTransport(String name, String url,
      [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    raw.callMethodVarArgs('webTransport'.toJS, args);
    return this;
  }

  /// Register an options-backed application host custom element. The definition is installed when the
  /// module runs. The host element is a native custom element backed by an AngularTS child scope.
  ///
  /// Parameters:
  /// - [name]: Custom element tag name.
  /// - [options]: App component options.
  GeneratedNgRouterModule appComponent(String name, Object? options) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(options));

    raw.callMethodVarArgs('appComponent'.toJS, args);
    return this;
  }

  /// Register a user-authored native custom element backed by an AngularTS scope. The element class
  /// must extend `ScopeElement`. Its static template, shadow, scope, inputs, and isolate properties
  /// configure the AngularTS wiring.
  ///
  /// Parameters:
  /// - [name]: Custom element tag name.
  /// - [elementClass]: Custom element class.
  GeneratedNgRouterModule webComponent(String name, Object? elementClass) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(elementClass));

    raw.callMethodVarArgs('webComponent'.toJS, args);
    return this;
  }

  /// Register a router tree while preserving this module's route map.
  ///
  /// Parameters:
  /// - [declaration]: Value supplied for the declaration parameter.
  GeneratedNgRouterModule router(Object? declaration) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(declaration));

    raw.callMethodVarArgs('router'.toJS, args);
    return this;
  }

  /// Register a lazy state namespace while preserving this module route map.
  ///
  /// Parameters:
  /// - [prefix]: Value supplied for the prefix parameter.
  /// - [loader]: Value supplied for the loader parameter.
  GeneratedNgRouterModule lazyState(Object? prefix, Object? loader) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(prefix));
    args.add(unsafe.dartToJs(loader));

    raw.callMethodVarArgs('lazyState'.toJS, args);
    return this;
  }
}

/// Module-owned router state tree declaration. Use this with [[NgModule.router]] when a module owns
/// a route subtree. Child state names are relative to their parent unless they contain a dot.
///
/// Raw JavaScript facade for `ng.RouterModuleDeclaration`.
base class GeneratedNgRouterModuleDeclaration extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RouterModuleDeclaration.
  const GeneratedNgRouterModuleDeclaration(super.raw);

  /// Child states owned by this module route tree. Each child is lowered to a normal
  /// [[StateDeclaration]] before registration.
  Object? get children =>
      unsafe.jsToDart<Object?>(raw.getProperty('children'.toJS));

  /// The state name (required) A unique state name, e.g. `"home"`, `"about"`, `"contacts"`. To create
  /// a parent/child state use a dot, e.g. `"about.sales"`, `"home.newest"`. Note: [State] objects
  /// require unique names. The name is used like an id.
  String get name => _toDartString(raw.getProperty('name'.toJS))!;

  /// The state name (required) A unique state name, e.g. `"home"`, `"about"`, `"contacts"`. To create
  /// a parent/child state use a dot, e.g. `"about.sales"`, `"home.newest"`. Note: [State] objects
  /// require unique names. The name is used like an id.
  set name(String value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// Abstract state indicator An abstract state can never be directly activated. Use an abstract
  /// state to provide inherited properties (url, resolve, data, etc) to children states.
  bool? get abstractMember => _toDartBool(raw.getProperty('abstract'.toJS));

  /// Abstract state indicator An abstract state can never be directly activated. Use an abstract
  /// state to provide inherited properties (url, resolve, data, etc) to children states.
  set abstractMember(bool? value) {
    raw.setProperty('abstract'.toJS, unsafe.dartToJs(value));
  }

  /// The parent state Normally, a state's parent is implied from the state's [[name]], e.g.,
  /// `"parentstate.childstate"`. Alternatively, you can explicitly set the parent state using this
  /// property. This allows shorter state names, e.g., `&lt;a ng-state="'childstate'"&gt;Child&lt;/a&gt;` instead
  /// of `&lt;a ng-state="'parentstate.childstate'"&gt;Child&lt;/a&gt; When using this property, the state's name
  /// should not have any dots in it. #### Example: ```js var parentstate = { name: 'parentstate' }
  /// var childstate = { name: 'childstate', parent: 'parentstate' // or use a JS var which is the
  /// parent StateDeclaration, i.e.: // parent: parentstate } ```
  Object? get parent =>
      unsafe.jsToDart<Object?>(raw.getProperty('parent'.toJS));

  /// Named view declarations for this state. Each key targets an `ng-view`; each value is either a
  /// full view declaration or a string shorthand for `{ component: "componentName" }`. Examples:
  /// ```js views: { mymessages: "mymessages", messagelist: { component: "messageList" },
  /// "^.^.messagecontent": "message" } ```
  Object? get views => unsafe.jsToDart<Object?>(raw.getProperty('views'.toJS));

  /// Resolve - a mechanism to asynchronously fetch data, participating in the Transition lifecycle
  /// The `resolve:` property defines data (or other dependencies) to be fetched asynchronously when
  /// the state is being entered. After the data is fetched, it may be used in views, transition hooks
  /// or other resolves that belong to this state. The data may also be used in any views or resolves
  /// that belong to nested states. ### As an array Each array element should be a
  /// [[ResolvableLiteral]] object. #### Example: The `user` resolve injects the current `Transition`
  /// and the `UserService` (using its token, which is a string). The [[ResolvableLiteral.eager]] flag
  /// controls whether the resolve starts at transition start instead of when the owning state is
  /// entered. The `user` data, fetched asynchronously, can then be used in a view. ```js var state =
  /// { name: 'user', url: '/user/:userId resolve: [ { token: 'user', eager: true, deps:
  /// ['UserService', Transition], resolveFn: (userSvc, trans) =&gt;
  /// userSvc.fetchUser(trans.params().userId) }, } ] } ``` ### As an object The `resolve` property
  /// may be an object where: - Each key (string) is the name of the dependency. - Each value
  /// (function) is an injectable function which returns the dependency, or a promise for the
  /// dependency. This style is based on AngularTS injectable functions. Dependency-bearing functions
  /// must use array annotation or a static `$inject` property. #### AngularTS Example: ```js resolve:
  /// { // If you inject `myStateDependency` into a controller, you'll get "abc" myStateDependency:
  /// function() { return "abc"; }, // Dependencies are annotated in "Inline Array Annotation"
  /// myAsyncData: ['$http', '$transition$' function($http, $transition$) { // Return a promise
  /// (async) for the data return $http.get("/foos/" + $transition$.params().foo); }] } ``` Note: You
  /// cannot mark individual entries as eager, nor can you use non-string tokens when using the object
  /// style `resolve:` block. ### Lifecycle Since a resolve function can return a promise, the router
  /// will delay entering the state until the promises are ready. If any of the promises are rejected,
  /// the Transition is aborted with an Error. By default, resolves for a state are fetched just
  /// before that state is entered. Note that only states which are being *entered* during the
  /// `Transition` have their resolves fetched. States that are "retained" do not have their resolves
  /// re-fetched. If you are currently in a parent state `parent` and are transitioning to a child
  /// state `parent.child`, the previously resolved data for state `parent` can be injected into
  /// `parent.child` without delay. Any resolved data for `parent.child` is retained until
  /// `parent.child` is exited, e.g., by transitioning back to the `parent` state. Because of this
  /// scoping and lifecycle, resolves are a great place to fetch your application's primary data. ###
  /// Injecting resolves into other things During a transition, Resolve data can be injected into: -
  /// Views (the components which fill a `ng-view` tag) - Transition Hooks - Other resolves (a resolve
  /// may depend on asynchronous data from a different resolve) ### Injecting other things into
  /// resolves Resolve functions usually have dependencies on some other API(s). The dependencies are
  /// usually declared and injected into the resolve function. A common pattern is to inject a custom
  /// service such as `UserService`. The resolve then delegates to a service method, such as
  /// `UserService.list()`; #### Special injectable tokens - `Transition`: The current [[Transition]]
  /// object; information and API about the current transition, such as "to" and "from" State
  /// Parameters and transition options. - `'$transition$'`: A string alias for the `Transition`
  /// injectable - `'$state$'`: For `onEnter`/`onExit`/`onRetain`, the state being
  /// entered/exited/retained. - Other resolve tokens: A resolve can depend on another resolve, either
  /// from the same state, or from any parent state. #### Example: ```js // Injecting a resolve into
  /// another resolve resolve: [ // Define a resolve 'allusers' which delegates to the
  /// UserService.list() // which returns a promise (async) for all the users { token: 'allusers',
  /// resolveFn: (UserService) =&gt; UserService.list(), deps: [UserService] }, // Define a resolve
  /// 'user' which depends on the allusers resolve. // This resolve function is not called until
  /// 'allusers' is ready. { token: 'user', resolveFn: (allusers, trans) =&gt; _.find(allusers,
  /// trans.params().userId), deps: ['allusers', Transition] } } ```
  Object? get resolve =>
      unsafe.jsToDart<Object?>(raw.getProperty('resolve'.toJS));

  /// The url fragment for the state A URL fragment (with optional parameters) which is used to match
  /// the browser location with this state. This fragment will be appended to the parent state's URL
  /// in order to build up the overall URL for this state. It may include path parameters, typed
  /// parameters, and query parameters.
  String? get url => _toDartString(raw.getProperty('url'.toJS));

  /// The url fragment for the state A URL fragment (with optional parameters) which is used to match
  /// the browser location with this state. This fragment will be appended to the parent state's URL
  /// in order to build up the overall URL for this state. It may include path parameters, typed
  /// parameters, and query parameters.
  set url(String? value) {
    raw.setProperty('url'.toJS, unsafe.dartToJs(value));
  }

  /// Params configuration An object which optionally configures parameters declared in the url, or
  /// defines additional non-url parameters. For each parameter being configured, add a
  /// [[ParamDeclaration]] keyed to the name of the parameter. #### Example: ```js params: { param1: {
  /// type: "int", array: true, value: [] }, param2: { value: "index" } } ```
  Object? get params =>
      unsafe.jsToDart<Object?>(raw.getProperty('params'.toJS));

  /// An inherited property to store state data This is a spot for you to store inherited state
  /// metadata. Child states' `data` object will prototypally inherit from their parent state. Use
  /// this for application metadata. Use `policy.navigation` for framework navigation decisions such
  /// as authentication, permissions, or redirects. Note: because prototypal inheritance is used,
  /// changes to parent `data` objects reflect in the child `data` objects. Care should be taken if
  /// you are using `hasOwnProperty` on the `data` object. Properties from parent objects will return
  /// false for `hasOwnProperty`.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// Declarative state policy metadata consumed by AngularTS framework services. `policy.navigation`
  /// is inherited through the state tree and evaluated by the router's security navigation hook
  /// before resolves, controllers, or views run. `policy.transition.canExit` is evaluated before
  /// exiting states are torn down. `policy.retention` declares keep-alive route subtree behavior and
  /// can override router-wide retention defaults.
  Object? get policy =>
      unsafe.jsToDart<Object?>(raw.getProperty('policy'.toJS));

  /// Synchronously or asynchronously redirects Transitions to a different state/params If this
  /// property is defined, a Transition directly to this state will be redirected based on the
  /// property's value. - If the value is a `string`, the Transition is redirected to the state named
  /// by the string. - If the property is an object with a `state` and/or `params` property, the
  /// Transition is redirected to the named `state` and/or `params`. - If the value is a
  /// [[TargetState]] the Transition is redirected to the `TargetState` - If the property is a
  /// function: - The function is called with the current [[Transition]] - The return value is
  /// processed using the previously mentioned rules. - If the return value is a promise, the promise
  /// is waited for, then the resolved async value is processed using the same rules. Note:
  /// `redirectTo` is processed as an `onStart` hook, before non-eager resolves. If your redirect
  /// function relies on resolve data, get the [[Transition.injector]] and request the resolve data
  /// with `getAsync()`. #### Example: ```js // a string .state('A', { redirectTo: 'A.B' }) // a
  /// {state, params} object .state('C', { redirectTo: { state: 'C.D', params: { foo: 'index' } } })
  /// // a fn .state('E', { redirectTo: () =&gt; "A" }) // a fn conditionally returning a {state, params}
  /// .state('F', { redirectTo: (trans) =&gt; { if (trans.params().foo &lt; 10) return { state: 'F', params:
  /// { foo: 10 } }; } }) // a fn returning a promise for a redirect .state('G', { redirectTo: (trans)
  /// =&gt; { let svc = trans.injector().get('SomeAsyncService') let promise =
  /// svc.getAsyncRedirectTo(trans.params.foo); return promise; } }) // a fn that fetches resolve data
  /// .state('G', { redirectTo: (trans) =&gt; { // getAsync tells the resolve to load let resolvePromise
  /// = trans.injector().getAsync('SomeResolve') return resolvePromise.then(resolveData =&gt; resolveData
  /// === 'login' ? 'login' : null); } }) ```
  Object? get redirectTo =>
      unsafe.jsToDart<Object?>(raw.getProperty('redirectTo'.toJS));

  /// A state hook invoked when a state is being entered. The hook can inject global services. It can
  /// also inject `$transition$` or `$state$` (from the current transition). ### Example: ```js
  /// app.router({ name: 'mystate', onEnter: (MyService, $transition$, $state$) =&gt; { return
  /// MyService.doSomething($state$.name, $transition$.params()); } }); ``` #### Example:` ```js
  /// app.router({ name: 'mystate', onEnter: [ 'MyService', '$transition$', '$state$', function
  /// (MyService, $transition$, $state$) { return MyService.doSomething($state$.name,
  /// $transition$.params()); } ] }); ```
  Object? get onEnter =>
      unsafe.jsToDart<Object?>(raw.getProperty('onEnter'.toJS));

  /// A state hook invoked when a state is being retained. The hook can inject global services. It can
  /// also inject `$transition$` or `$state$` (from the current transition). #### Example: ```js
  /// app.router({ name: 'mystate', onRetain: (MyService, $transition$, $state$) =&gt; { return
  /// MyService.doSomething($state$.name, $transition$.params()); } }); ``` #### Example:` ```js
  /// app.router({ name: 'mystate', onRetain: [ 'MyService', '$transition$', '$state$', function
  /// (MyService, $transition$, $state$) { return MyService.doSomething($state$.name,
  /// $transition$.params()); } ] }); ```
  Object? get onRetain =>
      unsafe.jsToDart<Object?>(raw.getProperty('onRetain'.toJS));

  /// A state hook invoked when a state is being exited. The hook can inject global services. It can
  /// also inject `$transition$` or `$state$` (from the current transition). ### Example: ```js
  /// app.router({ name: 'mystate', onExit: (MyService, $transition$, $state$) =&gt; { return
  /// MyService.doSomething($state$.name, $transition$.params()); } }); ``` #### Example:` ```js
  /// app.router({ name: 'mystate', onExit: [ 'MyService', '$transition$', '$state$', function
  /// (MyService, $transition$, $state$) { return MyService.doSomething($state$.name,
  /// $transition$.params()); } ] }); ```
  Object? get onExit =>
      unsafe.jsToDart<Object?>(raw.getProperty('onExit'.toJS));

  /// Marks all the state's parameters as `dynamic`. All parameters on the state will use this value
  /// for `dynamic` as a default. Individual parameters may override this default using
  /// [[ParamDeclaration.dynamic]] in the [[params]] block. This default applies to all parameters
  /// declared on this state.
  bool? get dynamicMember => _toDartBool(raw.getProperty('dynamic'.toJS));

  /// Marks all the state's parameters as `dynamic`. All parameters on the state will use this value
  /// for `dynamic` as a default. Individual parameters may override this default using
  /// [[ParamDeclaration.dynamic]] in the [[params]] block. This default applies to all parameters
  /// declared on this state.
  set dynamicMember(bool? value) {
    raw.setProperty('dynamic'.toJS, unsafe.dartToJs(value));
  }

  /// The name of the component to use for this view. The name of an AngularTS `.component()` which
  /// will be used for this view. Resolve data can be provided to the component via the component's
  /// `bindings` object. For each binding declared on the component, any resolve with the same name is
  /// set on the component's controller instance. Note: Mapping from resolve names to component inputs
  /// may be specified using [[bindings]]. #### Example: ```js .state('profile', { // Use the
  /// &lt;my-profile&gt;&lt;/my-profile&gt; component for this state. component: 'MyProfile', } ``` Note: When
  /// using `component` to define a view, you may _not_ use any of: `template`, `templateUrl`,
  /// `controller`.
  Object? get component =>
      unsafe.jsToDart<Object?>(raw.getProperty('component'.toJS));

  /// An object which maps `resolve`s to [[component]] `bindings`. When using a [[component]]
  /// declaration (`component: 'myComponent'`), each input binding for the component is supplied data
  /// from a resolve of the same name, by default. You may supply data from a different resolve name
  /// by mapping it here. Each key in this object is the name of one of the component's input
  /// bindings. Each value is the name of the resolve that should be provided to that binding. Any
  /// component bindings that are omitted from this map get the default behavior of mapping to a
  /// resolve of the same name. #### Example: ```js app.router('foo', { resolve: { foo: ['FooService',
  /// function(FooService) { return FooService.get(); }], bar: ['BarService', function(BarService) {
  /// return BarService.get(); }] }, component: 'Baz', // The component's `baz` binding gets data from
  /// the `bar` resolve // The component's `foo` binding gets data from the `foo` resolve (default
  /// behavior) bindings: { baz: 'bar' } }); app.component('Baz', { templateUrl: 'baz.html',
  /// controller: 'BazController', bindings: { foo: '&lt;', // foo binding baz: '&lt;' // baz binding } });
  /// ```
  Object? get bindings =>
      unsafe.jsToDart<Object?>(raw.getProperty('bindings'.toJS));

  /// The view's controller function or name The controller function, or the name of a registered
  /// controller. The controller function will be used to control the contents of the
  /// [[directives.ngVIew]] directive. See: [[Ng1Controller]] for information about component-level
  /// router hooks.
  Object? get controller =>
      unsafe.jsToDart<Object?>(raw.getProperty('controller'.toJS));

  /// The HTML template for the view. HTML template as a string, or a function which returns an html
  /// template as a string. This template will be used to render the corresponding
  /// [[directives.ngVIew]] directive. This property takes precedence over templateUrl. If `template`
  /// is a function, it will be called with the Transition parameters as the first argument. ####
  /// Example: ```js template: "&lt;h1&gt;inline template definition&lt;/h1&gt;&lt;div ng-view&gt;&lt;/div&gt;" ``` ####
  /// Example: ```js template: function(params) { return "&lt;h1&gt;generated template&lt;/h1&gt;"; } ```
  Object? get template =>
      unsafe.jsToDart<Object?>(raw.getProperty('template'.toJS));

  /// The URL for the HTML template for the view. A path or a function that returns a path to an html
  /// template. The template will be fetched and used to render the corresponding
  /// [[directives.ngVIew]] directive. If `templateUrl` is a function, it will be called with the
  /// Transition parameters as the first argument. #### Example: ```js templateUrl:
  /// "/templates/home.html" ``` #### Example: ```js templateUrl: function(params) { return
  /// myTemplates[params.pageId]; } ```
  Object? get templateUrl =>
      unsafe.jsToDart<Object?>(raw.getProperty('templateUrl'.toJS));
}

/// Derives the public route map for a literal `router(...)` tree.
///
/// Raw JavaScript facade for `ng.RoutesOf`.
base class GeneratedNgRoutesOf extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.RoutesOf.
  const GeneratedNgRoutesOf(super.raw);
}

/// Public AngularTS SceDelegateService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.SceDelegateService`.
base class GeneratedNgSceDelegateService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.SceDelegateService.
  const GeneratedNgSceDelegateService(super.raw);

  /// The getTrusted member of ng.SceDelegateService.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [mayBeTrusted]: Value supplied for the mayBeTrusted parameter.
  Object? getTrusted(String typeValue, Object? mayBeTrusted) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    args.add(unsafe.dartToJs(mayBeTrusted));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getTrusted'.toJS, args));
  }

  /// The trustAs member of ng.SceDelegateService.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [value]: Value supplied for the value parameter.
  Object? trustAs(String typeValue, Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    args.add(unsafe.dartToJs(value));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('trustAs'.toJS, args));
  }

  /// The valueOf member of ng.SceDelegateService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? valueOf([Object? value = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(value, _undefinedArgument)) {
      args.add(unsafe.dartToJs(value));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('valueOf'.toJS, args));
  }
}

/// Public AngularTS SceService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.SceService`.
base class GeneratedNgSceService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.SceService.
  const GeneratedNgSceService(super.raw);

  /// The getTrusted member of ng.SceService.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [mayBeTrusted]: Value supplied for the mayBeTrusted parameter.
  Object? getTrusted(String typeValue, Object? mayBeTrusted) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    args.add(unsafe.dartToJs(mayBeTrusted));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getTrusted'.toJS, args));
  }

  /// The getTrustedHtml member of ng.SceService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? getTrustedHtml(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getTrustedHtml'.toJS, args));
  }

  /// The getTrustedResourceUrl member of ng.SceService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? getTrustedResourceUrl(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('getTrustedResourceUrl'.toJS, args));
  }

  /// The getTrustedUrl member of ng.SceService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? getTrustedUrl(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getTrustedUrl'.toJS, args));
  }

  /// The getTrustedMediaUrl member of ng.SceService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? getTrustedMediaUrl(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('getTrustedMediaUrl'.toJS, args));
  }

  /// The parse member of ng.SceService.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [expression]: Value supplied for the expression parameter.
  Object? parse(String typeValue, String expression) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    args.add(unsafe.dartToJs(expression));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('parse'.toJS, args));
  }

  /// The parseAsHtml member of ng.SceService.
  ///
  /// Parameters:
  /// - [expression]: Value supplied for the expression parameter.
  Object? parseAsHtml(String expression) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(expression));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('parseAsHtml'.toJS, args));
  }

  /// The parseAsResourceUrl member of ng.SceService.
  ///
  /// Parameters:
  /// - [expression]: Value supplied for the expression parameter.
  Object? parseAsResourceUrl(String expression) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(expression));

    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('parseAsResourceUrl'.toJS, args));
  }

  /// The parseAsUrl member of ng.SceService.
  ///
  /// Parameters:
  /// - [expression]: Value supplied for the expression parameter.
  Object? parseAsUrl(String expression) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(expression));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('parseAsUrl'.toJS, args));
  }

  /// The parseAsMediaUrl member of ng.SceService.
  ///
  /// Parameters:
  /// - [expression]: Value supplied for the expression parameter.
  Object? parseAsMediaUrl(String expression) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(expression));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('parseAsMediaUrl'.toJS, args));
  }

  /// The trustAs member of ng.SceService.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [value]: Value supplied for the value parameter.
  Object? trustAs(String typeValue, Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    args.add(unsafe.dartToJs(value));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('trustAs'.toJS, args));
  }

  /// The trustAsHtml member of ng.SceService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? trustAsHtml(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('trustAsHtml'.toJS, args));
  }

  /// The trustAsResourceUrl member of ng.SceService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? trustAsResourceUrl(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('trustAsResourceUrl'.toJS, args));
  }

  /// The trustAsUrl member of ng.SceService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? trustAsUrl(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('trustAsUrl'.toJS, args));
  }

  /// The trustAsMediaUrl member of ng.SceService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? trustAsMediaUrl(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('trustAsMediaUrl'.toJS, args));
  }

  /// The isEnabled member of ng.SceService.
  bool isEnabled() {
    return _toDartBool(
        raw.callMethodVarArgs('isEnabled'.toJS, const <JSAny?>[]))!;
  }

  /// The valueOf member of ng.SceService.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  Object? valueOf([Object? value = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(value, _undefinedArgument)) {
      args.add(unsafe.dartToJs(value));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('valueOf'.toJS, args));
  }
}

/// Scope class for the Proxy. It intercepts operations like property access (get) and property
/// setting (set), and adds support for deep change tracking and observer-like behavior.
///
/// Raw JavaScript facade for `ng.Scope`.
base class GeneratedNgScope extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Scope.
  const GeneratedNgScope(super.raw);

  /// The id member of ng.Scope.
  num get id => _toDartNumber(raw.getProperty('id'.toJS))!;

  /// The id member of ng.Scope.
  set id(num value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The root member of ng.Scope.
  Object? get root => unsafe.jsToDart<Object?>(raw.getProperty('root'.toJS));

  /// The parent member of ng.Scope.
  Object? get parent =>
      unsafe.jsToDart<Object?>(raw.getProperty('parent'.toJS));

  /// The scopeName member of ng.Scope.
  String? get scopeName => _toDartString(raw.getProperty('scopeName'.toJS));

  /// The scopeName member of ng.Scope.
  set scopeName(String? value) {
    raw.setProperty('scopeName'.toJS, unsafe.dartToJs(value));
  }

  /// Intercepts and handles property assignments on the target object. Scopeable objects are stored
  /// as raw model values and proxied lazily when read.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being set.
  /// - [value]: The new value being assigned to the property.
  /// - [proxy]: The proxy intercepting property access.
  bool setMember(
      Object? target, String property, Object? value, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(value));
    args.add(unsafe.dartToJs(proxy));

    return _toDartBool(raw.callMethodVarArgs('set'.toJS, args))!;
  }

  /// Intercepts property access on the target object. It checks for specific properties (`watch` and
  /// `sync`) and binds their methods. For other properties, it returns the value directly.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being accessed.
  /// - [proxy]: The proxy object being invoked.
  Object? getMember(Object? target, Object? property, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(proxy));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('get'.toJS, args));
  }

  /// The deleteProperty member of ng.Scope.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being deleted.
  bool deleteProperty(Object? target, Object? property) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));

    return _toDartBool(raw.callMethodVarArgs('deleteProperty'.toJS, args))!;
  }

  /// Runs synchronous scope mutations as one batch. Listener notifications are queued while the
  /// callback runs and flushed once after the outermost batch exits. Mutations are not rolled back if
  /// the callback throws.
  ///
  /// Parameters:
  /// - [fn]: Value supplied for the fn parameter.
  Object? batch(JSFunction fn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(fn));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('batch'.toJS, args));
  }

  /// Registers a watcher for a property along with a listener function. The listener function is
  /// invoked when changes to that property are detected.
  ///
  /// Parameters:
  /// - [watchProp]: An expression to be watched in the context of this model.
  /// - [listenerFn]: A function to execute when changes are detected on watched context.
  /// - [lazy]: A flag to indicate if the listener should be invoked immediately. Defaults to false.
  /// - [directLeaf]: Value supplied for the directLeaf parameter.
  Object? watch(String watchProp,
      [Object? listenerFn = _undefinedArgument,
      Object? lazy = _undefinedArgument,
      Object? directLeaf = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(watchProp));
    if (!identical(listenerFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(listenerFn));
    }
    if (!identical(lazy, _undefinedArgument)) {
      args.add(unsafe.dartToJs(lazy));
    }
    if (!identical(directLeaf, _undefinedArgument)) {
      args.add(unsafe.dartToJs(directLeaf));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('watch'.toJS, args));
  }

  /// Creates a prototypically inherited child scope.
  ///
  /// Parameters:
  /// - [childInstance]: Value supplied for the childInstance parameter.
  GeneratedNgScope newMember([Object? childInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(childInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(childInstance));
    }

    raw.callMethodVarArgs('new'.toJS, args);
    return this;
  }

  /// Creates an isolate child scope that does not inherit watchable properties directly.
  ///
  /// Parameters:
  /// - [instance]: Value supplied for the instance parameter.
  GeneratedNgScope newIsolate([Object? instance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(instance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(instance));
    }

    raw.callMethodVarArgs('newIsolate'.toJS, args);
    return this;
  }

  /// Creates a transcluded child scope linked to this scope and an optional parent instance.
  ///
  /// Parameters:
  /// - [parentInstance]: Value supplied for the parentInstance parameter.
  GeneratedNgScope transcluded([Object? parentInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(parentInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(parentInstance));
    }

    raw.callMethodVarArgs('transcluded'.toJS, args);
    return this;
  }

  /// Merges enumerable properties from the provided object into the current scope target.
  ///
  /// Parameters:
  /// - [newTarget]: Value supplied for the newTarget parameter.
  void merge(Object? newTarget) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(newTarget));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('merge'.toJS, args));
  }

  /// Registers an event listener on this scope and returns a deregistration function.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [listener]: Value supplied for the listener parameter.
  Object? onMember(String name, JSFunction listener) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(listener));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('on'.toJS, args));
  }

  /// Emits an event upward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? emit(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('emit'.toJS, args));
  }

  /// Broadcasts an event downward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? broadcast(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('broadcast'.toJS, args));
  }

  /// Destroys this scope and its descendants.
  void destroy() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('destroy'.toJS, const <JSAny?>[]));
  }

  /// Searches this scope tree for a scope with the given id.
  ///
  /// Parameters:
  /// - [id]: Value supplied for the id parameter.
  Object? getById(Object? id) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getById'.toJS, args));
  }

  /// Searches the scope tree for a scope registered under the provided name.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  Object? searchByName(String name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('searchByName'.toJS, args));
  }
}

/// Native custom element base class backed by an AngularTS child scope.
///
/// Raw JavaScript facade for `ng.ScopeElement`.
base class GeneratedNgScopeElement extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ScopeElement.
  const GeneratedNgScopeElement(super.raw);

  /// Scope owned by this custom element instance.
  Object? get scope => unsafe.jsToDart<Object?>(raw.getProperty('scope'.toJS));

  /// Injector used by the AngularTS app that registered this element.
  Object? get injector =>
      unsafe.jsToDart<Object?>(raw.getProperty('injector'.toJS));

  /// Render root used for compiled template content.
  Object? get root => unsafe.jsToDart<Object?>(raw.getProperty('root'.toJS));

  /// The connectedCallback member of ng.ScopeElement.
  void connectedCallback() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('connectedCallback'.toJS, const <JSAny?>[]));
  }

  /// The disconnectedCallback member of ng.ScopeElement.
  void disconnectedCallback() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('disconnectedCallback'.toJS, const <JSAny?>[]));
  }

  /// The attributeChangedCallback member of ng.ScopeElement.
  ///
  /// Parameters:
  /// - [attribute]: Value supplied for the attribute parameter.
  /// - [oldValue]: Value supplied for the oldValue parameter.
  /// - [newValue]: Value supplied for the newValue parameter.
  void attributeChangedCallback(
      String attribute, String? oldValue, String? newValue) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(attribute));
    args.add(unsafe.dartToJs(oldValue));
    args.add(unsafe.dartToJs(newValue));

    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('attributeChangedCallback'.toJS, args));
  }

  /// Called after the AngularTS scope and template are connected.
  Object? connected() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('connected'.toJS, const <JSAny?>[]));
  }

  /// Called before the AngularTS scope is destroyed.
  void disconnected() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('disconnected'.toJS, const <JSAny?>[]));
  }

  /// Called after an observed input attribute changes.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [oldValue]: Value supplied for the oldValue parameter.
  /// - [newValue]: Value supplied for the newValue parameter.
  void attributeChanged(String name, String? oldValue, String? newValue) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(oldValue));
    args.add(unsafe.dartToJs(newValue));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('attributeChanged'.toJS, args));
  }

  /// Dispatch a composed bubbling DOM event from this custom element.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [detail]: Value supplied for the detail parameter.
  /// - [init]: Value supplied for the init parameter.
  bool dispatch(String typeValue,
      [Object? detail = _undefinedArgument,
      Object? init = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    if (!identical(detail, _undefinedArgument)) {
      args.add(unsafe.dartToJs(detail));
    }
    if (!identical(init, _undefinedArgument)) {
      args.add(unsafe.dartToJs(init));
    }

    return _toDartBool(raw.callMethodVarArgs('dispatch'.toJS, args))!;
  }
}

/// Public AngularTS ScopeElementConstructor contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.ScopeElementConstructor`.
base class GeneratedNgScopeElementConstructor extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ScopeElementConstructor.
  const GeneratedNgScopeElementConstructor(super.raw);

  /// Template compiled into the host or shadow root.
  String? get template => _toDartString(raw.getProperty('template'.toJS));

  /// Template compiled into the host or shadow root.
  set template(String? value) {
    raw.setProperty('template'.toJS, unsafe.dartToJs(value));
  }

  /// Enables shadow DOM, or passes ShadowRootInit options.
  Object? get shadow =>
      unsafe.jsToDart<Object?>(raw.getProperty('shadow'.toJS));

  /// Initial scope state, or a factory returning it.
  Object? get scope => unsafe.jsToDart<Object?>(raw.getProperty('scope'.toJS));

  /// Declared DOM attributes/properties that sync into the scope.
  Object? get inputs =>
      unsafe.jsToDart<Object?>(raw.getProperty('inputs'.toJS));

  /// Use an isolate child scope instead of inheriting parent properties.
  bool? get isolate => _toDartBool(raw.getProperty('isolate'.toJS));

  /// Use an isolate child scope instead of inheriting parent properties.
  set isolate(bool? value) {
    raw.setProperty('isolate'.toJS, unsafe.dartToJs(value));
  }
}

/// Event object passed to `emit` and `broadcast` listeners. Tracks target scope, current scope,
/// name, propagation/default flags, and control methods.
///
/// Raw JavaScript facade for `ng.ScopeEvent`.
base class GeneratedNgScopeEvent extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ScopeEvent.
  const GeneratedNgScopeEvent(super.raw);

  /// The targetScope member of ng.ScopeEvent.
  Object? get targetScope =>
      unsafe.jsToDart<Object?>(raw.getProperty('targetScope'.toJS));

  /// The currentScope member of ng.ScopeEvent.
  Object? get currentScope =>
      unsafe.jsToDart<Object?>(raw.getProperty('currentScope'.toJS));

  /// The name member of ng.ScopeEvent.
  String get name => _toDartString(raw.getProperty('name'.toJS))!;

  /// The name member of ng.ScopeEvent.
  set name(String value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// The stopPropagation member of ng.ScopeEvent.
  void stopPropagation() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('stopPropagation'.toJS, const <JSAny?>[]));
  }

  /// The preventDefault member of ng.ScopeEvent.
  void preventDefault() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('preventDefault'.toJS, const <JSAny?>[]));
  }

  /// The stopped member of ng.ScopeEvent.
  bool get stopped => _toDartBool(raw.getProperty('stopped'.toJS))!;

  /// The stopped member of ng.ScopeEvent.
  set stopped(bool value) {
    raw.setProperty('stopped'.toJS, unsafe.dartToJs(value));
  }

  /// The defaultPrevented member of ng.ScopeEvent.
  bool get defaultPrevented =>
      _toDartBool(raw.getProperty('defaultPrevented'.toJS))!;

  /// The defaultPrevented member of ng.ScopeEvent.
  set defaultPrevented(bool value) {
    raw.setProperty('defaultPrevented'.toJS, unsafe.dartToJs(value));
  }
}

/// Scope class for the Proxy. It intercepts operations like property access (get) and property
/// setting (set), and adds support for deep change tracking and observer-like behavior.
///
/// Raw JavaScript facade for `ng.ScopeService`.
base class GeneratedNgScopeService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ScopeService.
  const GeneratedNgScopeService(super.raw);

  /// The id member of ng.ScopeService.
  num get id => _toDartNumber(raw.getProperty('id'.toJS))!;

  /// The id member of ng.ScopeService.
  set id(num value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The root member of ng.ScopeService.
  Object? get root => unsafe.jsToDart<Object?>(raw.getProperty('root'.toJS));

  /// The parent member of ng.ScopeService.
  Object? get parent =>
      unsafe.jsToDart<Object?>(raw.getProperty('parent'.toJS));

  /// The scopeName member of ng.ScopeService.
  String? get scopeName => _toDartString(raw.getProperty('scopeName'.toJS));

  /// The scopeName member of ng.ScopeService.
  set scopeName(String? value) {
    raw.setProperty('scopeName'.toJS, unsafe.dartToJs(value));
  }

  /// Intercepts and handles property assignments on the target object. Scopeable objects are stored
  /// as raw model values and proxied lazily when read.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being set.
  /// - [value]: The new value being assigned to the property.
  /// - [proxy]: The proxy intercepting property access.
  bool setMember(
      Object? target, String property, Object? value, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(value));
    args.add(unsafe.dartToJs(proxy));

    return _toDartBool(raw.callMethodVarArgs('set'.toJS, args))!;
  }

  /// Intercepts property access on the target object. It checks for specific properties (`watch` and
  /// `sync`) and binds their methods. For other properties, it returns the value directly.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being accessed.
  /// - [proxy]: The proxy object being invoked.
  Object? getMember(Object? target, Object? property, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(proxy));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('get'.toJS, args));
  }

  /// The deleteProperty member of ng.ScopeService.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being deleted.
  bool deleteProperty(Object? target, Object? property) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));

    return _toDartBool(raw.callMethodVarArgs('deleteProperty'.toJS, args))!;
  }

  /// Runs synchronous scope mutations as one batch. Listener notifications are queued while the
  /// callback runs and flushed once after the outermost batch exits. Mutations are not rolled back if
  /// the callback throws.
  ///
  /// Parameters:
  /// - [fn]: Value supplied for the fn parameter.
  Object? batch(JSFunction fn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(fn));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('batch'.toJS, args));
  }

  /// Registers a watcher for a property along with a listener function. The listener function is
  /// invoked when changes to that property are detected.
  ///
  /// Parameters:
  /// - [watchProp]: An expression to be watched in the context of this model.
  /// - [listenerFn]: A function to execute when changes are detected on watched context.
  /// - [lazy]: A flag to indicate if the listener should be invoked immediately. Defaults to false.
  /// - [directLeaf]: Value supplied for the directLeaf parameter.
  Object? watch(String watchProp,
      [Object? listenerFn = _undefinedArgument,
      Object? lazy = _undefinedArgument,
      Object? directLeaf = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(watchProp));
    if (!identical(listenerFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(listenerFn));
    }
    if (!identical(lazy, _undefinedArgument)) {
      args.add(unsafe.dartToJs(lazy));
    }
    if (!identical(directLeaf, _undefinedArgument)) {
      args.add(unsafe.dartToJs(directLeaf));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('watch'.toJS, args));
  }

  /// Creates a prototypically inherited child scope.
  ///
  /// Parameters:
  /// - [childInstance]: Value supplied for the childInstance parameter.
  GeneratedNgScopeService newMember(
      [Object? childInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(childInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(childInstance));
    }

    raw.callMethodVarArgs('new'.toJS, args);
    return this;
  }

  /// Creates an isolate child scope that does not inherit watchable properties directly.
  ///
  /// Parameters:
  /// - [instance]: Value supplied for the instance parameter.
  GeneratedNgScopeService newIsolate([Object? instance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(instance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(instance));
    }

    raw.callMethodVarArgs('newIsolate'.toJS, args);
    return this;
  }

  /// Creates a transcluded child scope linked to this scope and an optional parent instance.
  ///
  /// Parameters:
  /// - [parentInstance]: Value supplied for the parentInstance parameter.
  GeneratedNgScopeService transcluded(
      [Object? parentInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(parentInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(parentInstance));
    }

    raw.callMethodVarArgs('transcluded'.toJS, args);
    return this;
  }

  /// Merges enumerable properties from the provided object into the current scope target.
  ///
  /// Parameters:
  /// - [newTarget]: Value supplied for the newTarget parameter.
  void merge(Object? newTarget) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(newTarget));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('merge'.toJS, args));
  }

  /// Registers an event listener on this scope and returns a deregistration function.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [listener]: Value supplied for the listener parameter.
  Object? onMember(String name, JSFunction listener) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(listener));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('on'.toJS, args));
  }

  /// Emits an event upward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? emit(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('emit'.toJS, args));
  }

  /// Broadcasts an event downward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? broadcast(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('broadcast'.toJS, args));
  }

  /// Destroys this scope and its descendants.
  void destroy() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('destroy'.toJS, const <JSAny?>[]));
  }

  /// Searches this scope tree for a scope with the given id.
  ///
  /// Parameters:
  /// - [id]: Value supplied for the id parameter.
  Object? getById(Object? id) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getById'.toJS, args));
  }

  /// Searches the scope tree for a scope registered under the provided name.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  Object? searchByName(String name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('searchByName'.toJS, args));
  }
}

/// Public AngularTS SecurityConfig contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.SecurityConfig`.
base class GeneratedNgSecurityConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.SecurityConfig.
  const GeneratedNgSecurityConfig(super.raw);

  /// The fallback member of ng.SecurityConfig.
  String? get fallback => _toDartString(raw.getProperty('fallback'.toJS));

  /// The fallback member of ng.SecurityConfig.
  set fallback(String? value) {
    raw.setProperty('fallback'.toJS, unsafe.dartToJs(value));
  }

  /// HTTP origins explicitly permitted to carry credentials.
  Object? get allowInsecureOrigins =>
      unsafe.jsToDart<Object?>(raw.getProperty('allowInsecureOrigins'.toJS));

  /// The credentials member of ng.SecurityConfig.
  Object? get credentials =>
      unsafe.jsToDart<Object?>(raw.getProperty('credentials'.toJS));

  /// The isAuthenticated member of ng.SecurityConfig.
  Object? get isAuthenticated =>
      unsafe.jsToDart<Object?>(raw.getProperty('isAuthenticated'.toJS));

  /// The permissions member of ng.SecurityConfig.
  Object? get permissions =>
      unsafe.jsToDart<Object?>(raw.getProperty('permissions'.toJS));
}

/// Public AngularTS SecurityCredentialsConfig contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.SecurityCredentialsConfig`.
base class GeneratedNgSecurityCredentialsConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.SecurityCredentialsConfig.
  const GeneratedNgSecurityCredentialsConfig(super.raw);

  /// The bearer member of ng.SecurityCredentialsConfig.
  Object? get bearer =>
      unsafe.jsToDart<Object?>(raw.getProperty('bearer'.toJS));

  /// The basic member of ng.SecurityCredentialsConfig.
  Object? get basic => unsafe.jsToDart<Object?>(raw.getProperty('basic'.toJS));

  /// The cookie member of ng.SecurityCredentialsConfig.
  bool? get cookie => _toDartBool(raw.getProperty('cookie'.toJS));

  /// The cookie member of ng.SecurityCredentialsConfig.
  set cookie(bool? value) {
    raw.setProperty('cookie'.toJS, unsafe.dartToJs(value));
  }

  /// The order member of ng.SecurityCredentialsConfig.
  Object? get order => unsafe.jsToDart<Object?>(raw.getProperty('order'.toJS));
}

/// Public AngularTS SecurityPolicy contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.SecurityPolicy`.
base class GeneratedNgSecurityPolicy extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.SecurityPolicy.
  const GeneratedNgSecurityPolicy(super.raw);

  /// The check member of ng.SecurityPolicy.
  ///
  /// Parameters:
  /// - [context]: Value supplied for the context parameter.
  Object? check(Object? context) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(context));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('check'.toJS, args));
  }
}

/// Declarative defaults used when registering an application service worker. This config
/// intentionally maps only browser registration options and safe observation policy. Activation,
/// reload, cache strategy, push, and background sync remain explicit application or adapter policy.
///
/// Raw JavaScript facade for `ng.ServiceWorkerConfig`.
base class GeneratedNgServiceWorkerConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ServiceWorkerConfig.
  const GeneratedNgServiceWorkerConfig(super.raw);

  /// Register automatically when the runtime service is created.
  bool? get autoRegister => _toDartBool(raw.getProperty('autoRegister'.toJS));

  /// Register automatically when the runtime service is created.
  set autoRegister(bool? value) {
    raw.setProperty('autoRegister'.toJS, unsafe.dartToJs(value));
  }

  /// Check for an updated worker after registration succeeds.
  bool? get checkForUpdatesOnRegister =>
      _toDartBool(raw.getProperty('checkForUpdatesOnRegister'.toJS));

  /// Check for an updated worker after registration succeeds.
  set checkForUpdatesOnRegister(bool? value) {
    raw.setProperty('checkForUpdatesOnRegister'.toJS, unsafe.dartToJs(value));
  }
}

/// Stable failure codes reported by ServiceWorkerError.
///
/// Raw JavaScript facade for `ng.ServiceWorkerErrorCode`.
base class GeneratedNgServiceWorkerErrorCode extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ServiceWorkerErrorCode.
  const GeneratedNgServiceWorkerErrorCode(super.raw);
}

/// Message event normalized by `$serviceWorker`.
///
/// Raw JavaScript facade for `ng.ServiceWorkerMessageEvent`.
base class GeneratedNgServiceWorkerMessageEvent extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ServiceWorkerMessageEvent.
  const GeneratedNgServiceWorkerMessageEvent(super.raw);

  /// Message payload from the native service worker event.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// Native event for callers that need browser-specific fields.
  Object? get event => unsafe.jsToDart<Object?>(raw.getProperty('event'.toJS));

  /// Native source that sent the message, when supplied by the browser.
  Object? get source =>
      unsafe.jsToDart<Object?>(raw.getProperty('source'.toJS));
}

/// Explicit message target for `$serviceWorker.post(...)`.
///
/// Raw JavaScript facade for `ng.ServiceWorkerMessageTarget`.
base class GeneratedNgServiceWorkerMessageTarget extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ServiceWorkerMessageTarget.
  const GeneratedNgServiceWorkerMessageTarget(super.raw);
}

/// Options for ServiceWorkerService.post.
///
/// Raw JavaScript facade for `ng.ServiceWorkerPostOptions`.
base class GeneratedNgServiceWorkerPostOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ServiceWorkerPostOptions.
  const GeneratedNgServiceWorkerPostOptions(super.raw);

  /// Transferable objects sent with `postMessage(...)`.
  Object? get transfer =>
      unsafe.jsToDart<Object?>(raw.getProperty('transfer'.toJS));

  /// Worker target for this message.
  String? get target => _toDartString(raw.getProperty('target'.toJS));

  /// Worker target for this message.
  set target(String? value) {
    raw.setProperty('target'.toJS, unsafe.dartToJs(value));
  }
}

/// Template-friendly snapshot of the current registration.
///
/// Raw JavaScript facade for `ng.ServiceWorkerRegistrationState`.
base class GeneratedNgServiceWorkerRegistrationState extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ServiceWorkerRegistrationState.
  const GeneratedNgServiceWorkerRegistrationState(super.raw);

  /// True when a registration is currently known by the service.
  bool get registered => _toDartBool(raw.getProperty('registered'.toJS))!;

  /// Registration scope, when available.
  String? get scope => _toDartString(raw.getProperty('scope'.toJS));

  /// Update cache policy reported by the browser registration.
  String? get updateViaCache =>
      _toDartString(raw.getProperty('updateViaCache'.toJS));

  /// State of the installing worker, when present.
  String? get installing => _toDartString(raw.getProperty('installing'.toJS));

  /// State of the waiting worker, when present.
  String? get waiting => _toDartString(raw.getProperty('waiting'.toJS));

  /// State of the active worker, when present.
  String? get active => _toDartString(raw.getProperty('active'.toJS));
}

/// Per-request options for ServiceWorkerService.request.
///
/// Raw JavaScript facade for `ng.ServiceWorkerRequestOptions`.
base class GeneratedNgServiceWorkerRequestOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ServiceWorkerRequestOptions.
  const GeneratedNgServiceWorkerRequestOptions(super.raw);

  /// Request timeout in milliseconds.
  num? get timeout => _toDartNumber(raw.getProperty('timeout'.toJS));

  /// Request timeout in milliseconds.
  set timeout(num? value) {
    raw.setProperty('timeout'.toJS, unsafe.dartToJs(value));
  }

  /// Transferable objects sent with `postMessage(...)`.
  Object? get transfer =>
      unsafe.jsToDart<Object?>(raw.getProperty('transfer'.toJS));

  /// Worker target for this message.
  String? get target => _toDartString(raw.getProperty('target'.toJS));

  /// Worker target for this message.
  set target(String? value) {
    raw.setProperty('target'.toJS, unsafe.dartToJs(value));
  }
}

/// Injectable service-worker lifecycle and messaging facade.
///
/// Raw JavaScript facade for `ng.ServiceWorkerService`.
base class GeneratedNgServiceWorkerService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ServiceWorkerService.
  const GeneratedNgServiceWorkerService(super.raw);

  /// Support flag for templates and guards.
  bool get supported => _toDartBool(raw.getProperty('supported'.toJS))!;

  /// Template-facing lifecycle status for the latest service-worker operation.
  String get status => _toDartString(raw.getProperty('status'.toJS))!;

  /// Current native controller, if the page is controlled.
  Object? get controller =>
      unsafe.jsToDart<Object?>(raw.getProperty('controller'.toJS));

  /// Latest known native registration.
  Object? get registration =>
      unsafe.jsToDart<Object?>(raw.getProperty('registration'.toJS));

  /// Template-friendly registration snapshot.
  Object? get registrationState =>
      unsafe.jsToDart<Object?>(raw.getProperty('registrationState'.toJS));

  /// Template-friendly update snapshot.
  Object? get updateState =>
      unsafe.jsToDart<Object?>(raw.getProperty('updateState'.toJS));

  /// Register the configured script or an explicit script URL.
  ///
  /// Parameters:
  /// - [scriptOrOptions]: Value supplied for the scriptOrOptions parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> register(
      [Object? scriptOrOptions = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(scriptOrOptions, _undefinedArgument)) {
      args.add(unsafe.dartToJs(scriptOrOptions));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('register'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Resolve when the browser reports an active ready registration.
  JSPromise<JSAny?> ready() {
    return raw.callMethodVarArgs('ready'.toJS, const <JSAny?>[])
        as JSPromise<JSAny?>;
  }

  /// Ask the latest known registration to check for an updated worker.
  JSPromise<JSAny?> update() {
    return raw.callMethodVarArgs('update'.toJS, const <JSAny?>[])
        as JSPromise<JSAny?>;
  }

  /// Unregister the latest known registration.
  JSPromise<JSAny?> unregister() {
    return raw.callMethodVarArgs('unregister'.toJS, const <JSAny?>[])
        as JSPromise<JSAny?>;
  }

  /// Send a message to the current controller or an explicit worker target.
  ///
  /// Parameters:
  /// - [message]: Value supplied for the message parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> post(Object? message,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(message));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('post'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Send a request through a dedicated `MessageChannel`.
  ///
  /// Parameters:
  /// - [message]: Value supplied for the message parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> request(Object? message,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(message));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('request'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Subscribe to messages from the service worker container.
  ///
  /// Parameters:
  /// - [callback]: Value supplied for the callback parameter.
  Object? onMessage(JSFunction callback) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(callback));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onMessage'.toJS, args));
  }

  /// Subscribe to controller-change notifications.
  ///
  /// Parameters:
  /// - [callback]: Value supplied for the callback parameter.
  Object? onControllerChange(JSFunction callback) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(callback));

    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('onControllerChange'.toJS, args));
  }

  /// Subscribe to update-state notifications.
  ///
  /// Parameters:
  /// - [callback]: Value supplied for the callback parameter.
  Object? onUpdate(JSFunction callback) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(callback));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onUpdate'.toJS, args));
  }
}

/// Template-friendly snapshot of update-related service-worker state.
///
/// Raw JavaScript facade for `ng.ServiceWorkerUpdateState`.
base class GeneratedNgServiceWorkerUpdateState extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.ServiceWorkerUpdateState.
  const GeneratedNgServiceWorkerUpdateState(super.raw);

  /// True while an explicit update check is in flight.
  bool get checking => _toDartBool(raw.getProperty('checking'.toJS))!;

  /// True when a waiting worker has been discovered.
  bool get waiting => _toDartBool(raw.getProperty('waiting'.toJS))!;

  /// True when the active worker changed during the current page lifetime.
  bool get controllerChanged =>
      _toDartBool(raw.getProperty('controllerChanged'.toJS))!;

  /// Last successful update-check time in epoch milliseconds.
  num? get lastCheckedAt =>
      _toDartNumber(raw.getProperty('lastCheckedAt'.toJS));

  /// Latest observed service worker lifecycle phase.
  String? get phase => _toDartString(raw.getProperty('phase'.toJS));

  /// Worker associated with the latest update event.
  Object? get worker =>
      unsafe.jsToDart<Object?>(raw.getProperty('worker'.toJS));

  /// Registration associated with the latest update event.
  Object? get registration =>
      unsafe.jsToDart<Object?>(raw.getProperty('registration'.toJS));

  /// Stable failure code from the last update-related operation.
  String? get errorCode => _toDartString(raw.getProperty('errorCode'.toJS));

  /// Native error preserved for diagnostics.
  Object? get error => unsafe.jsToDart<Object?>(raw.getProperty('error'.toJS));
}

/// SSE-specific configuration
///
/// Raw JavaScript facade for `ng.SseConfig`.
base class GeneratedNgSseConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.SseConfig.
  const GeneratedNgSseConfig(super.raw);

  /// Include cookies/credentials when connecting
  bool? get withCredentials =>
      _toDartBool(raw.getProperty('withCredentials'.toJS));

  /// Include cookies/credentials when connecting
  set withCredentials(bool? value) {
    raw.setProperty('withCredentials'.toJS, unsafe.dartToJs(value));
  }

  /// Optional query parameters appended to the URL
  Object? get params =>
      unsafe.jsToDart<Object?>(raw.getProperty('params'.toJS));

  /// Called when the connection opens
  Object? get onOpen =>
      unsafe.jsToDart<Object?>(raw.getProperty('onOpen'.toJS));

  /// Called when a message is received
  Object? get onMessage =>
      unsafe.jsToDart<Object?>(raw.getProperty('onMessage'.toJS));

  /// Called with every registered connection message, including custom SSE event types
  Object? get onEvent =>
      unsafe.jsToDart<Object?>(raw.getProperty('onEvent'.toJS));

  /// Called when an error occurs
  Object? get onError =>
      unsafe.jsToDart<Object?>(raw.getProperty('onError'.toJS));

  /// Called when a WebSocket connection closes
  Object? get onClose =>
      unsafe.jsToDart<Object?>(raw.getProperty('onClose'.toJS));

  /// Called when a reconnect attempt happens
  Object? get onReconnect =>
      unsafe.jsToDart<Object?>(raw.getProperty('onReconnect'.toJS));

  /// Delay between reconnect attempts in milliseconds
  num? get retryDelay => _toDartNumber(raw.getProperty('retryDelay'.toJS));

  /// Delay between reconnect attempts in milliseconds
  set retryDelay(num? value) {
    raw.setProperty('retryDelay'.toJS, unsafe.dartToJs(value));
  }

  /// Maximum number of reconnect attempts
  num? get maxRetries => _toDartNumber(raw.getProperty('maxRetries'.toJS));

  /// Maximum number of reconnect attempts
  set maxRetries(num? value) {
    raw.setProperty('maxRetries'.toJS, unsafe.dartToJs(value));
  }

  /// Timeout in milliseconds to detect heartbeat inactivity
  num? get heartbeatTimeout =>
      _toDartNumber(raw.getProperty('heartbeatTimeout'.toJS));

  /// Timeout in milliseconds to detect heartbeat inactivity
  set heartbeatTimeout(num? value) {
    raw.setProperty('heartbeatTimeout'.toJS, unsafe.dartToJs(value));
  }

  /// Function to transform incoming messages
  Object? get transformMessage =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformMessage'.toJS));

  /// Additional EventSource event names to subscribe to
  Object? get eventTypes =>
      unsafe.jsToDart<Object?>(raw.getProperty('eventTypes'.toJS));
}

/// Managed SSE connection object returned by $sse. Provides a safe way to close the connection and
/// stop reconnection attempts.
///
/// Raw JavaScript facade for `ng.SseConnection`.
base class GeneratedNgSseConnection extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.SseConnection.
  const GeneratedNgSseConnection(super.raw);

  /// Manually close the SSE connection and stop all reconnect attempts
  void close() {
    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('close'.toJS, const <JSAny?>[]));
  }

  /// Manually restart the SSE connection.
  void reconnect() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('reconnect'.toJS, const <JSAny?>[]));
  }
}

/// $sse service type Returns a managed SSE connection that automatically reconnects when needed.
///
/// Raw JavaScript facade for `ng.SseService`.
base class GeneratedNgSseService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.SseService.
  const GeneratedNgSseService(super.raw);

  /// Calls the ng.SseService function.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [config]: Value supplied for the config parameter.
  Object? call(String url, [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// The StateDeclaration object is used to define a state or nested state. #### Example: ```js //
/// StateDeclaration object var foldersState = { name: 'folders', url: '/folders', component:
/// FoldersComponent, resolve: { allfolders: ['FolderService', function(FolderService) { return
/// FolderService.list(); }] }, } registry.register(foldersState); ```
///
/// Raw JavaScript facade for `ng.StateDeclaration`.
base class GeneratedNgStateDeclaration extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.StateDeclaration.
  const GeneratedNgStateDeclaration(super.raw);

  /// The state name (required) A unique state name, e.g. `"home"`, `"about"`, `"contacts"`. To create
  /// a parent/child state use a dot, e.g. `"about.sales"`, `"home.newest"`. Note: [State] objects
  /// require unique names. The name is used like an id.
  String get name => _toDartString(raw.getProperty('name'.toJS))!;

  /// The state name (required) A unique state name, e.g. `"home"`, `"about"`, `"contacts"`. To create
  /// a parent/child state use a dot, e.g. `"about.sales"`, `"home.newest"`. Note: [State] objects
  /// require unique names. The name is used like an id.
  set name(String value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// Abstract state indicator An abstract state can never be directly activated. Use an abstract
  /// state to provide inherited properties (url, resolve, data, etc) to children states.
  bool? get abstractState => _toDartBool(raw.getProperty('abstract'.toJS));

  /// Abstract state indicator An abstract state can never be directly activated. Use an abstract
  /// state to provide inherited properties (url, resolve, data, etc) to children states.
  set abstractState(bool? value) {
    raw.setProperty('abstract'.toJS, unsafe.dartToJs(value));
  }

  /// The parent state Normally, a state's parent is implied from the state's [[name]], e.g.,
  /// `"parentstate.childstate"`. Alternatively, you can explicitly set the parent state using this
  /// property. This allows shorter state names, e.g., `&lt;a ng-state="'childstate'"&gt;Child&lt;/a&gt;` instead
  /// of `&lt;a ng-state="'parentstate.childstate'"&gt;Child&lt;/a&gt; When using this property, the state's name
  /// should not have any dots in it. #### Example: ```js var parentstate = { name: 'parentstate' }
  /// var childstate = { name: 'childstate', parent: 'parentstate' // or use a JS var which is the
  /// parent StateDeclaration, i.e.: // parent: parentstate } ```
  Object? get parent =>
      unsafe.jsToDart<Object?>(raw.getProperty('parent'.toJS));

  /// Named view declarations for this state. Each key targets an `ng-view`; each value is either a
  /// full view declaration or a string shorthand for `{ component: "componentName" }`. Examples:
  /// ```js views: { mymessages: "mymessages", messagelist: { component: "messageList" },
  /// "^.^.messagecontent": "message" } ```
  Object? get views => unsafe.jsToDart<Object?>(raw.getProperty('views'.toJS));

  /// Resolve - a mechanism to asynchronously fetch data, participating in the Transition lifecycle
  /// The `resolve:` property defines data (or other dependencies) to be fetched asynchronously when
  /// the state is being entered. After the data is fetched, it may be used in views, transition hooks
  /// or other resolves that belong to this state. The data may also be used in any views or resolves
  /// that belong to nested states. ### As an array Each array element should be a
  /// [[ResolvableLiteral]] object. #### Example: The `user` resolve injects the current `Transition`
  /// and the `UserService` (using its token, which is a string). The [[ResolvableLiteral.eager]] flag
  /// controls whether the resolve starts at transition start instead of when the owning state is
  /// entered. The `user` data, fetched asynchronously, can then be used in a view. ```js var state =
  /// { name: 'user', url: '/user/:userId resolve: [ { token: 'user', eager: true, deps:
  /// ['UserService', Transition], resolveFn: (userSvc, trans) =&gt;
  /// userSvc.fetchUser(trans.params().userId) }, } ] } ``` ### As an object The `resolve` property
  /// may be an object where: - Each key (string) is the name of the dependency. - Each value
  /// (function) is an injectable function which returns the dependency, or a promise for the
  /// dependency. This style is based on AngularTS injectable functions. Dependency-bearing functions
  /// must use array annotation or a static `$inject` property. #### AngularTS Example: ```js resolve:
  /// { // If you inject `myStateDependency` into a controller, you'll get "abc" myStateDependency:
  /// function() { return "abc"; }, // Dependencies are annotated in "Inline Array Annotation"
  /// myAsyncData: ['$http', '$transition$' function($http, $transition$) { // Return a promise
  /// (async) for the data return $http.get("/foos/" + $transition$.params().foo); }] } ``` Note: You
  /// cannot mark individual entries as eager, nor can you use non-string tokens when using the object
  /// style `resolve:` block. ### Lifecycle Since a resolve function can return a promise, the router
  /// will delay entering the state until the promises are ready. If any of the promises are rejected,
  /// the Transition is aborted with an Error. By default, resolves for a state are fetched just
  /// before that state is entered. Note that only states which are being *entered* during the
  /// `Transition` have their resolves fetched. States that are "retained" do not have their resolves
  /// re-fetched. If you are currently in a parent state `parent` and are transitioning to a child
  /// state `parent.child`, the previously resolved data for state `parent` can be injected into
  /// `parent.child` without delay. Any resolved data for `parent.child` is retained until
  /// `parent.child` is exited, e.g., by transitioning back to the `parent` state. Because of this
  /// scoping and lifecycle, resolves are a great place to fetch your application's primary data. ###
  /// Injecting resolves into other things During a transition, Resolve data can be injected into: -
  /// Views (the components which fill a `ng-view` tag) - Transition Hooks - Other resolves (a resolve
  /// may depend on asynchronous data from a different resolve) ### Injecting other things into
  /// resolves Resolve functions usually have dependencies on some other API(s). The dependencies are
  /// usually declared and injected into the resolve function. A common pattern is to inject a custom
  /// service such as `UserService`. The resolve then delegates to a service method, such as
  /// `UserService.list()`; #### Special injectable tokens - `Transition`: The current [[Transition]]
  /// object; information and API about the current transition, such as "to" and "from" State
  /// Parameters and transition options. - `'$transition$'`: A string alias for the `Transition`
  /// injectable - `'$state$'`: For `onEnter`/`onExit`/`onRetain`, the state being
  /// entered/exited/retained. - Other resolve tokens: A resolve can depend on another resolve, either
  /// from the same state, or from any parent state. #### Example: ```js // Injecting a resolve into
  /// another resolve resolve: [ // Define a resolve 'allusers' which delegates to the
  /// UserService.list() // which returns a promise (async) for all the users { token: 'allusers',
  /// resolveFn: (UserService) =&gt; UserService.list(), deps: [UserService] }, // Define a resolve
  /// 'user' which depends on the allusers resolve. // This resolve function is not called until
  /// 'allusers' is ready. { token: 'user', resolveFn: (allusers, trans) =&gt; _.find(allusers,
  /// trans.params().userId), deps: ['allusers', Transition] } } ```
  Object? get resolve =>
      unsafe.jsToDart<Object?>(raw.getProperty('resolve'.toJS));

  /// The url fragment for the state A URL fragment (with optional parameters) which is used to match
  /// the browser location with this state. This fragment will be appended to the parent state's URL
  /// in order to build up the overall URL for this state. It may include path parameters, typed
  /// parameters, and query parameters.
  String? get url => _toDartString(raw.getProperty('url'.toJS));

  /// The url fragment for the state A URL fragment (with optional parameters) which is used to match
  /// the browser location with this state. This fragment will be appended to the parent state's URL
  /// in order to build up the overall URL for this state. It may include path parameters, typed
  /// parameters, and query parameters.
  set url(String? value) {
    raw.setProperty('url'.toJS, unsafe.dartToJs(value));
  }

  /// Params configuration An object which optionally configures parameters declared in the url, or
  /// defines additional non-url parameters. For each parameter being configured, add a
  /// [[ParamDeclaration]] keyed to the name of the parameter. #### Example: ```js params: { param1: {
  /// type: "int", array: true, value: [] }, param2: { value: "index" } } ```
  Object? get params =>
      unsafe.jsToDart<Object?>(raw.getProperty('params'.toJS));

  /// An inherited property to store state data This is a spot for you to store inherited state
  /// metadata. Child states' `data` object will prototypally inherit from their parent state. Use
  /// this for application metadata. Use `policy.navigation` for framework navigation decisions such
  /// as authentication, permissions, or redirects. Note: because prototypal inheritance is used,
  /// changes to parent `data` objects reflect in the child `data` objects. Care should be taken if
  /// you are using `hasOwnProperty` on the `data` object. Properties from parent objects will return
  /// false for `hasOwnProperty`.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// Declarative state policy metadata consumed by AngularTS framework services. `policy.navigation`
  /// is inherited through the state tree and evaluated by the router's security navigation hook
  /// before resolves, controllers, or views run. `policy.transition.canExit` is evaluated before
  /// exiting states are torn down. `policy.retention` declares keep-alive route subtree behavior and
  /// can override router-wide retention defaults.
  Object? get policy =>
      unsafe.jsToDart<Object?>(raw.getProperty('policy'.toJS));

  /// Synchronously or asynchronously redirects Transitions to a different state/params If this
  /// property is defined, a Transition directly to this state will be redirected based on the
  /// property's value. - If the value is a `string`, the Transition is redirected to the state named
  /// by the string. - If the property is an object with a `state` and/or `params` property, the
  /// Transition is redirected to the named `state` and/or `params`. - If the value is a
  /// [[TargetState]] the Transition is redirected to the `TargetState` - If the property is a
  /// function: - The function is called with the current [[Transition]] - The return value is
  /// processed using the previously mentioned rules. - If the return value is a promise, the promise
  /// is waited for, then the resolved async value is processed using the same rules. Note:
  /// `redirectTo` is processed as an `onStart` hook, before non-eager resolves. If your redirect
  /// function relies on resolve data, get the [[Transition.injector]] and request the resolve data
  /// with `getAsync()`. #### Example: ```js // a string .state('A', { redirectTo: 'A.B' }) // a
  /// {state, params} object .state('C', { redirectTo: { state: 'C.D', params: { foo: 'index' } } })
  /// // a fn .state('E', { redirectTo: () =&gt; "A" }) // a fn conditionally returning a {state, params}
  /// .state('F', { redirectTo: (trans) =&gt; { if (trans.params().foo &lt; 10) return { state: 'F', params:
  /// { foo: 10 } }; } }) // a fn returning a promise for a redirect .state('G', { redirectTo: (trans)
  /// =&gt; { let svc = trans.injector().get('SomeAsyncService') let promise =
  /// svc.getAsyncRedirectTo(trans.params.foo); return promise; } }) // a fn that fetches resolve data
  /// .state('G', { redirectTo: (trans) =&gt; { // getAsync tells the resolve to load let resolvePromise
  /// = trans.injector().getAsync('SomeResolve') return resolvePromise.then(resolveData =&gt; resolveData
  /// === 'login' ? 'login' : null); } }) ```
  Object? get redirectTo =>
      unsafe.jsToDart<Object?>(raw.getProperty('redirectTo'.toJS));

  /// A state hook invoked when a state is being entered. The hook can inject global services. It can
  /// also inject `$transition$` or `$state$` (from the current transition). ### Example: ```js
  /// app.router({ name: 'mystate', onEnter: (MyService, $transition$, $state$) =&gt; { return
  /// MyService.doSomething($state$.name, $transition$.params()); } }); ``` #### Example:` ```js
  /// app.router({ name: 'mystate', onEnter: [ 'MyService', '$transition$', '$state$', function
  /// (MyService, $transition$, $state$) { return MyService.doSomething($state$.name,
  /// $transition$.params()); } ] }); ```
  Object? get onEnter =>
      unsafe.jsToDart<Object?>(raw.getProperty('onEnter'.toJS));

  /// A state hook invoked when a state is being retained. The hook can inject global services. It can
  /// also inject `$transition$` or `$state$` (from the current transition). #### Example: ```js
  /// app.router({ name: 'mystate', onRetain: (MyService, $transition$, $state$) =&gt; { return
  /// MyService.doSomething($state$.name, $transition$.params()); } }); ``` #### Example:` ```js
  /// app.router({ name: 'mystate', onRetain: [ 'MyService', '$transition$', '$state$', function
  /// (MyService, $transition$, $state$) { return MyService.doSomething($state$.name,
  /// $transition$.params()); } ] }); ```
  Object? get onRetain =>
      unsafe.jsToDart<Object?>(raw.getProperty('onRetain'.toJS));

  /// A state hook invoked when a state is being exited. The hook can inject global services. It can
  /// also inject `$transition$` or `$state$` (from the current transition). ### Example: ```js
  /// app.router({ name: 'mystate', onExit: (MyService, $transition$, $state$) =&gt; { return
  /// MyService.doSomething($state$.name, $transition$.params()); } }); ``` #### Example:` ```js
  /// app.router({ name: 'mystate', onExit: [ 'MyService', '$transition$', '$state$', function
  /// (MyService, $transition$, $state$) { return MyService.doSomething($state$.name,
  /// $transition$.params()); } ] }); ```
  Object? get onExit =>
      unsafe.jsToDart<Object?>(raw.getProperty('onExit'.toJS));

  /// Marks all the state's parameters as `dynamic`. All parameters on the state will use this value
  /// for `dynamic` as a default. Individual parameters may override this default using
  /// [[ParamDeclaration.dynamic]] in the [[params]] block. This default applies to all parameters
  /// declared on this state.
  bool? get dynamicState => _toDartBool(raw.getProperty('dynamic'.toJS));

  /// Marks all the state's parameters as `dynamic`. All parameters on the state will use this value
  /// for `dynamic` as a default. Individual parameters may override this default using
  /// [[ParamDeclaration.dynamic]] in the [[params]] block. This default applies to all parameters
  /// declared on this state.
  set dynamicState(bool? value) {
    raw.setProperty('dynamic'.toJS, unsafe.dartToJs(value));
  }

  /// The name of the component to use for this view. The name of an AngularTS `.component()` which
  /// will be used for this view. Resolve data can be provided to the component via the component's
  /// `bindings` object. For each binding declared on the component, any resolve with the same name is
  /// set on the component's controller instance. Note: Mapping from resolve names to component inputs
  /// may be specified using [[bindings]]. #### Example: ```js .state('profile', { // Use the
  /// &lt;my-profile&gt;&lt;/my-profile&gt; component for this state. component: 'MyProfile', } ``` Note: When
  /// using `component` to define a view, you may _not_ use any of: `template`, `templateUrl`,
  /// `controller`.
  Object? get component =>
      unsafe.jsToDart<Object?>(raw.getProperty('component'.toJS));

  /// An object which maps `resolve`s to [[component]] `bindings`. When using a [[component]]
  /// declaration (`component: 'myComponent'`), each input binding for the component is supplied data
  /// from a resolve of the same name, by default. You may supply data from a different resolve name
  /// by mapping it here. Each key in this object is the name of one of the component's input
  /// bindings. Each value is the name of the resolve that should be provided to that binding. Any
  /// component bindings that are omitted from this map get the default behavior of mapping to a
  /// resolve of the same name. #### Example: ```js app.router('foo', { resolve: { foo: ['FooService',
  /// function(FooService) { return FooService.get(); }], bar: ['BarService', function(BarService) {
  /// return BarService.get(); }] }, component: 'Baz', // The component's `baz` binding gets data from
  /// the `bar` resolve // The component's `foo` binding gets data from the `foo` resolve (default
  /// behavior) bindings: { baz: 'bar' } }); app.component('Baz', { templateUrl: 'baz.html',
  /// controller: 'BazController', bindings: { foo: '&lt;', // foo binding baz: '&lt;' // baz binding } });
  /// ```
  Object? get bindings =>
      unsafe.jsToDart<Object?>(raw.getProperty('bindings'.toJS));

  /// The view's controller function or name The controller function, or the name of a registered
  /// controller. The controller function will be used to control the contents of the
  /// [[directives.ngVIew]] directive. See: [[Ng1Controller]] for information about component-level
  /// router hooks.
  Object? get controller =>
      unsafe.jsToDart<Object?>(raw.getProperty('controller'.toJS));

  /// The HTML template for the view. HTML template as a string, or a function which returns an html
  /// template as a string. This template will be used to render the corresponding
  /// [[directives.ngVIew]] directive. This property takes precedence over templateUrl. If `template`
  /// is a function, it will be called with the Transition parameters as the first argument. ####
  /// Example: ```js template: "&lt;h1&gt;inline template definition&lt;/h1&gt;&lt;div ng-view&gt;&lt;/div&gt;" ``` ####
  /// Example: ```js template: function(params) { return "&lt;h1&gt;generated template&lt;/h1&gt;"; } ```
  Object? get template =>
      unsafe.jsToDart<Object?>(raw.getProperty('template'.toJS));

  /// The URL for the HTML template for the view. A path or a function that returns a path to an html
  /// template. The template will be fetched and used to render the corresponding
  /// [[directives.ngVIew]] directive. If `templateUrl` is a function, it will be called with the
  /// Transition parameters as the first argument. #### Example: ```js templateUrl:
  /// "/templates/home.html" ``` #### Example: ```js templateUrl: function(params) { return
  /// myTemplates[params.pageId]; } ```
  Object? get templateUrl =>
      unsafe.jsToDart<Object?>(raw.getProperty('templateUrl'.toJS));
}

/// Public AngularTS StatePolicyDeclaration contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.StatePolicyDeclaration`.
base class GeneratedNgStatePolicyDeclaration extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.StatePolicyDeclaration.
  const GeneratedNgStatePolicyDeclaration(super.raw);

  /// The navigation member of ng.StatePolicyDeclaration.
  Object? get navigation =>
      unsafe.jsToDart<Object?>(raw.getProperty('navigation'.toJS));

  /// The transition member of ng.StatePolicyDeclaration.
  Object? get transition =>
      unsafe.jsToDart<Object?>(raw.getProperty('transition'.toJS));

  /// The retention member of ng.StatePolicyDeclaration.
  Object? get retention =>
      unsafe.jsToDart<Object?>(raw.getProperty('retention'.toJS));
}

/// Public `$stateRegistry` contract for dynamic route registration. Module-owned static routes
/// should normally use [[NgModule.router]]. Use this service when routes must be added or removed
/// at runtime.
///
/// Raw JavaScript facade for `ng.StateRegistryService`.
base class GeneratedNgStateRegistryService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.StateRegistryService.
  const GeneratedNgStateRegistryService(super.raw);

  /// The onStatesChanged member of ng.StateRegistryService.
  ///
  /// Parameters:
  /// - [listener]: Value supplied for the listener parameter.
  Object? onStatesChanged(JSFunction listener) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(listener));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onStatesChanged'.toJS, args));
  }

  /// The root member of ng.StateRegistryService.
  Object? root() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('root'.toJS, const <JSAny?>[]));
  }

  /// The register member of ng.StateRegistryService.
  ///
  /// Parameters:
  /// - [stateDefinition]: Value supplied for the stateDefinition parameter.
  Object? register(Object? stateDefinition) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stateDefinition));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('register'.toJS, args));
  }

  /// The deregister member of ng.StateRegistryService.
  ///
  /// Parameters:
  /// - [stateOrName]: Value supplied for the stateOrName parameter.
  Object? deregister(Object? stateOrName) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stateOrName));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('deregister'.toJS, args));
  }

  /// The getAll member of ng.StateRegistryService.
  Object? getAll() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('getAll'.toJS, const <JSAny?>[]));
  }

  /// The get member of ng.StateRegistryService.
  ///
  /// Parameters:
  /// - [stateOrName]: Value supplied for the stateOrName parameter.
  /// - [base]: Value supplied for the base parameter.
  Object? getMember(
      [Object? stateOrName = _undefinedArgument,
      Object? base = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(stateOrName, _undefinedArgument)) {
      args.add(unsafe.dartToJs(stateOrName));
    }
    if (!identical(base, _undefinedArgument)) {
      args.add(unsafe.dartToJs(base));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('get'.toJS, args));
  }
}

/// Public AngularTS StateService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.StateService`.
base class GeneratedNgStateService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.StateService.
  const GeneratedNgStateService(super.raw);

  /// The latest successful state parameters.
  Object? get params =>
      unsafe.jsToDart<Object?>(raw.getProperty('params'.toJS));

  /// The current state declaration, when navigation has selected one.
  Object? get current =>
      unsafe.jsToDart<Object?>(raw.getProperty('current'.toJS));

  /// Overload for typed route names and params. Untyped overload used when no route map is supplied.
  ///
  /// Parameters:
  /// - [to]: Value supplied for the to parameter.
  /// - [params]: Value supplied for the params parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> go(Object? to,
      [Object? params = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(to));
    if (!identical(params, _undefinedArgument)) {
      args.add(unsafe.dartToJs(params));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('go'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Prefetch lazy declarations and templates for a typed route. Prefetch lazy declarations and
  /// templates without navigating.
  ///
  /// Parameters:
  /// - [stateOrName]: Value supplied for the stateOrName parameter.
  /// - [params]: Value supplied for the params parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> prefetch(Object? stateOrName,
      [Object? params = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stateOrName));
    if (!identical(params, _undefinedArgument)) {
      args.add(unsafe.dartToJs(params));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('prefetch'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Overload for typed route names and params. Untyped overload used when no route map is supplied.
  ///
  /// Parameters:
  /// - [stateOrName]: Value supplied for the stateOrName parameter.
  /// - [params]: Value supplied for the params parameter.
  /// - [options]: Value supplied for the options parameter.
  String? href(Object? stateOrName,
      [Object? params = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stateOrName));
    if (!identical(params, _undefinedArgument)) {
      args.add(unsafe.dartToJs(params));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return _toDartString(raw.callMethodVarArgs('href'.toJS, args));
  }

  /// Build a target that can be returned from a transition hook.
  ///
  /// Parameters:
  /// - [identifier]: Value supplied for the identifier parameter.
  /// - [params]: Value supplied for the params parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? target(Object? identifier,
      [Object? params = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(identifier));
    if (!identical(params, _undefinedArgument)) {
      args.add(unsafe.dartToJs(params));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('target'.toJS, args));
  }

  /// Get all states or a matching public state declaration.
  ///
  /// Parameters:
  /// - [stateOrName]: Value supplied for the stateOrName parameter.
  /// - [base]: Value supplied for the base parameter.
  Object? getMember(
      [Object? stateOrName = _undefinedArgument,
      Object? base = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(stateOrName, _undefinedArgument)) {
      args.add(unsafe.dartToJs(stateOrName));
    }
    if (!identical(base, _undefinedArgument)) {
      args.add(unsafe.dartToJs(base));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('get'.toJS, args));
  }

  /// Check whether the current state matches a state, ancestor, or glob.
  ///
  /// Parameters:
  /// - [stateOrName]: Value supplied for the stateOrName parameter.
  /// - [params]: Value supplied for the params parameter.
  /// - [options]: Value supplied for the options parameter.
  bool matches(Object? stateOrName,
      [Object? params = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stateOrName));
    if (!identical(params, _undefinedArgument)) {
      args.add(unsafe.dartToJs(params));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return _toDartBool(raw.callMethodVarArgs('matches'.toJS, args))!;
  }
}

/// Public AngularTS StorageBackend contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.StorageBackend`.
base class GeneratedNgStorageBackend extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.StorageBackend.
  const GeneratedNgStorageBackend(super.raw);

  /// Read a stored serialized value.
  ///
  /// Parameters:
  /// - [key]: Value supplied for the key parameter.
  String? getMember(String key) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));

    return _toDartString(raw.callMethodVarArgs('get'.toJS, args));
  }

  /// Store a serialized value.
  ///
  /// Parameters:
  /// - [key]: Value supplied for the key parameter.
  /// - [value]: Value supplied for the value parameter.
  void setMember(String key, String value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));
    args.add(unsafe.dartToJs(value));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('set'.toJS, args));
  }

  /// Remove a stored value.
  ///
  /// Parameters:
  /// - [key]: Value supplied for the key parameter.
  void remove(String key) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(key));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('remove'.toJS, args));
  }
}

/// Creates a proxy that automatically persists an object's state into a storage backend whenever a
/// property is set. The proxy also restores previously serialized state on creation and persists
/// deletions in addition to property assignments.
///
/// Raw JavaScript facade for `ng.StorageService`.
base class GeneratedNgStorageService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.StorageService.
  const GeneratedNgStorageService(super.raw);

  /// Creates a proxy that automatically persists an object's state into a storage backend whenever a
  /// property is set. The proxy also restores previously serialized state on creation and persists
  /// deletions in addition to property assignments.
  ///
  /// Parameters:
  /// - [target]: Value supplied for the target parameter.
  /// - [key]: Value supplied for the key parameter.
  /// - [storage]: Value supplied for the storage parameter.
  /// - [options]: Value supplied for the options parameter.
  Object? call(Object? target, String key, Object? storage,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(key));
    args.add(unsafe.dartToJs(storage));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Built-in persistent storage backends understood by `NgModule.store()`.
///
/// Raw JavaScript facade for `ng.StorageType`.
base class GeneratedNgStorageType extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.StorageType.
  const GeneratedNgStorageType(super.raw);
}

/// Public AngularTS StreamService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.StreamService`.
base class GeneratedNgStreamService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.StreamService.
  const GeneratedNgStreamService(super.raw);

  /// Returns true when a value is a native readable byte stream.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  bool isReadableStream(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return _toDartBool(raw.callMethodVarArgs('isReadableStream'.toJS, args))!;
  }

  /// Decodes a byte stream and calls `onChunk` without retaining decoded text.
  ///
  /// Parameters:
  /// - [stream]: Value supplied for the stream parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> consumeText(ReadableStream stream,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stream));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('consumeText'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Decodes a byte stream into text chunks.
  ///
  /// Parameters:
  /// - [stream]: Value supplied for the stream parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> readText(ReadableStream stream,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stream));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('readText'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Decodes a byte stream and emits complete lines.
  ///
  /// Parameters:
  /// - [stream]: Value supplied for the stream parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> readLines(ReadableStream stream,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stream));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('readLines'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Decodes newline-delimited JSON without retaining parsed values.
  ///
  /// Parameters:
  /// - [stream]: Value supplied for the stream parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> consumeJsonLines(ReadableStream stream,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stream));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('consumeJsonLines'.toJS, args)
        as JSPromise<JSAny?>;
  }

  /// Decodes newline-delimited JSON and returns all parsed values.
  ///
  /// Parameters:
  /// - [stream]: Value supplied for the stream parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> readJsonLines(ReadableStream stream,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(stream));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('readJsonLines'.toJS, args)
        as JSPromise<JSAny?>;
  }
}

/// Possible values for `data-swap` and realtime protocol `swap` fields. Union type representing all
/// possible DOM insertion modes.
///
/// Raw JavaScript facade for `ng.SwapMode`.
base class GeneratedNgSwapMode extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.SwapMode.
  const GeneratedNgSwapMode(super.raw);
}

/// Downloads a template using $http and, upon success, stores the contents inside of
/// $templateCache. If the HTTP request fails or the response data of the HTTP request is empty then
/// a $compile error will be thrown (unless {ignoreRequestError} is set to true).
///
/// Raw JavaScript facade for `ng.TemplateRequestService`.
base class GeneratedNgTemplateRequestService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.TemplateRequestService.
  const GeneratedNgTemplateRequestService(super.raw);

  /// Calls the ng.TemplateRequestService function.
  ///
  /// Parameters:
  /// - [templateUrl]: Value supplied for the templateUrl parameter.
  JSPromise<JSAny?> call(String templateUrl) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(templateUrl));

    return _callFunction(raw as JSFunction, args) as JSPromise<JSAny?>;
  }
}

/// A function passed to directive link functions for transcluded content. It behaves like a linking
/// function, with the `scope` argument automatically created as a new child of the transcluded
/// parent scope. The function returns the DOM content to be injected (transcluded) into the
/// directive.
///
/// Raw JavaScript facade for `ng.TranscludeFn`.
base class GeneratedNgTranscludeFn extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.TranscludeFn.
  const GeneratedNgTranscludeFn(super.raw);

  /// $transclude(cloneAttachFn, futureParentElement?, slotName?) (no explicit scope passed)
  ///
  /// Parameters:
  /// - [cloneAttachFn]: Value supplied for the cloneAttachFn parameter.
  /// - [futureParentElement]: Value supplied for the futureParentElement parameter.
  /// - [slotName]: Value supplied for the slotName parameter.
  Object? call(JSFunction cloneAttachFn,
      [Object? futureParentElement = _undefinedArgument,
      Object? slotName = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(cloneAttachFn));
    if (!identical(futureParentElement, _undefinedArgument)) {
      args.add(unsafe.dartToJs(futureParentElement));
    }
    if (!identical(slotName, _undefinedArgument)) {
      args.add(unsafe.dartToJs(slotName));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }

  /// Added by your `controllersBoundTransclude` wrapper.
  ///
  /// Parameters:
  /// - [slotName]: Value supplied for the slotName parameter.
  bool isSlotFilled(Object? slotName) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(slotName));

    return _toDartBool(raw.callMethodVarArgs('isSlotFilled'.toJS, args))!;
  }
}

/// Represents a transition between two states. A transition contains the source and destination
/// states, parameters, resolves, and the states entered or exited during navigation.
///
/// Raw JavaScript facade for `ng.Transition`.
base class GeneratedNgTransition extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Transition.
  const GeneratedNgTransition(super.raw);

  /// The promise member of ng.Transition.
  JSPromise<JSAny?> get promise =>
      raw.getProperty('promise'.toJS) as JSPromise<JSAny?>;

  /// The id member of ng.Transition.
  num get id => _toDartNumber(raw.getProperty('id'.toJS))!;

  /// The id member of ng.Transition.
  set id(num value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The success member of ng.Transition.
  bool? get success => _toDartBool(raw.getProperty('success'.toJS));

  /// The success member of ng.Transition.
  set success(bool? value) {
    raw.setProperty('success'.toJS, unsafe.dartToJs(value));
  }

  /// The applyViewConfigs member of ng.Transition.
  void applyViewConfigs() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('applyViewConfigs'.toJS, const <JSAny?>[]));
  }

  /// Returns the "from state" Returns the state that the transition is coming *from*.
  Object? from() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('from'.toJS, const <JSAny?>[]));
  }

  /// Returns the "to state" Returns the state that the transition is going *to*.
  Object? to() {
    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('to'.toJS, const <JSAny?>[]));
  }

  /// The params member of ng.Transition.
  ///
  /// Parameters:
  /// - [pathname]: Value supplied for the pathname parameter.
  Object? params([Object? pathname = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(pathname, _undefinedArgument)) {
      args.add(unsafe.dartToJs(pathname));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('params'.toJS, args));
  }

  /// Gets the states being entered.
  Object? entering() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('entering'.toJS, const <JSAny?>[]));
  }

  /// Gets the states being exited.
  Object? exiting() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('exiting'.toJS, const <JSAny?>[]));
  }

  /// Creates a new transition that is a redirection of the current one. This transition can be
  /// returned from a [[TransitionService]] hook to redirect a transition to a new state and/or set of
  /// parameters.
  ///
  /// Parameters:
  /// - [targetState]: the new target state for the redirected transition
  GeneratedNgTransition redirect(Object? targetState) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(targetState));

    raw.callMethodVarArgs('redirect'.toJS, args);
    return this;
  }

  /// Returns true if the transition is dynamic. A transition is dynamic if no states are entered nor
  /// exited, but at least one dynamic parameter has changed.
  bool dynamicMember() {
    return _toDartBool(
        raw.callMethodVarArgs('dynamic'.toJS, const <JSAny?>[]))!;
  }

  /// Checks if this transition is currently active/running.
  bool isActive() {
    return _toDartBool(
        raw.callMethodVarArgs('isActive'.toJS, const <JSAny?>[]))!;
  }

  /// Checks if the Transition is valid
  bool valid() {
    return _toDartBool(raw.callMethodVarArgs('valid'.toJS, const <JSAny?>[]))!;
  }

  /// Aborts this transition Imperative API to abort a Transition. This only applies to Transitions
  /// that are not yet complete.
  void abort() {
    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('abort'.toJS, const <JSAny?>[]));
  }

  /// A string representation of the Transition
  String toStringMember() {
    return _toDartString(
        raw.callMethodVarArgs('toString'.toJS, const <JSAny?>[]))!;
  }
}

/// Names the destination and source routes carried by a typed transition.
///
/// Raw JavaScript facade for `ng.TransitionRouteContract`.
base class GeneratedNgTransitionRouteContract extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.TransitionRouteContract.
  const GeneratedNgTransitionRouteContract(super.raw);

  /// The from member of ng.TransitionRouteContract.
  Object? get from => unsafe.jsToDart<Object?>(raw.getProperty('from'.toJS));

  /// The to member of ng.TransitionRouteContract.
  Object? get to => unsafe.jsToDart<Object?>(raw.getProperty('to'.toJS));
}

/// This interface specifies the api for registering Transition Hooks. Both the
/// [[TransitionService]] and also the [[Transition]] object itself implement this interface. Note:
/// the Transition object only allows hooks to be registered before the Transition is started.
///
/// Raw JavaScript facade for `ng.TransitionsService`.
base class GeneratedNgTransitionsService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.TransitionsService.
  const GeneratedNgTransitionsService(super.raw);

  /// Registers a [[TransitionHookFn]], called *before a transition starts*. Registers a transition
  /// lifecycle hook, which is invoked before a transition even begins. This hook can be useful to
  /// implement logic which prevents a transition from even starting, such as authentication,
  /// redirection See [[TransitionHookFn]] for the signature of the function. The
  /// [[HookMatchCriteria]] is used to determine which Transitions the hook should be invoked for. To
  /// match all Transitions, use an empty criteria object `{}`. ### Lifecycle `onBefore` hooks are
  /// invoked *before a Transition starts*. No resolves have been fetched yet. Each `onBefore` hook is
  /// invoked synchronously, in the same call stack as [[StateService.transitionTo]]. The registered
  /// `onBefore` hooks are invoked in priority order. Note: during the `onBefore` phase, additional
  /// hooks can be added to the specific [[Transition]] instance. These "on-the-fly" hooks only affect
  /// the currently running transition.. ### Return value The hook's return value can be used to
  /// pause, cancel, or redirect the current Transition. See [[HookResult]] for more information. If
  /// any hook modifies the transition *synchronously* (by throwing, returning `false`, or returning a
  /// [[TargetState]]), the remainder of the hooks are skipped. If a hook returns a promise, the
  /// remainder of the `onBefore` hooks are still invoked synchronously. All promises are resolved,
  /// and processed asynchronously before the `onStart` phase of the Transition. ### Examples ####
  /// Default Substate This example redirects any transition from 'home' to 'home.dashboard'. This is
  /// commonly referred to as a "default substate".
  ///
  /// Parameters:
  /// - [matchCriteria]: defines which Transitions the Hook should be invoked for.
  /// - [callback]: the hook function which will be invoked.
  /// - [options]: Value supplied for the options parameter.
  Object? onBefore(Object? matchCriteria, JSFunction callback,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(matchCriteria));
    args.add(unsafe.dartToJs(callback));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onBefore'.toJS, args));
  }

  /// Registers a [[TransitionHookFn]], called when a transition starts. Registers a transition
  /// lifecycle hook, which is invoked as a transition starts running. This hook can be useful to
  /// perform some asynchronous action before completing a transition. See [[TransitionHookFn]] for
  /// the signature of the function. The [[HookMatchCriteria]] is used to determine which Transitions
  /// the hook should be invoked for. To match all Transitions, use an empty criteria object `{}`. ###
  /// Lifecycle `onStart` hooks are invoked asynchronously when the Transition starts running. This
  /// happens after the `onBefore` phase is complete. At this point, the Transition has not yet exited
  /// nor entered any states. The registered `onStart` hooks are invoked in priority order. Note: A
  /// built-in `onStart` hook with high priority is used to fetch any eager resolve data. ### Return
  /// value The hook's return value can be used to pause, cancel, or redirect the current Transition.
  /// See [[HookResult]] for more information. ### Example #### Load feature shell data during
  /// transition This example pauses transitions into a reporting branch while an application-level
  /// feature shell loads. Use state `resolve` for route data and `policy.navigation` for security;
  /// use transition hooks for advanced orchestration that intentionally spans multiple states. ####
  /// Example: ```js $transitions.onStart({ to: 'reports.**' }, function(trans) { var reportsShell =
  /// trans.injector().get('ReportsShell'); return reportsShell.ensureLoaded(); }); ```
  ///
  /// Parameters:
  /// - [matchCriteria]: defines which Transitions the Hook should be invoked for.
  /// - [callback]: the hook function which will be injected and invoked.
  /// - [options]: Value supplied for the options parameter.
  Object? onStart(Object? matchCriteria, JSFunction callback,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(matchCriteria));
    args.add(unsafe.dartToJs(callback));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onStart'.toJS, args));
  }

  /// Registers a [[TransitionStateHookFn]], called when a specific state is entered. Registers a
  /// lifecycle hook, which is invoked (during a transition) when a specific state is being entered.
  /// Since this hook is run only when the specific state is being *entered*, it can be useful for
  /// performing tasks when entering a submodule/feature area such as initializing a stateful service,
  /// or for guarding access to a submodule/feature area. See [[TransitionStateHookFn]] for the
  /// signature of the function. The [[HookMatchCriteria]] is used to determine which Transitions the
  /// hook should be invoked for. `onEnter` hooks generally specify `{ entering: 'somestate' }`. To
  /// match all Transitions, use an empty criteria object `{}`. ### Lifecycle `onEnter` hooks are
  /// invoked when the Transition is entering a state. States are entered after the `onRetain` phase
  /// is complete. If more than one state is being entered, the parent state is entered first. The
  /// registered `onEnter` hooks for a state are invoked in priority order. Note: A built-in `onEnter`
  /// hook with high priority is used to fetch lazy resolve data for states being entered. ### Return
  /// value The hook's return value can be used to pause, cancel, or redirect the current Transition.
  /// See [[HookResult]] for more information. ### Inside a state declaration Instead of registering
  /// `onEnter` hooks using the [[TransitionService]], you may define an `onEnter` hook directly on a
  /// state declaration (see: [[StateDeclaration.onEnter]]). ### Examples #### Audit Log This example
  /// uses a service to log that a user has entered the admin section of an app. This assumes that
  /// there are substates of the "admin" state, such as "admin.users", "admin.pages", etc.
  ///
  /// Parameters:
  /// - [matchCriteria]: defines which Transitions the Hook should be invoked for.
  /// - [callback]: the hook function which will be injected and invoked.
  /// - [options]: Value supplied for the options parameter.
  Object? onEnter(Object? matchCriteria, JSFunction callback,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(matchCriteria));
    args.add(unsafe.dartToJs(callback));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onEnter'.toJS, args));
  }

  /// Registers a [[TransitionStateHookFn]], called when a specific state is retained/kept. Registers
  /// a lifecycle hook, which is invoked (during a transition) for a specific state that was
  /// previously active will remain active (is not being entered nor exited). This hook is invoked
  /// when a state is "retained" or "kept". It means the transition is coming *from* a substate of the
  /// retained state *to* a substate of the retained state. This hook can be used to perform actions
  /// when the user moves from one substate to another, such as between steps in a wizard. The
  /// [[HookMatchCriteria]] is used to determine which Transitions the hook should be invoked for.
  /// `onRetain` hooks generally specify `{ retained: 'somestate' }`. To match all Transitions, use an
  /// empty criteria object `{}`. ### Lifecycle `onRetain` hooks are invoked after any `onExit` hooks
  /// have been fired. If more than one state is retained, the child states' `onRetain` hooks are
  /// invoked first. The registered `onRetain` hooks for a state are invoked in priority order. ###
  /// Return value The hook's return value can be used to pause, cancel, or redirect the current
  /// Transition. See [[HookResult]] for more information. ### Inside a state declaration Instead of
  /// registering `onRetain` hooks using the [[TransitionService]], you may define an `onRetain` hook
  /// directly on a state declaration (see: [[StateDeclaration.onRetain]]).
  ///
  /// Parameters:
  /// - [matchCriteria]: defines which Transitions the Hook should be invoked for.
  /// - [callback]: the hook function which will be injected and invoked.
  /// - [options]: Value supplied for the options parameter.
  Object? onRetain(Object? matchCriteria, JSFunction callback,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(matchCriteria));
    args.add(unsafe.dartToJs(callback));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onRetain'.toJS, args));
  }

  /// Registers a [[TransitionStateHookFn]], called when a specific state is exited. Registers a
  /// lifecycle hook, which is invoked (during a transition) when a specific state is being exited.
  /// Since this hook is run only when the specific state is being *exited*, it can be useful for
  /// performing tasks when leaving a submodule/feature area such as cleaning up a stateful service,
  /// or for preventing the user from leaving a state or submodule until some criteria is satisfied.
  /// See [[TransitionStateHookFn]] for the signature of the function. The [[HookMatchCriteria]] is
  /// used to determine which Transitions the hook should be invoked for. `onExit` hooks generally
  /// specify `{ exiting: 'somestate' }`. To match all Transitions, use an empty criteria object `{}`.
  /// ### Lifecycle `onExit` hooks are invoked when the Transition is exiting a state. States are
  /// exited after any `onStart` phase is complete. If more than one state is being exited, the child
  /// states are exited first. The registered `onExit` hooks for a state are invoked in priority
  /// order. ### Return value The hook's return value can be used to pause, cancel, or redirect the
  /// current Transition. See [[HookResult]] for more information. ### Inside a state declaration
  /// Instead of registering `onExit` hooks using the [[TransitionService]], you may define an
  /// `onExit` hook directly on a state declaration (see: [[StateDeclaration.onExit]]).
  ///
  /// Parameters:
  /// - [matchCriteria]: defines which Transitions the Hook should be invoked for.
  /// - [callback]: the hook function which will be injected and invoked.
  /// - [options]: Value supplied for the options parameter.
  Object? onExit(Object? matchCriteria, JSFunction callback,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(matchCriteria));
    args.add(unsafe.dartToJs(callback));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('onExit'.toJS, args));
  }

  /// Registers a [[TransitionHookFn]], called *just before a transition finishes*. Registers a
  /// transition lifecycle hook, which is invoked just before a transition finishes. This hook is a
  /// last chance to cancel or redirect a transition. See [[TransitionHookFn]] for the signature of
  /// the function. The [[HookMatchCriteria]] is used to determine which Transitions the hook should
  /// be invoked for. To match all Transitions, use an empty criteria object `{}`. ### Lifecycle
  /// `onFinish` hooks are invoked after the `onEnter` phase is complete. These hooks are invoked just
  /// before the transition is "committed". Each hook is invoked in priority order. ### Return value
  /// The hook's return value can be used to pause, cancel, or redirect the current Transition. See
  /// [[HookResult]] for more information.
  ///
  /// Parameters:
  /// - [matchCriteria]: defines which Transitions the Hook should be invoked for.
  /// - [callback]: the hook function which will be injected and invoked.
  /// - [options]: Value supplied for the options parameter.
  Object? onFinish(Object? matchCriteria, JSFunction callback,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(matchCriteria));
    args.add(unsafe.dartToJs(callback));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onFinish'.toJS, args));
  }

  /// Registers a [[TransitionHookFn]], called after a successful transition completed. Registers a
  /// transition lifecycle hook, which is invoked after a transition successfully completes. See
  /// [[TransitionHookFn]] for the signature of the function. The [[HookMatchCriteria]] is used to
  /// determine which Transitions the hook should be invoked for. To match all Transitions, use an
  /// empty criteria object `{}`. ### Lifecycle `onSuccess` hooks are chained off the Transition's
  /// promise (see [[Transition.promise]]). If the Transition is successful and its promise is
  /// resolved, then the `onSuccess` hooks are invoked. Since these hooks are run after the transition
  /// is over, their return value is ignored. The `onSuccess` hooks are invoked in priority order. ###
  /// Return value Since the Transition is already completed, the hook's return value is ignored
  ///
  /// Parameters:
  /// - [matchCriteria]: defines which Transitions the Hook should be invoked for.
  /// - [callback]: the hook function which will be injected and invoked.
  /// - [options]: Value supplied for the options parameter.
  Object? onSuccess(Object? matchCriteria, JSFunction callback,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(matchCriteria));
    args.add(unsafe.dartToJs(callback));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onSuccess'.toJS, args));
  }

  /// Registers a [[TransitionHookFn]], called after a transition has errored. Registers a transition
  /// lifecycle hook, which is invoked after a transition has been rejected for any reason. See
  /// [[TransitionHookFn]] for the signature of the function. The [[HookMatchCriteria]] is used to
  /// determine which Transitions the hook should be invoked for. To match all Transitions, use an
  /// empty criteria object `{}`. ### Lifecycle The `onError` hooks are chained off the Transition's
  /// promise (see [[Transition.promise]]). If a Transition fails, its promise is rejected and the
  /// `onError` hooks are invoked. The `onError` hooks are invoked in priority order. Since these
  /// hooks are run after the transition is over, their return value is ignored. A transition "errors"
  /// if it was started, but failed to complete (for any reason). A *non-exhaustive list* of reasons a
  /// transition can error: - A transition was cancelled because a new transition started while it was
  /// still running (`Transition superseded`) - A transition was cancelled by a Transition Hook
  /// returning false - A transition was redirected by a Transition Hook returning a [[TargetState]] -
  /// A Transition Hook or resolve function threw an error - A Transition Hook returned a rejected
  /// promise - A resolve function returned a rejected promise To check the failure reason, inspect
  /// the return value of [[Transition.error]]. Note: `onError` should be used for targeted error
  /// handling, or error recovery. For catch-all error reporting, configure `$router.error` or
  /// `$exceptionHandler`. ### Return value Since the Transition is already completed, the hook's
  /// return value is ignored
  ///
  /// Parameters:
  /// - [matchCriteria]: defines which Transitions the Hook should be invoked for.
  /// - [callback]: the hook function which will be injected and invoked.
  /// - [options]: Value supplied for the options parameter.
  Object? onError(Object? matchCriteria, JSFunction callback,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(matchCriteria));
    args.add(unsafe.dartToJs(callback));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onError'.toJS, args));
  }
}

/// Public AngularTS Validator contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.Validator`.
base class GeneratedNgValidator extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Validator.
  const GeneratedNgValidator(super.raw);

  /// Calls the ng.Validator function.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  bool call(Object? value) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(value));

    return _toDartBool(_callFunction(raw as JSFunction, args))!;
  }
}

/// Active connection between one AngularTS target and a WebAssembly guest.
///
/// Raw JavaScript facade for `ng.WasmBinding`.
base class GeneratedNgWasmBinding extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmBinding.
  const GeneratedNgWasmBinding(super.raw);

  /// The name member of ng.WasmBinding.
  String get rawName => _toDartString(raw.getProperty('name'.toJS))!;

  /// The target member of ng.WasmBinding.
  Object? get rawTarget =>
      unsafe.jsToDart<Object?>(raw.getProperty('target'.toJS));

  /// The disposed member of ng.WasmBinding.
  bool get rawDisposed => _toDartBool(raw.getProperty('disposed'.toJS))!;

  /// The dispose member of ng.WasmBinding.
  void rawDispose() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('dispose'.toJS, const <JSAny?>[]));
  }
}

/// Options for binding one reactive target to a WebAssembly guest.
///
/// Raw JavaScript facade for `ng.WasmBindingOptions`.
base class GeneratedNgWasmBindingOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmBindingOptions.
  const GeneratedNgWasmBindingOptions(super.raw);

  /// Stable name exposed to the guest.
  String? get name => _toDartString(raw.getProperty('name'.toJS));

  /// Stable name exposed to the guest.
  set name(String? value) {
    raw.setProperty('name'.toJS, unsafe.dartToJs(value));
  }

  /// Reactive paths delivered to the guest's update callback.
  Object? get watch => unsafe.jsToDart<Object?>(raw.getProperty('watch'.toJS));

  /// Deliver each watched path's current value when binding. Defaults to `true`.
  bool? get initial => _toDartBool(raw.getProperty('initial'.toJS));

  /// Deliver each watched path's current value when binding. Defaults to `true`.
  set initial(bool? value) {
    raw.setProperty('initial'.toJS, unsafe.dartToJs(value));
  }
}

/// Standard WebAssembly compilation options forwarded without translation.
///
/// Raw JavaScript facade for `ng.WasmCompileOptions`.
base class GeneratedNgWasmCompileOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmCompileOptions.
  const GeneratedNgWasmCompileOptions(super.raw);

  /// Native WebAssembly builtin modules enabled while compiling.
  Object? get builtins =>
      unsafe.jsToDart<Object?>(raw.getProperty('builtins'.toJS));

  /// Native module name used for imported global string constants.
  String? get importedStringConstants =>
      _toDartString(raw.getProperty('importedStringConstants'.toJS));

  /// Native module name used for imported global string constants.
  set importedStringConstants(String? value) {
    raw.setProperty('importedStringConstants'.toJS, unsafe.dartToJs(value));
  }
}

/// Structured error raised by the high-level WebAssembly host.
///
/// Raw JavaScript facade for `ng.WasmError`.
base class GeneratedNgWasmError extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmError.
  const GeneratedNgWasmError(super.raw);

  /// The code member of ng.WasmError.
  String get rawCode => _toDartString(raw.getProperty('code'.toJS))!;

  /// The source member of ng.WasmError.
  Object? get rawSource =>
      unsafe.jsToDart<Object?>(raw.getProperty('source'.toJS));

  /// The stage member of ng.WasmError.
  String? get stage => _toDartString(raw.getProperty('stage'.toJS));
}

/// Error categories reported by the high-level WebAssembly host.
///
/// Raw JavaScript facade for `ng.WasmErrorCode`.
base class GeneratedNgWasmErrorCode extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmErrorCode.
  const GeneratedNgWasmErrorCode(super.raw);
}

/// Lifecycle stage at which a WebAssembly operation failed.
///
/// Raw JavaScript facade for `ng.WasmErrorStage`.
base class GeneratedNgWasmErrorStage extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmErrorStage.
  const GeneratedNgWasmErrorStage(super.raw);
}

/// Declarative options for loading one WebAssembly module.
///
/// Raw JavaScript facade for `ng.WasmLoadOptions`.
base class GeneratedNgWasmLoadOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmLoadOptions.
  const GeneratedNgWasmLoadOptions(super.raw);

  /// URL, request, response, bytes, or compiled WebAssembly module.
  Object? get source =>
      unsafe.jsToDart<Object?>(raw.getProperty('source'.toJS));

  /// Imports supplied in addition to the AngularTS reactive ABI.
  Object? get imports =>
      unsafe.jsToDart<Object?>(raw.getProperty('imports'.toJS));

  /// Standard options forwarded to WebAssembly compilation.
  Object? get compile =>
      unsafe.jsToDart<Object?>(raw.getProperty('compile'.toJS));

  /// Publish lifecycle timing entries through the browser Performance API.
  bool? get diagnostics => _toDartBool(raw.getProperty('diagnostics'.toJS));

  /// Publish lifecycle timing entries through the browser Performance API.
  set diagnostics(bool? value) {
    raw.setProperty('diagnostics'.toJS, unsafe.dartToJs(value));
  }
}

/// Loaded WebAssembly module with owned reactive bindings and lifecycle.
///
/// Raw JavaScript facade for `ng.WasmResource`.
base class GeneratedNgWasmResource extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmResource.
  const GeneratedNgWasmResource(super.raw);

  /// The source member of ng.WasmResource.
  Object? get rawSource =>
      unsafe.jsToDart<Object?>(raw.getProperty('source'.toJS));

  /// The status member of ng.WasmResource.
  String get rawStatus => _toDartString(raw.getProperty('status'.toJS))!;

  /// The ready member of ng.WasmResource.
  JSPromise<JSAny?> get rawReady =>
      raw.getProperty('ready'.toJS) as JSPromise<JSAny?>;

  /// The error member of ng.WasmResource.
  Object? get rawError =>
      unsafe.jsToDart<Object?>(raw.getProperty('error'.toJS));

  /// The instance member of ng.WasmResource.
  Object? get rawInstance =>
      unsafe.jsToDart<Object?>(raw.getProperty('instance'.toJS));

  /// The module member of ng.WasmResource.
  Object? get rawModule =>
      unsafe.jsToDart<Object?>(raw.getProperty('module'.toJS));

  /// The exports member of ng.WasmResource.
  Object? get rawExports =>
      unsafe.jsToDart<Object?>(raw.getProperty('exports'.toJS));

  /// The disposed member of ng.WasmResource.
  bool get rawDisposed => _toDartBool(raw.getProperty('disposed'.toJS))!;

  /// The bind member of ng.WasmResource.
  ///
  /// Parameters:
  /// - [target]: Value supplied for the target parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> rawBind(Object? target,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('bind'.toJS, args) as JSPromise<JSAny?>;
  }

  /// The dispose member of ng.WasmResource.
  void rawDispose() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('dispose'.toJS, const <JSAny?>[]));
  }
}

/// Lifecycle state of a WebAssembly resource.
///
/// Raw JavaScript facade for `ng.WasmResourceStatus`.
base class GeneratedNgWasmResourceStatus extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmResourceStatus.
  const GeneratedNgWasmResourceStatus(super.raw);
}

/// High-level WebAssembly host service.
///
/// Raw JavaScript facade for `ng.WasmService`.
base class GeneratedNgWasmService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmService.
  const GeneratedNgWasmService(super.raw);

  /// Loads one module and returns its owned resource.
  ///
  /// Parameters:
  /// - [options]: Value supplied for the options parameter.
  Object? rawLoad(Object? options) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(options));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('load'.toJS, args));
  }
}

/// Source accepted by the WebAssembly loader.
///
/// Raw JavaScript facade for `ng.WasmSource`.
base class GeneratedNgWasmSource extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmSource.
  const GeneratedNgWasmSource(super.raw);
}

/// Scope class for the Proxy. It intercepts operations like property access (get) and property
/// setting (set), and adds support for deep change tracking and observer-like behavior.
///
/// Raw JavaScript facade for `ng.WasmTarget`.
base class GeneratedNgWasmTarget extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WasmTarget.
  const GeneratedNgWasmTarget(super.raw);

  /// The id member of ng.WasmTarget.
  num get id => _toDartNumber(raw.getProperty('id'.toJS))!;

  /// The id member of ng.WasmTarget.
  set id(num value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The root member of ng.WasmTarget.
  Object? get root => unsafe.jsToDart<Object?>(raw.getProperty('root'.toJS));

  /// The parent member of ng.WasmTarget.
  Object? get parent =>
      unsafe.jsToDart<Object?>(raw.getProperty('parent'.toJS));

  /// The scopeName member of ng.WasmTarget.
  String? get scopeName => _toDartString(raw.getProperty('scopeName'.toJS));

  /// The scopeName member of ng.WasmTarget.
  set scopeName(String? value) {
    raw.setProperty('scopeName'.toJS, unsafe.dartToJs(value));
  }

  /// Intercepts and handles property assignments on the target object. Scopeable objects are stored
  /// as raw model values and proxied lazily when read.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being set.
  /// - [value]: The new value being assigned to the property.
  /// - [proxy]: The proxy intercepting property access.
  bool setMember(
      Object? target, String property, Object? value, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(value));
    args.add(unsafe.dartToJs(proxy));

    return _toDartBool(raw.callMethodVarArgs('set'.toJS, args))!;
  }

  /// Intercepts property access on the target object. It checks for specific properties (`watch` and
  /// `sync`) and binds their methods. For other properties, it returns the value directly.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being accessed.
  /// - [proxy]: The proxy object being invoked.
  Object? getMember(Object? target, Object? property, Scope<Object?> proxy) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));
    args.add(unsafe.dartToJs(proxy));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('get'.toJS, args));
  }

  /// The deleteProperty member of ng.WasmTarget.
  ///
  /// Parameters:
  /// - [target]: The target object.
  /// - [property]: The name of the property being deleted.
  bool deleteProperty(Object? target, Object? property) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(target));
    args.add(unsafe.dartToJs(property));

    return _toDartBool(raw.callMethodVarArgs('deleteProperty'.toJS, args))!;
  }

  /// Runs synchronous scope mutations as one batch. Listener notifications are queued while the
  /// callback runs and flushed once after the outermost batch exits. Mutations are not rolled back if
  /// the callback throws.
  ///
  /// Parameters:
  /// - [fn]: Value supplied for the fn parameter.
  Object? batch(JSFunction fn) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(fn));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('batch'.toJS, args));
  }

  /// Registers a watcher for a property along with a listener function. The listener function is
  /// invoked when changes to that property are detected.
  ///
  /// Parameters:
  /// - [watchProp]: An expression to be watched in the context of this model.
  /// - [listenerFn]: A function to execute when changes are detected on watched context.
  /// - [lazy]: A flag to indicate if the listener should be invoked immediately. Defaults to false.
  /// - [directLeaf]: Value supplied for the directLeaf parameter.
  Object? watch(String watchProp,
      [Object? listenerFn = _undefinedArgument,
      Object? lazy = _undefinedArgument,
      Object? directLeaf = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(watchProp));
    if (!identical(listenerFn, _undefinedArgument)) {
      args.add(unsafe.dartToJs(listenerFn));
    }
    if (!identical(lazy, _undefinedArgument)) {
      args.add(unsafe.dartToJs(lazy));
    }
    if (!identical(directLeaf, _undefinedArgument)) {
      args.add(unsafe.dartToJs(directLeaf));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('watch'.toJS, args));
  }

  /// Creates a prototypically inherited child scope.
  ///
  /// Parameters:
  /// - [childInstance]: Value supplied for the childInstance parameter.
  GeneratedNgWasmTarget newMember(
      [Object? childInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(childInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(childInstance));
    }

    raw.callMethodVarArgs('new'.toJS, args);
    return this;
  }

  /// Creates an isolate child scope that does not inherit watchable properties directly.
  ///
  /// Parameters:
  /// - [instance]: Value supplied for the instance parameter.
  GeneratedNgWasmTarget newIsolate([Object? instance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(instance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(instance));
    }

    raw.callMethodVarArgs('newIsolate'.toJS, args);
    return this;
  }

  /// Creates a transcluded child scope linked to this scope and an optional parent instance.
  ///
  /// Parameters:
  /// - [parentInstance]: Value supplied for the parentInstance parameter.
  GeneratedNgWasmTarget transcluded(
      [Object? parentInstance = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(parentInstance, _undefinedArgument)) {
      args.add(unsafe.dartToJs(parentInstance));
    }

    raw.callMethodVarArgs('transcluded'.toJS, args);
    return this;
  }

  /// Merges enumerable properties from the provided object into the current scope target.
  ///
  /// Parameters:
  /// - [newTarget]: Value supplied for the newTarget parameter.
  void merge(Object? newTarget) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(newTarget));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('merge'.toJS, args));
  }

  /// Registers an event listener on this scope and returns a deregistration function.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [listener]: Value supplied for the listener parameter.
  Object? onMember(String name, JSFunction listener) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(listener));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('on'.toJS, args));
  }

  /// Emits an event upward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? emit(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('emit'.toJS, args));
  }

  /// Broadcasts an event downward through the scope hierarchy.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [args1]: Value supplied for the args parameter.
  /// - [args2]: Value supplied for the args parameter.
  /// - [args3]: Value supplied for the args parameter.
  /// - [args4]: Value supplied for the args parameter.
  Object? broadcast(String name,
      [Object? args1 = _undefinedArgument,
      Object? args2 = _undefinedArgument,
      Object? args3 = _undefinedArgument,
      Object? args4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    if (!identical(args1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args1));
    }
    if (!identical(args2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args2));
    }
    if (!identical(args3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args3));
    }
    if (!identical(args4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(args4));
    }

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('broadcast'.toJS, args));
  }

  /// Destroys this scope and its descendants.
  void destroy() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('destroy'.toJS, const <JSAny?>[]));
  }

  /// Searches this scope tree for a scope with the given id.
  ///
  /// Parameters:
  /// - [id]: Value supplied for the id parameter.
  Object? getById(Object? id) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('getById'.toJS, args));
  }

  /// Searches the scope tree for a scope registered under the provided name.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  Object? searchByName(String name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('searchByName'.toJS, args));
  }
}

/// Application-wide defaults for scoped custom elements.
///
/// Raw JavaScript facade for `ng.WebComponentConfig`.
base class GeneratedNgWebComponentConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebComponentConfig.
  const GeneratedNgWebComponentConfig(super.raw);

  /// Defaults merged into every `appComponent(...)` declaration.
  Object? get defaults =>
      unsafe.jsToDart<Object?>(raw.getProperty('defaults'.toJS));
}

/// Public AngularTS WebComponentContext contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WebComponentContext`.
base class GeneratedNgWebComponentContext extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebComponentContext.
  const GeneratedNgWebComponentContext(super.raw);

  /// Custom element host.
  HTMLElement get host =>
      unsafe.jsToDart<HTMLElement>(raw.getProperty('host'.toJS));

  /// Scope owned by the custom element.
  Object? get scope => unsafe.jsToDart<Object?>(raw.getProperty('scope'.toJS));

  /// Injector used by the AngularTS app that registered the element.
  Object? get injector =>
      unsafe.jsToDart<Object?>(raw.getProperty('injector'.toJS));

  /// Render root used for template content.
  Object? get root => unsafe.jsToDart<Object?>(raw.getProperty('root'.toJS));

  /// Shadow root when `shadow` is enabled.
  Object? get shadowRoot =>
      unsafe.jsToDart<Object?>(raw.getProperty('shadowRoot'.toJS));

  /// Dispatch a composed bubbling DOM event from the host.
  ///
  /// Parameters:
  /// - [typeValue]: Value supplied for the type parameter.
  /// - [detail]: Value supplied for the detail parameter.
  /// - [init]: Value supplied for the init parameter.
  bool dispatch(String typeValue,
      [Object? detail = _undefinedArgument,
      Object? init = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(typeValue));
    if (!identical(detail, _undefinedArgument)) {
      args.add(unsafe.dartToJs(detail));
    }
    if (!identical(init, _undefinedArgument)) {
      args.add(unsafe.dartToJs(init));
    }

    return _toDartBool(raw.callMethodVarArgs('dispatch'.toJS, args))!;
  }
}

/// Public AngularTS WebComponentInput contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WebComponentInput`.
base class GeneratedNgWebComponentInput extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebComponentInput.
  const GeneratedNgWebComponentInput(super.raw);

  /// Calls the ng.WebComponentInput function.
  ///
  /// Parameters:
  /// - [value]: Value supplied for the value parameter.
  String call([Object? value = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(value, _undefinedArgument)) {
      args.add(unsafe.dartToJs(value));
    }

    return _toDartString(_callFunction(raw as JSFunction, args))!;
  }
}

/// Public AngularTS WebComponentInputConfig contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WebComponentInputConfig`.
base class GeneratedNgWebComponentInputConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebComponentInputConfig.
  const GeneratedNgWebComponentInputConfig(super.raw);

  /// Attribute name. Defaults to the kebab-case property name.
  String? get attribute => _toDartString(raw.getProperty('attribute'.toJS));

  /// Attribute name. Defaults to the kebab-case property name.
  set attribute(String? value) {
    raw.setProperty('attribute'.toJS, unsafe.dartToJs(value));
  }

  /// Attribute/property coercion function. Defaults to `String`.
  Object? get typeMember =>
      unsafe.jsToDart<Object?>(raw.getProperty('type'.toJS));

  /// Reflect property writes back to the DOM attribute.
  bool? get reflect => _toDartBool(raw.getProperty('reflect'.toJS));

  /// Reflect property writes back to the DOM attribute.
  set reflect(bool? value) {
    raw.setProperty('reflect'.toJS, unsafe.dartToJs(value));
  }

  /// Initial value used when no attribute or property was provided.
  Object? get defaultMember =>
      unsafe.jsToDart<Object?>(raw.getProperty('default'.toJS));
}

/// Public AngularTS WebComponentService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WebComponentService`.
base class GeneratedNgWebComponentService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebComponentService.
  const GeneratedNgWebComponentService(super.raw);

  /// Define an options-backed application host custom element.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [options]: Value supplied for the options parameter.
  CustomElementConstructor defineAppComponent(String name, Object? options) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(options));

    return unsafe.jsToDart<CustomElementConstructor>(
        raw.callMethodVarArgs('defineAppComponent'.toJS, args));
  }

  /// Define a native custom element backed by an AngularTS child scope.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  /// - [elementClass]: Value supplied for the elementClass parameter.
  CustomElementConstructor defineElement(String name, Object? elementClass) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));
    args.add(unsafe.dartToJs(elementClass));

    return unsafe.jsToDart<CustomElementConstructor>(
        raw.callMethodVarArgs('defineElement'.toJS, args));
  }

  /// Create and attach a normal AngularTS child scope for a custom element.
  ///
  /// Parameters:
  /// - [host]: Value supplied for the host parameter.
  /// - [initialState]: Value supplied for the initialState parameter.
  /// - [options]: Value supplied for the options parameter.
  Scope<TState> createElementScope<TState>(HTMLElement host,
      [Object? initialState = _undefinedArgument,
      Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(host));
    if (!identical(initialState, _undefinedArgument)) {
      args.add(unsafe.dartToJs(initialState));
    }
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return Scope<TState>.unsafe(
        raw.callMethodVarArgs('createElementScope'.toJS, args));
  }
}

/// WebSocket-specific configuration
///
/// Raw JavaScript facade for `ng.WebSocketConfig`.
base class GeneratedNgWebSocketConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebSocketConfig.
  const GeneratedNgWebSocketConfig(super.raw);

  /// Optional WebSocket subprotocols
  Object? get protocols =>
      unsafe.jsToDart<Object?>(raw.getProperty('protocols'.toJS));

  /// Called when a decoded message uses the realtime protocol shape.
  Object? get onProtocolMessage =>
      unsafe.jsToDart<Object?>(raw.getProperty('onProtocolMessage'.toJS));

  /// Called when the connection opens
  Object? get onOpen =>
      unsafe.jsToDart<Object?>(raw.getProperty('onOpen'.toJS));

  /// Called when a message is received
  Object? get onMessage =>
      unsafe.jsToDart<Object?>(raw.getProperty('onMessage'.toJS));

  /// Called with every registered connection message, including custom SSE event types
  Object? get onEvent =>
      unsafe.jsToDart<Object?>(raw.getProperty('onEvent'.toJS));

  /// Called when an error occurs
  Object? get onError =>
      unsafe.jsToDart<Object?>(raw.getProperty('onError'.toJS));

  /// Called when a WebSocket connection closes
  Object? get onClose =>
      unsafe.jsToDart<Object?>(raw.getProperty('onClose'.toJS));

  /// Called when a reconnect attempt happens
  Object? get onReconnect =>
      unsafe.jsToDart<Object?>(raw.getProperty('onReconnect'.toJS));

  /// Delay between reconnect attempts in milliseconds
  num? get retryDelay => _toDartNumber(raw.getProperty('retryDelay'.toJS));

  /// Delay between reconnect attempts in milliseconds
  set retryDelay(num? value) {
    raw.setProperty('retryDelay'.toJS, unsafe.dartToJs(value));
  }

  /// Maximum number of reconnect attempts
  num? get maxRetries => _toDartNumber(raw.getProperty('maxRetries'.toJS));

  /// Maximum number of reconnect attempts
  set maxRetries(num? value) {
    raw.setProperty('maxRetries'.toJS, unsafe.dartToJs(value));
  }

  /// Timeout in milliseconds to detect heartbeat inactivity
  num? get heartbeatTimeout =>
      _toDartNumber(raw.getProperty('heartbeatTimeout'.toJS));

  /// Timeout in milliseconds to detect heartbeat inactivity
  set heartbeatTimeout(num? value) {
    raw.setProperty('heartbeatTimeout'.toJS, unsafe.dartToJs(value));
  }

  /// Function to transform incoming messages
  Object? get transformMessage =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformMessage'.toJS));

  /// Additional EventSource event names to subscribe to
  Object? get eventTypes =>
      unsafe.jsToDart<Object?>(raw.getProperty('eventTypes'.toJS));
}

/// Managed WebSocket connection returned by $websocket.
///
/// Raw JavaScript facade for `ng.WebSocketConnection`.
base class GeneratedNgWebSocketConnection extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebSocketConnection.
  const GeneratedNgWebSocketConnection(super.raw);

  /// Manually restart the WebSocket connection.
  void reconnect() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('reconnect'.toJS, const <JSAny?>[]));
  }

  /// Send a JSON-serialized message through the native WebSocket.
  ///
  /// Parameters:
  /// - [data]: Value supplied for the data parameter.
  void send(Object? data) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(data));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('send'.toJS, args));
  }

  /// Close the WebSocket connection and stop reconnect attempts.
  void close() {
    unsafe
        .jsToDart<void>(raw.callMethodVarArgs('close'.toJS, const <JSAny?>[]));
  }
}

/// Public AngularTS WebSocketService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WebSocketService`.
base class GeneratedNgWebSocketService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebSocketService.
  const GeneratedNgWebSocketService(super.raw);

  /// Calls the ng.WebSocketService function.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [config]: Value supplied for the config parameter.
  Object? call(String url, [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS WebTransportBufferInput contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WebTransportBufferInput`.
base class GeneratedNgWebTransportBufferInput extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebTransportBufferInput.
  const GeneratedNgWebTransportBufferInput(super.raw);
}

/// Options passed to `$webTransport`.
///
/// Raw JavaScript facade for `ng.WebTransportConfig`.
base class GeneratedNgWebTransportConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebTransportConfig.
  const GeneratedNgWebTransportConfig(super.raw);

  /// Called whenever the current native transport resolves `ready`.
  Object? get onOpen =>
      unsafe.jsToDart<Object?>(raw.getProperty('onOpen'.toJS));

  /// Called when the managed connection closes without another reconnect.
  Object? get onClose =>
      unsafe.jsToDart<Object?>(raw.getProperty('onClose'.toJS));

  /// Called when opening, reading, writing, or closing fails.
  Object? get onError =>
      unsafe.jsToDart<Object?>(raw.getProperty('onError'.toJS));

  /// Called with each incoming datagram.
  Object? get onDatagram =>
      unsafe.jsToDart<Object?>(raw.getProperty('onDatagram'.toJS));

  /// Called when a decoded datagram uses the realtime protocol shape.
  Object? get onProtocolMessage =>
      unsafe.jsToDart<Object?>(raw.getProperty('onProtocolMessage'.toJS));

  /// Converts incoming datagram bytes into the value passed as `event.message`.
  Object? get transformDatagram =>
      unsafe.jsToDart<Object?>(raw.getProperty('transformDatagram'.toJS));

  /// Reopen the native WebTransport session when it closes unexpectedly.
  bool? get reconnect => _toDartBool(raw.getProperty('reconnect'.toJS));

  /// Reopen the native WebTransport session when it closes unexpectedly.
  set reconnect(bool? value) {
    raw.setProperty('reconnect'.toJS, unsafe.dartToJs(value));
  }

  /// Delay before each reconnect attempt. Defaults to 1000ms.
  Object? get retryDelay =>
      unsafe.jsToDart<Object?>(raw.getProperty('retryDelay'.toJS));

  /// Maximum reconnect attempts before `closed` settles. Defaults to no limit.
  num? get maxRetries => _toDartNumber(raw.getProperty('maxRetries'.toJS));

  /// Maximum reconnect attempts before `closed` settles. Defaults to no limit.
  set maxRetries(num? value) {
    raw.setProperty('maxRetries'.toJS, unsafe.dartToJs(value));
  }

  /// Called after a replacement session is ready so callers can renegotiate state.
  Object? get onReconnect =>
      unsafe.jsToDart<Object?>(raw.getProperty('onReconnect'.toJS));
}

/// Managed WebTransport connection returned by `$webTransport`. The connection wraps the
/// browser-native `WebTransport` object and keeps its promise/stream model visible while adding
/// small conveniences for sending bytes, text, datagrams, and unidirectional streams.
///
/// Raw JavaScript facade for `ng.WebTransportConnection`.
base class GeneratedNgWebTransportConnection extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebTransportConnection.
  const GeneratedNgWebTransportConnection(super.raw);

  /// Resolves after the current native WebTransport session is ready.
  JSPromise<JSAny?> get ready =>
      raw.getProperty('ready'.toJS) as JSPromise<JSAny?>;

  /// Resolves or rejects when the managed connection closes permanently.
  JSPromise<JSAny?> get closed =>
      raw.getProperty('closed'.toJS) as JSPromise<JSAny?>;

  /// Current browser-native WebTransport instance. Replaced after reconnects.
  Object? get transport =>
      unsafe.jsToDart<Object?>(raw.getProperty('transport'.toJS));

  /// Send one unreliable datagram.
  ///
  /// Parameters:
  /// - [data]: Value supplied for the data parameter.
  JSPromise<JSAny?> sendDatagram(Object? data) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(data));

    return raw.callMethodVarArgs('sendDatagram'.toJS, args)
        as JSPromise<JSAny?>;
  }

  /// Send UTF-8 text as one unreliable datagram.
  ///
  /// Parameters:
  /// - [data]: Value supplied for the data parameter.
  JSPromise<JSAny?> sendText(String data) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(data));

    return raw.callMethodVarArgs('sendText'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Send data on a client-opened reliable unidirectional stream.
  ///
  /// Parameters:
  /// - [data]: Value supplied for the data parameter.
  JSPromise<JSAny?> sendStream(Object? data) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(data));

    return raw.callMethodVarArgs('sendStream'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Open a reliable bidirectional stream.
  JSPromise<JSAny?> createBidirectionalStream() {
    return raw.callMethodVarArgs(
            'createBidirectionalStream'.toJS, const <JSAny?>[])
        as JSPromise<JSAny?>;
  }

  /// Close the WebTransport session.
  ///
  /// Parameters:
  /// - [closeInfo]: Value supplied for the closeInfo parameter.
  void close([Object? closeInfo = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(closeInfo, _undefinedArgument)) {
      args.add(unsafe.dartToJs(closeInfo));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('close'.toJS, args));
  }
}

/// Event emitted for each incoming WebTransport datagram.
///
/// Raw JavaScript facade for `ng.WebTransportDatagramEvent`.
base class GeneratedNgWebTransportDatagramEvent extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebTransportDatagramEvent.
  const GeneratedNgWebTransportDatagramEvent(super.raw);

  /// Raw bytes received from the browser WebTransport datagram stream.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// Value after `transformDatagram`, or the raw bytes when no transform is configured.
  Object? get message =>
      unsafe.jsToDart<Object?>(raw.getProperty('message'.toJS));
}

/// Event passed to WebTransport reconnect and renegotiation hooks.
///
/// Raw JavaScript facade for `ng.WebTransportReconnectEvent`.
base class GeneratedNgWebTransportReconnectEvent extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebTransportReconnectEvent.
  const GeneratedNgWebTransportReconnectEvent(super.raw);

  /// Stable managed connection whose native `transport` was reopened.
  Object? get connection =>
      unsafe.jsToDart<Object?>(raw.getProperty('connection'.toJS));

  /// One-based reconnect attempt count for this connection.
  num get attempt => _toDartNumber(raw.getProperty('attempt'.toJS))!;

  /// One-based reconnect attempt count for this connection.
  set attempt(num value) {
    raw.setProperty('attempt'.toJS, unsafe.dartToJs(value));
  }

  /// Error or close reason that caused the reconnect attempt, when available.
  Object? get error => unsafe.jsToDart<Object?>(raw.getProperty('error'.toJS));

  /// URL used to open the replacement WebTransport session.
  String get url => _toDartString(raw.getProperty('url'.toJS))!;

  /// URL used to open the replacement WebTransport session.
  set url(String value) {
    raw.setProperty('url'.toJS, unsafe.dartToJs(value));
  }
}

/// Delay, in milliseconds, before a reconnect attempt is opened.
///
/// Raw JavaScript facade for `ng.WebTransportRetryDelay`.
base class GeneratedNgWebTransportRetryDelay extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebTransportRetryDelay.
  const GeneratedNgWebTransportRetryDelay(super.raw);

  /// Calls the ng.WebTransportRetryDelay function.
  ///
  /// Parameters:
  /// - [attempt]: Value supplied for the attempt parameter.
  /// - [error]: Value supplied for the error parameter.
  num call(num attempt, [Object? error = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(attempt));
    if (!identical(error, _undefinedArgument)) {
      args.add(unsafe.dartToJs(error));
    }

    return _toDartNumber(_callFunction(raw as JSFunction, args))!;
  }
}

/// Factory function exposed as `$webTransport`.
///
/// Raw JavaScript facade for `ng.WebTransportService`.
base class GeneratedNgWebTransportService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WebTransportService.
  const GeneratedNgWebTransportService(super.raw);

  /// Calls the ng.WebTransportService function.
  ///
  /// Parameters:
  /// - [url]: Value supplied for the url parameter.
  /// - [config]: Value supplied for the config parameter.
  Object? call(String url, [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(url));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// The **`Window`** interface represents a window containing a DOM document; the `document`
/// property points to the DOM document loaded in that window. [MDN
/// Reference](https://developer.mozilla.org/docs/Web/API/Window)
///
/// Raw JavaScript facade for `ng.WindowService`.
base class GeneratedNgWindowService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WindowService.
  const GeneratedNgWindowService(super.raw);

  /// The angular member of ng.WindowService.
  Object? get angular =>
      unsafe.jsToDart<Object?>(raw.getProperty('angular'.toJS));
}

/// Native worker options plus AngularTS decoding and restart policy.
///
/// Raw JavaScript facade for `ng.WorkerConfig`.
base class GeneratedNgWorkerConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerConfig.
  const GeneratedNgWorkerConfig(super.raw);

  /// Decode an inbound native message before delivering it to subscribers.
  Object? get decode =>
      unsafe.jsToDart<Object?>(raw.getProperty('decode'.toJS));

  /// Restart the worker after a native runtime error. Automatic restart is disabled by default.
  bool? get restart => _toDartBool(raw.getProperty('restart'.toJS));

  /// Restart the worker after a native runtime error. Automatic restart is disabled by default.
  set restart(bool? value) {
    raw.setProperty('restart'.toJS, unsafe.dartToJs(value));
  }

  /// Base restart delay. Exponential backoff is capped at 30s.
  num? get restartDelay => _toDartNumber(raw.getProperty('restartDelay'.toJS));

  /// Base restart delay. Exponential backoff is capped at 30s.
  set restartDelay(num? value) {
    raw.setProperty('restartDelay'.toJS, unsafe.dartToJs(value));
  }

  /// Maximum automatic restarts. Defaults to 3.
  num? get maxRestarts => _toDartNumber(raw.getProperty('maxRestarts'.toJS));

  /// Maximum automatic restarts. Defaults to 3.
  set maxRestarts(num? value) {
    raw.setProperty('maxRestarts'.toJS, unsafe.dartToJs(value));
  }
}

/// Typed failure reported by a managed worker.
///
/// Raw JavaScript facade for `ng.WorkerError`.
base class GeneratedNgWorkerError extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerError.
  const GeneratedNgWorkerError(super.raw);

  /// The code member of ng.WorkerError.
  String get code => _toDartString(raw.getProperty('code'.toJS))!;

  /// The event member of ng.WorkerError.
  Object? get event => unsafe.jsToDart<Object?>(raw.getProperty('event'.toJS));
}

/// Public AngularTS WorkerErrorCode contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkerErrorCode`.
base class GeneratedNgWorkerErrorCode extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerErrorCode.
  const GeneratedNgWorkerErrorCode(super.raw);
}

/// Managed, scope-bindable handle to one page-owned Web Worker.
///
/// Raw JavaScript facade for `ng.WorkerHandle`.
base class GeneratedNgWorkerHandle extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerHandle.
  const GeneratedNgWorkerHandle(super.raw);

  /// Current managed lifecycle state.
  String get status => _toDartString(raw.getProperty('status'.toJS))!;

  /// Latest managed failure, retained across worker replacement.
  Object? get error => unsafe.jsToDart<Object?>(raw.getProperty('error'.toJS));

  /// Number of explicit or automatic worker restarts.
  num get restartCount => _toDartNumber(raw.getProperty('restartCount'.toJS))!;

  /// Send a typed message and optional transferable ownership list.
  ///
  /// Parameters:
  /// - [message]: Value supplied for the message parameter.
  /// - [transfer]: Value supplied for the transfer parameter.
  void post(Object? message, [Object? transfer = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(message));
    if (!identical(transfer, _undefinedArgument)) {
      args.add(unsafe.dartToJs(transfer));
    }

    unsafe.jsToDart<void>(raw.callMethodVarArgs('post'.toJS, args));
  }

  /// Send a correlated request using the AngularTS worker envelope.
  ///
  /// Parameters:
  /// - [message]: Value supplied for the message parameter.
  /// - [options]: Value supplied for the options parameter.
  JSPromise<JSAny?> request(Object? message,
      [Object? options = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(message));
    if (!identical(options, _undefinedArgument)) {
      args.add(unsafe.dartToJs(options));
    }

    return raw.callMethodVarArgs('request'.toJS, args) as JSPromise<JSAny?>;
  }

  /// Adapt this handle to the standard model synchronization contract.
  ///
  /// Parameters:
  /// - [channel]: Value supplied for the channel parameter.
  Object? model([Object? channel = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(channel, _undefinedArgument)) {
      args.add(unsafe.dartToJs(channel));
    }

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('model'.toJS, args));
  }

  /// Subscribe to decoded worker messages.
  ///
  /// Parameters:
  /// - [listener]: Value supplied for the listener parameter.
  Object? onMessage(JSFunction listener) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(listener));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onMessage'.toJS, args));
  }

  /// Subscribe to runtime, message, decoding, and request failures.
  ///
  /// Parameters:
  /// - [listener]: Value supplied for the listener parameter.
  Object? onError(JSFunction listener) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(listener));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('onError'.toJS, args));
  }

  /// Permanently terminate this managed connection.
  void terminate() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('terminate'.toJS, const <JSAny?>[]));
  }

  /// Replace the native worker unless this connection was terminated.
  void restart() {
    unsafe.jsToDart<void>(
        raw.callMethodVarArgs('restart'.toJS, const <JSAny?>[]));
  }
}

/// Standard messages used by WorkerHandle.model.
///
/// Raw JavaScript facade for `ng.WorkerModelMessage`.
base class GeneratedNgWorkerModelMessage extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerModelMessage.
  const GeneratedNgWorkerModelMessage(super.raw);

  /// The type member of ng.WorkerModelMessage.
  String get typeMember => _toDartString(raw.getProperty('type'.toJS))!;

  /// The type member of ng.WorkerModelMessage.
  set typeMember(String value) {
    raw.setProperty('type'.toJS, unsafe.dartToJs(value));
  }

  /// The channel member of ng.WorkerModelMessage.
  String get channel => _toDartString(raw.getProperty('channel'.toJS))!;

  /// The channel member of ng.WorkerModelMessage.
  set channel(String value) {
    raw.setProperty('channel'.toJS, unsafe.dartToJs(value));
  }
}

/// Request envelope understood by WorkerHandle.request.
///
/// Raw JavaScript facade for `ng.WorkerRequest`.
base class GeneratedNgWorkerRequest extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerRequest.
  const GeneratedNgWorkerRequest(super.raw);

  /// The type member of ng.WorkerRequest.
  String get typeMember => _toDartString(raw.getProperty('type'.toJS))!;

  /// The type member of ng.WorkerRequest.
  set typeMember(String value) {
    raw.setProperty('type'.toJS, unsafe.dartToJs(value));
  }

  /// The id member of ng.WorkerRequest.
  String get id => _toDartString(raw.getProperty('id'.toJS))!;

  /// The id member of ng.WorkerRequest.
  set id(String value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The payload member of ng.WorkerRequest.
  Object? get payload =>
      unsafe.jsToDart<Object?>(raw.getProperty('payload'.toJS));
}

/// Options for one correlated worker request.
///
/// Raw JavaScript facade for `ng.WorkerRequestOptions`.
base class GeneratedNgWorkerRequestOptions extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerRequestOptions.
  const GeneratedNgWorkerRequestOptions(super.raw);

  /// Reject the request after this many milliseconds. Defaults to 30 seconds.
  num? get timeout => _toDartNumber(raw.getProperty('timeout'.toJS));

  /// Reject the request after this many milliseconds. Defaults to 30 seconds.
  set timeout(num? value) {
    raw.setProperty('timeout'.toJS, unsafe.dartToJs(value));
  }

  /// Abort the request without terminating the worker.
  AbortSignal? get signal =>
      unsafe.jsToDart<AbortSignal?>(raw.getProperty('signal'.toJS));

  /// Transfer ownership of values contained by the request payload.
  Object? get transfer =>
      unsafe.jsToDart<Object?>(raw.getProperty('transfer'.toJS));
}

/// Response envelope returned for a correlated worker request.
///
/// Raw JavaScript facade for `ng.WorkerResponse`.
base class GeneratedNgWorkerResponse extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerResponse.
  const GeneratedNgWorkerResponse(super.raw);

  /// The type member of ng.WorkerResponse.
  String get typeMember => _toDartString(raw.getProperty('type'.toJS))!;

  /// The type member of ng.WorkerResponse.
  set typeMember(String value) {
    raw.setProperty('type'.toJS, unsafe.dartToJs(value));
  }

  /// The id member of ng.WorkerResponse.
  String get id => _toDartString(raw.getProperty('id'.toJS))!;

  /// The id member of ng.WorkerResponse.
  set id(String value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The ok member of ng.WorkerResponse.
  bool get ok => _toDartBool(raw.getProperty('ok'.toJS))!;

  /// The ok member of ng.WorkerResponse.
  set ok(bool value) {
    raw.setProperty('ok'.toJS, unsafe.dartToJs(value));
  }
}

/// Injectable factory for typed managed Web Worker connections.
///
/// Raw JavaScript facade for `ng.WorkerService`.
base class GeneratedNgWorkerService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerService.
  const GeneratedNgWorkerService(super.raw);

  /// Calls the ng.WorkerService function.
  ///
  /// Parameters:
  /// - [scriptPath]: Value supplied for the scriptPath parameter.
  /// - [config]: Value supplied for the config parameter.
  Object? call(Object? scriptPath, [Object? config = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(scriptPath));
    if (!identical(config, _undefinedArgument)) {
      args.add(unsafe.dartToJs(config));
    }

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Lifecycle state exposed by a managed WorkerHandle.
///
/// Raw JavaScript facade for `ng.WorkerStatus`.
base class GeneratedNgWorkerStatus extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkerStatus.
  const GeneratedNgWorkerStatus(super.raw);
}

/// Public AngularTS Workflow contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.Workflow`.
base class GeneratedNgWorkflow extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.Workflow.
  const GeneratedNgWorkflow(super.raw);

  /// The id member of ng.Workflow.
  String get id => _toDartString(raw.getProperty('id'.toJS))!;

  /// The state member of ng.Workflow.
  Object? get state => unsafe.jsToDart<Object?>(raw.getProperty('state'.toJS));

  /// The data member of ng.Workflow.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// The diagnostics member of ng.Workflow.
  Object? get diagnostics =>
      unsafe.jsToDart<Object?>(raw.getProperty('diagnostics'.toJS));

  /// The history member of ng.Workflow.
  Object? get history =>
      unsafe.jsToDart<Object?>(raw.getProperty('history'.toJS));

  /// The can member of ng.Workflow.
  ///
  /// Parameters:
  /// - [command]: Value supplied for the command parameter.
  bool can(Object? command) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(command));

    return _toDartBool(raw.callMethodVarArgs('can'.toJS, args))!;
  }

  /// The run member of ng.Workflow.
  ///
  /// Parameters:
  /// - [command]: Value supplied for the command parameter.
  /// - [input1]: Value supplied for the input parameter.
  /// - [input2]: Value supplied for the input parameter.
  /// - [input3]: Value supplied for the input parameter.
  /// - [input4]: Value supplied for the input parameter.
  JSPromise<JSAny?> run(Object? command,
      [Object? input1 = _undefinedArgument,
      Object? input2 = _undefinedArgument,
      Object? input3 = _undefinedArgument,
      Object? input4 = _undefinedArgument]) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(command));
    if (!identical(input1, _undefinedArgument)) {
      args.add(unsafe.dartToJs(input1));
    }
    if (!identical(input2, _undefinedArgument)) {
      args.add(unsafe.dartToJs(input2));
    }
    if (!identical(input3, _undefinedArgument)) {
      args.add(unsafe.dartToJs(input3));
    }
    if (!identical(input4, _undefinedArgument)) {
      args.add(unsafe.dartToJs(input4));
    }

    return raw.callMethodVarArgs('run'.toJS, args) as JSPromise<JSAny?>;
  }

  /// The cancel member of ng.Workflow.
  ///
  /// Parameters:
  /// - [command]: Value supplied for the command parameter.
  num cancel([Object? command = _undefinedArgument]) {
    final args = <JSAny?>[];
    if (!identical(command, _undefinedArgument)) {
      args.add(unsafe.dartToJs(command));
    }

    return _toDartNumber(raw.callMethodVarArgs('cancel'.toJS, args))!;
  }

  /// The snapshot member of ng.Workflow.
  Object? snapshot() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('snapshot'.toJS, const <JSAny?>[]));
  }

  /// The restore member of ng.Workflow.
  ///
  /// Parameters:
  /// - [snapshot]: Value supplied for the snapshot parameter.
  void restore(Object? snapshot) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(snapshot));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('restore'.toJS, args));
  }
}

/// Public AngularTS WorkflowCommand contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowCommand`.
base class GeneratedNgWorkflowCommand extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowCommand.
  const GeneratedNgWorkflowCommand(super.raw);

  /// Calls the ng.WorkflowCommand function.
  ///
  /// Parameters:
  /// - [context]: Value supplied for the context parameter.
  Object? call(Object? context) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(context));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS WorkflowCommandContext contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowCommandContext`.
base class GeneratedNgWorkflowCommandContext extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowCommandContext.
  const GeneratedNgWorkflowCommandContext(super.raw);

  /// The data member of ng.WorkflowCommandContext.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// The input member of ng.WorkflowCommandContext.
  Object? get input => unsafe.jsToDart<Object?>(raw.getProperty('input'.toJS));

  /// The command member of ng.WorkflowCommandContext.
  String get command => _toDartString(raw.getProperty('command'.toJS))!;

  /// The cleanup member of ng.WorkflowCommandContext.
  ///
  /// Parameters:
  /// - [callback]: Value supplied for the callback parameter.
  void cleanup(JSFunction callback) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(callback));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('cleanup'.toJS, args));
  }

  /// Stop the command with a controlled, recorded diagnostic.
  ///
  /// Parameters:
  /// - [diagnostic]: Value supplied for the diagnostic parameter.
  Object? reject(Object? diagnostic) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(diagnostic));

    return unsafe.jsToDart<Object?>(raw.callMethodVarArgs('reject'.toJS, args));
  }

  /// The signal member of ng.WorkflowCommandContext.
  AbortSignal get signal =>
      unsafe.jsToDart<AbortSignal>(raw.getProperty('signal'.toJS));
}

/// Input and output carried by a workflow command.
///
/// Raw JavaScript facade for `ng.WorkflowCommandContract`.
base class GeneratedNgWorkflowCommandContract extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowCommandContract.
  const GeneratedNgWorkflowCommandContract(super.raw);

  /// The input member of ng.WorkflowCommandContract.
  Object? get input => unsafe.jsToDart<Object?>(raw.getProperty('input'.toJS));

  /// The output member of ng.WorkflowCommandContract.
  Object? get output =>
      unsafe.jsToDart<Object?>(raw.getProperty('output'.toJS));
}

/// Public AngularTS WorkflowCommandDefinition contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowCommandDefinition`.
base class GeneratedNgWorkflowCommandDefinition extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowCommandDefinition.
  const GeneratedNgWorkflowCommandDefinition(super.raw);

  /// The from member of ng.WorkflowCommandDefinition.
  Object? get from => unsafe.jsToDart<Object?>(raw.getProperty('from'.toJS));

  /// The pending member of ng.WorkflowCommandDefinition.
  Object? get pending =>
      unsafe.jsToDart<Object?>(raw.getProperty('pending'.toJS));

  /// The execute member of ng.WorkflowCommandDefinition.
  Object? get execute =>
      unsafe.jsToDart<Object?>(raw.getProperty('execute'.toJS));

  /// The success member of ng.WorkflowCommandDefinition.
  Object? get success =>
      unsafe.jsToDart<Object?>(raw.getProperty('success'.toJS));

  /// The failure member of ng.WorkflowCommandDefinition.
  Object? get failure =>
      unsafe.jsToDart<Object?>(raw.getProperty('failure'.toJS));

  /// The cancelled member of ng.WorkflowCommandDefinition.
  Object? get cancelled =>
      unsafe.jsToDart<Object?>(raw.getProperty('cancelled'.toJS));

  /// The timeout member of ng.WorkflowCommandDefinition.
  Object? get timeout =>
      unsafe.jsToDart<Object?>(raw.getProperty('timeout'.toJS));

  /// The concurrency member of ng.WorkflowCommandDefinition.
  String? get concurrency => _toDartString(raw.getProperty('concurrency'.toJS));

  /// The concurrency member of ng.WorkflowCommandDefinition.
  set concurrency(String? value) {
    raw.setProperty('concurrency'.toJS, unsafe.dartToJs(value));
  }

  /// The commandTimeout member of ng.WorkflowCommandDefinition.
  num? get commandTimeout =>
      _toDartNumber(raw.getProperty('commandTimeout'.toJS));

  /// The commandTimeout member of ng.WorkflowCommandDefinition.
  set commandTimeout(num? value) {
    raw.setProperty('commandTimeout'.toJS, unsafe.dartToJs(value));
  }

  /// The retry member of ng.WorkflowCommandDefinition.
  num? get retry => _toDartNumber(raw.getProperty('retry'.toJS));

  /// The retry member of ng.WorkflowCommandDefinition.
  set retry(num? value) {
    raw.setProperty('retry'.toJS, unsafe.dartToJs(value));
  }
}

/// Labeled type contract carried by a workflow definition and instance.
///
/// Raw JavaScript facade for `ng.WorkflowContract`.
base class GeneratedNgWorkflowContract extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowContract.
  const GeneratedNgWorkflowContract(super.raw);

  /// The data member of ng.WorkflowContract.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// The commands member of ng.WorkflowContract.
  Object? get commands =>
      unsafe.jsToDart<Object?>(raw.getProperty('commands'.toJS));

  /// The state member of ng.WorkflowContract.
  String get state => _toDartString(raw.getProperty('state'.toJS))!;

  /// The state member of ng.WorkflowContract.
  set state(String value) {
    raw.setProperty('state'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS WorkflowResult contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowResult`.
base class GeneratedNgWorkflowResult extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowResult.
  const GeneratedNgWorkflowResult(super.raw);

  /// The ok member of ng.WorkflowResult.
  bool get ok => _toDartBool(raw.getProperty('ok'.toJS))!;

  /// The status member of ng.WorkflowResult.
  String get status => _toDartString(raw.getProperty('status'.toJS))!;

  /// The diagnostics member of ng.WorkflowResult.
  Object? get diagnostics =>
      unsafe.jsToDart<Object?>(raw.getProperty('diagnostics'.toJS));
}

/// Public AngularTS WorkflowService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowService`.
base class GeneratedNgWorkflowService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowService.
  const GeneratedNgWorkflowService(super.raw);

  /// Calls the ng.WorkflowService function.
  ///
  /// Parameters:
  /// - [config]: Value supplied for the config parameter.
  Object? call(Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(config));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS WorkflowSnapshot contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowSnapshot`.
base class GeneratedNgWorkflowSnapshot extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowSnapshot.
  const GeneratedNgWorkflowSnapshot(super.raw);

  /// The version member of ng.WorkflowSnapshot.
  num get version => _toDartNumber(raw.getProperty('version'.toJS))!;

  /// The id member of ng.WorkflowSnapshot.
  String get id => _toDartString(raw.getProperty('id'.toJS))!;

  /// The state member of ng.WorkflowSnapshot.
  Object? get state => unsafe.jsToDart<Object?>(raw.getProperty('state'.toJS));

  /// The data member of ng.WorkflowSnapshot.
  Object? get data => unsafe.jsToDart<Object?>(raw.getProperty('data'.toJS));

  /// The diagnostics member of ng.WorkflowSnapshot.
  Object? get diagnostics =>
      unsafe.jsToDart<Object?>(raw.getProperty('diagnostics'.toJS));

  /// The history member of ng.WorkflowSnapshot.
  Object? get history =>
      unsafe.jsToDart<Object?>(raw.getProperty('history'.toJS));
}

/// Public AngularTS WorkflowSupervisor contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowSupervisor`.
base class GeneratedNgWorkflowSupervisor extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowSupervisor.
  const GeneratedNgWorkflowSupervisor(super.raw);

  /// The id member of ng.WorkflowSupervisor.
  String get id => _toDartString(raw.getProperty('id'.toJS))!;

  /// The status member of ng.WorkflowSupervisor.
  String get status => _toDartString(raw.getProperty('status'.toJS))!;

  /// The diagnostics member of ng.WorkflowSupervisor.
  Object? get diagnostics =>
      unsafe.jsToDart<Object?>(raw.getProperty('diagnostics'.toJS));

  /// The ready member of ng.WorkflowSupervisor.
  JSPromise<JSAny?> get ready =>
      raw.getProperty('ready'.toJS) as JSPromise<JSAny?>;

  /// The workflow member of ng.WorkflowSupervisor.
  ///
  /// Parameters:
  /// - [name]: Value supplied for the name parameter.
  Object? workflow(Object? name) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(name));

    return unsafe
        .jsToDart<Object?>(raw.callMethodVarArgs('workflow'.toJS, args));
  }

  /// The cancelAll member of ng.WorkflowSupervisor.
  num cancelAll() {
    return _toDartNumber(
        raw.callMethodVarArgs('cancelAll'.toJS, const <JSAny?>[]))!;
  }

  /// The snapshot member of ng.WorkflowSupervisor.
  Object? snapshot() {
    return unsafe.jsToDart<Object?>(
        raw.callMethodVarArgs('snapshot'.toJS, const <JSAny?>[]));
  }

  /// The restore member of ng.WorkflowSupervisor.
  ///
  /// Parameters:
  /// - [snapshot]: Value supplied for the snapshot parameter.
  void restore(Object? snapshot) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(snapshot));

    unsafe.jsToDart<void>(raw.callMethodVarArgs('restore'.toJS, args));
  }

  /// The persist member of ng.WorkflowSupervisor.
  JSPromise<JSAny?> persist() {
    return raw.callMethodVarArgs('persist'.toJS, const <JSAny?>[])
        as JSPromise<JSAny?>;
  }

  /// The recover member of ng.WorkflowSupervisor.
  JSPromise<JSAny?> recover() {
    return raw.callMethodVarArgs('recover'.toJS, const <JSAny?>[])
        as JSPromise<JSAny?>;
  }
}

/// Public AngularTS WorkflowSupervisorConfig contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowSupervisorConfig`.
base class GeneratedNgWorkflowSupervisorConfig extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowSupervisorConfig.
  const GeneratedNgWorkflowSupervisorConfig(super.raw);

  /// The id member of ng.WorkflowSupervisorConfig.
  String get id => _toDartString(raw.getProperty('id'.toJS))!;

  /// The id member of ng.WorkflowSupervisorConfig.
  set id(String value) {
    raw.setProperty('id'.toJS, unsafe.dartToJs(value));
  }

  /// The workflows member of ng.WorkflowSupervisorConfig.
  Object? get workflows =>
      unsafe.jsToDart<Object?>(raw.getProperty('workflows'.toJS));

  /// The persistence member of ng.WorkflowSupervisorConfig.
  Object? get persistence =>
      unsafe.jsToDart<Object?>(raw.getProperty('persistence'.toJS));

  /// Persist a fresh supervisor snapshot after each completed command.
  bool? get autoPersist => _toDartBool(raw.getProperty('autoPersist'.toJS));

  /// Persist a fresh supervisor snapshot after each completed command.
  set autoPersist(bool? value) {
    raw.setProperty('autoPersist'.toJS, unsafe.dartToJs(value));
  }

  /// Restore persisted state and retry recoverable commands on startup.
  bool? get autoRecover => _toDartBool(raw.getProperty('autoRecover'.toJS));

  /// Restore persisted state and retry recoverable commands on startup.
  set autoRecover(bool? value) {
    raw.setProperty('autoRecover'.toJS, unsafe.dartToJs(value));
  }
}

/// Public AngularTS WorkflowSupervisorPersistence contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowSupervisorPersistence`.
base class GeneratedNgWorkflowSupervisorPersistence extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowSupervisorPersistence.
  const GeneratedNgWorkflowSupervisorPersistence(super.raw);

  /// The load member of ng.WorkflowSupervisorPersistence.
  ///
  /// Parameters:
  /// - [id]: Value supplied for the id parameter.
  JSPromise<JSAny?> load(String id) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));

    return raw.callMethodVarArgs('load'.toJS, args) as JSPromise<JSAny?>;
  }

  /// The save member of ng.WorkflowSupervisorPersistence.
  ///
  /// Parameters:
  /// - [id]: Value supplied for the id parameter.
  /// - [snapshot]: Value supplied for the snapshot parameter.
  JSPromise<JSAny?> save(String id, Object? snapshot) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(id));
    args.add(unsafe.dartToJs(snapshot));

    return raw.callMethodVarArgs('save'.toJS, args) as JSPromise<JSAny?>;
  }
}

/// Built-in IndexedDB persistence selected by a workflow supervisor.
///
/// Raw JavaScript facade for `ng.WorkflowSupervisorPersistenceConfig`.
base class GeneratedNgWorkflowSupervisorPersistenceConfig
    extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowSupervisorPersistenceConfig.
  const GeneratedNgWorkflowSupervisorPersistenceConfig(super.raw);

  /// The type member of ng.WorkflowSupervisorPersistenceConfig.
  String get typeMember => _toDartString(raw.getProperty('type'.toJS))!;

  /// The type member of ng.WorkflowSupervisorPersistenceConfig.
  set typeMember(String value) {
    raw.setProperty('type'.toJS, unsafe.dartToJs(value));
  }

  /// The database member of ng.WorkflowSupervisorPersistenceConfig.
  String? get database => _toDartString(raw.getProperty('database'.toJS));

  /// The database member of ng.WorkflowSupervisorPersistenceConfig.
  set database(String? value) {
    raw.setProperty('database'.toJS, unsafe.dartToJs(value));
  }

  /// The store member of ng.WorkflowSupervisorPersistenceConfig.
  String? get store => _toDartString(raw.getProperty('store'.toJS));

  /// The store member of ng.WorkflowSupervisorPersistenceConfig.
  set store(String? value) {
    raw.setProperty('store'.toJS, unsafe.dartToJs(value));
  }

  /// The version member of ng.WorkflowSupervisorPersistenceConfig.
  num? get version => _toDartNumber(raw.getProperty('version'.toJS));

  /// The version member of ng.WorkflowSupervisorPersistenceConfig.
  set version(num? value) {
    raw.setProperty('version'.toJS, unsafe.dartToJs(value));
  }

  /// The indexedDB member of ng.WorkflowSupervisorPersistenceConfig.
  Object? get indexedDB =>
      unsafe.jsToDart<Object?>(raw.getProperty('indexedDB'.toJS));
}

/// Public AngularTS WorkflowSupervisorService contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowSupervisorService`.
base class GeneratedNgWorkflowSupervisorService extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowSupervisorService.
  const GeneratedNgWorkflowSupervisorService(super.raw);

  /// Calls the ng.WorkflowSupervisorService function.
  ///
  /// Parameters:
  /// - [config]: Value supplied for the config parameter.
  Object? call(Object? config) {
    final args = <JSAny?>[];
    args.add(unsafe.dartToJs(config));

    return unsafe.jsToDart<Object?>(_callFunction(raw as JSFunction, args));
  }
}

/// Public AngularTS WorkflowSupervisorSnapshot contract exposed through the ng namespace.
///
/// Raw JavaScript facade for `ng.WorkflowSupervisorSnapshot`.
base class GeneratedNgWorkflowSupervisorSnapshot extends GeneratedNgFacade {
  /// Creates a generated raw facade for ng.WorkflowSupervisorSnapshot.
  const GeneratedNgWorkflowSupervisorSnapshot(super.raw);

  /// The version member of ng.WorkflowSupervisorSnapshot.
  num get version => _toDartNumber(raw.getProperty('version'.toJS))!;

  /// The id member of ng.WorkflowSupervisorSnapshot.
  String get id => _toDartString(raw.getProperty('id'.toJS))!;

  /// The status member of ng.WorkflowSupervisorSnapshot.
  String get status => _toDartString(raw.getProperty('status'.toJS))!;

  /// The workflows member of ng.WorkflowSupervisorSnapshot.
  Object? get workflows =>
      unsafe.jsToDart<Object?>(raw.getProperty('workflows'.toJS));

  /// The diagnostics member of ng.WorkflowSupervisorSnapshot.
  Object? get diagnostics =>
      unsafe.jsToDart<Object?>(raw.getProperty('diagnostics'.toJS));

  /// The updatedAt member of ng.WorkflowSupervisorSnapshot.
  num get updatedAt => _toDartNumber(raw.getProperty('updatedAt'.toJS))!;
}
