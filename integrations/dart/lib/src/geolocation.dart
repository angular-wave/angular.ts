import 'generated/ng_facades.dart';

/// Plain location snapshot written by the AngularTS `geolocation` directive.
final class GeolocationValue extends GeneratedNgGeolocationValue {
  /// Wraps a raw AngularTS geolocation model value.
  const GeolocationValue(super.raw);
}
