<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pulse">
        <activity
            android:name="io.github.angularwave.android.navigation.activities.AngularNativeHostActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.github.angularwave.android.START_LOCATION"
                android:value="http://127.0.0.1:4175/" />
            <meta-data
                android:name="io.github.angularwave.android.NAVIGATOR_NAME"
                android:value="pulse" />
            <meta-data
                android:name="io.github.angularwave.android.PATH_CONFIGURATION_ASSET"
                android:value="pulse/path-configuration.json" />
            <meta-data
                android:name="io.github.angularwave.android.USER_AGENT_PREFIX"
                android:value="Pulse Sample;" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
